courseData.kt = {
    "Getting Started": {
        "What is Kotlin": { exp: "Kotlin is a modern, statically-typed language that runs on the JVM, JS, and Native targets. It's concise, safe, and fully interoperable with Java.", code: "fun main() {\n    println(\"Hello, Kotlin!\")\n}" },
        "Kotlin Syntax": { exp: "Kotlin omits semicolons and uses `fun` for functions, `val` for read-only references, and `var` for mutable ones. Types come after colons.", code: "val name: String = \"Doge\"\nvar age = 5\nfun greet() = \"Hello\"\nprintln(\"$name is $age\")" },
        "Variables & Types": { exp: "Kotlin infers types. `val` (immutable like final) and `var` (mutable). Basic types: String, Int, Long, Double, Float, Boolean, Char, Byte, Short.", code: "val pi = 3.14159\nvar counter = 0\nval visible: Boolean = true\nval items = listOf(1, 2, 3)\nprintln(pi::class.simpleName)" },
        "Null Safety": { exp: "Kotlin eliminates null pointer exceptions with nullable types (`?`), safe calls (`?.`), Elvis operator (`?:`), and the not-null assertion (`!!`).", code: "var name: String? = null\nprintln(name?.length)\nprintln(name ?: \"Default\")\nname = \"Doge\"\nprintln(name!!.length)" },
        "Kotlin Playground": { exp: "The Kotlin Playground is an online IDE to experiment with Kotlin code. It supports multiplatform targets, embedded snippets, and sharing via URLs.", code: "// https://play.kotlinlang.org\nfun main() {\n    val msg = \"Try Kotlin\"\n    println(\"$msg — online!\")\n}" },
        "Multiplatform Overview": { exp: "Kotlin Multiplatform shares business logic across JVM, JS, iOS, Android, and Native. Write once in shared modules; platform code in target modules.", code: "plugins { kotlin(\"multiplatform\") }\n\nkotlin {\n    jvm()\n    js()\n    iosX64()\n    sourceSets.commonMain.dependencies {\n        implementation(kotlin(\"stdlib-common\"))\n    }\n}" },
        "Project Structure": { exp: "Standard Kotlin projects use Gradle with `src/main/kotlin` and `src/test/kotlin`. Multiplatform adds `commonMain`, `jvmMain`, `iosMain` source sets.", code: "Project/\n \u251c\u2500\u2500 build.gradle.kts\n \u251c\u2500\u2500 settings.gradle.kts\n \u2514\u2500\u2500 src/\n    \u251c\u2500\u2500 main/kotlin/\n    \u2502   \u2514\u2500\u2500 Main.kt\n    \u2514\u2500\u2500 test/kotlin/\n        \u2514\u2500\u2500 MainTest.kt" }
    },
    "Control Flow": {
        "If & When expressions": { exp: "`if` and `when` are expressions that return values. `when` replaces Java's switch and supports smart casts, ranges, and arbitrary conditions.", code: "val score = 85\nval grade = when {\n    score >= 90 -> 'A'\n    score >= 80 -> 'B'\n    else -> 'F'\n}\nval max = if (a > b) a else b\nprintln(grade)" },
        "for & while Loops": { exp: "`for` iterates over ranges, collections, or anything with an iterator. `while` and `do..while` work as expected. Ranges use `..`, `until`, `downTo`, `step`.", code: "for (i in 1..5) print(i)\nfor (i in 5 downTo 1 step 2) print(i)\nfor ((index, value) in listOf('a','b').withIndex())\n    println(\"$index: $value\")" },
        "Range & Progression": { exp: "Ranges (`1..10`) define intervals. Progressions control step direction. Use `in` to check membership, `until` for exclusive end.", code: "val closed = 1..5\nval half = 1 until 5\nval descending = 5 downTo 1\nval step = 0..10 step 2\nprintln(3 in closed)\nprintln(3 in half)" },
        "Equality Checks": { exp: "`==` calls `equals()` (structural equality). `===` checks referential identity. `!=` and `!==` are the negated forms.", code: "val a = listOf(1, 2)\nval b = listOf(1, 2)\nval c = a\nprintln(a == b)   // true — structural\nprintln(a === b)  // false — different references\nprintln(a === c)  // true — same reference" },
        "Exception Handling": { exp: "Use `try-catch-finally`. Kotlin has no checked exceptions. `catch` blocks can filter by exception type, and `finally` always executes.", code: "fun divide(a: Int, b: Int): Int {\n    return try {\n        a / b\n    } catch (e: ArithmeticException) {\n        println(\"Cannot divide by zero\")\n        0\n    } finally {\n        println(\"Division attempted\")\n    }\n}" },
        "try as Expression": { exp: "`try` is an expression in Kotlin. It returns the last expression in the `try` block or the matching `catch` block. Use `finally` for side effects.", code: "val input = \"42\"\nval parsed: Int? = try {\n    input.toInt()\n} catch (e: NumberFormatException) {\n    null\n}\nprintln(parsed ?: \"Invalid number\")" }
    },
    "Functions": {
        "Function Declaration": { exp: "Functions use `fun` with explicit return types. Expression bodies omit braces. Single-expression functions infer the return type automatically.", code: "fun sum(a: Int, b: Int): Int = a + b\nfun greet(name: String) = \"Hello $name\"\nfun printSum(a: Int, b: Int): Unit { println(a + b) }" },
        "Default & Named Args": { exp: "Parameters can have default values. Named arguments allow skipping or reordering parameters, improving readability for functions with many parameters.", code: "fun createUser(name: String, age: Int = 18, active: Boolean = true) {}\ncreateUser(\"Alice\")\ncreateUser(\"Bob\", active = false)\ncreateUser(age = 25, name = \"Charlie\")" },
        "Lambda & Higher-Order": { exp: "Lambdas are function literals. Higher-order functions take or return functions. Trailing lambda syntax removes parentheses for clean DSL-like code.", code: "val numbers = listOf(1, 2, 3, 4, 5)\nval doubled = numbers.map { it * 2 }\nval sum = numbers.reduce { acc, n -> acc + n }\nnumbers.filter { it > 2 }.forEach { println(it) }" },
        "Extension Functions": { exp: "Add new functions to existing classes without inheritance. Extensions are resolved statically — they don't actually modify the class.", code: "fun String.isEmail() = this.contains('@') && this.contains('.')\nfun List<Int>.product() = fold(1) { acc, i -> acc * i }\n\nprintln(\"test@example.com\".isEmail())\nprintln(listOf(2, 3, 4).product())" },
        "Infix Functions": { exp: "Infix functions are called without dot or parens. Single-parameter member or extension functions marked with `infix`. Common in DSLs.", code: "infix fun Int.times(str: String) = str.repeat(this)\nprintln(3 times \"Ha \")  // Ha Ha Ha\n\n// Built-in: to\nval map = mapOf(1 to \"one\", 2 to \"two\")" },
        "Operator Overloading": { exp: "Kotlin allows overloading operators like `+`, `-`, `*`, `[]`, `()` via functions named `plus`, `minus`, `times`, `get`, `invoke`.", code: "data class Vector(val x: Int, val y: Int) {\n    operator fun plus(other: Vector) = Vector(x + other.x, y + other.y)\n    operator fun get(index: Int) = if (index == 0) x else y\n}\nval v1 = Vector(1, 2)\nval v2 = Vector(3, 4)\nprintln(v1 + v2)  // Vector(x=4, y=6)" },
        "tailrec": { exp: "`tailrec` optimises recursive functions into iterative loops when the recursive call is in tail position. No stack overflow risk.", code: "tailrec fun factorial(n: Int, acc: Int = 1): Int {\n    return if (n <= 1) acc else factorial(n - 1, n * acc)\n}\n\nprintln(factorial(5))  // 120\nprintln(factorial(10000))  // no stack overflow" },
        "Inline Functions": { exp: "`inline` inlines the function body at call sites, avoiding lambda object allocation. Use for higher-order functions with lambda parameters.", code: "inline fun <T> measure(block: () -> T): T {\n    val start = System.nanoTime()\n    val result = block()\n    val end = System.nanoTime()\n    println(\"Took ${end - start} ns\")\n    return result\n}\n\nmeasure { Thread.sleep(10); 42 }" }
    },
    "Collections": {
        "Lists & Arrays": { exp: "`listOf` creates read-only lists, `mutableListOf` for mutable. `arrayOf` for arrays. Lists are covariant; arrays are invariant.", code: "val readOnly = listOf(\"a\", \"b\", \"c\")\nval mutable = mutableListOf(\"a\", \"b\")\nmutable.add(\"d\")\nval arr = arrayOf(1, 2, 3)\nprintln(readOnly[1])\nprintln(mutable)" },
        "Maps & Sets": { exp: "`mapOf`/`mutableMapOf` for key-value pairs. `setOf`/`mutableSetOf` for unique elements. Immutable by default.", code: "val map = mapOf(\"key1\" to 1, \"key2\" to 2)\nval set = setOf(1, 2, 3, 2, 1)\nval mutableMap = mutableMapOf<String, Int>()\nmutableMap[\"Doge\"] = 5\nprintln(map[\"key1\"])\nprintln(set)" },
        "Sequences": { exp: "Sequences provide lazy evaluation for collection chains. Use `asSequence()` for large datasets to avoid intermediate list allocations.", code: "val result = (1..1_000_000)\n    .asSequence()\n    .filter { it % 2 == 0 }\n    .map { it * 3 }\n    .take(5)\n    .toList()\nprintln(result)" },
        "Functional Operations": { exp: "Kotlin provides rich functional APIs: map, filter, reduce, fold, flatMap, sortedBy, distinct, zip, unzip, associate, and more.", code: "val nums = listOf(1, 2, 3, 4, 5)\nprintln(nums.map { it * 2 })\nprintln(nums.filter { it % 2 == 0 })\nprintln(nums.fold(0) { acc, i -> acc + i })\nprintln(nums.flatMap { listOf(it, -it) })\nprintln(nums.associateWith { it * it })" },
        "Grouping & Partitioning": { exp: "`groupBy` groups elements by a key selector. `partition` splits into two lists based on a predicate. `groupingBy` for eager grouping.", code: "val words = listOf(\"apple\", \"bat\", \"cat\", \"a\", \"bb\")\nprintln(words.groupBy { it.length })\nprintln(words.partition { it.length > 2 })\n// groupBy: {1=[a], 2=[bat, bb], 3=[cat], 5=[apple]}\n// partition: ([apple, bat, cat], [a, bb])" },
        "Windowed & Chunked": { exp: "`chunked(size)` splits collection into fixed-size lists. `windowed(size)` creates sliding windows. Both support partial windows and steps.", code: "val nums = (1..8).toList()\nprintln(nums.chunked(3))       // [[1,2,3], [4,5,6], [7,8]]\nprintln(nums.windowed(3))      // [[1,2,3], [2,3,4], ... [6,7,8]]\nprintln(nums.windowed(3, 2))   // [[1,2,3], [3,4,5], [5,6,7]]" },
        "Element Operations": { exp: "`first`, `last`, `single`, `elementAt`, `firstOrNull`, `minOrNull`, `maxOrNull`, `take`, `drop`, `slice` for element selection.", code: "val nums = listOf(1, 2, 3, 2, 1)\nprintln(nums.first { it > 1 })    // 2\nprintln(nums.singleOrNull { it > 5 }) // null\nprintln(nums.take(2))             // [1, 2]\nprintln(nums.drop(2))             // [3, 2, 1]\nprintln(nums.distinct())          // [1, 2, 3]" }
    },
    "Classes & OOP": {
        "Class Basics": { exp: "Classes use `class` with a primary constructor in the header. Properties declared in the constructor are automatically generated with getters/setters.", code: "class Person(val name: String, var age: Int)\nval p = Person(\"Alice\", 30)\nprintln(p.name)\np.age = 31" },
        "Inheritance & Interfaces": { exp: "Classes are final by default — use `open` to allow inheritance. Override methods with `override`. Interfaces can have abstract and default implementations.", code: "open class Animal(open val name: String) {\n    open fun speak() = println(\"$name makes a sound\")\n}\ninterface Pet {\n    fun play() = println(\"Playing\")\n}\nclass Dog(override val name: String) : Animal(name), Pet {\n    override fun speak() = println(\"$name barks\")\n}" },
        "Data Classes": { exp: "`data class` automatically generates equals(), hashCode(), toString(), copy(), and componentN() functions. Perfect for DTOs and value objects.", code: "data class User(val id: Int, val name: String)\nval u1 = User(1, \"Alice\")\nval u2 = u1.copy(name = \"Bob\")\nprintln(u1)\nprintln(u1 == User(1, \"Alice\"))\nval (id, name) = u1  // destructuring" },
        "Sealed Classes & Interfaces": { exp: "Sealed types restrict hierarchies to a fixed set of subclasses defined in the same file. Perfect for representing states or algebraic data types.", code: "sealed class Result {\n    data class Success(val data: String) : Result()\n    data class Error(val msg: String) : Result()\n    object Loading : Result()\n}\nfun handle(r: Result) = when (r) {\n    is Result.Success -> println(r.data)\n    is Result.Error -> println(\"Error: ${r.msg}\")\n    Result.Loading -> println(\"Loading...\")\n}" },
        "Object & Companion": { exp: "`object` declares a singleton. `companion object` defines static-like members inside a class. Objects can also implement interfaces as anonymous instances.", code: "object Config {\n    const val BASE_URL = \"https://api.example.com\"\n}\nclass App {\n    companion object {\n        fun create() = App()\n    }\n}\nprintln(Config.BASE_URL)" },
        "Value & Inline Classes": { exp: "`value class` wraps a single value without heap allocation overhead. Use `@JvmInline` for JVM interop. Prevents primitive boxing in many cases.", code: "@JvmInline\nvalue class Password(val value: String) {\n    init {\n        require(value.length >= 8) { \"Too short\" }\n    }\n}\nval pwd = Password(\"secure123\")\nprintln(pwd.value)\n// No extra object at runtime — inlined to String" },
        "Type Aliases": { exp: "Type aliases provide alternative names for existing types, improving readability without introducing new types. Useful for function types and complex generics.", code: "typealias UserMap = Map<Int, User>\ntypealias Predicate<T> = (T) -> Boolean\ntypealias Callback = (Result) -> Unit\n\nval users: UserMap = mapOf(1 to User(1, \"Alice\"))\nval isAdult: Predicate<Int> = { it >= 18 }" },
        "Delegation": { exp: "`by` delegates interface implementation to another object. `by lazy` for lazy initialization. Delegated properties via `observable`, `vetoable`, or custom delegates.", code: "interface Sound { fun makeSound(): String }\nclass Bark : Sound { override fun makeSound() = \"Woof\" }\nclass Dog(sound: Sound) : Sound by sound\n\nval dog = Dog(Bark())\nprintln(dog.makeSound())  // Woof\n\nval lazyVal: String by lazy { \"Computed once\" }" },
        "Abstract Classes & Nested": { exp: "Abstract classes can have constructors, state, and both abstract and concrete methods. Nested/inner classes differ by whether they hold an outer reference.", code: "abstract class Shape {\n    abstract fun area(): Double\n}\nclass Circle(val r: Double) : Shape() {\n    override fun area() = Math.PI * r * r\n}\nclass Outer {\n    class Nested {}\n    inner class Inner { fun ref() = this@Outer }\n}\nprintln(Circle(3.0).area())" }
    },
    "Generics": {
        "Generic Functions & Classes": { exp: "Generics enable type-safe code with type parameters. Functions and classes can be parameterised with one or more type parameters.", code: "class Box<T>(val value: T)\nfun <T> List<T>.secondOrNull(): T? = if (size >= 2) this[1] else null\n\nval intBox = Box(42)\nval strBox = Box(\"Hello\")\nprintln(listOf(1, 2, 3).secondOrNull())" },
        "Variance — out & in": { exp: "`out T` makes a type covariant (producer). `in T` makes it contravariant (consumer). Declaration-site variance avoids wildcards.", code: "interface Producer<out T> { fun produce(): T }\ninterface Consumer<in T> { fun consume(item: T) }\n\nclass StringProducer : Producer<String> {\n    override fun produce() = \"Hello\"\n}\nval p: Producer<Any> = StringProducer()  // OK: out\nprintln(p.produce())" },
        "Reified Type Parameters": { exp: "`reified` in inline functions preserves the type at runtime, enabling `is` checks, T::class, and `createInstance()` without passing Class<T>.", code: "inline fun <reified T> isA(value: Any) = value is T\ninline fun <reified T> className() = T::class.simpleName\n\nprintln(isA<String>(\"hello\"))  // true\nprintln(className<Int>())       // \"Int\"" },
        "Star Projections": { exp: "`*` for unknown type arguments. `MutableList<*>` is a mutable list of unknown type — safe to read as `Any?`, but not to write.", code: "fun printSize(list: List<*>) = println(list.size)\nfun printFirst(list: MutableList<*>) {\n    val item: Any? = list.first()\n    // list.add(1)  // ERROR: cannot write to `*`\n    println(item)\n}\nprintSize(listOf(1, 2, 3))" },
        "Type Bounds & Where": { exp: "`T : UpperBound` constrains type parameters. Multiple bounds use `where` clause. Use `Nothing` for impossible values.", code: "fun <T : Comparable<T>> maxOf(a: T, b: T) = if (a > b) a else b\n\ninterface Serializable\nfun <T> copy(value: T): T where T : Comparable<T>, T : Serializable\n    = value\n\nprintln(maxOf(3, 7))" }
    },
    "Coroutines": {
        "Launch & Async": { exp: "`launch` fires a fire-and-forget coroutine. `async` starts a coroutine that returns a `Deferred` result via `await()`. Both are non-blocking.", code: "import kotlinx.coroutines.*\n\nrunBlocking {\n    val job = launch { delay(1000); println(\"Done\") }\n    val deferred = async { delay(500); 42 }\n    println(\"Result: ${deferred.await()}\")\n    job.join()\n}" },
        "Dispatchers": { exp: "Dispatchers determine thread pools: Default for CPU, IO for I/O, Main for UI, Unconfined for testing. `withContext` switches dispatchers.", code: "import kotlinx.coroutines.*\n\nsuspend fun loadData(): String = withContext(Dispatchers.IO) {\n    // Network call on IO thread\n    delay(100)\n    \"Data loaded\"\n}\n\nrunBlocking {\n    println(loadData())\n}" },
        "Structured Concurrency": { exp: "Coroutines lifetime ties to parent scope. Cancellation propagates downward; errors bubble up. `coroutineScope` waits for all children to complete.", code: "runBlocking {\n    coroutineScope {\n        launch { delay(1000); println(\"1 done\") }\n        launch { delay(500); println(\"2 done\") }\n    }\n    println(\"All done\")\n}\n// 2 done, 1 done, All done" },
        "supervisorScope": { exp: "supervisorScope isolates child failures: one child's exception doesn't cancel siblings or parent. Use for parallel independent work.", code: "runBlocking {\n    supervisorScope {\n        launch {\n            try { delay(100); throw RuntimeException(\"Oops\") }\n            catch (e: Exception) { println(\"Caught: ${e.message}\") }\n        }\n        launch { delay(200); println(\"Still runs!\") }\n    }\n    println(\"Parent continues\")\n}" },
        "Coroutine Context & Scope": { exp: "CoroutineContext combines Dispatcher, Job, and elements. `CoroutineScope` manages lifecycle. `Job` tracks lifecycle of a coroutine.", code: "val scope = CoroutineScope(Dispatchers.Default + SupervisorJob())\n\nval job = scope.launch(CoroutineName(\"worker\")) {\n    println(\"Running in ${coroutineContext[CoroutineName]}\")\n    delay(500)\n}\nscope.cancel()  // cancels all coroutines" },
        "Cancellation & Timeouts": { exp: "Cancellation is cooperative — check `isActive` or use `yield()`. `withTimeout` throws TimeoutCancellationException. `withTimeoutOrNull` returns null.", code: "runBlocking {\n    val result = withTimeoutOrNull(100) {\n        repeat(10) {\n            delay(50)\n            yield()\n        }\n        \"Finished\"\n    }\n    println(result)  // null (timed out)\n\n    // Cooperative cancellation:\n    val job = launch {\n        repeat(1000) {\n            if (!isActive) return@launch\n            // do work\n        }\n    }\n    delay(10); job.cancelAndJoin()\n}" },
        "Exception Handling": { exp: "Uncaught exceptions cancel parent. Use `try-catch` inside coroutine, or `CoroutineExceptionHandler` for top-level exceptions.", code: "val handler = CoroutineExceptionHandler { _, e ->\n    println(\"Caught: ${e.message}\")\n}\n\nrunBlocking {\n    val scope = CoroutineScope(handler + SupervisorJob())\n    scope.launch {\n        throw IllegalStateException(\"Something broke\")\n    }\n    delay(100)\n    println(\"Scope still active\")\n}" },
        "coroutineScope vs runBlocking": { exp: "`coroutineScope` suspends without blocking a thread. `runBlocking` blocks the current thread for bridge mode. Use coroutineScope in suspend functions.", code: "suspend fun fetchAll() = coroutineScope {\n    val one = async { fetchData(1) }\n    val two = async { fetchData(2) }\n    one.await() + two.await()\n}\n\n// runBlocking — bridge from non-suspend world\nfun main() = runBlocking {\n    println(fetchAll())\n}" }
    },
    "Flows": {
        "Flow Basics": { exp: "Flow is a cold async stream. Use `flow { }` builder with `emit()`, collect with `collect()`. Cold means nothing happens until collected.", code: "import kotlinx.coroutines.flow.*\n\nfun countUp(): Flow<Int> = flow {\n    for (i in 1..3) {\n        delay(100)\n        emit(i)\n    }\n}\n\nrunBlocking { countUp().collect { println(it) } }" },
        "Flow Builders": { exp: "Additional builders: `flowOf` for fixed values, `asFlow` on collections/ranges, `channelFlow` for concurrent emission, `callbackFlow` for callback APIs.", code: "val f1 = flowOf(1, 2, 3)\nval f2 = (1..3).asFlow()\nval f3 = channelFlow {\n    launch { send(1) }\n    launch { send(2) }\n}\nrunBlocking { f3.collect { println(it) } }" },
        "Flow Operators": { exp: "Intermediate operators: map, filter, transform, take, drop, debounce, sample, distinctUntilChanged, flatMapConcat, flatMapMerge, flatMapLatest.", code: "runBlocking {\n    (1..5).asFlow()\n        .filter { it % 2 == 0 }\n        .map { \"Num: $it\" }\n        .debounce(100)\n        .collect { println(it) }\n\n    // flatMapLatest — cancels previous if new emission\n    flow {\n        emit(100); delay(50); emit(200)\n    }.flatMapLatest { delayMs ->\n        flow { emit(\"Start $delayMs\"); delay(delayMs); emit(\"End $delayMs\") }\n    }.collect { println(it) }\n}" },
        "StateFlow": { exp: "StateFlow holds observable state. Hot flow — always has a value. Emits only distinct values. Ideal for ViewModels and UI state.", code: "class MyViewModel {\n    private val _state = MutableStateFlow(\"Idle\")\n    val state: StateFlow<String> = _state\n\n    fun start() {\n        _state.value = \"Loading\"\n        // ...\n        _state.value = \"Done\"\n    }\n}\n\nrunBlocking {\n    val vm = MyViewModel()\n    launch { vm.state.drop(1).collect { println(it) } }\n    vm.start()\n}" },
        "SharedFlow": { exp: "SharedFlow emits events to multiple collectors. Hot flow with replay cache. Use `MutableSharedFlow` for one-shot events like toasts.", code: "runBlocking {\n    val events = MutableSharedFlow<String>(replay = 0)\n\n    launch {\n        repeat(3) { events.emit(\"Event $it\"); delay(100) }\n    }\n\n    launch {\n        events.collect { println(\"Collector 1: $it\") }\n    }\n    launch {\n        delay(50)\n        events.collect { println(\"Collector 2: $it\") }\n    }\n}" },
        "CallbackFlow": { exp: "callbackFlow bridges callback-based APIs into Flows. Use `send()` inside, `awaitClose()` to clean up. Context preserving.", code: "fun flowFromCallback(): Flow<String> = callbackFlow {\n    val listener = object : SomeListener {\n        override fun onEvent(data: String) {\n            trySend(data)\n        }\n        override fun onClose() {\n            close()\n        }\n    }\n    registerListener(listener)\n    awaitClose { unregisterListener(listener) }\n}" },
        "Terminal Flow Operators": { exp: "Terminal ops trigger collection: `collect`, `toList`, `toSet`, `first`, `single`, `reduce`, `fold`, `launchIn`, `collectLatest`.", code: "runBlocking {\n    val flow = (1..5).asFlow()\n\n    println(flow.toList())           // [1,2,3,4,5]\n    println(flow.first())            // 1\n    println(flow.reduce { a, b -> a + b })  // 15\n    println(flow.count { it % 2 == 0 })     // 2\n\n    // launchIn for fire-and-forget\n    flow.onEach { print(\"$it \") }.launchIn(this)\n}" },
        "Combining Flows": { exp: "`zip` pairs elements one-to-one. `combine` merges latest values. `merge` interleaves multiple flows into one.", code: "runBlocking {\n    val nums = (1..3).asFlow()\n    val letters = flowOf(\"A\", \"B\", \"C\")\n\n    nums.zip(letters) { n, l -> \"$n$l\" }\n        .collect { println(it) }  // 1A, 2B, 3C\n\n    // combine — re-emits when either flow emits\n    nums.combine(letters) { n, l -> \"$n$l\" }\n        .collect { println(it) }  // 3C (latest of each)\n}" }
    },
    "Advanced Kotlin": {
        "Context Receivers": { exp: "Context receivers declare dependency on context types without inheritance. Functions can receive multiple contexts for modular APIs.", code: "interface LoggingContext {\n    fun log(msg: String)\n}\ncontext(LoggingContext)\nfun doWork() {\n    log(\"Working...\")\n}\n\nobject ConsoleLogger : LoggingContext {\n    override fun log(msg: String) = println(\"[LOG] $msg\")\n}\n\nfun main() {\n    with(ConsoleLogger) { doWork() }\n}" },
        "Contracts": { exp: "Contracts tell the compiler about function behavior. Use `callsInPlace`, `returnsNotNull`, `implies` for smarter analysis and fewer warnings.", code: "import kotlin.contracts.*\n\nfun requireNotNull(value: Any?): Any {\n    contract {\n        returns() implies (value != null)\n    }\n    if (value == null) throw IllegalArgumentException()\n    return value\n}\n\nval x: String? = \"Hello\"\nrequireNotNull(x)\nprintln(x.length)  // Smart-cast without !! thanks to contract" },
        "@JvmInline & JvmAnnotations": { exp: "`@JvmInline` marks inline classes on JVM. `@JvmStatic`, `@JvmOverloads`, `@JvmField`, `@Throws` control Kotlin-to-Java interop.", code: "@JvmInline\nvalue class Email(val value: String)\n\nclass Greeter {\n    companion object {\n        @JvmStatic\n        fun greet() = \"Hello\"\n\n        @JvmOverloads\n        fun create(name: String, age: Int = 0) = \"$name $age\"\n    }\n}\n// Java calls Greeter.greet() without Companion" },
        "Type Erasure & Reified": { exp: "JVM erases generic types at runtime. `inline` + `reified` preserves them. `@Suppress(\"UNCHECKED_CAST\")` for manual casts.", code: "inline fun <reified T> typeInfo() {\n    println(T::class.qualifiedName)\n    println(T::class.isAbstract)\n}\n\n// Erased version (won't compile without reified)\n// fun <T> erased() = T::class  // ERROR\n\ntypeInfo<String>()\ntypeInfo<List<Int>>()" },
        "Reflection": { exp: "Kotlin reflection (`::class`, `::memberProperty`) inspects types at runtime. Use `memberProperties`, `functions`, `annotations` for meta-programming.", code: "data class Person(val name: String, val age: Int)\n\nval kClass = Person::class\nprintln(kClass.simpleName)  // Person\nprintln(kClass.memberProperties)\n\nval prop = Person::name\nval person = Person(\"Alice\", 30)\nprintln(prop.get(person))  // Alice" },
        "Annotations": { exp: "Kotlin annotations: `@Target`, `@Retention`, custom annotation classes. Apply to classes, functions, properties, or expressions with `@file:`, `@get:` etc.", code: "@Target(AnnotationTarget.CLASS, AnnotationTarget.FUNCTION)\n@Retention(AnnotationRetention.RUNTIME)\nannotation class Fancy(val author: String = \"Unknown\")\n\n@Fancy(author = \"Alice\")\nclass MyService {\n    @Fancy\n    fun doWork() {}\n}\n\n// Read at runtime\nval ann = MyService::class.findAnnotation<Fancy>()\nprintln(ann?.author)" },
        "Destructuring Declarations": { exp: "Destructuring decomposes objects into variables with `componentN()` functions. Data classes auto-generate them; custom classes can implement operator fun componentN().", code: "data class Point(val x: Int, val y: Int, val z: Int)\n\nval p = Point(1, 2, 3)\nval (x, y, z) = p\nprintln(\"$x, $y, $z\")  // 1,2,3\n\n// Destructuring in lambdas\nval points = listOf(Point(1,2,3))\npoints.forEach { (x, y) -> println(\"$x, $y\") }\n\n// For Map\nfor ((key, value) in mapOf(\"a\" to 1)) {}\nval (name, age) = Person(\"Alice\", 30)" }
    },
    "Kotlin Multiplatform": {
        "KMP Overview": { exp: "Kotlin Multiplatform shares logic across platforms. Business logic, data models, and networking in commonMain; platform UIs in platform-specific source sets.", code: "plugins { kotlin(\"multiplatform\") version \"1.9.0\" }\n\nkotlin {\n    jvm()\n    iosArm64()\n    iosSimulatorArm64()\n    sourceSets {\n        commonMain.dependencies {}\n        iosMain.dependencies {}\n    }\n}" },
        "expect/actual": { exp: "`expect` declares platform-agnostic API in common code. `actual` provides platform-specific implementations. Compiler ensures all expects have actuals.", code: "// commonMain\nexpect fun platformName(): String\nexpect class Platform\n\n// androidMain\nactual fun platformName(): String = \"Android\"\nactual typealias Platform = android.os.Build\n\n// iosMain\nactual fun platformName(): String = \"iOS\"\nactual typealias Platform = UIDevice" },
        "Common Code": { exp: "CommonMain uses Kotlin stdlib-common, kotlinx.coroutines, kotlinx.serialization. Shared data models, repository logic, and ViewModels live here.", code: "// commonMain\n@Serializable\ndata class User(val id: Int, val name: String)\n\ninterface UserRepository {\n    suspend fun getUser(id: Int): User\n}\n\nclass GetUserUseCase(private val repo: UserRepository) {\n    suspend operator fun invoke(id: Int) = repo.getUser(id)\n}" },
        "Platform-Specific Code": { exp: "Platform source sets (jvmMain, iosMain, jsMain) provide actual implementations. Access platform APIs and libraries from each target.", code: "// androidMain\nactual class NotificationManager(private val ctx: Context) {\n    actual fun show(title: String, msg: String) {\n        // Android notification using NotificationCompat\n    }\n}\n\n// iosMain\nactual class NotificationManager {\n    actual fun show(title: String, msg: String) {\n        // iOS notification using UNUserNotificationCenter\n    }\n}" },
        "KMP Libraries & Frameworks": { exp: "KMP has a growing ecosystem: Ktor for networking, SQLDelight for databases, Koin for DI, kotlinx.serialization, and Compose Multiplatform for UI.", code: "// build.gradle.kts\ncommonMain.dependencies {\n    implementation(\"io.ktor:ktor-client-core:2.3.0\")\n    implementation(\"org.jetbrains.kotlinx:kotlinx-coroutines-core:1.7.0\")\n    implementation(\"org.jetbrains.kotlinx:kotlinx-serialization-json:1.5.0\")\n    implementation(\"com.squareup.sqldelight:runtime:2.0.0\")\n}" }
    },
    "Testing": {
        "kotlin.test Basics": { exp: "`kotlin.test` provides `@Test`, `assertEquals`, `assertTrue`, `assertNull`, `assertThrows`. Annotations work with JUnit 4/5, and multiplatform testing.", code: "import kotlin.test.*\n\nclass CalculatorTest {\n    @Test\n    fun `addition works`() {\n        assertEquals(4, 2 + 2)\n    }\n\n    @Test\n    fun `division throws`() {\n        assertThrows<ArithmeticException> {\n            val x = 1 / 0\n        }\n    }\n}" },
        "Kotest": { exp: "Kotest is a multiplatform test framework with behavior specs, property-based testing, matchers, and data-driven tests.", code: "class MyTest : StringSpec({\n    \"string length\".config(enabled = true) {\n        \"hello\".length shouldBe 5\n        \"hello\" shouldContain \"ell\"\n    }\n\n    \"numbers\" {\n        listOf(1, 2, 3).shouldContainExactlyInAnyOrder(3, 1, 2)\n    }\n})" },
        "MockK": { exp: "MockK is a Kotlin-first mocking library. Supports coroutines, spies, relaxed mocks, capturing, and verification with expressive DSL.", code: "class UserService(private val repo: UserRepository) {\n    suspend fun getUser(id: Int) = repo.findById(id)\n}\n\n@Test\nfun `mockk coroutine test`() = runBlocking {\n    val repo = mockk<UserRepository>(relaxed = true)\n    val service = UserService(repo)\n\n    coEvery { repo.findById(1) } returns User(1, \"Alice\")\n\n    val result = service.getUser(1)\n    assertEquals(\"Alice\", result.name)\n\n    coVerify { repo.findById(1) }\n}" },
        "Property-Based Testing": { exp: "Property testing verifies invariants across many random inputs. Kotest and kotlin-check use generators (`Arb`) and properties (`forAll`).", code: "class PropertyTest : StringSpec({\n    \"reverse reverses\" {\n        forAll<String> { str ->\n            str.reversed().reversed() == str\n        }\n    }\n\n    \"addition commutes\" {\n        forAll<Int, Int> { a, b ->\n            a + b == b + a\n        }\n    }\n})" },
        "Coroutine Testing": { exp: "`kotlinx-coroutines-test` provides `runTest`, `TestScope`, and virtual time. `advanceTimeBy`, `advanceUntilIdle` skip delays instantly.", code: "class CoroutineTest {\n    @Test\n    fun `test with virtual time`() = runTest {\n        var result = \"\"\n\n        launch {\n            delay(2000)\n            result = \"done\"\n        }\n\n        advanceTimeBy(2000)\n        assertEquals(\"done\", result)\n\n        // Or: advanceUntilIdle() — runs all pending\n    }\n\n    @Test\n    fun `test launch behavior`() = runTest {\n        var called = false\n        launch { called = true }\n        advanceUntilIdle()\n        assertTrue(called)\n    }\n}" },
        "JUnit 5 Integration": { exp: "Kotlin works seamlessly with JUnit 5. Use `@ParameterizedTest`, `@MethodSource`, `@ValueSource` with Kotlin collections and lambdas.", code: "class JUnit5Test {\n    @ParameterizedTest\n    @ValueSource(strings = [\"racecar\", \"level\", \"radar\"])\n    fun `palindromes`(word: String) {\n        assertEquals(word, word.reversed())\n    }\n\n    @TestFactory\n    fun `dynamic tests`() = listOf(1, 2, 3).map { n ->\n        DynamicTest.dynamicTest(\"Test $n squared\") {\n            assertEquals(n * n, n * n)\n        }\n    }\n}" }
    },
    "Build & Tooling": {
        "Gradle Kotlin DSL": { exp: "Gradle Kotlin DSL provides type-safe build scripts with autocomplete and refactoring. Use `build.gradle.kts` with `plugins { }` and `dependencies { }`.", code: "plugins {\n    kotlin(\"jvm\") version \"1.9.22\"\n    application\n}\n\napplication {\n    mainClass.set(\"com.example.MainKt\")\n}\n\ndependencies {\n    implementation(kotlin(\"stdlib\"))\n    testImplementation(kotlin(\"test\"))\n}\n\nkotlin {\n    jvmToolchain(17)\n}" },
        "Version Catalogs": { exp: "Version catalogs in `gradle/libs.versions.toml` centralise dependency versions. Access via `libs.some.dependency` in build scripts.", code: "# gradle/libs.versions.toml\n[versions]\nkotlin = \"1.9.22\"\nkotlinx-coroutines = \"1.7.3\"\n\n[libraries]\nkotlinx-coroutines-core = { module = \"org.jetbrains.kotlinx:kotlinx-coroutines-core\", version.ref = \"kotlinx-coroutines\" }\n\n# build.gradle.kts\ndependencies {\n    implementation(libs.kotlinx.coroutines.core)\n}" },
        "buildSrc & Convention Plugins": { exp: "buildSrc stores shared build logic as Kotlin sources. Convention plugins apply common configurations across modules — DRY for multi-module projects.", code: "// buildSrc/src/main/kotlin/kotlin-common.gradle.kts\nplugins {\n    kotlin(\"jvm\")\n}\ndependencies {\n    testImplementation(kotlin(\"test\"))\n}\ntasks.withType<Test> {\n    useJUnitPlatform()\n}\n\n// In module's build.gradle.kts\nplugins {\n    id(\"kotlin-common\")\n}" },
        "KSP & K2 Compiler": { exp: "KSP (Kotlin Symbol Processing) is a compiler plugin for annotation processing. K2 compiler (1.9+) offers faster compilation and improved type inference.", code: "plugins {\n    id(\"com.google.devtools.ksp\") version \"1.9.22-1.0.17\"\n}\n\n// K2 compiler (gradle.properties)\nkotlin.experimental.tryK2=true\n\n// Or explicitly:\ntasks.withType<org.jetbrains.kotlin.gradle.tasks.KotlinCompile> {\n    kotlinOptions.jvmTarget = \"17\"\n    kotlinOptions.freeCompilerArgs += \"-Xcompiler-collect-comments\"\n}" },
        "Dokka & Tooling": { exp: "Dokka generates Kotlin documentation (KDoc). Supports multiplatform projects, HTML/MD/JavaDoc outputs. Often used with Gradle's `dokka` plugin.", code: "plugins {\n    id(\"org.jetbrains.dokka\") version \"1.9.10\"\n}\n\ntasks.dokkaHtml {\n    moduleName.set(\"My Library\")\n    outputDirectory.set(buildDir.resolve(\"dokka\"))\n}\n\n// KDoc style\n/**\n * Calculates sum of two integers.\n * @param a first addend\n * @param b second addend\n * @return sum of a and b\n */\nfun sum(a: Int, b: Int) = a + b" }
    },
    "Kotlin Idioms & DSL": {
        "Scope Functions Deep": { "exp": "Kotlin's five scope functions differ by receiver (this vs it) and return value (context vs lambda result). `let` for transformations, `apply` for configuration, `run` for computations, `with` for grouping calls, `also` for side effects.", "code": "data class Person(var name: String, var age: Int)\n\nval alice = Person(\"Alice\", 30)\n\n// let: lambda result, 'it' as receiver. Use for null checks + transformations\nval greeting = alice?.let { \"Hello, ${it.name}!\" }\nprintln(greeting)\n\n// apply: context object, 'this' as receiver. Use for configuration\nval bob = Person(\"Bob\", 0).apply {\n    age = 25  // this.age — 'this' is implicit\n    name = \"Robert\"\n}\nprintln(bob)\n\n// run: lambda result, 'this' as receiver. Use for computations\nval description = bob.run {\n    \"${name} is ${age} years old\"\n}\nprintln(description)\n\n// with: non-extension, 'this' as receiver. Use for grouping\nval info = with(bob) {\n    \"Name: $name, Age: $age\"\n}\nprintln(info)\n\n// also: context object, 'it' as receiver. Use for side effects\nval numbers = mutableListOf(1, 2, 3).also {\n    println(\"Adding to list: $it\")\n    it.add(4)\n}\nprintln(numbers)" },
        "Lambda with Receiver": { "exp": "Lambda with receiver allows calling methods on an implicit `this` inside the lambda. The `receiver` type is the context. Used by `apply`, `run`, `with`, `buildString`, and custom DSLs. From Kotlin in Action.", "code": "// buildString uses lambda with StringBuilder receiver\nval html = buildString {\n    append(\"<!DOCTYPE html>\\n\")\n    appendLine(\"<html>\")\n    appendLine(\"  <body>\")\n    append(\"    <h1>Hello</h1>\\n\")\n    appendLine(\"  </body>\")\n    append(\"</html>\")\n}\nprintln(html)\n\n// Custom builder using lambda with receiver\nclass HtmlBuilder {\n    private val sb = StringBuilder()\n    fun tag(name: String, content: String) {\n        sb.appendLine(\"<$name>$content</$name>\")\n    }\n    fun build(): String = sb.toString()\n}\n\nfun html(init: HtmlBuilder.() -> Unit): String {\n    val builder = HtmlBuilder()\n    builder.init()\n    return builder.build()\n}\n\nval page = html {\n    tag(\"h1\", \"Welcome\")     // 'this' is HtmlBuilder\n    tag(\"p\", \"Kotlin DSLs!\")\n}\nprintln(page)" },
        "Type-Safe Builders & @DslMarker": { "exp": "Type-safe builders (from Kotlin in Action) use nested lambdas with receivers. `@DslMarker` restricts implicit receivers to the nearest enclosing scope, preventing ambiguous `this` in nested builders.", "code": "@DslMarker\nannotation class HtmlMarker\n\n@HtmlMarker\nclass TableBuilder {\n    private val rows = mutableListOf<String>()\n    fun tr(init: RowBuilder.() -> Unit) {\n        rows.add(RowBuilder().apply(init).build())\n    }\n    fun build() = \"\"\"\n        <table>\n            ${rows.joinToString(\"\\n\")}\n        </table>\n    \"\"\".trimIndent()\n}\n\n@HtmlMarker\nclass RowBuilder {\n    private val cells = mutableListOf<String>()\n    fun td(content: String) { cells.add(\"<td>$content</td>\") }\n    fun build() = \"<tr>${cells.joinToString(\"\")}</tr>\"\n}\n\nfun table(init: TableBuilder.() -> Unit): String {\n    return TableBuilder().apply(init).build()\n}\n\n// Usage — @DslMarker prevents accessing outer receiver\nval rendered = table {\n    tr {\n        td(\"Name\")  // only RowBuilder methods available\n        td(\"Age\")   // td on RowBuilder, not TableBuilder\n    }\n    tr {\n        td(\"Alice\")\n        td(\"30\")\n    }\n}\nprintln(rendered)" },
        "SAM Conversion": { "exp": "Single Abstract Method (SAM) conversion lets you pass a lambda where a Java functional interface is expected. Kotlin has built-in function types, so SAM conversion mainly applies to Java interop. From Head First Kotlin.", "code": "// Java interface\n// public interface OnClickListener {\n//     void onClick(View v);\n// }\n\n// Without SAM conversion (object expression)\nbutton.setOnClickListener(object : OnClickListener {\n    override fun onClick(v: View) {\n        println(\"Clicked!\")\n    }\n})\n\n// With SAM conversion (lambda — works if method expects Java SAM interface)\nbutton.setOnClickListener { v -> println(\"Clicked!\") }\n\n// Kotlin function types don't need SAM — use lambda directly\nval callback: (String) -> Unit = { println(it) }\n\n// With Kotlin functional interface (fun interface)\nfun interface StringTransformer {\n    fun transform(input: String): String\n}\n\nval upper = StringTransformer { it.uppercase() }\nval reversed = StringTransformer { it.reversed() }\nprintln(upper.transform(\"hello\"))  // HELLO\nprintln(reversed.transform(\"hello\"))  // olleh" },
        "Java Interop": { "exp": "Kotlin is 100% interoperable with Java. Key annotations: `@JvmStatic` for static methods, `@JvmOverloads` for overloaded methods, `@JvmField` for fields, `@Throws` for checked exceptions, `@JvmName` to resolve signature clashes.", "code": "// Kotlin side\nclass InteropDemo {\n    companion object {\n        @JvmStatic\n        fun staticMethod() = \"Called from Java statically\"\n\n        @JvmOverloads\n        fun greet(name: String, greeting: String = \"Hello\") = \"$greeting, $name\"\n    }\n\n    @JvmField\n    val instanceField = \"Direct field access\"\n\n    @Throws(IOException::class)\n    fun riskyOperation() {\n        throw IOException(\"Checked exception in Java\")\n    }\n\n    // Resolve signature clash\n    @JvmName(\"findByName\")\n    fun find(name: String): Person? = null\n\n    fun find(id: Int): Person? = null\n}\n\n// Java calls:\n// InteropDemo.staticMethod();               // @JvmStatic\n// InteropDemo.greet(\"Alice\");               // @JvmOverloads\n// InteropDemo.greet(\"Alice\", \"Hi\");\n// String f = demo.instanceField;            // @JvmField\n// try { demo.riskyOperation(); }            // @Throws\n\n// Platform types — String! means nullable unknown\nval list = mutableListOf<String>()\nlist.add(\"Hello\")\nval item: String? = list[0]  // platform type, treat as nullable" },
        "Kotlin Idioms": { "exp": "Common Kotlin idioms from Head First Kotlin and Kotlin in Action: default arguments for builders, `require`/`check` for preconditions, `use` for Closeable, `takeIf`/`takeUnless` for conditional returns, `TODO()` for stubs, `runCatching` for try-catch expressions.", "code": "// require — validate arguments\nfun factorial(n: Int): Int {\n    require(n >= 0) { \"n must be non-negative, got $n\" }\n    return if (n <= 1) 1 else n * factorial(n - 1)\n}\n\n// use — auto-close Closeable\nfun readFile(path: String): String {\n    return BufferedReader(FileReader(path)).use { it.readText() }\n}\n\n// takeIf / takeUnless — conditional return\nval adult = Person(\"Alice\", 30).takeIf { it.age >= 18 }\nprintln(adult)\n\n// runCatching — expression-based error handling\nval result = runCatching {\n    \"42\".toInt()\n}.fold(\n    onSuccess = { \"Parsed: $it\" },\n    onFailure = { \"Error: ${it.message}\" }\n)\nprintln(result)\n\n// TODO — placeholder that always throws\nfun notImplemented(): Nothing = TODO(\"Implement this later\")\n\n// apply for object construction\nval config = mutableMapOf<String, Any>().apply {\n    put(\"host\", \"localhost\")\n    put(\"port\", 8080)\n    put(\"debug\", true)\n}" }
    },
    "Android Development": {
        "Jetpack Compose Basics": { exp: "Jetpack Compose is a declarative UI toolkit. State-driven, composable functions with `@Composable`, `remember`, `mutableStateOf`, and modifiers.", code: "@Composable\nfun Greeting(name: String) {\n    var count by remember { mutableStateOf(0) }\n\n    Column(modifier = Modifier.padding(16.dp)) {\n        Text(\"Hello, $name!\")\n        Text(\"Count: $count\")\n        Button(onClick = { count++ }) {\n            Text(\"Increment\")\n        }\n    }\n}" },
        "ViewModel & State": { exp: "ViewModel survives configuration changes. Use `viewModel()` in Compose, expose `StateFlow`, and collect as Compose state with `collectAsState()`.", code: "class CounterViewModel : ViewModel() {\n    private val _count = MutableStateFlow(0)\n    val count: StateFlow<Int> = _count\n\n    fun increment() { _count.value++ }\n}\n\n@Composable\nfun CounterScreen(viewModel: CounterViewModel = viewModel()) {\n    val count by viewModel.count.collectAsState()\n\n    Text(\"Count: $count\")\n    Button(onClick = { viewModel.increment() }) {\n        Text(\"+1\")\n    }\n}" },
        "LiveData & StateFlow": { exp: "LiveData is lifecycle-aware. StateFlow is the coroutines-native alternative. Use `asLiveData()` to bridge StateFlow to LiveData for legacy code.", code: "class MyViewModel : ViewModel() {\n    private val _data = MutableLiveData<String>()\n    val data: LiveData<String> = _data\n\n    private val _state = MutableStateFlow(\"initial\")\n    val state: StateFlow<String> = _state\n\n    fun load() {\n        viewModelScope.launch {\n            _data.value = \"Loading\"\n            _state.value = \"Loading\"\n            val result = repository.fetch()\n            _data.value = result\n            _state.value = result\n        }\n    }\n}\n\n// StateFlow to LiveData bridge\nval liveData: LiveData<String> = stateFlow.asLiveData()" },
        "Room Database": { exp: "Room is a SQLite ORM. Use `@Entity`, `@Dao`, `@Database`. Coroutines support via suspend functions and Flow return types in DAOs.", code: "@Entity\ndata class User(\n    @PrimaryKey val id: Int,\n    val name: String,\n    val email: String\n)\n\n@Dao\ninterface UserDao {\n    @Query(\"SELECT * FROM user\")\n    fun getAll(): Flow<List<User>>\n\n    @Insert\n    suspend fun insert(user: User)\n\n    @Delete\n    suspend fun delete(user: User)\n}\n\n@Database(entities = [User::class], version = 1)\nabstract class AppDatabase : RoomDatabase() {\n    abstract fun userDao(): UserDao\n}" },
        "Retrofit & Networking": { exp: "Retrofit type-safe HTTP client used with Kotlin coroutines. Define `suspend` functions in API interfaces. Use kotlinx.serialization or Moshi for JSON.", code: "interface ApiService {\n    @GET(\"users/{id}\")\n    suspend fun getUser(@Path(\"id\") id: Int): User\n\n    @POST(\"users\")\n    suspend fun createUser(@Body user: User): User\n}\n\nobject RetrofitClient {\n    val instance: ApiService by lazy {\n        Retrofit.Builder()\n            .baseUrl(\"https://api.example.com/\")\n            .addConverterFactory(jsonConverterFactory)\n            .build()\n            .create(ApiService::class.java)\n    }\n}\n\nclass UserRepo(private val api: ApiService) {\n    suspend fun getUser(id: Int) = api.getUser(id)\n}" },
        "Navigation Compose": { exp: "Navigation Compose provides type-safe navigation in Compose. Define routes with `NavHost`, navigate with `navController.navigate()`, pass safe args.", code: "sealed class Screen(val route: String) {\n    object Home : Screen(\"home\")\n    object Profile : Screen(\"profile/{userId}\") {\n        fun createRoute(userId: Int) = \"profile/$userId\"\n    }\n}\n\n@Composable\nfun AppNavigation() {\n    val navController = rememberNavController()\n    NavHost(navController, startDestination = Screen.Home.route) {\n        composable(Screen.Home.route) {\n            HomeScreen(onProfile = { id ->\n                navController.navigate(Screen.Profile.createRoute(id))\n            })\n        }\n        composable(\n            route = Screen.Profile.route,\n            arguments = listOf(navArgument(\"userId\") { type = NavType.IntType })\n        ) { backStackEntry ->\n            val userId = backStackEntry.arguments?.getInt(\"userId\") ?: 0\n            ProfileScreen(userId)\n        }\n    }\n}" }
    }
};

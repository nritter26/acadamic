courseData.ts = {
    "Fundamentals": {
        "What is TypeScript": { exp: "TypeScript is a typed superset of JavaScript that adds static type checking and enhanced developer tooling. It compiles to plain JS, catching errors at compile time rather than runtime. A common mistake is treating TypeScript as a completely different language rather than JS with types.", code: "const message: string = 'Hello TypeScript';\nconsole.log(message);\n// Compile with: tsc file.ts" },
        "Type Annotations": { exp: "Annotations declare the intended type of variables, parameters, and return values. The compiler checks that values match their annotations. A common mistake is over-annotating when TypeScript can infer the type, or forgetting to annotate function parameters.", code: "function greet(name: string): string {\n  return 'Hello, ' + name;\n}\ngreet(42); // Error: Argument of type 'number' not assignable to string" },
        "Basic Types": { exp: "TypeScript's basic types mirror JavaScript primitives: string, number, boolean, null, undefined, void, never, any, bigint, and symbol. A common mistake is assuming null/undefined are assignable to every type — they aren't with strictNullChecks enabled.", code: "let name: string = 'Alice';\nlet age: number = 30;\nlet isActive: boolean = true;\nlet big: bigint = 100n;\nlet sym: symbol = Symbol('key');" },
        "unknown vs any": { exp: "any disables type checking entirely — it's an escape hatch that should be avoided. unknown is the type-safe counterpart: you must narrow it via typeof, instanceof, or type guards before use. Common mistake: using any instead of unknown when the type truly isn't known.", code: "let data: unknown = JSON.parse('{\"a\":1}');\n// data.a; // Error: Object is of type 'unknown'\nif (typeof data === 'object' && data !== null) {\n  console.log(data);\n}\n// any: let x: any = 5; x(); // no error" },
        "Enums": { exp: "Enums define named numeric or string constants. Numeric enums auto-increment from 0; string enums require explicit values. Use const enum to avoid runtime overhead, but beware that const enums cannot be used with isolatedModules. Common mistake: assuming enums are just strings — numeric enums have reverse mappings.", code: "enum Direction {\n  Up,    // 0\n  Down,  // 1\n  Left,  // 2\n  Right  // 3\n}\nconsole.log(Direction.Up); // 0\nconsole.log(Direction[0]); // 'Up' (reverse mapping)\n\nenum Status {\n  Success = 'SUCCESS',\n  Failure = 'FAILURE'\n}" },
        "Type Inference": { exp: "TypeScript infers types from values when no annotation is given. Inference works for variable initialization, return values, and generic calls. A common mistake is assuming inference is always correct — complex scenarios may need explicit annotations for clarity.", code: "let count = 0;           // inferred as number\nconst name = 'Alice';     // inferred as 'Alice' (literal type)\nfunction add(a: number, b: number) {\n  return a + b;            // inferred as number\n}\nlet items = [];           // inferred as never[] — may need annotation" },
        "Literal Types": { exp: "Literal types restrict a variable to an exact string, number, or boolean value. Combine with union types for constrained options. A common mistake is thinking any string matching the literal format is assignable — the value must be exactly the literal.", code: "type Direction = 'up' | 'down' | 'left' | 'right';\nlet dir: Direction = 'up';\ndir = 'diagonal'; // Error\n\ntype Port = 80 | 443 | 8080;\nconst port: Port = 443;" },
        "Strict Mode": { exp: "Enable strict: true in tsconfig to activate all strict checks: noImplicitAny, strictNullChecks, strictFunctionTypes, strictBindCallApply, strictPropertyInitialization, noImplicitThis, and alwaysStrict. A common mistake is starting a project without strict mode and adding it later — much harder to migrate.", code: "// tsconfig.json\n{\n  \"compilerOptions\": {\n    \"strict\": true\n  }\n}\n// With strictNullChecks:\nlet x: string | null = null;\nx.length; // Error: Object is possibly 'null'" }
    },
    "Type System": {
        "Union Types": { exp: "Union types allow a value to be one of several types. Use them for branching data like API responses. A common mistake is forgetting to narrow the type before accessing type-specific properties — TypeScript will force you to check first.", code: "let id: string | number;\nid = 'abc123';\nid = 42;\n// id.toUpperCase(); // Error: can't call on number\nif (typeof id === 'string') {\n  id.toUpperCase(); // OK: narrowed to string\n}" },
        "Intersection Types": { exp: "Intersection types combine multiple types into one, requiring all properties from each type. Use & to merge object types. A common mistake is intersecting primitives (like string & number), which results in never.", code: "type HasName = { name: string };\ntype HasAge = { age: number };\ntype Person = HasName & HasAge;\nconst p: Person = { name: 'Alice', age: 30 };\n// type Impossible = string & number; // never" },
        "Type Aliases": { exp: "Type aliases create reusable names for unions, tuples, objects, or primitives. Unlike interfaces, aliases can represent unions and primitives but cannot be extended or declaration-merged. A common mistake is using type when interface would be more appropriate for object shapes.", code: "type Point = { x: number; y: number };\ntype ID = string | number;\ntype Pair<T> = [T, T];\ntype Callback = (data: string) => void;" },
        "Interfaces": { exp: "Interfaces describe object shapes and support declaration merging, extends, and implements. Prefer interfaces for public API object types. A common mistake is forgetting that interfaces are open — declaring the same interface twice merges them rather than erroring.", code: "interface User {\n  name: string;\n  age: number;\n}\ninterface Admin extends User {\n  role: string;\n}\n// Declaration merging:\ninterface User { email?: string; }\n// User now has name, age, and email" },
        "Structural Typing": { exp: "TypeScript uses structural (duck) typing: type compatibility is determined by shape, not name. Two types with identical members are interchangeable. A common mistake is expecting nominal typing behavior where type names should differ.", code: "interface Point { x: number; y: number; }\ninterface Coord { x: number; y: number; }\nconst p: Point = { x: 1, y: 2 };\nconst c: Coord = p; // OK: same structure\n\nclass Car { drive() {} }\nclass Boat { drive() {} }\nconst v: Car = new Boat(); // OK: same shape" },
        "Type Assertions": { exp: "Type assertions (as) tell TypeScript you know more about a type than it does. They do not perform runtime checks. A common mistake is using assertions to silence errors instead of fixing the real type issue. Prefer 'as' over angle-bracket syntax in JSX.", code: "const input = document.getElementById('myInput') as HTMLInputElement;\ninput.value = 'hello';\n\nconst value = JSON.parse('42') as number;\n// No runtime check — if JSON is wrong, assertion lies" },
        "Branded Types": { exp: "Branded types add a phantom property to simulate nominal typing. This prevents mixing structurally compatible types like UserId and OrderId. A common mistake is forgetting that brands are compile-time only — they have no runtime effect.", code: "type UserId = string & { readonly __brand: 'UserId' };\ntype OrderId = string & { readonly __brand: 'OrderId' };\nfunction getUser(id: UserId) { /* ... */ }\nconst uid = 'abc123' as UserId;\ngetUser(uid);\n// getUser('abc123'); // Error: not branded" },
        "satisfies Operator": { exp: "The satisfies operator validates that a value matches a type without widening the inferred type. This preserves the narrowest type for autocomplete and type-checking. A common mistake is using a type annotation instead, which widens the type.", code: "const palette = {\n  red: [255, 0, 0],\n  green: '#00ff00',\n} satisfies Record<string, string | number[]>;\n// palette.red inferred as number[], not string | number[]\npalette.red.map(x => x); // OK, typed as number[]" }
    },
    "Functions": {
        "Function Types": { exp: "Function types describe callable signatures: parameter types and return type. Use type aliases or inline syntax. A common mistake is forgetting that callbacks can have optional parameters and void return types are assignable from any return value.", code: "type BinaryFn = (a: number, b: number) => number;\nconst add: BinaryFn = (x, y) => x + y;\n\ntype LogFn = (msg: string) => void;\nconst log: LogFn = (msg) => console.log(msg);" },
        "Optional & Default Parameters": { exp: "Mark parameters as optional with ? or provide default values. Optional params must follow required ones. Default parameters are always optional and infer their type from the default value. A common mistake is mixing up order — optional params can't precede required ones.", code: "function greet(name?: string): string {\n  return `Hello, ${name ?? 'Guest'}`;\n}\nfunction multiply(a: number, b: number = 1): number {\n  return a * b;\n}\ngreet();      // 'Hello, Guest'\nmultiply(5);  // 5" },
        "Generic Functions": { exp: "Generic functions capture the caller's type argument and preserve it through the return type. TypeScript often infers the type parameter from arguments. A common mistake is not constraining the type parameter when you need to access properties on the generic value.", code: "function first<T>(arr: T[]): T | undefined {\n  return arr[0];\n}\nconst num = first([1, 2, 3]); // T inferred as number\nconst str = first(['a', 'b']); // T inferred as string" },
        "Function Overloads": { exp: "Overloads declare multiple call signatures for a single implementation. The implementation signature must be compatible with all overloads but is not callable directly. A common mistake is having an implementation signature that can't satisfy all overloads.", code: "function combine(a: string, b: string): string;\nfunction combine(a: number, b: number): number;\nfunction combine(a: any, b: any): any {\n  return a + b;\n}\ncombine('a', 'b'); // string\ncombine(1, 2);     // number\n// combine(1, 'b'); // Error: no matching overload" },
        "Rest Parameters": { exp: "Rest parameters collect remaining arguments into an array. Type them as an array type. TypeScript also supports typed tuples for rest parameters in variadic patterns. A common mistake is thinking rest must be the last parameter — it must be the only last parameter.", code: "function sum(...numbers: number[]): number {\n  return numbers.reduce((a, b) => a + b, 0);\n}\nsum(1, 2, 3, 4); // 10\n\nfunction logValues(...args: [string, ...number[]]) {\n  console.log(args[0], args.slice(1));\n}\nlogValues('scores', 95, 87);" },
        "this Parameters": { exp: "TypeScript uses a fake first parameter named 'this' to type the function's context. This catches errors where 'this' is used incorrectly, common in callbacks and event handlers. A common mistake is forgetting to bind 'this' or using arrow functions when a dynamic 'this' is needed.", code: "function onClick(this: HTMLButtonElement, event: MouseEvent) {\n  this.disabled = true;\n}\ndocument.querySelector('button')!.addEventListener('click', onClick);\n\nconst obj = {\n  data: [1, 2, 3],\n  process(this: { data: number[] }) {\n    return this.data.map(x => x * 2);\n  }\n};" },
        "Async Functions": { exp: "Async functions always return Promise<T> where T is the resolved type. Annotate the return type as Promise<T> for clarity. A common mistake is forgetting that async functions wrap the returned value in Promise — you must await or .then() to get the value.", code: "async function fetchData(): Promise<{ id: number }> {\n  const res = await fetch('/api/data');\n  return res.json();\n}\n// Return type is Promise<{ id: number }>, not { id: number }\nfetchData().then(data => console.log(data.id));" },
        "void & never Return Types": { exp: "void means the function completes normally but returns nothing meaningful (actually returns undefined). never means the function never completes — it always throws or loops forever. A common mistake is assigning void to something expecting a value, or thinking never is the same as void.", code: "function log(msg: string): void {\n  console.log(msg);\n  // returns undefined implicitly\n}\nfunction fail(msg: string): never {\n  throw new Error(msg);\n}\nfunction infinite(): never {\n  while (true) {}\n}\n// never is assignable to every type; void is not" }
    },
    "Classes & OOP": {
        "Classes": { exp: "TypeScript classes add type annotations to fields, constructors, and methods. Fields must be declared with types or initialized. A common mistake is forgetting to initialize a field or declare it — the strictPropertyInitialization check will catch this.", code: "class Person {\n  name: string;\n  age: number;\n  constructor(name: string, age: number) {\n    this.name = name;\n    this.age = age;\n  }\n  greet(): string {\n    return `Hi, I'm ${this.name}`;\n  }\n}" },
        "Inheritance": { exp: "Use extends to create subclasses. Call super() before accessing this in the constructor. A common mistake is forgetting super() entirely or calling super() after accessing this. Use the override keyword to explicitly mark method overrides.", code: "class Animal {\n  constructor(public name: string) {}\n  speak(): void {\n    console.log(`${this.name} makes a sound`);\n  }\n}\nclass Dog extends Animal {\n  constructor(name: string, public breed: string) {\n    super(name);\n  }\n  override speak(): void {\n    console.log(`${this.name} barks`);\n  }\n}" },
        "Access Modifiers": { exp: "public (default), private, and protected control member visibility. private prevents access outside the class; protected allows access in subclasses. A common mistake is relying on these for runtime security — they are compile-time only and erased at runtime.", code: "class BankAccount {\n  private balance: number = 0;\n  protected owner: string;\n  constructor(owner: string) { this.owner = owner; }\n  deposit(amount: number): void {\n    this.balance += amount;\n  }\n  getBalance(): number {\n    return this.balance;\n  }\n}\n// Also: readonly prevents reassignment but not mutation" },
        "Abstract Classes": { exp: "Abstract classes define shared behavior with abstract methods that subclasses must implement. They cannot be instantiated directly. A common mistake is trying to use new on an abstract class or forgetting to implement all abstract methods in a subclass.", code: "abstract class Shape {\n  abstract getArea(): number;\n  describe(): string {\n    return `Area: ${this.getArea()}`;\n  }\n}\nclass Circle extends Shape {\n  constructor(private radius: number) { super(); }\n  getArea(): number {\n    return Math.PI * this.radius ** 2;\n  }\n}\n// new Shape(); // Error: cannot instantiate abstract class" },
        "Interfaces with Classes": { exp: "Classes can implement interfaces to guarantee a shape. A class can implement multiple interfaces. Unlike extends, implements doesn't inherit implementation, only enforces shape. A common mistake is thinking implements provides default implementations — it only enforces type contracts.", code: "interface Serializable {\n  serialize(): string;\n}\ninterface Loggable {\n  log(): void;\n}\nclass User implements Serializable, Loggable {\n  constructor(private name: string) {}\n  serialize(): string {\n    return JSON.stringify({ name: this.name });\n  }\n  log(): void {\n    console.log(this.name);\n  }\n}" },
        "Static Members": { exp: "Static properties and methods belong to the class, not instances. Access them via ClassName.property. A common mistake is trying to access static members through an instance or using this inside a static method to access instance members.", code: "class Config {\n  static apiUrl: string = 'https://api.example.com';\n  static getTimeout(): number {\n    return 5000;\n  }\n  static isProduction(): boolean {\n    return Config.apiUrl.includes('production');\n  }\n}\nconsole.log(Config.apiUrl);\nconsole.log(Config.getTimeout());\n// const c = new Config();\n// c.apiUrl; // Error" },
        "Parameter Properties": { exp: "Parameter properties combine declaration and assignment in one step. Prefix a constructor parameter with public, private, protected, or readonly to auto-create the field. A common mistake is forgetting that the access modifier is required — without it, it's just a regular parameter, not a property.", code: "class Person {\n  constructor(\n    public name: string,\n    private age: number,\n    protected id: string,\n    readonly email: string\n  ) {}\n}\n// Equivalent to:\n// class Person {\n//   public name: string;\n//   private age: number;\n//   protected id: string;\n//   readonly email: string;\n//   constructor(name: string, age: number, id: string, email: string) {\n//     this.name = name; this.age = age; this.id = id; this.email = email;\n//   }\n// }" },
        "Mixins": { exp: "Mixins compose behavior from multiple classes using constructor type patterns. TypeScript doesn't support multiple inheritance, but mixins provide a composable alternative. A common mistake is not properly typing the constructor return type or forgetting to call super in a mixin.", code: "type Constructor<T = {}> = new (...args: any[]) => T;\n\nfunction Timestamped<TBase extends Constructor>(Base: TBase) {\n  return class extends Base {\n    timestamp = new Date();\n  };\n}\nfunction Activatable<TBase extends Constructor>(Base: TBase) {\n  return class extends Base {\n    isActive = false;\n    toggle() { this.isActive = !this.isActive; }\n  };\n}\nclass User { name: string = ''; }\nconst TimestampedUser = Timestamped(Activatable(User));\nconst user = new TimestampedUser();\nconsole.log(user.timestamp, user.isActive);" }
    },
    "Generics": {
        "Generic Types": { exp: "Generic types parameterize types with type variables, enabling reusable, type-safe containers. Type variables are placeholders for actual types determined at use. A common mistake is not providing enough type parameters or omitting them entirely when inference fails.", code: "type Box<T> = { value: T };\ntype Pair<T, U> = { first: T; second: U };\ntype Result<T> = { data: T; error?: string };\n\nconst box: Box<number> = { value: 42 };\nconst pair: Pair<string, number> = { first: 'a', second: 1 };" },
        "Constraints": { exp: "Use extends to constrain type parameters to types with specific properties. This allows the generic implementation to safely access those properties. A common mistake is not constraining when you need to call methods on the generic value.", code: "interface HasLength { length: number }\nfunction logLength<T extends HasLength>(arg: T): number {\n  console.log(arg.length);\n  return arg.length;\n}\nlogLength('hello');     // 5\nlogLength([1, 2, 3]);   // 3\n// logLength(123); // Error: number doesn't have length" },
        "Utility Types": { exp: "TypeScript provides built-in utility types: Partial<T>, Required<T>, Readonly<T>, Record<K,T>, Pick<T,K>, Omit<T,K>, Exclude<T,U>, Extract<T,U>, NonNullable<T>, and more. They reduce boilerplate for common type transformations.", code: "interface User {\n  name: string;\n  age: number;\n  email: string;\n}\ntype PartialUser = Partial<User>;    // all optional\ntype JustName = Pick<User, 'name'>;   // { name: string }\ntype WithoutEmail = Omit<User, 'email'>;\ntype NameOrAge = Pick<User, 'name' | 'age'>;\nconst record: Record<string, number> = { a: 1, b: 2 };" },
        "Generic Classes": { exp: "Generic classes maintain type parameters across all members and methods. The type parameter is fixed at construction time. A common mistake is not specifying the type parameter when creating an instance, causing inference from the constructor args.", code: "class Stack<T> {\n  private items: T[] = [];\n  push(item: T): void {\n    this.items.push(item);\n  }\n  pop(): T | undefined {\n    return this.items.pop();\n  }\n  peek(): T | undefined {\n    return this.items[this.items.length - 1];\n  }\n}\nconst numStack = new Stack<number>();\nnumStack.push(1);\n// numStack.push('a'); // Error" },
        "Conditional Types": { exp: "Conditional types select a type based on a condition using T extends U ? X : Y. They distribute over union types when the checked type is a bare type parameter. A common mistake is forgetting about distribution — conditional types work on each member of a union separately.", code: "type IsString<T> = T extends string ? true : false;\ntype A = IsString<'hello'>; // true\ntype B = IsString<42>;       // false\ntype C = IsString<string | number>;\n// boolean (distributes: true | false)\n\ntype ArrayElement<T> = T extends (infer U)[] ? U : never;\ntype El = ArrayElement<string[]>; // string" },
        "Mapped Types": { exp: "Mapped types transform each property of a type using { [K in keyof T]: T[K] }. Add modifiers like ? and readonly to create variations. A common mistake is forgetting that mapped types preserve modifiers by default — use -? and -readonly to remove them.", code: "type Nullable<T> = {\n  [K in keyof T]: T[K] | null;\n};\ntype Mutable<T> = {\n  -readonly [K in keyof T]: T[K];\n};\ninterface Point { x: number; y: number; }\ntype NullablePoint = Nullable<Point>;\n// { x: number | null; y: number | null }" },
        "Template Literal Types": { exp: "Template literal types construct string types at the type level using embedded type placeholders. Combined with union types, they generate all possible combinations. A common mistake is thinking they work at runtime — they are purely compile-time constructs.", code: "type EventName<T extends string> = `on${Capitalize<T>}`;\ntype Color = 'red' | 'green' | 'blue';\ntype DarkColor = `dark-${Color}`;\n// 'dark-red' | 'dark-green' | 'dark-blue'\ntype ClickEvent = EventName<'click'>; // 'onClick'\ntype Events = EventName<'click' | 'focus'>;\n// 'onClick' | 'onFocus'" },
        "infer Keyword": { exp: "The infer keyword introduces a type variable within a conditional type's extends clause, capturing a type from a larger structure. It powers most advanced utility types like ReturnType. A common mistake is using infer outside a conditional type — it's only valid in the extends clause.", code: "type ReturnType<T> = T extends (...args: any[]) => infer R ? R : never;\ntype Fn = (x: number) => string;\ntype R = ReturnType<Fn>; // string\n\ntype FirstParam<T> = T extends (first: infer F, ...args: any[]) => any ? F : never;\ntype P = FirstParam<(name: string, age: number) => void>; // string\n\ntype PromiseValue<T> = T extends Promise<infer V> ? V : T;\ntype PV = PromiseValue<Promise<number>>; // number" }
    },
    "Advanced Types": {
        "Conditional Types": { exp: "Advanced conditional types enable type-level branching and filtering. They distribute over unions unless wrapped with brackets. Use them for building utilities like Exclude and Extract. A common mistake is assuming conditionals work at runtime — they are purely compile-time.", code: "type IsArray<T> = T extends any[] ? true : false;\ntype Check = IsArray<string | number[]>; // boolean (distributed)\n\ntype NonNullable<T> = T extends null | undefined ? never : T;\ntype Clean = NonNullable<string | null | undefined>; // string\n\ntype ElementType<T> = T extends (infer U)[] ? U : never;\ntype E = ElementType<string[]>; // string" },
        "Mapped Types": { exp: "Advanced mapped types iterate over object keys with keyof. Use the as clause for key remapping and combine with conditional types to filter keys. A common mistake is not accounting for readonly and optional modifiers that are preserved by default.", code: "type Getters<T> = {\n  [K in keyof T as `get${Capitalize<string & K>}`]: () => T[K]\n};\ninterface Person { name: string; age: number; }\ntype PersonGetters = Getters<Person>;\n// { getName: () => string; getAge: () => number; }\n\ntype OnlyString<T> = {\n  [K in keyof T as T[K] extends string ? K : never]: T[K]\n};" },
        "Indexed Access Types": { exp: "Indexed access types (T[K]) retrieve the type of a property at key K. Use T[keyof T] for all values or T[number] for array elements. A common mistake is using a key that doesn't exist on the type, which produces a compile-time error.", code: "interface Person {\n  name: string;\n  age: number;\n  address: { city: string; zip: string };\n}\ntype NameType = Person['name'];              // string\ntype AgeOrName = Person['name' | 'age'];      // string | number\ntype AddressType = Person['address'];         // { city: string; zip: string }\ntype Values = Person[keyof Person];           // string | number | { city: string; zip: string }\ntype Element = string[number];                  // string" },
        "Type Guards": { exp: "Type guards narrow types in conditional blocks using x is T predicates. Use typeof, instanceof, or custom functions. A common mistake is writing guards that are too broad or forgetting that in operator also acts as a type guard in TS 5.5+.", code: "function isString(value: unknown): value is string {\n  return typeof value === 'string';\n}\nfunction isError(err: unknown): err is Error {\n  return err instanceof Error;\n}\nfunction process(value: string | number) {\n  if (isString(value)) {\n    console.log(value.toUpperCase()); // narrowed to string\n  }\n}\n// 'value is string' tells TS the predicate; without it, narrowing fails" },
        "Variadic Tuple Types": { exp: "Variadic tuple types use spread in tuple positions to capture variable-length patterns. They enable strongly typed concatenation, partial application, and parameter forwarding. A common mistake is forgetting that spreads in tuples create new tuple types.", code: "type Concat<T extends any[], U extends any[]> = [...T, ...U];\ntype Result = Concat<[string, number], [boolean]>;\n// [string, number, boolean]\n\nfunction partialCall<T extends any[], U extends any[], R>(\n  fn: (...args: [...T, ...U]) => R,\n  ...args: T\n): (...args: U) => R {\n  return (...rest) => fn(...args, ...rest);\n}" },
        "Recursive Types": { exp: "Recursive types reference themselves to model nested structures like JSON, trees, or linked lists. TypeScript supports recursive type aliases but has depth limits to prevent infinite expansion. A common mistake is creating infinitely recursive types without a base case.", code: "type JSONValue =\n  | string\n  | number\n  | boolean\n  | null\n  | JSONValue[]\n  | { [key: string]: JSONValue };\n\ntype TreeNode<T> = {\n  value: T;\n  children: TreeNode<T>[];\n};\nconst tree: TreeNode<number> = {\n  value: 1,\n  children: [\n    { value: 2, children: [] }\n  ]\n};" },
        "Discriminated Unions": { exp: "Discriminated unions use a common literal property (the discriminant) to narrow union members in switch or if statements. TypeScript tracks which member is active based on the discriminant value. A common mistake is forgetting to handle all union members — use the never type for exhaustive checks.", code: "type Shape =\n  | { kind: 'circle'; radius: number }\n  | { kind: 'square'; side: number }\n  | { kind: 'triangle'; base: number; height: number };\n\nfunction getArea(shape: Shape): number {\n  switch (shape.kind) {\n    case 'circle':  return Math.PI * shape.radius ** 2;\n    case 'square':  return shape.side ** 2;\n    case 'triangle': return (shape.base * shape.height) / 2;\n  }\n}\n// Exhaustive check helper:\nfunction assertNever(x: never): never { throw new Error(); }" },
        "keyof & typeof Operators": { exp: "keyof T produces a union of the keys of T. typeof x extracts the type of a value at compile time. Combine them for dynamic property access patterns. A common mistake is using typeof at runtime instead of compile-time — typeof at runtime is JavaScript's typeof.", code: "interface Person {\n  name: string;\n  age: number;\n  email: string;\n}\ntype PersonKeys = keyof Person; // 'name' | 'age' | 'email'\n\nconst config = {\n  url: 'https://api.example.com',\n  timeout: 5000,\n} as const;\ntype ConfigType = typeof config;\n// { readonly url: string; readonly timeout: number; }\n\ntype ValueOf<T> = T[keyof T];\ntype PersonValues = ValueOf<Person>; // string | number" }
    },
    "Modules & Tooling": {
        "ESM Imports": { exp: "TypeScript supports ES module import/export syntax. Use import type for type-only imports to avoid runtime overhead. A common mistake is mixing default and named imports incorrectly, or forgetting that type-only imports are erased at compile time.", code: "// math.ts\nexport function add(a: number, b: number): number { return a + b; }\nexport type Operation = 'add' | 'subtract';\n\n// app.ts\nimport { add } from './math.js';          // value import\nimport type { Operation } from './math.js'; // type-only\nimport { type Operation } from './math.js'; // inline type (TS 5.0+)" },
        "Namespaces": { exp: "Namespaces group related code under a global namespace. They are legacy constructs — prefer ES modules for new code. Still useful for ambient declarations and library definitions. A common mistake is mixing namespace and module imports.", code: "namespace Validation {\n  export interface StringValidator {\n    isValid(s: string): boolean;\n  }\n  export class LettersOnlyValidator implements StringValidator {\n    isValid(s: string): boolean {\n      return /^[A-Za-z]+$/.test(s);\n    }\n  }\n}\nconst v = new Validation.LettersOnlyValidator();\nconsole.log(v.isValid('Hello')); // true" },
        "Decorators": { exp: "Decorators modify classes, methods, properties, or parameters. They are experimental in TypeScript (enable experimentalDecorators). Use them for metaprogramming, logging, and dependency injection. A common mistake is forgetting that decorator evaluation order matters.", code: "function logMethod(\n  target: any,\n  propertyKey: string,\n  descriptor: PropertyDescriptor\n) {\n  const original = descriptor.value;\n  descriptor.value = function (...args: any[]) {\n    console.log(`Called ${propertyKey} with`, args);\n    return original.apply(this, args);\n  };\n}\nclass Calculator {\n  @logMethod\n  add(a: number, b: number): number {\n    return a + b;\n  }\n}\n// tsconfig: { \"experimentalDecorators\": true }" },
        "tsconfig": { exp: "tsconfig.json configures the TypeScript compiler. Key options: target, module, strict, outDir, rootDir, and lib. A common mistake is not including files in the project or using conflicting module and moduleResolution settings.", code: "{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"module\": \"ESNext\",\n    \"moduleResolution\": \"bundler\",\n    \"strict\": true,\n    \"outDir\": \"./dist\",\n    \"rootDir\": \"./src\",\n    \"lib\": [\"ES2022\", \"DOM\"],\n    \"skipLibCheck\": true\n  },\n  \"include\": [\"src/**/*\"],\n  \"exclude\": [\"node_modules\", \"dist\"]\n}" },
        "Declaration Files": { exp: "Declaration files (.d.ts) describe JavaScript module shapes without implementation. Use declare module for third-party libs and declare global for augmenting globals. A common mistake is writing implementation code in .d.ts files — they should only contain type declarations.", code: "// my-lib.d.ts\ndeclare module 'my-lib' {\n  export function doStuff(options: { timeout: number }): Promise<string>;\n  export const version: string;\n}\n\ndeclare global {\n  interface Window {\n    myApp: { version: string };\n  }\n}" },
        "Triple-Slash Directives": { exp: "Triple-slash directives are single-line comments referencing other files. Used in declaration files for legacy reference management. Types, path, and lib directives exist. A common mistake is using these in modern projects — prefer ESM imports instead.", code: "/// <reference types=\"vite/client\" />\n/// <reference path=\"./types.d.ts\" />\n/// <reference lib=\"es2022\" />\n\n// Use cases:\n// types: pulls in global types from a package\n// path: relative path to another declaration file\n// lib: includes a built-in lib definition" }
    },
    "Asynchronous TypeScript": {
        "Promise Types": { exp: "Promise<T> represents an asynchronous value. The type parameter T is the resolved type. A common mistake is treating Promise<T> as T — you must await or .then() to unwrap the value. Promise<void> is common for fire-and-forget async operations.", code: "function fetchUser(id: string): Promise<{ name: string }> {\n  return fetch(`/users/${id}`)\n    .then(r => r.json() as Promise<{ name: string }>);\n}\nfetchUser('1').then(user => {\n  console.log(user.name); // typed as string\n});\n\nconst p: Promise<number> = Promise.resolve(42);\n// p is not number, it's Promise<number>" },
        "Async/Await": { exp: "Async functions always return Promise<T>. Await unwraps the promise inside an async function. TypeScript tracks types through await. A common mistake is forgetting to await a promise or wrapping an already-async function in another Promise unnecessarily.", code: "async function getData(): Promise<number> {\n  const response = await fetch('/api/data');\n  const json: { value: number } = await response.json();\n  return json.value;\n}\n\n// Common mistake: forgetting await\nconst value = getData(); // Promise<number>, not number\n\n// Proper usage:\nconst result = await getData(); // number" },
        "Async Iterators": { exp: "Async iterators (AsyncIterable<T>) produce values asynchronously. Use for await...of to consume them. Useful for streaming data, paginated APIs, and event streams. A common mistake is using for...of (sync) on an async iterable, which causes a runtime error.", code: "async function* generateNumbers(): AsyncIterable<number> {\n  for (let i = 0; i < 3; i++) {\n    await new Promise(r => setTimeout(r, 100));\n    yield i;\n  }\n}\n\nasync function consume() {\n  for await (const num of generateNumbers()) {\n    console.log(num); // 0, 1, 2\n  }\n}\n// for...of would fail — must use for await...of" },
        "Typed Event Emitters": { exp: "Typed event emitters use generics to map event names to payload types. This provides type-safe on/emit patterns. A common mistake is not typing the event map, resulting in any-typed event data throughout the codebase.", code: "interface EventMap {\n  login: { userId: string };\n  logout: { userId: string; timestamp: number };\n  error: { message: string; code: number };\n}\n\nclass TypedEmitter<T extends Record<string, any>> {\n  private handlers = new Map<keyof T, Set<Function>>();\n  on<K extends keyof T>(event: K, handler: (data: T[K]) => void): void {}\n  emit<K extends keyof T>(event: K, data: T[K]): void {}\n}\nconst emitter = new TypedEmitter<EventMap>();\nemitter.on('login', data => console.log(data.userId)); // typed" },
        "Promise Combinators": { exp: "Promise.all, Promise.race, Promise.allSettled, and Promise.any have typed return values. TypeScript infers tuples/unions from the input array. A common mistake is assuming Promise.all returns a loose array — it returns a tuple when given a tuple literal.", code: "const p1 = Promise.resolve(42);\nconst p2 = Promise.resolve('hello');\nconst p3 = Promise.resolve(true);\n\nconst all = Promise.all([p1, p2, p3]);\n//    ^? Promise<[number, string, boolean]>\n\nconst settled = Promise.allSettled([p1, p2, p3]);\n//    ^? Promise<PromiseSettledResult<number | string | boolean>[]>\n\nconst any = Promise.any([p1, p2, p3]);\n//    ^? Promise<number | string | boolean>" },
        "Error Handling in Async Code": { exp: "Async errors require try/catch. In strict mode, caught errors are typed as unknown — you must narrow them. A common mistake is assuming the error is always an Error instance or not handling non-Error throws like strings or objects.", code: "async function safeFetch(url: string): Promise<unknown> {\n  try {\n    const res = await fetch(url);\n    if (!res.ok) throw new Error(`HTTP ${res.status}`);\n    return await res.json();\n  } catch (error: unknown) {\n    if (error instanceof Error) {\n      console.error(error.message);\n    } else if (typeof error === 'string') {\n      console.error(error);\n    } else {\n      console.error('Unknown error', error);\n    }\n    return null;\n  }\n}" }
    },
    "Modern Patterns": {
        "Template Literal Types": { exp: "Template literal types construct string types at compile time using embedded type expressions. Combined with unions, they generate all possible string combinations automatically. A common mistake is assuming these can be used at runtime — they're erased during compilation.", code: "type EventName = `on${Capitalize<'click' | 'focus' | 'blur'>}`;\n// 'onClick' | 'onFocus' | 'onBlur'\n\ntype CSSValue = `${number}px` | `${number}rem` | `${number}%`;\nconst size: CSSValue = '16px';\n// const bad: CSSValue = '10'; // Error\n\ntype ExtractParams<S extends string> =\n  S extends `${string}:${infer P}/${infer Rest}` ? P | ExtractParams<Rest> : never;" },
        "satisfies Operator": { exp: "satisfies validates a value's type while preserving the narrowest inferred type for autocomplete. Unlike type annotations, it doesn't widen. A common mistake is using a type annotation instead of satisfies, which loses the specific types of individual properties.", code: "const config = {\n  api: 'https://api.example.com',\n  timeout: 5000,\n  retries: 3,\n} satisfies Record<string, string | number>;\n// config.api is string, not string | number\nconfig.api.toUpperCase(); // OK\n\nconst rgb = {\n  r: [255, 0, 0],\n  g: '#00ff00',\n} satisfies Record<string, string | number[]>;\n// rgb.r inferred as number[], not string | number[]" },
        "Assertion Functions": { exp: "Assertion functions use the asserts return type to narrow types after a call. They throw if the assertion fails, making subsequent code safe. A common mistake is writing assertion functions without the asserts keyword, which prevents TypeScript from narrowing.", code: "function assertDefined<T>(value: T): asserts value is NonNullable<T> {\n  if (value === null || value === undefined) {\n    throw new Error('Value is null or undefined');\n  }\n}\nconst x: string | null = Math.random() > 0.5 ? 'hello' : null;\nassertDefined(x);\nconsole.log(x.toUpperCase()); // x narrowed to string\n\nfunction assertString(value: unknown): asserts value is string {\n  if (typeof value !== 'string') throw new Error('Not a string');\n}" },
        "Branded Types": { exp: "Branded types add a unique phantom property to create distinguishable types from structurally identical ones. This prevents mixing values like emails and phone numbers. A common mistake is forgetting to cast at the boundary — brands are not automatically applied.", code: "type Email = string & { readonly __brand: 'Email' };\ntype Phone = string & { readonly __brand: 'Phone' };\n\nfunction sendEmail(to: Email, body: string): void {\n  console.log(`Sending to ${to}`);\n}\n\nconst addr = 'user@example.com' as Email;\nsendEmail(addr, 'hello');\n\nconst phone = '555-0100' as Phone;\n// sendEmail(phone, 'hello'); // Error: Phone not assignable to Email\n\n// Factory function as safer boundary:\nfunction createEmail(s: string): Email {\n  if (!s.includes('@')) throw new Error('Invalid email');\n  return s as Email;\n}" },
        "Nominal Typing": { exp: "TypeScript uses structural typing by default. To simulate nominal typing (type identity matters), use brands, enum discriminants, or intersection with unique class types. A common mistake is expecting structural typing to catch nominal mismatches automatically.", code: "class Nominal<T> {\n  private readonly _brand!: T;\n}\n\ntype USD = Nominal<{ readonly __type: 'USD' }> & number;\ntype EUR = Nominal<{ readonly __type: 'EUR' }> & number;\n\nfunction addMoney(a: USD, b: USD): USD {\n  return (a + b) as USD;\n}\n\nconst dollars = 100 as USD;\nconst euros = 200 as EUR;\naddMoney(dollars, dollars); // OK\n// addMoney(dollars, euros); // Error: types don't match" },
        "Covariance & Contravariance": { exp: "Covariance preserves assignment direction (Dog[] is Animal[]). Contravariance reverses it (function parameters). TypeScript is covariant for most types but checks function parameters bivariantly by default — strictFunctionTypes fixes this. A common mistake is expecting array invariance.", code: "class Animal {}\nclass Dog extends Animal {}\n\nconst dogs: Dog[] = [new Dog()];\nconst animals: Animal[] = dogs; // Covariant: OK (but unsound)\n\n// Functions are contravariant in params with strictFunctionTypes:\ntype Handler = (animal: Animal) => void;\nconst handleDog: Handler = (dog: Dog) => {}; // Error with strictFunctionTypes\n\n// Methods are bivariant:\ninterface Container {\n  handle(animal: Animal): void; // method syntax — bivariant\n}" }
    },
    "DOM & TypeScript": {
        "Typed DOM APIs": { exp: "TypeScript includes DOM type definitions. Use specific element types (HTMLInputElement, HTMLCanvasElement) instead of generic Element for type-safe property access. A common mistake is using querySelector without the generic parameter, resulting in an Element type.", code: "const button = document.querySelector<HTMLButtonElement>('.submit');\nif (button) {\n  button.disabled = true;\n  button.textContent = 'Loading...';\n}\nconst canvas = document.getElementById('canvas') as HTMLCanvasElement;\nconst ctx = canvas.getContext('2d'); // typed as CanvasRenderingContext2D | null\n\n// Without generic: button is Element — no .disabled access\nconst bad = document.querySelector('.submit'); // Element | null" },
        "Event Typing": { exp: "DOM events have specific types (MouseEvent, KeyboardEvent, FocusEvent, SubmitEvent) with typed properties. Use addEventListener's generic for proper typing. A common mistake is typing all events as Event, losing access to event-specific properties like clientX or key.", code: "const input = document.querySelector('input')!;\ninput.addEventListener('input', (e: Event) => {\n  const target = e.target as HTMLInputElement;\n  console.log(target.value);\n});\n\nconst div = document.querySelector('div')!;\ndiv.addEventListener('mouseenter', (e: MouseEvent) => {\n  console.log(e.clientX, e.clientY);\n});\n\ndocument.addEventListener('keydown', (e: KeyboardEvent) => {\n  console.log(e.key, e.ctrlKey);\n});" },
        "Form Typing": { exp: "Type form access with HTMLFormElement and HTMLInputElement. FormData provides key-value access. A common mistake is forgetting to check if a form element exists before accessing it, or not casting data.get() results.", code: "const form = document.querySelector('form')!;\nform.addEventListener('submit', (e: SubmitEvent) => {\n  e.preventDefault();\n  const data = new FormData(form);\n  const name = data.get('username');\n  if (typeof name === 'string') {\n    console.log(name.toUpperCase());\n  }\n  const email = data.get('email') as string | null;\n});\n\ninterface FormFields {\n  username: string;\n  age: number;\n}" },
        "Fetch Typing": { exp: "The fetch API returns Promise<Response>. Parse the body with json(), text(), etc. Type the parsed data, not the fetch call. A common mistake is not checking res.ok or assuming JSON parsing always succeeds.", code: "interface User {\n  id: number;\n  name: string;\n  email: string;\n}\n\nasync function getUser(id: number): Promise<User> {\n  const res = await fetch(`/api/users/${id}`);\n  if (!res.ok) {\n    throw new Error(`HTTP ${res.status}: ${res.statusText}`);\n  }\n  return res.json() as Promise<User>;\n}\n\n// Generic fetch wrapper:\nasync function fetchJSON<T>(url: string): Promise<T> {\n  const res = await fetch(url);\n  if (!res.ok) throw new Error(`HTTP ${res.status}`);\n  return res.json() as Promise<T>;\n}\nconst users = await fetchJSON<User[]>('/api/users');" }
    },
    "Error Handling": {
        "Typed Error Classes": { exp: "Create custom Error subclasses with typed properties. Set the name property for proper instanceof checks. A common mistake is forgetting to set error.name — without it, instanceof may work inconsistently across inheritance chains.", code: "class ValidationError extends Error {\n  constructor(\n    message: string,\n    public field: string,\n    public code: number\n  ) {\n    super(message);\n    this.name = 'ValidationError';\n  }\n}\n\nclass NetworkError extends Error {\n  constructor(\n    message: string,\n    public statusCode: number\n  ) {\n    super(message);\n    this.name = 'NetworkError';\n  }\n}\n\ntry {\n  throw new ValidationError('Invalid email', 'email', 400);\n} catch (err) {\n  if (err instanceof ValidationError) {\n    console.log(err.field, err.code);\n  }\n}" },
        "Try/Catch with unknown": { exp: "In strict mode, catch variables are unknown, not any. Narrow them with instanceof, typeof, or custom guards. A common mistake is accessing err.message without checking — TypeScript will error with 'Object is of type unknown'.", code: "try {\n  riskyOperation();\n} catch (err: unknown) {\n  if (err instanceof Error) {\n    console.log(err.message);\n  } else if (typeof err === 'string') {\n    console.log(err);\n  } else if (err && typeof err === 'object' && 'message' in err) {\n    console.log((err as { message: string }).message);\n  } else {\n    console.log('Unexpected error type');\n  }\n}" },
        "Result Types": { exp: "Result types model success/failure without exceptions using a discriminated union. This forces callers to handle both paths explicitly. A common mistake is using exceptions for expected failure modes instead of modeling them in the type system.", code: "type Result<T, E = Error> =\n  | { ok: true; data: T }\n  | { ok: false; error: E };\n\nfunction divide(a: number, b: number): Result<number, string> {\n  if (b === 0) return { ok: false, error: 'Division by zero' };\n  return { ok: true, data: a / b };\n}\n\nconst result = divide(10, 2);\nif (result.ok) {\n  console.log(result.data); // number\n} else {\n  console.error(result.error); // string\n}" },
        "Definite Assignment Assertions": { exp: "Use the ! assertion (!:) to tell TypeScript that a variable is definitely assigned, even when the compiler can't prove it. Common in class properties initialized in constructor helpers or lifecycle methods. A common mistake is overusing it to silence warnings instead of restructuring code.", code: "class Component {\n  private data!: string[]; // Definite assignment\n  init(): void {\n    this.data = ['a', 'b', 'c'];\n  }\n  process(): void {\n    console.log(this.data.length); // OK\n  }\n}\n// Without !:, TS would error: Property 'data' not initialized\n\n// Better: initialize at declaration\nclass Better {\n  private data: string[] = [];\n}" },
        "Null Checks": { exp: "With strictNullChecks, null and undefined are distinct types. Use optional chaining (?.), nullish coalescing (??), and type guards to handle nullable values safely. A common mistake is using || instead of ?? for defaults — || treats empty string and 0 as falsy.", code: "interface Config {\n  timeout?: number;\n  url?: string;\n  retries?: number;\n}\nfunction process(config: Config) {\n  const timeout = config.timeout ?? 5000; // 0 is a valid value\n  const url = config.url?.toLowerCase();\n  const retries = config.retries ?? 3;\n  // With ||: timeout would use default even if config.timeout = 0\n  if (config.url) {\n    console.log(config.url); // narrowed to string\n  }\n}" }
    },
    "Configuration & Build": {
        "Strict Compiler Options": { exp: "Beyond strict: true, individual strict flags include noImplicitAny, strictNullChecks, strictFunctionTypes, strictBindCallApply, strictPropertyInitialization, noImplicitThis, and alwaysStrict. Additional safety: noUncheckedIndexedAccess and exactOptionalPropertyTypes.", code: "{\n  \"compilerOptions\": {\n    \"strict\": true,\n    \"noImplicitAny\": true,\n    \"strictNullChecks\": true,\n    \"strictFunctionTypes\": true,\n    \"noUncheckedIndexedAccess\": true,\n    \"exactOptionalPropertyTypes\": true\n  }\n}" },
        "Module Resolution": { exp: "moduleResolution controls how TypeScript resolves imports: classic, node, node16, nodenext, or bundler. bundler is best for modern bundlers like Vite and esbuild. A common mistake is using node with ESNext module output, causing resolution mismatches.", code: "{\n  \"compilerOptions\": {\n    \"module\": \"ESNext\",\n    \"moduleResolution\": \"bundler\",\n    \"allowImportingTsExtensions\": true,\n    \"noEmit\": true\n  }\n}\n\n// With nodenext:\nimport { foo } from './bar.js'; // must include .js extension" },
        "Path Aliases": { exp: "Path aliases allow cleaner imports by mapping directories to short prefixes. Configure in tsconfig paths and sync with your bundler. A common mistake is setting baseUrl but forgetting to configure the bundler's resolve alias to match.", code: "{\n  \"compilerOptions\": {\n    \"baseUrl\": \".\",\n    \"paths\": {\n      \"@components/*\": [\"./src/components/*\"],\n      \"@utils/*\": [\"./src/utils/*\"],\n      \"@types/*\": [\"./src/types/*\"]\n    }\n  }\n}\n// Usage: import { Button } from '@components/Button';" },
        "Project References": { exp: "Project references split a codebase into independently buildable sub-projects. Each has a tsconfig with composite: true. Improves build performance via incremental compilation and enforces module boundaries. A common mistake is circular references between projects.", code: "// tsconfig.json (root)\n{\n  \"references\": [\n    { \"path\": \"./packages/core\" },\n    { \"path\": \"./packages/utils\" },\n    { \"path\": \"./packages/app\" }\n  ]\n}\n\n// packages/core/tsconfig.json\n{\n  \"compilerOptions\": {\n    \"composite\": true,\n    \"outDir\": \"./dist\",\n    \"rootDir\": \"./src\"\n  }\n}" },
        "Build Modes": { exp: "Use tsc -b for build mode with project references. It builds all referenced projects in dependency order with incremental compilation. --clean removes build artifacts. A common mistake is running plain tsc instead of tsc -b in a monorepo with references.", code: "// Build all projects\ntsc -b\n\n// Build specific project\ntsc -b packages/core\n\n// Clean builds\ntsc -b --clean\n\n// tsconfig with incremental\n{\n  \"compilerOptions\": {\n    \"incremental\": true,\n    \"tsBuildInfoFile\": \"./.tsbuildinfo\"\n  }\n}" }
    }
};

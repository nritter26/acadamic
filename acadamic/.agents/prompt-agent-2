You are agent-2. Read data/coordination.json. Find the first phase with status "unclaimed". Set claimed_by to "agent-2" and status to "in_progress". Read the phase's "prompt" field for detailed instructions and "files" field for which files to modify. Implement it fully. Run `npm run typecheck` after each file change. When done, update status to "done" and claimed_by to null. Print a summary of what was completed and any issues encountered.
{
 "CI/CD Fundamentals": {
  "What is CI/CD": ["<p><strong>CI/CD</strong> stands for <strong>Continuous Integration</strong> and <strong>Continuous Delivery/Deployment</strong>. It is a method of frequently delivering apps to customers by introducing automation into the stages of app development. CI/CD bridges the gap between development and operations teams by automating build, test, and deployment processes.</p><p><strong>Continuous Integration</strong> ensures every code change is automatically built and tested, catching bugs early. <strong>Continuous Delivery</strong> extends CI by automatically deploying code to production-like environments for testing. <strong>Continuous Deployment</strong> takes it further — every change that passes all stages of the pipeline is automatically released to production.</p>"],
  "Pipeline Architecture": ["<p>A <strong>CI/CD pipeline</strong> is a series of automated steps that code goes through from commit to deployment. The typical stages are:</p><ul><li><strong>Source</strong> — code is pushed to version control (Git)</li><li><strong>Build</strong> — code is compiled and dependencies are resolved</li><li><strong>Test</strong> — automated tests (unit, integration, e2e) are run</li><li><strong>Analyze</strong> — static analysis, linting, and security scanning</li><li><strong>Package</strong> — artifacts (binaries, containers) are built and stored</li><li><strong>Deploy</strong> — artifacts are deployed to staging or production</li></ul><p>Each stage must pass before the next begins — this is called a <strong>gated pipeline</strong>. Modern pipelines run stages in parallel where possible for speed.</p>"],
  "Benefits of CI/CD": ["<p>Adopting CI/CD brings several key benefits:</p><ul><li><strong>Faster releases</strong> — automate manual steps to ship code multiple times a day</li><li><strong>Reduced risk</strong> — catch bugs early with automated testing in every commit</li><li><strong>Higher quality</strong> — consistent testing and code analysis standards</li><li><strong>Developer productivity</strong> — less time spent on manual deployments and debugging</li><li><strong>Auditability</strong> — every change is tracked, tested, and logged</li><li><strong>Team confidence</strong> — automated validation means safer deployments</li></ul><p>Companies practicing CI/CD deploy code 208x more frequently than those that don't (DORA State of DevOps report).</p>"]
 },
 "Version Control & Git Integration": {
  "Git Hooks": ["<p><strong>Git hooks</strong> are scripts that run automatically before or after Git events like commit, push, or merge. They are stored in the <code>.git/hooks</code> directory and can enforce policies locally before code reaches the remote repository.</p><p>Common CI/CD use cases include:</p><ul><li><strong>pre-commit</strong> — run linters, format code, check for secrets</li><li><strong>pre-push</strong> — run unit tests before pushing</li><li><strong>commit-msg</strong> — enforce conventional commit format</li><li><strong>post-merge</strong> — install new dependencies after pulling</li></ul><p>Tools like <strong>Husky</strong> and <strong>lint-staged</strong> make managing hooks easier across teams.</p>"],
  "Branching Strategies": ["<p>CI/CD pipelines work hand-in-hand with branching strategies. Common approaches include:</p><ul><li><strong>GitFlow</strong> — main, develop, feature, release, and hotfix branches. Good for scheduled releases but complex.</li><li><strong>GitHub Flow</strong> — single main branch with feature branches and pull requests. Simpler, ideal for continuous deployment.</li><li><strong>Trunk-based Development</strong> — short-lived feature branches merged to main multiple times a day. Best for CI/CD with automated testing.</li></ul><p>CI/CD pipelines typically trigger on push to specific branches (e.g., <code>main</code>) and on pull requests for validation before merge.</p>"],
  "Pull Request Workflows": ["<p>Pull requests (PRs) are at the heart of modern CI/CD. A typical PR workflow:</p><ol><li>Developer creates a feature branch, commits changes, and pushes</li><li>PR is opened, triggering a <strong>CI pipeline</strong> that runs tests and analysis</li><li>PR checks report status (pass/fail) directly in the PR view</li><li>Team reviews code; additional commits trigger re-runs</li><li>Once approved and all checks pass, the PR is merged</li><li>Merge triggers a <strong>CD pipeline</strong> to build and deploy</li></ol><p>Tools like <strong>GitHub Actions</strong>, <strong>GitLab CI</strong>, and <strong>Azure DevOps</strong> integrate PR checks natively with status badges and required checks.</p>"]
 },
 "Continuous Integration": {
  "Build Automation": ["<p><strong>Build automation</strong> is the process of automating the compilation of source code into executable artifacts. Every commit should produce a build to verify the code compiles correctly.</p><p>Common build tools by language:</p><ul><li><strong>JavaScript/TypeScript</strong> — npm, yarn, pnpm, vite, webpack</li><li><strong>Java</strong> — Maven, Gradle</li><li><strong>Python</strong> — setuptools, poetry, pipenv</li><li><strong>Go</strong> — go build</li><li><strong>Rust</strong> — cargo build</li><li><strong>C/C++</strong> — CMake, Make, GCC</li></ul><p>The build stage should be fast (under 5 minutes) to provide quick feedback. Cache dependencies between builds to speed up execution.</p>"],
  "Automated Testing": ["<p>Testing is the heart of CI. Every pipeline should run multiple levels of tests:</p><ul><li><strong>Unit tests</strong> — test individual functions and methods (fastest, most coverage)</li><li><strong>Integration tests</strong> — test interactions between components (database, API, services)</li><li><strong>End-to-end tests</strong> — test the full system from user perspective (slowest, least coverage)</li></ul><p>The <strong>test pyramid</strong> suggests many unit tests, fewer integration tests, and few e2e tests. Tests should be deterministic — always pass or fail the same way given the same code. Flaky tests undermine CI trust.</p>"],
  "Code Quality & Linting": ["<p>Automated code quality checks are essential in CI/CD. These include:</p><ul><li><strong>Linters</strong> — ESLint (JS/TS), Ruff/Flake8 (Python), Clippy (Rust), golangci-lint (Go)</li><li><strong>Formatters</strong> — Prettier, Black, gofmt, rustfmt</li><li><strong>Static analysis</strong> — SonarQube, CodeQL, Coverity</li><li><strong>Type checking</strong> — TypeScript (tsc), mypy (Python), Flow</li></ul><p>These checks should fail the pipeline if thresholds are not met (e.g., code coverage &lt; 80%, lint errors, type errors). Many teams enforce formatting with pre-commit hooks and reserve CI for deeper analysis.</p>"]
 },
  "CI/CD Tools": {
   "GitHub Actions": ["<p><strong>GitHub Actions</strong> is GitHub's built-in CI/CD platform. Workflows are defined as YAML files in <code>.github/workflows/</code>. It offers deep GitHub integration, a large marketplace of pre-built actions, and supports Linux, macOS, Windows, and containers.</p><p>Key concepts: <strong>workflows</strong> (automated processes), <strong>jobs</strong> (sets of steps), <strong>steps</strong> (individual tasks), <strong>runners</strong> (execution environments). GitHub provides free minutes for public repositories and paid minutes for private ones.</p>"],
   "Jenkins": ["<p><strong>Jenkins</strong> is the most widely used open-source automation server. It supports hundreds of plugins for building, deploying, and automating projects. Jenkins pipelines can be defined as code using <strong>Jenkinsfile</strong> with Declarative or Scripted pipeline syntax.</p><p>Strengths: extreme flexibility, massive plugin ecosystem, mature community. Weaknesses: requires server management, UI can feel dated, plugin compatibility issues. Best for complex enterprise environments needing fine-grained control.</p>"],
   "CircleCI": ["<p><strong>CircleCI</strong> is a cloud-native CI/CD platform known for speed and reliability. Pipelines are configured in a <code>.circleci/config.yml</code> file. CircleCI supports parallelism, caching, and <strong>Orbs</strong> — reusable configuration packages that simplify complex pipelines.</p><p>Key features: Docker layer caching, SSH debugging for troubleshooting, dynamic configuration via pipeline parameters, and optimized resource classes (CPU/RAM per job). CircleCI offers a generous free tier for individual developers and open-source projects.</p>"],
   "Azure DevOps": ["<p><strong>Azure DevOps</strong> is Microsoft's full DevSecOps platform providing a complete toolchain: <strong>Azure Pipelines</strong> (CI/CD), <strong>Azure Boards</strong> (work tracking), <strong>Azure Repos</strong> (Git repos), <strong>Azure Artifacts</strong> (package management), and <strong>Azure Test Plans</strong> (testing).</p><p>Azure Pipelines supports any language, platform, and cloud. Key strengths: deep Azure integration, deployment groups for multi-machine rollouts, Kubernetes integration, and YAML or classic editor pipeline definitions. Free tier includes 1,800 minutes per month.</p>"]
  },
  "GitLab CI/CD": {
  "GitLab CI/CD Overview": ["<p><strong>GitLab CI/CD</strong> is GitLab's built-in continuous integration, delivery, and deployment system. Pipelines are defined declaratively in a <code>.gitlab-ci.yml</code> file at the repository root. GitLab CI/CD is deeply integrated into the GitLab platform, providing a unified experience for source control, CI/CD, container registry, package registry, and deployment.</p><p>Key strengths: single application for the entire DevOps lifecycle, built-in auto-scaling runners, review apps for per-branch environments, and a generous free tier for both public and private repositories.</p>"],
  "GitLab CI Pipeline Configuration": ["<p>GitLab CI pipelines are defined in YAML with these core concepts:</p><ul><li><strong>Stages</strong> — groups of jobs that run in order (e.g., build, test, deploy). Jobs in the same stage run in parallel.</li><li><strong>Jobs</strong> — define what to run (script, image, dependencies). Each job belongs to a stage.</li><li><strong>Runners</strong> — agents that execute jobs. GitLab provides shared runners (Linux, Windows, macOS) and you can register your own.</li><li><strong>Variables</strong> — environment variables for configuration. Can be set at project, group, or instance level.</li><li><strong>Cache & Artifacts</strong> — cache speeds up dependency installs; artifacts pass build outputs between stages.</li></ul><p>Example pipeline: lint → test → build → deploy. Jobs can run conditionally based on branch, tags, or changes to specific files.</p>"],
  "GitLab Auto DevOps": ["<p><strong>Auto DevOps</strong> is GitLab's zero-configuration CI/CD pipeline. When enabled, GitLab automatically detects your application language and builds a complete pipeline including:</p><ul><li>Code quality analysis (SonarQube)</li><li>SAST and Dependency Scanning</li><li>Container scanning and DAST</li><li>License compliance review</li><li>Automatic deployment to staging and production</li><li>Review Apps (temporary environments per merge request)</li><li>Performance testing and browser testing</li></ul><p>Auto DevOps uses a <strong>Dockerfile</strong> or <strong>buildpack</strong> to build your app. It's ideal for teams new to CI/CD who want instant value without writing pipeline config.</p>"],
  "GitLab Runners & Scaling": ["<p><strong>GitLab Runners</strong> are the agents that execute CI/CD jobs. Types include:</p><ul><li><strong>Shared runners</strong> — GitLab-hosted, available to all projects (Linux, Windows, macOS)</li><li><strong>Group runners</strong> — available to all projects in a group</li><li><strong>Project runners</strong> — dedicated to a single project</li><li><strong>Specific runners</strong> — locked to specific projects or tags</li></ul><p>Scaling strategies: use <strong>Docker autoscaling</strong> with Docker Machine or <strong>Kubernetes</strong> with the GitLab Runner Helm chart to spin up runners dynamically based on job demand. GitLab Runner supports executors including Docker, Kubernetes, SSH, and VirtualBox.</p>"]
 },
 "Continuous Delivery & Deployment": {
  "Deployment Strategies": ["<p>Different deployment strategies balance risk, speed, and user impact:</p><ul><li><strong>Rolling Update</strong> — gradually replaces instances of the old version with the new one. Zero downtime, easy rollback.</li><li><strong>Blue/Green</strong> — run two identical environments. Switch traffic from blue (current) to green (new) instantly. Instant rollback by switching back.</li><li><strong>Canary</strong> — route a small percentage of traffic to the new version, then gradually increase. Real user validation with minimal risk.</li><li><strong>Feature Flags</strong> — deploy code hidden behind toggles, then enable for specific users. Separates deployment from release.</li></ul><p>Modern CD pipelines use a combination of these strategies depending on the application and risk tolerance.</p>"],
  "Rollback & Recovery": ["<p>Even the best pipelines can fail in production. CD systems must support fast, reliable rollback:</p><ul><li><strong>Automated rollback</strong> — if health checks fail after deployment, automatically revert to the last known-good version</li><li><strong>Database rollbacks</strong> — use reversible migrations (up/down) to safely revert schema changes</li><li><strong>Version pinning</strong> — always tag deployed versions (git tags, container tags) for easy identification</li><li><strong>Audit trail</strong> — every deployment should log who deployed what, when, and the result</li></ul><p>The golden rule: <strong>if it hurts, do it more often</strong>. Frequent, small deployments are easier to roll back than rare, large ones.</p>"],
  "Release Management": ["<p>Release management in CI/CD coordinates how changes move through environments:</p><ul><li><strong>Environment promotion</strong> — code flows: dev → staging → production, with gates at each stage</li><li><strong>Semantic versioning</strong> — MAJOR.MINOR.PATCH (e.g., 2.1.0) based on change impact</li><li><strong>Changelogs</strong> — auto-generated from conventional commits</li><li><strong>Release branches</strong> — for scheduled releases, create a release branch from main</li><li><strong>Approval gates</strong> — manual approval steps before production deployment (compliance, QA sign-off)</li></ul><p>Tools like <strong>Release Please</strong>, <strong>Semantic Release</strong>, and <strong>Changesets</strong> automate versioning and changelog generation.</p>"]
 },
 "Infrastructure as Code CI/CD": {
  "Terraform in CI/CD": ["<p>Applying Infrastructure as Code (IaC) through CI/CD brings infrastructure changes under the same governance as application changes.</p><p>Terraform CI/CD best practices:</p><ul><li>Run <code>terraform fmt</code> and <code>terraform validate</code> in CI to check formatting and syntax</li><li>Use <code>terraform plan</code> in PRs to show infrastructure changes without applying them</li><li>Store state remotely (S3, Terraform Cloud) with state locking</li><li>Apply only after PR approval and plan review</li><li>Use workspaces or directories for environment separation</li></ul><p>The <strong>terraform plan</strong> output in a PR comment is one of the most valuable CI/CD practices for infrastructure teams.</p>"],
  "Configuration Management": ["<p>Configuration management tools maintain consistent system state across environments:</p><ul><li><strong>Ansible</strong> — agentless, push-based, YAML playbooks. Great for app deployment and server configuration.</li><li><strong>Chef</strong> — Ruby-based DSL, pull-based with Chef Client and Server. Good for large-scale server fleets.</li><li><strong>Puppet</strong> — declarative DSL, pull-based. Strong in enterprise environments.</li></ul><p>In CI/CD, run config management as part of the deployment stage to ensure servers match the desired state after deployment.</p>"],
  "Policy as Code": ["<p><strong>Policy as Code</strong> (PaC) enforces rules automatically in the pipeline:</p><ul><li><strong>Open Policy Agent (OPA)</strong> — general-purpose policy engine. Enforce rules like \"all S3 buckets must be encrypted\" before apply.</li><li><strong>Sentinel</strong> — HashiCorp's policy framework for Terraform and Vault.</li><li><strong>Checkov</strong> — static analysis for IaC to find misconfigurations (open ports, hardcoded secrets).</li><li><strong>Conftest</strong> — test configuration files against OPA policies in CI.</li></ul><p>PaC shifts security left — catching policy violations before infrastructure is provisioned.</p>"]
 },
 "Container CI/CD": {
  "Docker Build Pipelines": ["<p>Container-based CI/CD pipelines use Docker throughout the pipeline:</p><ul><li>Build stage produces a <strong>Docker image</strong> with the application and all dependencies</li><li>Tag images with commit SHA, branch name, and semantic version</li><li>Push to a <strong>container registry</strong> (Docker Hub, ECR, GCR, GHCR)</li><li>Use <strong>multi-stage builds</strong> to minimize image size</li><li>Scan images for vulnerabilities (Trivy, Snyk, Docker Scout)</li></ul><p>The same image built in CI is deployed everywhere — no \"works on my machine\" problems. Images are immutable and versioned.</p>"],
  "Kubernetes Deployments": ["<p>Deploying to Kubernetes from CI/CD involves:</p><ul><li>Update the <strong>deployment manifest</strong> (YAML) with the new image tag</li><li>Use <strong>kubectl set image</strong> or patch the deployment</li><li>Apply with <strong>kubectl apply</strong> to trigger rolling update</li><li>Use <strong>Helm</strong> for templated releases — pass the image tag as a value</li><li>Monitor rollout with <strong>kubectl rollout status</strong></li></ul><p>Advanced setups use <strong>ArgoCD</strong> or <strong>Flux</strong> (GitOps) where the CD system watches a Git repository for manifest changes and synchronizes the cluster automatically.</p>"],
  "GitOps": ["<p><strong>GitOps</strong> is a deployment model where the Git repository is the single source of truth for both application code and infrastructure. Changes are made via PRs, and an operator synchronizes the live environment to match the repository state.</p><p>Key tools:</p><ul><li><strong>ArgoCD</strong> — Kubernetes-native CD tool. Syncs clusters to a Git repository with automatic or manual sync policies.</li><li><strong>Flux</strong> — GitOps operator from Weaveworks. Supports multi-tenancy, image updates, and Kustomize/Helm.</li></ul><p>Benefits: complete audit trail (every change is a Git commit), easy rollback (revert commit), and a single workflow for both dev and ops.</p>"]
 },
 "Security in CI/CD": {
  "SAST & DAST": ["<p>Security testing should be integrated into every pipeline:</p><ul><li><strong>SAST (Static Application Security Testing)</strong> — analyzes source code for vulnerabilities without running it. Tools: <strong>SonarQube</strong>, <strong>CodeQL</strong>, <strong>Semgrep</strong>, <strong>Checkmarx</strong>. Runs early in the pipeline (build stage).</li><li><strong>DAST (Dynamic Application Security Testing)</strong> — tests running applications for vulnerabilities. Tools: <strong>OWASP ZAP</strong>, <strong>Burp Suite</strong>. Runs against staging environments later in the pipeline.</li></ul><p>SAST catches issues early (shift left), while DAST catches runtime vulnerabilities. Both should be gated — fail the pipeline on high-severity findings.</p>"],
  "Secret Scanning": ["<p>Accidentally committing secrets (API keys, passwords, tokens) is one of the most common CI/CD security issues. Prevent and detect with:</p><ul><li><strong>Pre-commit hooks</strong> — tools like <strong>talisman</strong>, <strong>git-secrets</strong>, and <strong>detect-secrets</strong> scan before commits</li><li><strong>CI scanning</strong> — <strong>GitHub Secret Scanning</strong>, <strong>GitLab Secret Detection</strong>, <strong>TruffleHog</strong> scan in the pipeline</li><li><strong>Secret managers</strong> — HashiCorp Vault, AWS Secrets Manager, Azure Key Vault, GCP Secret Manager</li></ul><p>CI/CD configs should reference secrets from the CI platform's built-in secrets store (GitHub Actions secrets, GitLab CI variables, Jenkins credentials), never hardcode them.</p>"],
  "SBOM & Supply Chain": ["<p>Software supply chain security is critical in 2026:</p><ul><li><strong>SBOM (Software Bill of Materials)</strong> — a machine-readable inventory of all components in your software. Generated by tools like <strong>CycloneDX</strong> and <strong>Syft</strong>.</li><li><strong>Dependency scanning</strong> — <strong>Dependabot</strong>, <strong>Renovate</strong>, <strong>Snyk</strong> automatically detect and update vulnerable dependencies</li><li><strong>Artifact signing</strong> — sign container images and binaries with <strong>Cosign</strong> to verify authenticity</li><li><strong>Attestations</strong> — create signed metadata about how and where artifacts were built</li></ul><p>AI-generated code increases the importance of SBOM scanning. Every CI/CD pipeline should generate and verify SBOMs before deployment.</p>"]
 },
 "Monitoring & Observability": {
  "Pipeline Metrics": ["<p>Track these key metrics to measure CI/CD pipeline health:</p><ul><li><strong>Pipeline duration</strong> — time from commit to deployment. Target: under 15 minutes total.</li><li><strong>Build failure rate</strong> — percentage of builds that fail. Target: under 10%.</li><li><strong>Deployment frequency</strong> — how often you deploy to production. Elite teams deploy multiple times per day.</li><li><strong>Mean Time to Recover (MTTR)</strong> — time from detecting a failure to recovery. Target: under 1 hour.</li><li><strong>Change failure rate</strong> — percentage of deployments causing incidents. Target: under 5%.</li></ul><p>These are the <strong>DORA metrics</strong> — the industry standard for measuring DevOps and CI/CD performance.</p>"],
  "Pipeline Notifications": ["<p>Keep teams informed about pipeline status with automated notifications:</p><ul><li><strong>Slack/Discord</strong> — send messages on failure, success, or deployment events. Include links to the pipeline run.</li><li><strong>Email</strong> — send summaries for failed pipelines or approval requests</li><li><strong>Status badges</strong> — embed pipeline status (passing/failing) in README files</li><li><strong>PR comments</strong> — post test results, coverage changes, and deployment URLs directly in PRs</li></ul><p>Most CI/CD platforms have built-in notification integrations. Focus on actionable alerts — don't spam teams with every successful build.</p>"],
  "Incident Response": ["<p>When a CI/CD pipeline failure or production incident occurs, a structured response process is essential:</p><ol><li><strong>Detect</strong> — automated monitoring and alerts detect the failure</li><li><strong>Stop the bleeding</strong> — rollback the deployment or block the pipeline</li><li><strong>Triage</strong> — determine severity, impact, and affected teams</li><li><strong>Investigate</strong> — review logs, metrics, and pipeline output to find root cause</li><li><strong>Remediate</strong> — fix the issue and push the fix through the same CI/CD pipeline</li><li><strong>Review</strong> — post-mortem to prevent recurrence</li></ol><p>CI/CD pipelines themselves should be treated as critical infrastructure with their own monitoring, redundancy, and incident response plans.</p>"]
 }
}

{
  "id": "kanban-board",
  "title": "Kanban Board",
  "difficulty": "advanced",
  "languages": [
    "javascript",
    "typescript",
    "python"
  ],
  "estimatedMinutes": 75,
  "concepts": [
    "state-management",
    "arrays",
    "objects",
    "CRUD",
    "persistence"
  ],
  "description": "Build a Kanban board with columns for task management. You'll create columns, add/move/edit/delete cards, and persist state to localStorage.",
  "steps": [
    {
      "id": 1,
      "title": "Board columns",
      "description": "Create a `createBoard` function that returns a board object with three columns: `\"To Do\"`, `\"In Progress\"`, and `\"Done\"`. Each column has a `name` and a `cards` array.",
      "hints": [
        "Return an object with a columns array",
        "Each column should be { id, name, cards: [] }",
        "Use IDs like 'todo', 'in-progress', 'done'"
      ],
      "codeTemplate": {
        "javascript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\nfunction createBoard() {\n  // Return a board with 3 columns\n}\n\nconst board = createBoard();\nconsole.log(JSON.stringify(board));\n</script>\n",
        "typescript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\ninterface Card {\n  id: number;\n  text: string;\n}\n\ninterface Column {\n  id: string;\n  name: string;\n  cards: Card[];\n}\n\ninterface Board {\n  columns: Column[];\n}\n\nfunction createBoard(): Board {\n  // Return a board with 3 columns\n}\n\nconst board = createBoard();\nconsole.log(JSON.stringify(board));\n</script>\n",
        "python": "import json\n\ndef createBoard():\n  # Return a board with 3 columns\n  pass\n\nboard = createBoard()\nprint(json.dumps(board))\n"
      },
      "verification": {
        "test": "output.includes('To Do') && output.includes('In Progress') && output.includes('Done')",
        "successMessage": "Board has all three columns!",
        "failureMessage": "Make sure your board has 'To Do', 'In Progress', and 'Done' columns."
      }
    },
    {
      "id": 2,
      "title": "Add card",
      "description": "Create an `addCard` function that adds a new card to a specified column. Each card should have an `id` and `text`.",
      "hints": [
        "Find the column by its id: board.columns.find(c => c.id === columnId)",
        "Create a card object with a unique id and the given text",
        "Push the card to the column's cards array"
      ],
      "codeTemplate": {
        "javascript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\nfunction createBoard() {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board, columnId, text) {\n  // Add a card to the specified column\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Learn Kanban');\naddCard(board, 'todo', 'Build a board');\nconsole.log(JSON.stringify(board));\n</script>\n",
        "typescript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\ninterface Card {\n  id: number;\n  text: string;\n}\n\ninterface Column {\n  id: string;\n  name: string;\n  cards: Card[];\n}\n\ninterface Board {\n  columns: Column[];\n}\n\nfunction createBoard(): Board {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board: Board, columnId: string, text: string): void {\n  // Add a card to the specified column\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Learn Kanban');\naddCard(board, 'todo', 'Build a board');\nconsole.log(JSON.stringify(board));\n</script>\n",
        "python": "import json\n\ndef createBoard():\n  return {\n    \"columns\": [\n      {\"id\": 'todo', \"name\": 'To Do', \"cards\": []},\n      {\"id\": 'in-progress', \"name\": 'In Progress', \"cards\": []},\n      {\"id\": 'done', \"name\": 'Done', \"cards\": []}\n    ]\n  }\n\nnextCardId = 1\n\ndef addCard(board, columnId, text):\n  # Add a card to the specified column\n  pass\n\nboard = createBoard()\naddCard(board, 'todo', 'Learn Kanban')\naddCard(board, 'todo', 'Build a board')\nprint(json.dumps(board))\n"
      },
      "verification": {
        "test": "output.includes('Learn Kanban') && output.includes('Build a board')",
        "successMessage": "Cards can be added to columns!",
        "failureMessage": "Make sure addCard adds the card to the correct column with the given text."
      }
    },
    {
      "id": 3,
      "title": "Move between columns",
      "description": "Create a `moveCard` function that moves a card from one column to another. It should find the card by `cardId`, remove it from its source column, and add it to the destination column.",
      "hints": [
        "Loop through all columns to find the card with matching cardId",
        "Use .splice() or .filter() to remove the card from the source column",
        "Use .push() to add the card to the destination column",
        "Return the moved card so the caller knows what was moved"
      ],
      "codeTemplate": {
        "javascript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\nfunction createBoard() {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board, columnId, text) {\n  const column = board.columns.find(c => c.id === columnId);\n  const card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction moveCard(board, cardId, destColumnId) {\n  // Find and remove card from its current column, add to destination\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Task 1');\naddCard(board, 'todo', 'Task 2');\nmoveCard(board, 1, 'in-progress');\nconsole.log(JSON.stringify(board));\n</script>\n",
        "typescript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\ninterface Card {\n  id: number;\n  text: string;\n}\n\ninterface Column {\n  id: string;\n  name: string;\n  cards: Card[];\n}\n\ninterface Board {\n  columns: Column[];\n}\n\nfunction createBoard(): Board {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board: Board, columnId: string, text: string): void {\n  const column = board.columns.find(c => c.id === columnId)!;\n  const card: Card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction moveCard(board: Board, cardId: number, destColumnId: string): Card | null {\n  // Find and remove card from its current column, add to destination\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Task 1');\naddCard(board, 'todo', 'Task 2');\nmoveCard(board, 1, 'in-progress');\nconsole.log(JSON.stringify(board));\n</script>\n",
        "python": "import json\n\ndef createBoard():\n  return {\n    \"columns\": [\n      {\"id\": 'todo', \"name\": 'To Do', \"cards\": []},\n      {\"id\": 'in-progress', \"name\": 'In Progress', \"cards\": []},\n      {\"id\": 'done', \"name\": 'Done', \"cards\": []}\n    ]\n  }\n\nnextCardId = 1\n\ndef addCard(board, columnId, text):\n  global nextCardId\n  column = next(c for c in board[\"columns\"] if c[\"id\"] == columnId)\n  card = {\"id\": nextCardId, \"text\": text}\n  nextCardId += 1\n  column[\"cards\"].append(card)\n\ndef moveCard(board, cardId, destColumnId):\n  # Find and remove card from its current column, add to destination\n  pass\n\nboard = createBoard()\naddCard(board, 'todo', 'Task 1')\naddCard(board, 'todo', 'Task 2')\nmoveCard(board, 1, 'in-progress')\nprint(json.dumps(board))\n"
      },
      "verification": {
        "test": "output.includes('Task 1') && !output.includes('todo') || (output.includes('in-progress') && output.includes('Task 1'))",
        "successMessage": "Card moved to the correct column!",
        "failureMessage": "Make sure the card is removed from its original column and added to the destination."
      }
    },
    {
      "id": 4,
      "title": "Edit card",
      "description": "Create an `editCard` function that finds a card by `cardId` across all columns and updates its `text`.",
      "hints": [
        "Loop through all columns and their cards to find the matching cardId",
        "Update the card's text property with the new text",
        "Return true if found and updated, false if not found"
      ],
      "codeTemplate": {
        "javascript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\nfunction createBoard() {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board, columnId, text) {\n  const column = board.columns.find(c => c.id === columnId);\n  const card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction editCard(board, cardId, newText) {\n  // Find the card and update its text\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Old text');\neditCard(board, 1, 'Updated text');\nconsole.log(JSON.stringify(board));\n</script>\n",
        "typescript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\ninterface Card {\n  id: number;\n  text: string;\n}\n\ninterface Column {\n  id: string;\n  name: string;\n  cards: Card[];\n}\n\ninterface Board {\n  columns: Column[];\n}\n\nfunction createBoard(): Board {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board: Board, columnId: string, text: string): void {\n  const column = board.columns.find(c => c.id === columnId)!;\n  const card: Card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction editCard(board: Board, cardId: number, newText: string): boolean {\n  // Find the card and update its text\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Old text');\neditCard(board, 1, 'Updated text');\nconsole.log(JSON.stringify(board));\n</script>\n",
        "python": "import json\n\ndef createBoard():\n  return {\n    \"columns\": [\n      {\"id\": 'todo', \"name\": 'To Do', \"cards\": []},\n      {\"id\": 'in-progress', \"name\": 'In Progress', \"cards\": []},\n      {\"id\": 'done', \"name\": 'Done', \"cards\": []}\n    ]\n  }\n\nnextCardId = 1\n\ndef addCard(board, columnId, text):\n  global nextCardId\n  column = next(c for c in board[\"columns\"] if c[\"id\"] == columnId)\n  card = {\"id\": nextCardId, \"text\": text}\n  nextCardId += 1\n  column[\"cards\"].append(card)\n\ndef editCard(board, cardId, newText):\n  # Find the card and update its text\n  pass\n\nboard = createBoard()\naddCard(board, 'todo', 'Old text')\neditCard(board, 1, 'Updated text')\nprint(json.dumps(board))\n"
      },
      "verification": {
        "test": "output.includes('Updated text') && !output.includes('Old text')",
        "successMessage": "Card text updated successfully!",
        "failureMessage": "Make sure editCard finds the card and updates its text."
      }
    },
    {
      "id": 5,
      "title": "Delete card",
      "description": "Create a `deleteCard` function that removes a card by `cardId` from any column.",
      "hints": [
        "Loop through each column's cards array",
        "Use .filter() or .splice() to remove the card with matching cardId",
        "Return true if the card was found and deleted, false otherwise"
      ],
      "codeTemplate": {
        "javascript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\nfunction createBoard() {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board, columnId, text) {\n  const column = board.columns.find(c => c.id === columnId);\n  const card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction deleteCard(board, cardId) {\n  // Remove the card from any column\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Delete me');\ndeleteCard(board, 1);\nconsole.log(JSON.stringify(board));\n</script>\n",
        "typescript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\ninterface Card {\n  id: number;\n  text: string;\n}\n\ninterface Column {\n  id: string;\n  name: string;\n  cards: Card[];\n}\n\ninterface Board {\n  columns: Column[];\n}\n\nfunction createBoard(): Board {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board: Board, columnId: string, text: string): void {\n  const column = board.columns.find(c => c.id === columnId)!;\n  const card: Card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction deleteCard(board: Board, cardId: number): boolean {\n  // Remove the card from any column\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Delete me');\ndeleteCard(board, 1);\nconsole.log(JSON.stringify(board));\n</script>\n",
        "python": "import json\n\ndef createBoard():\n  return {\n    \"columns\": [\n      {\"id\": 'todo', \"name\": 'To Do', \"cards\": []},\n      {\"id\": 'in-progress', \"name\": 'In Progress', \"cards\": []},\n      {\"id\": 'done', \"name\": 'Done', \"cards\": []}\n    ]\n  }\n\nnextCardId = 1\n\ndef addCard(board, columnId, text):\n  global nextCardId\n  column = next(c for c in board[\"columns\"] if c[\"id\"] == columnId)\n  card = {\"id\": nextCardId, \"text\": text}\n  nextCardId += 1\n  column[\"cards\"].append(card)\n\ndef deleteCard(board, cardId):\n  # Remove the card from any column\n  pass\n\nboard = createBoard()\naddCard(board, 'todo', 'Delete me')\ndeleteCard(board, 1)\nprint(json.dumps(board))\n"
      },
      "verification": {
        "test": "!output.includes('Delete me')",
        "successMessage": "Card deleted successfully!",
        "failureMessage": "Make sure deleteCard removes the card completely from all columns."
      }
    },
    {
      "id": 6,
      "title": "Persist state",
      "description": "Create `saveBoard` and `loadBoard` functions that save the board state to `localStorage` and load it back. Simulate localStorage with a simple object store.",
      "hints": [
        "Create a simple store object to simulate localStorage { data: null }",
        "saveBoard: use JSON.stringify to serialize, store in localStorage",
        "loadBoard: retrieve from localStorage, use JSON.parse to deserialize",
        "Handle the case where no saved data exists (return null)"
      ],
      "codeTemplate": {
        "javascript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\nfunction createBoard() {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board, columnId, text) {\n  const column = board.columns.find(c => c.id === columnId);\n  const card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction saveBoard(board) {\n  // Save board to localStorage\n}\n\nfunction loadBoard() {\n  // Load board from localStorage\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Persist me!');\nsaveBoard(board);\nconst loaded = loadBoard();\nif (loaded) {\n  console.log(loaded.columns[0].cards[0].text);\n} else {\n  console.log('No board found');\n}\n</script>\n",
        "typescript": "<div id=\"board\"><div class=\"column\" id=\"col-todo\"><h3>To Do</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-in-progress\"><h3>In Progress</h3><div class=\"cards\"></div></div><div class=\"column\" id=\"col-done\"><h3>Done</h3><div class=\"cards\"></div></div></div>\n<script>\ninterface Card {\n  id: number;\n  text: string;\n}\n\ninterface Column {\n  id: string;\n  name: string;\n  cards: Card[];\n}\n\ninterface Board {\n  columns: Column[];\n}\n\nfunction createBoard(): Board {\n  return {\n    columns: [\n      { id: 'todo', name: 'To Do', cards: [] },\n      { id: 'in-progress', name: 'In Progress', cards: [] },\n      { id: 'done', name: 'Done', cards: [] }\n    ]\n  };\n}\n\nlet nextCardId = 1;\n\nfunction addCard(board: Board, columnId: string, text: string): void {\n  const column = board.columns.find(c => c.id === columnId)!;\n  const card: Card = { id: nextCardId++, text };\n  column.cards.push(card);\n}\n\nfunction saveBoard(board: Board): void {\n  // Save board to localStorage\n}\n\nfunction loadBoard(): Board | null {\n  // Load board from localStorage\n}\n\nconst board = createBoard();\naddCard(board, 'todo', 'Persist me!');\nsaveBoard(board);\nconst loaded = loadBoard();\nif (loaded) {\n  console.log(loaded.columns[0].cards[0].text);\n} else {\n  console.log('No board found');\n}\n</script>\n",
        "python": "import json\n\nstore = {}\n\ndef createBoard():\n  return {\n    \"columns\": [\n      {\"id\": 'todo', \"name\": 'To Do', \"cards\": []},\n      {\"id\": 'in-progress', \"name\": 'In Progress', \"cards\": []},\n      {\"id\": 'done', \"name\": 'Done', \"cards\": []}\n    ]\n  }\n\nnextCardId = 1\n\ndef addCard(board, columnId, text):\n  global nextCardId\n  column = next(c for c in board[\"columns\"] if c[\"id\"] == columnId)\n  card = {\"id\": nextCardId, \"text\": text}\n  nextCardId += 1\n  column[\"cards\"].append(card)\n\ndef saveBoard(board):\n  # Save board to localStorage\n  pass\n\ndef loadBoard():\n  # Load board from localStorage\n  pass\n\nboard = createBoard()\naddCard(board, 'todo', 'Persist me!')\nsaveBoard(board)\nloaded = loadBoard()\nif loaded:\n  print(loaded[\"columns\"][0][\"cards\"][0][\"text\"])\nelse:\n  print('No board found')\n"
      },
      "verification": {
        "test": "output.includes('Persist me!') && !output.includes('No board found')",
        "successMessage": "Your Kanban board is complete with persistence! State survives save/load cycles.",
        "failureMessage": "Make sure saveBoard stores and loadBoard retrieves the board correctly."
      }
    }
  ]
}

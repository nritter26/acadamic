# ════════════════════════════════════════════════════════════════
# DOGE'S LAB - Environment Configuration
# ════════════════════════════════════════════════════════════════

# ── PORT & RUNTIME ──
PORT=3000
NODE_ENV=development

# ── AI PROVIDER CONFIGURATION ──
# Options: hybrid (default), keyword, openai, anthropic, local
# - local: Ollama, LM Studio, etc. (requires local server running)
# - openai: GPT-4, GPT-3.5, etc. (requires API key)
# - anthropic: Claude models (requires API key)
# - gemini: Google Gemini models (requires API key)
# - hybrid: Keyword cascade + transformers.js (gte-small, ~60MB). Works out of the box, no API key.
# - keyword: Free, no API key, uses pattern matching only (good for learning, no downloads)
AI_PROVIDER=local

# ── OpenAI Configuration ──
# Uncomment and fill in to use OpenAI GPT models
# Get API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_ENDPOINT=https://api.openai.com/v1

# ── Anthropic Configuration ──
# Uncomment and fill in to use Claude models
# Get API key from: https://console.anthropic.com/
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-haiku-20240307

# ── Local LLM Configuration (Ollama) ──
# Install Ollama from https://ollama.ai, then pull a model:
#   ollama pull qwen2.5-coder:7b
# For more options: qwen2.5-coder:1.5b (lighter), qwen2.5:7b (general), llama3.2, mistral, codellama
LOCAL_LLM_ENDPOINT=http://localhost:11434/v1
LOCAL_LLM_MODEL=qwen2.5-coder:7b

# ── Database Connections (Optional) ──
# SQLite is built-in with sample data — no configuration needed.
# Uncomment these to enable live PostgreSQL or MySQL execution in the Database tab.
#
# PostgreSQL (psql must be installed):
# PG_CONNECTION_STRING=postgresql://user:password@localhost:5432/mydb
#
# MySQL (mysql CLI must be installed):
# MYSQL_CONNECTION_STRING=mysql://user:password@localhost:3306/mydb

# ── Hybrid Tutor Settings ──
# TINY_LLM_MODEL=Xenova/gte-small    # Transformers.js model for hybrid mode
# TINY_LLM_CACHE_DIR=                # Custom cache directory for model files

# ── Learner Profile Settings ──
# STALE_TOPIC_DAYS=90         # Archive completed topics older than N days (default: 90)
# MAX_TOPICS_PER_LANG=500     # Maximum active topics stored per learner (default: 500)

# ── AI Tutor Settings ──
# AI_MAX_TOKENS=1024
# AI_SYSTEM_PROMPT=You are an expert programming tutor...

# ════════════════════════════════════════════════════════════════
# NOTES
# ════════════════════════════════════════════════════════════════
# • Copy this file to .env and fill in your configuration
# • Never commit .env to version control (it contains secrets)
# • Server logs on startup will show which providers are ready
# • Default (hybrid AI) works out of the box with no API keys — keyword + tiny LLM


[build]
  functions = "netlify/functions"
  publish = "."
  command = "cd backend/rust && cargo build --release && npm run build"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200

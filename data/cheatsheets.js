const cheatsheets = {
  py: {
    "Built-in Functions": [
      "print('hello')  // prints to stdout",
      "len([1,2,3])  // returns length of sequence",
      "range(5)  // generates 0..4",
      "range(2, 8, 2)  // start=2, stop=8, step=2 → 2,4,6",
      "map(str, [1,2,3])  // applies function to every item",
      "filter(lambda x: x>0, [-1,0,1])  // keeps items where predicate is truthy",
      "zip([1,2], ['a','b'])  // pairs elements from iterables",
      "enumerate(['a','b','c'])  // yields (index, value) tuples",
      "sorted([3,1,2])  // returns new sorted list",
      "sorted([3,1,2], reverse=True)  // sorted descending",
      "sorted([3,1,2], key=lambda x: -x)  // sorted with custom key",
      "reversed([1,2,3])  // returns reverse iterator",
      "sum([1,2,3])  // sums iterable",
      "min(3,1,2)  // returns smallest item",
      "max(3,1,2)  // returns largest item",
      "abs(-5)  // absolute value",
      "type(42)  // returns type of object",
      "isinstance(3, int)  // checks type inheritance",
      "hasattr(obj, 'attr')  // checks if attribute exists",
      "getattr(obj, 'attr')  // gets attribute value",
      "getattr(obj, 'attr', default)  // gets attribute with fallback",
      "setattr(obj, 'attr', val)  // sets attribute",
      "delattr(obj, 'attr')  // deletes attribute",
      "dir(obj)  // lists attributes of object",
      "vars(obj)  // returns __dict__ of object",
      "id(obj)  // returns memory address (CPython)",
      "str(42)  // converts to string",
      "int('42')  // converts to int",
      "int('ff', 16)  // base conversion",
      "float('3.14')  // converts to float",
      "bool(0)  // converts to bool (False)",
      "list('abc')  // creates list from iterable",
      "tuple([1,2])  // creates tuple from iterable",
      "set([1,2,2])  // creates set (deduped)",
      "dict([('a',1)])  // creates dict from pairs",
      "open('f.txt')  // opens file (default read text)",
      "open('f.txt', 'w')  // opens file for writing",
      "open('f.txt', encoding='utf-8')  // opens with encoding",
      "input('prompt: ')  // reads line from stdin",
      "super()  // returns proxy to parent class",
      "property(getter, setter)  // creates property descriptor",
      "classmethod(fn)  // decorator for class methods",
      "staticmethod(fn)  // decorator for static methods",
      "eval('1+2')  // evaluates string as expression",
      "exec('x=1')  // executes string as code",
      "compile('x=1','<string>','exec')  // compiles source to code object",
      "globals()  // returns global symbol table dict",
      "locals()  // returns local symbol table dict",
      "__name__  // module name, '__main__' for entry point",
      "__file__  // path to current module file",
      "iter([1,2,3])  // returns iterator object",
      "next(it)  // fetches next item, raises StopIteration",
      "next(it, default)  // fetches next with fallback",
      "all([True, False])  // True if all items are truthy",
      "any([True, False])  // True if any item is truthy",
      "repr(obj)  // unambiguous string representation",
      "ord('A')  // Unicode code point of char",
      "chr(65)  // char from Unicode code point",
      "bin(10)  // binary string '0b1010'",
      "oct(10)  // octal string '0o12'",
      "hex(10)  // hex string '0xa'",
      "round(3.14159, 2)  // rounds to 2 decimal places",
      "pow(2, 10)  // 2**10",
      "pow(2, 10, 7)  // (2**10) % 7 modular exponentiation",
      "divmod(13, 4)  // returns (3, 1) quotient and remainder",
      "format(255, 'x')  // formats value using format spec",
      "hash('hello')  // returns hash of immutable object",
      "help(print)  // invokes interactive help",
      "exit()  // raises SystemExit to exit interpreter",
      "quit()  // same as exit()",
      "object()  // creates base object instance",
      "callable(func)  // checks if object is callable",
      "del x  // deletes reference to x",
      "import os  // imports module",
      "__import__('os')  // low-level import function",
      "slice(1, 5, 2)  // creates slice object for [1:5:2]",
      "memoryview(b'abc')  // provides view of buffer",
      "bytearray(10)  // mutable sequence of bytes",
      "bytes([65,66])  // immutable sequence of bytes",
      "complex(1, 2)  // creates complex number 1+2j",
      "frozenset([1,2,3])  // immutable set",
    ],
    "List Methods": [
      "lst.append(4)  // adds item to end",
      "lst.extend([4,5])  // extends list with iterable",
      "lst.insert(0, 'a')  // inserts at index",
      "lst.remove('a')  // removes first occurrence (ValueError if missing)",
      "lst.pop()  // removes and returns last item",
      "lst.pop(0)  // removes and returns item at index",
      "lst.clear()  // removes all items",
      "lst.index('a')  // returns first index of value",
      "lst.index('a', 2, 5)  // index within slice range",
      "lst.count('a')  // counts occurrences",
      "lst.sort()  // sorts in-place",
      "lst.sort(reverse=True)  // sorts in-place descending",
      "lst.sort(key=len)  // sorts by key function",
      "lst.reverse()  // reverses in-place",
      "lst.copy()  // returns shallow copy",
    ],
    "Dict Methods": [
      "d.keys()  // returns view of keys",
      "d.values()  // returns view of values",
      "d.items()  // returns view of (key,value) pairs",
      "d.get('k')  // returns value or None",
      "d.get('k', default)  // returns value or default",
      "d.setdefault('k', [])  // returns value, sets default if missing",
      "d.update({'k': 2})  // merges dict into d",
      "d.pop('k')  // removes and returns value (KeyError if missing)",
      "d.pop('k', default)  // removes with fallback",
      "d.popitem()  // removes and returns (key,val) in LIFO order (3.7+)",
      "d.clear()  // removes all entries",
      "d.copy()  // returns shallow copy",
      "dict.fromkeys(['a','b'], 0)  // creates dict with default values",
      "d \x7c {'k':2}  // merge operator (3.9+)",
      "d \x7c= {'k':2}  // in-place merge (3.9+)",
    ],
    "String Methods": [
      "s.upper()  // converts to uppercase",
      "s.lower()  // converts to lowercase",
      "s.strip()  // removes leading/trailing whitespace",
      "s.strip('.,!')  // removes given chars from ends",
      "s.lstrip()  // removes leading whitespace",
      "s.rstrip()  // removes trailing whitespace",
      "s.split(',')  // splits into list on delimiter",
      "s.split()  // splits on whitespace",
      "s.split(',', 1)  // splits at most 1 time",
      "','.join(lst)  // joins iterable with separator",
      "s.replace('old', 'new')  // replaces all occurrences",
      "s.replace('old', 'new', 1)  // replaces at most 1",
      "s.find('sub')  // first index of substring (-1 if not found)",
      "s.index('sub')  // first index of substring (ValueError if not found)",
      "s.startswith('pre')  // checks prefix",
      "s.startswith(('a','b'))  // checks tuple of prefixes",
      "s.endswith('suf')  // checks suffix",
      "s.format('hello', 42)  // formats with positional args",
      "s.format(name='world')  // formats with keyword args",
      "f'hello {name}'  // f-string interpolation",
      "f'{x:.2f}'  // f-string with format specifier",
      "f'{x!r}'  // f-string with repr",
      "f'{x!s}'  // f-string with str",
      "s.encode('utf-8')  // encodes to bytes",
      "s.capitalize()  // first char upper, rest lower",
      "s.title()  // first char of each word upper",
      "s.swapcase()  // swaps case of each char",
      "s.center(10)  // centers in field of width 10",
      "s.center(10, '-')  // centers with fill char",
      "s.ljust(10)  // left-justifies in field",
      "s.rjust(10)  // right-justifies in field",
      "s.zfill(5)  // pads with zeros on left",
      "s.count('sub')  // counts non-overlapping occurrences",
      "s.partition(',')  // splits into (before, sep, after)",
      "s.rpartition(',')  // splits from right",
      "s.splitlines()  // splits on line boundaries",
      "s.isalpha()  // checks if all alphabetic",
      "s.isdigit()  // checks if all digits",
      "s.isnumeric()  // checks if numeric (incl. Unicode)",
      "s.isalnum()  // checks if alphanumeric",
      "s.isspace()  // checks if all whitespace",
      "s.isupper()  // checks if all cased chars are uppercase",
      "s.islower()  // checks if all cased chars are lowercase",
      "s.istitle()  // checks if titlecased",
      "s.isdecimal()  // checks if decimal characters",
      "s.isprintable()  // checks if all printable",
      "s.isidentifier()  // checks if valid Python identifier",
      "s.removeprefix('pre')  // strips prefix if present (3.9+)",
      "s.removesuffix('suf')  // strips suffix if present (3.9+)",
      "s.casefold()  // aggressive lower for caseless matching",
      "s.expandtabs(4)  // replaces tabs with spaces",
      "s.translate(table)  // translates chars via translation table",
      "str.maketrans('ab','xy')  // builds translation table",
    ],
    "Set Methods": [
      "s.add(5)  // adds element to set",
      "s.remove(5)  // removes element (KeyError if missing)",
      "s.discard(5)  // removes element if present",
      "s.pop()  // removes and returns arbitrary element",
      "s.clear()  // removes all elements",
      "s.union(t)  // returns s \x7c t",
      "s.intersection(t)  // returns s & t",
      "s.difference(t)  // returns s - t",
      "s.symmetric_difference(t)  // returns s ^ t",
      "s.update(t)  // in-place union s \x7c= t",
      "s.intersection_update(t)  // in-place intersection s &= t",
      "s.difference_update(t)  // in-place difference s -= t",
      "s.symmetric_difference_update(t)  // in-place s ^= t",
      "s.issubset(t)  // True if s subset of t",
      "s.issuperset(t)  // True if s superset of t",
      "s.isdisjoint(t)  // True if no common elements",
    ],
    "Operators": [
      "x + y  // addition or concatenation",
      "x - y  // subtraction",
      "x * y  // multiplication or repetition",
      "x / y  // true division (returns float)",
      "x // y  // floor division",
      "x % y  // modulo (remainder)",
      "x ** y  // exponentiation (pow)",
      "x @ y  // matrix multiplication (3.5+)",
      "x << y  // left bitwise shift",
      "x >> y  // right bitwise shift",
      "x & y  // bitwise AND",
      "x \x7c y  // bitwise OR",
      "x ^ y  // bitwise XOR",
      "~x  // bitwise NOT",
      "x < y  // less than",
      "x > y  // greater than",
      "x <= y  // less than or equal",
      "x >= y  // greater than or equal",
      "x == y  // equality check",
      "x != y  // inequality check",
      "x and y  // logical AND (short-circuit)",
      "x or y  // logical OR (short-circuit)",
      "not x  // logical NOT",
      "x in lst  // membership test",
      "x not in lst  // negated membership",
      "x is y  // identity test (same object)",
      "x is not y  // negated identity test",
      "x := expr  // walrus operator: assign and use in expression (3.8+)",
    ],
    "Control Flow": [
      "if x > 0:\n    pass  // conditional execution",
      "if x > 0:\n    pass\nelse:\n    pass  // if-else",
      "if x > 0:\n    pass\nelif x == 0:\n    pass\nelse:\n    pass  // if-elif-else",
      "for x in iterable:\n    pass  // iteration over sequence",
      "for i, val in enumerate(lst):\n    pass  // loop with index",
      "for x in lst:\n    if cond:\n        break  // exits loop early",
      "for x in lst:\n    if cond:\n        continue  // skips to next iteration",
      "for x in lst:\n    pass\nelse:\n    pass  // else runs if no break",
      "while cond:\n    pass  // loop while condition true",
      "while True:\n    break  // infinite loop with break",
      "try:\n    risky()\nexcept ValueError:\n    pass  // catches specific exception",
      "try:\n    risky()\nexcept (ValueError, TypeError) as e:\n    pass  // catches multiple",
      "try:\n    risky()\nexcept:\n    pass  // bare except (catches all)",
      "try:\n    risky()\nexcept Exception:\n    pass\nelse:\n    pass  // else runs if no exception",
      "try:\n    risky()\nexcept Exception:\n    pass\nfinally:\n    pass  // finally always runs",
      "with open('f.txt') as f:\n    pass  // context manager",
      "with A() as a, B() as b:\n    pass  // multiple context managers",
      "match val:\n    case 1:\n        pass  // pattern match (3.10+)",
      "match val:\n    case 'a' \x7c 'b':\n        pass  // OR pattern",
      "match val:\n    case [x, y]:\n        pass  // sequence pattern (destructuring)",
      "match val:\n    case {'k': v}:\n        pass  // mapping pattern",
      "match val:\n    case int():\n        pass  // class pattern (type check)",
      "match val:\n    case _:\n        pass  // wildcard pattern (default)",
    ],
    "Comprehensions": [
      "[x**2 for x in range(5)]  // list comprehension: [0,1,4,9,16]",
      "[x for x in range(10) if x%2==0]  // list comp with filter",
      "[x*y for x in [1,2] for y in [3,4]]  // nested loops in list comp",
      "{x: x**2 for x in range(5)}  // dict comprehension",
      "{x for x in [1,1,2,3]}  // set comprehension: {1,2,3}",
      "(x**2 for x in range(5))  // generator expression (lazy)",
      "[x for x in lst if cond else val]  // conditional expression in comp",
      "[[0]*n for _ in range(m)]  // nested list comprehension (matrix)",
    ],
    "Decorators": [
      "@property\n    def name(self):\n        return self._name  // computed attribute access",
      "@name.setter\n    def name(self, val):\n        self._name = val  // property setter",
      "@name.deleter\n    def name(self):\n        del self._name  // property deleter",
      "@staticmethod\n    def f(x):\n        return x  // static method (no self)",
      "@classmethod\n    def f(cls, x):\n        return x  // class method receives cls",
      "@functools.lru_cache(maxsize=128)\n    def f(x):\n        return x  // memoization decorator",
      "@functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        return func(*args, **kwargs)  // preserves metadata in wrapper",
      "@dataclass\n    class Point:\n        x: int  // auto-generates __init__, __repr__, __eq__",
      "@abc.abstractmethod\n    def f(self):\n        pass  // declares abstract method",
    ],
    "Generators": [
      "def gen():\n    yield 1\n    yield 2  // generator function with yield",
      "def gen():\n    yield from [1,2,3]  // delegates to sub-iterator",
      "next(gen())  // advances generator",
      "gen.send(val)  // sends value back to paused generator",
      "gen.throw(ValueError)  // raises exception inside generator",
      "gen.close()  // raises GeneratorExit inside generator",
    ],
    "Async": [
      "async def f():\n    pass  // defines coroutine function",
      "await coro  // suspends until coroutine completes",
      "async for x in async_iter:\n    pass  // async iteration",
      "async with async_cm:\n    pass  // async context manager",
      "asyncio.run(main())  // runs coroutine as entry point",
      "asyncio.gather(coro1(), coro2())  // runs coroutines concurrently",
      "asyncio.create_task(coro())  // schedules coroutine as task",
      "asyncio.sleep(1)  // non-blocking sleep",
      "async def __aiter__(self):\n    return self  // async iterator protocol",
      "async def __anext__(self):\n    return val  // async next method",
      "async def __aenter__(self):\n    return self  // async enter method",
      "async def __aexit__(self, *e):\n    pass  // async exit method",
    ],
    "Classes": [
      "class MyClass:\n    pass  // class definition",
      "class MyClass(Base):\n    pass  // class with inheritance",
      "class MyClass(Base1, Base2):\n    pass  // multiple inheritance",
      "def __init__(self, x):\n    self.x = x  // constructor (initializer)",
      "def __str__(self):\n    return 'str'  // string for user (str())",
      "def __repr__(self):\n    return 'Repr()'  // unambiguous repr (repr())",
      "def __len__(self):\n    return 0  // length for len()",
      "def __getitem__(self, k):\n    return self.data[k]  // indexing: obj[k]",
      "def __setitem__(self, k, v):\n    self.data[k] = v  // assignment: obj[k]=v",
      "def __delitem__(self, k):\n    del self.data[k]  // deletion: del obj[k]",
      "def __call__(self, x):\n    return x  // makes instance callable",
      "def __enter__(self):\n    return self  // context manager entry",
      "def __exit__(self, *e):\n    pass  // context manager exit",
      "def __iter__(self):\n    return iter(self.data)  // iterator protocol",
      "def __next__(self):\n    return next(self.data)  // iterator next",
      "def __contains__(self, x):\n    return x in self.data  // 'in' operator",
      "def __add__(self, o):\n    return self.val + o.val  // + operator",
      "def __eq__(self, o):\n    return self.x == o.x  // == operator",
      "def __hash__(self):\n    return hash(self.x)  // hash for dict/set",
      "def __bool__(self):\n    return bool(self.x)  // truthiness",
      "def __del__(self):\n    pass  // destructor (unreliable)",
      "def __new__(cls, *a, **kw):\n    return super().__new__(cls)  // allocator (before __init__)",
      "__slots__ = ('x', 'y')  // restricts attributes, saves memory",
      "self.__class__  // reference to instance's class",
    ],
    "Modules & Imports": [
      "import os  // imports module",
      "from os import path  // imports specific name",
      "from os import path as p  // imports with alias",
      "from os import *  // imports all public names",
      "import os.path  // imports submodule",
      "__all__ = ['func1', 'Class1']  // controls 'from module import *'",
      "if __name__ == '__main__':\n    main()  // guard for script execution",
      "importlib.reload(mod)  // reloads already-imported module",
    ],
    "Typing & Type Hints": [
      "x: int = 5  // type hint for variable",
      "def f(x: int) -> str:\n    return str(x)  // parameter and return type hint",
      "x: List[int] = [1,2,3]  // generic list type hint",
      "x: Dict[str, int] = {'a': 1}  // generic dict type hint",
      "x: Tuple[int, str] = (1, 'a')  // tuple with fixed types",
      "x: Tuple[int, ...] = (1,2,3)  // variable-length tuple",
      "x: Set[int] = {1,2}  // set type hint",
      "x: Optional[int] = None  // nullable type hint",
      "x: Union[int, str] = 5  // union type hint",
      "x: Any  // any type (no checking)",
      "Callable[[int, int], int]  // function type hint",
      "TypeVar('T')  // generic type variable",
      "Generic[T]  // generic base class",
      "Protocol  // structural subtyping (duck typing)",
      "Literal['a', 'b']  // literal type constraint",
      "Final = 42  // constant type (should not be reassigned)",
      "@overload\n    def f(x: int) -> int: ...  // overloaded signatures",
      "NewType('UserId', int)  // distinct type (at lint time only)",
      "x: Type[MyClass] = MyClass  // class itself as type",
      "cast(int, x)  // type cast (no runtime effect)",
      "TYPE_CHECKING  // True during type checking, False at runtime",
    ],
    "Exception Types": [
      "raise Exception('msg')  // base exception",
      "raise ValueError('bad value')  // value error",
      "raise TypeError('wrong type')  // type mismatch",
      "raise KeyError('missing')  // dict key not found",
      "raise IndexError('out of range')  // sequence index out of range",
      "raise AttributeError('no attr')  // attribute not found",
      "raise RuntimeError('generic')  // runtime error",
      "raise StopIteration  // signals end of iteration",
      "raise StopAsyncIteration  // signals end of async iteration",
      "raise ArithmeticError  // base for math errors",
      "raise EOFError  // input() hit EOF",
      "raise ImportError('no module')  // import failed",
      "raise ModuleNotFoundError('no module')  // module not found",
      "raise NameError('undefined')  // name not defined",
      "raise OSError('IO problem')  // OS error (base for IOError)",
      "raise FileNotFoundError('no file')  // file not found",
      "raise PermissionError('denied')  // permission denied",
      "raise RecursionError('depth')  // max recursion depth exceeded",
      "raise SystemExit(0)  // exit interpreter",
      "raise KeyboardInterrupt  // Ctrl+C pressed",
      "raise NotImplementedError  // abstract method not implemented",
      "raise AssertionError  // assert statement failed",
      "raise FloatingPointError  // float operation failed",
      "raise ZeroDivisionError  // division by zero",
      "raise MemoryError  // out of memory",
      "raise SystemError  // internal interpreter error",
    ],
    "File I/O": [
      "f = open('f.txt')  // opens for reading",
      "f = open('f.txt', 'w')  // opens for writing (truncates)",
      "f = open('f.txt', 'a')  // opens for appending",
      "f = open('f.txt', 'r+')  // opens for read and write",
      "f = open('f.txt', 'rb')  // opens in binary mode for read",
      "f = open('f.txt', 'x')  // exclusive creation, fails if exists",
      "f.read()  // reads entire file as string",
      "f.read(100)  // reads up to 100 chars/bytes",
      "f.readline()  // reads one line (including newline)",
      "f.readlines()  // reads all lines into list",
      "f.write('hello')  // writes string to file",
      "f.writelines(lst)  // writes iterable of strings (no newlines added)",
      "f.seek(0)  // moves to byte offset 0",
      "f.seek(10, os.SEEK_SET)  // seek from start",
      "f.seek(5, os.SEEK_CUR)  // seek from current position",
      "f.seek(-2, os.SEEK_END)  // seek from end",
      "f.tell()  // returns current file position",
      "f.flush()  // forces write buffer to disk",
      "f.close()  // closes file",
      "f.closed  // True if file is closed",
      "f.name  // filename",
      "f.mode  // access mode string",
      "with open('f.txt') as f:\n    data = f.read()  // auto-closes on exit",
      "with open('f.txt', 'w') as f:\n    f.write('data')  // write with auto-close",
    ],
    "os & sys": [
      "os.path.join('dir', 'f.txt')  // joins path components",
      "os.path.exists('f.txt')  // checks if path exists",
      "os.path.isfile('f.txt')  // checks if path is a file",
      "os.path.isdir('dir')  // checks if path is a directory",
      "os.path.abspath('f.txt')  // returns absolute path",
      "os.path.dirname('/a/b/c')  // returns '/a/b'",
      "os.path.basename('/a/b/c')  // returns 'c'",
      "os.path.splitext('f.txt')  // returns ('f', '.txt')",
      "os.getenv('HOME')  // gets environment variable",
      "os.getenv('VAR', 'default')  // env var with fallback",
      "os.environ['HOME']  // dict-like access to env vars",
      "os.listdir('.')  // lists directory contents",
      "os.mkdir('newdir')  // creates directory",
      "os.makedirs('a/b/c')  // creates nested directories",
      "os.remove('f.txt')  // deletes file",
      "os.rmdir('empty')  // removes empty directory",
      "os.rename('old', 'new')  // renames file/directory",
      "os.getcwd()  // returns current working directory",
      "os.chdir('/tmp')  // changes working directory",
      "os.walk('/root')  // generates (dir, subdirs, files) tuples",
      "sys.argv  // list of command-line arguments",
      "sys.exit(0)  // exits with status code",
      "sys.exit('error msg')  // exits with message and code 1",
      "sys.path  // list of module search paths",
      "sys.modules  // dict of imported modules",
      "sys.platform  // platform identifier ('linux', 'win32', etc)",
      "sys.version  // Python version string",
      "sys.stdin  // standard input file object",
      "sys.stdout  // standard output file object",
      "sys.stderr  // standard error file object",
      "sys.getrecursionlimit()  // returns recursion limit",
      "sys.setrecursionlimit(2000)  // sets recursion limit",
    ],
    "itertools": [
      "itertools.chain([1,2],[3,4])  // concatenates iterables",
      "itertools.cycle('ABC')  // cycles infinitely through elements",
      "itertools.repeat(10, 3)  // repeats value n times",
      "itertools.combinations('ABC', 2)  // all r-length combinations",
      "itertools.combinations_with_replacement('ABC', 2)  // combos with replacement",
      "itertools.permutations('ABC', 2)  // all r-length permutations",
      "itertools.product('AB', '12')  // cartesian product",
      "itertools.groupby(data, key=func)  // groups consecutive elements by key",
      "itertools.accumulate([1,2,3])  // running totals (or other binary op)",
      "itertools.count(start=10, step=2)  // infinite arithmetic progression",
      "itertools.compress(data, selectors)  // filters with boolean selector",
      "itertools.dropwhile(pred, seq)  // drops items while pred true, then yields rest",
      "itertools.takewhile(pred, seq)  // yields items while pred true",
      "itertools.filterfalse(pred, seq)  // filters where predicate is false",
      "itertools.starmap(pow, [(2,5),(3,2)])  // maps with expanded args",
      "itertools.tee(iter, n=3)  // splits iterator into n independent iterators",
      "itertools.zip_longest('AB', '123', fillvalue='?')  // zips to longest length",
      "itertools.pairwise([1,2,3,4])  // yields consecutive pairs (3.10+)",
      "itertools.islice(range(10), 2, 8, 2)  // lazy slicing on iterator",
    ],
    "functools": [
      "functools.reduce(operator.add, [1,2,3])  // folds sequence left to right",
      "functools.partial(func, arg1)  // partially applies arguments",
      "functools.lru_cache(maxsize=128)  // memoization decorator",
      "functools.cache  // unbounded lru_cache (3.9+)",
      "functools.wraps(func)  // copies metadata to wrapper",
      "functools.cached_property  // lazily computed, cached property",
      "functools.singledispatch  // single-dispatch generic function",
      "functools.update_wrapper(wrap, wrapped)  // updates wrapper metadata",
      "functools.total_ordering  // fills in missing comparison operators",
    ],
    "collections": [
      "collections.namedtuple('Point', ['x','y'])  // tuple with named fields",
      "collections.defaultdict(int)  // dict with default factory for missing keys",
      "collections.Counter('abcabc')  // multiset counting occurrences",
      "collections.deque([1,2,3])  // double-ended queue",
      "collections.deque([1,2,3], maxlen=5)  // bounded deque",
      "collections.OrderedDict()  // dict that preserves insertion order (3.7+ dicts do too)",
      "collections.ChainMap(d1, d2)  // views multiple dicts as one",
      "collections.UserDict()  // wrapper class for dict subclassing",
      "collections.UserList()  // wrapper class for list subclassing",
      "collections.UserString()  // wrapper class for string subclassing",
      "collections.abc.Sequence  // ABC for sequences",
      "collections.abc.MutableSequence  // ABC for mutable sequences",
      "collections.abc.Mapping  // ABC for mappings",
      "collections.abc.MutableMapping  // ABC for mutable mappings",
      "collections.abc.Set  // ABC for sets",
      "collections.abc.Iterable  // ABC for iterables",
      "collections.abc.Iterator  // ABC for iterators",
      "collections.abc.Callable  // ABC for callables",
    ],
  },

  go: {
    "Keywords": [
      "break  // exits innermost loop or switch",
      "default  // default case in switch/select",
      "func  // function declaration",
      "interface  // type with method set",
      "select  // waits on channel operations",
      "case  // case in switch, select",
      "defer  // schedules call to run at function return",
      "go  // launches goroutine",
      "map  // built-in associative data type",
      "struct  // composite type with fields",
      "chan  // channel type for concurrency",
      "else  // if-else alternative",
      "goto  // jumps to label",
      "package  // declares package name",
      "switch  // multi-way branch",
      "const  // immutable declaration",
      "fallthrough  // continues to next switch case",
      "if  // conditional execution",
      "range  // iterates over slices, maps, channels",
      "type  // type declaration or alias",
      "continue  // skips to next loop iteration",
      "for  // loop construct (only loop in Go)",
      "import  // imports packages",
      "return  // returns from function",
      "var  // variable declaration",
    ],
    "Types": [
      "int  // platform-dependent signed integer",
      "int8  // 8-bit signed integer",
      "int16  // 16-bit signed integer",
      "int32  // 32-bit signed integer",
      "int64  // 64-bit signed integer",
      "uint  // platform-dependent unsigned integer",
      "uint8  // 8-bit unsigned integer (byte)",
      "uint16  // 16-bit unsigned integer",
      "uint32  // 32-bit unsigned integer",
      "uint64  // 64-bit unsigned integer",
      "float32  // 32-bit IEEE 754 float",
      "float64  // 64-bit IEEE 754 float",
      "complex64  // complex with float32 real/imag",
      "complex128  // complex with float64 real/imag",
      "bool  // boolean (true/false)",
      "string  // immutable sequence of bytes (UTF-8)",
      "byte  // alias for uint8",
      "rune  // alias for int32 (Unicode code point)",
      "error  // built-in interface for errors",
      "any  // alias for interface{} (Go 1.18+)",
      "comparable  // constraint for comparable types (Go 1.18+)",
      "uintptr  // unsigned integer large enough for pointer bits",
      "nil  // zero value for pointers, slices, maps, channels, interfaces",
      "true  // boolean true literal",
      "false  // boolean false literal",
      "iota  // predeclared identifier for const enumerations",
    ],
    "Declaration & Variables": [
      "var x int = 5  // variable declaration with type",
      "var x = 5  // variable declaration with type inference",
      "var x, y int = 1, 2  // multiple declaration",
      "x := 5  // short declaration (inside functions)",
      "x, y := 1, 'a'  // short declaration multiple",
      "const Pi = 3.14  // constant declaration",
      "const (\n    A = iota\n    B\n    C\n)  // enumerated constants with iota",
      "const (\n    _ = iota\n    KB = 1 << (10 * iota)\n    MB\n)  // iota skip and expressions",
      "type Age int  // type definition",
      "type Person struct {\n    Name string\n}  // struct type declaration",
      "type Stringer interface {\n    String() string\n}  // interface type declaration",
      "type Handler func(w http.ResponseWriter, r *http.Request)  // function type",
      "type MySlice []int  // slice type alias",
      "var arr [3]int  // array declaration (fixed size)",
      "arr := [...]int{1,2,3}  // array with inferred size",
    ],
    "Control Flow": [
      "if x > 0 {\n}  // if statement (no parentheses)",
      "if x > 0 {\n} else {\n}  // if-else",
      "if x > 0 {\n} else if y > 0 {\n} else {\n}  // if-else if-else",
      "if err := do(); err != nil {\n}  // if with initialization statement",
      "for i := 0; i < 10; i++ {\n}  // C-style for loop",
      "for i < 10 {\n    i++\n}  // for as while loop",
      "for {\n    break\n}  // infinite loop",
      "for i, v := range slice {\n}  // range over slice (index, value)",
      "for v := range slice {\n}  // range with index only",
      "for _, v := range slice {\n}  // range skipping index",
      "for k, v := range m {\n}  // range over map",
      "for v := range ch {\n}  // range over channel (reads until closed)",
      "switch x {\ncase 1:\n    fmt.Println(\"one\")\ncase 2:\n    fallthrough\ncase 3:\n    fmt.Println(\"two or three\")\ndefault:\n    fmt.Println(\"other\")\n}  // switch (no need break; fallthrough goes to next)",
      "switch {\ncase x > 0:\n    fmt.Println(\"positive\")\ncase x < 0:\n    fmt.Println(\"negative\")\n}  // switch true (replaces if-else chain)",
      "switch x := val.(type) {\ncase int:\n    fmt.Println(x + 1)\ncase string:\n    fmt.Println(len(x))\n}  // type switch",
      "select {\ncase msg := <-ch1:\n    fmt.Println(msg)\ncase ch2 <- val:\n    fmt.Println(\"sent\")\ndefault:\n    fmt.Println(\"neither ready\")\n}  // select waits on channel operations",
      "defer f.Close()  // defers execution until surrounding function returns",
      "defer func() {\n    if r := recover(); r != nil {\n        fmt.Println(\"recovered:\", r)\n    }\n}()  // defer with recover to catch panic",
      "panic(\"something broke\")  // triggers panic (unwinds stack)",
      "recover()  // regains control from panic (only in deferred function)",
    ],
    "Functions": [
      "func add(a, b int) int {\n    return a + b\n}  // basic function",
      "func add(a, b int) (int, error) {\n    return a + b, nil\n}  // multiple return values",
      "func div(a, b int) (int, error) {\n    if b == 0 {\n        return 0, errors.New(\"division by zero\")\n    }\n    return a / b, nil\n}  // error return idiom",
      "func sum(nums ...int) int  // variadic function",
      "sum(1,2,3)  // calling variadic function",
      "sum(nums...)  // expanding slice as variadic args",
      "func() {\n    fmt.Println(\"anonymous\")\n}()  // anonymous function (closure)",
      "fn := func(x int) int { return x * 2 }  // assigning closure to variable",
      "func makeAdder(x int) func(int) int {\n    return func(y int) int { return x + y }\n}  // closure returning closure",
      "func init() {\n    // runs at package init\n}  // init function (no params, no returns)",
      "func main() {\n}  // program entry point",
      "func (r Rect) Area() float64 {\n    return r.W * r.H\n}  // method with value receiver",
      "func (r *Rect) Scale(f float64) {\n    r.W *= f\n}  // method with pointer receiver",
      "func named() (result int) {\n    result = 42\n    return\n}  // named return values (naked return)",
    ],
    "Concurrency": [
      "go doSomething()  // launches goroutine",
      "ch := make(chan int)  // creates unbuffered channel",
      "ch := make(chan int, 10)  // creates buffered channel",
      "ch <- 42  // sends value to channel",
      "val := <-ch  // receives from channel",
      "val, ok := <-ch  // receive; ok=false if channel closed",
      "close(ch)  // closes channel",
      "for v := range ch {\n}  // reads until channel is closed",
      "select {\ncase v := <-ch1:\n    fmt.Println(v)\ndefault:\n    fmt.Println(\"no ready\")\n}  // non-blocking select with default",
      "var mu sync.Mutex\nmu.Lock()\n// critical section\nmu.Unlock()  // mutex mutual exclusion",
      "var mu sync.RWMutex\nmu.RLock()\n// read section\nmu.RUnlock()  // read-lock (multiple concurrent readers)",
      "var wg sync.WaitGroup\nwg.Add(1)\ngo func() { defer wg.Done(); work() }()\nwg.Wait()  // waits for goroutines to complete",
      "var once sync.Once\nonce.Do(func() { fmt.Println(\"once\")\n})  // runs function exactly once",
      "var sm sync.Map\nsm.Store(\"k\", \"v\")\nsm.Load(\"k\")  // concurrent-safe map",
      "var counter atomic.Int64\ncounter.Add(1)\ncounter.Load()  // atomic operations (Go 1.19+)",
      "var v atomic.Value\nv.Store(x)\nx = v.Load().(T)  // atomic store/load for any type",
      "ctx, cancel := context.WithCancel(parent)\nctx, cancel := context.WithTimeout(parent, 5*time.Second)\nctx := context.WithValue(parent, key, val)  // context patterns",
      "ctx.Done()  // channel closed when context is cancelled/timeout",
      "ctx.Err()  // nil if not done, Canceled or DeadlineExceeded",
      "select {\ncase <-ctx.Done():\n    return ctx.Err()\ncase result <- work():\n}  // context-aware select",
      "runtime.GOMAXPROCS(4)  // sets max OS threads for goroutines",
      "runtime.NumGoroutine()  // returns current goroutine count",
    ],
    "Slices": [
      "s := []int{1, 2, 3}  // slice literal",
      "s := make([]int, 5)  // creates slice of length 5 (zeroed)",
      "s := make([]int, 5, 10)  // creates slice with length 5, capacity 10",
      "s = append(s, 4)  // appends to slice",
      "s = append(s, 5, 6, 7)  // appends multiple elements",
      "s = append(s, other...)  // appends all elements of another slice",
      "n := copy(dst, src)  // copies elements, returns count",
      "len(s)  // length of slice",
      "cap(s)  // capacity of slice",
      "s[1:3]  // slice slice (half-open range)",
      "s[:2]  // slice from start to index 2",
      "s[2:]  // slice from index 2 to end",
      "s[:]  // full slice (rebound without capacity change)",
      "var s []int  // nil slice (len=0, cap=0, no backing array)",
      "s := []int{}  // empty slice (not nil)",
      "s = nil  // sets slice to nil",
      "s[0] = 42  // assigns to element",
      "v := s[0]  // reads element",
    ],
    "Maps": [
      "m := make(map[string]int)  // creates empty map",
      "m := map[string]int{\"a\": 1}  // map literal",
      "m[\"b\"] = 2  // sets key-value",
      "v := m[\"a\"]  // gets value (zero value if missing)",
      "v, ok := m[\"a\"]  // comma ok idiom: ok=true if key exists",
      "delete(m, \"a\")  // deletes key from map",
      "len(m)  // number of entries",
      "for k, v := range m {\n}  // iterates over map (unsorted)",
      "for k := range m {\n}  // iterates over keys",
      "var m map[string]int  // nil map (cannot write, can read)",
      "m := map[string]int{}  // non-nil empty map",
      "clear(m)  // removes all entries (Go 1.21+)",
    ],
    "Strings & strconv": [
      "strings.Split(\"a,b,c\", \",\")  // splits on separator",
      "strings.SplitN(\"a,b,c\", \",\", 2)  // splits at most n parts",
      "strings.Join([]string{\"a\",\"b\"}, \",\")  // joins strings with separator",
      "strings.Trim(\"  hi  \", \" \")  // trims cutset from both ends",
      "strings.TrimSpace(\"  hi  \")  // trims whitespace",
      "strings.TrimPrefix(\"pre_hi\", \"pre_\")  // trims prefix",
      "strings.TrimSuffix(\"hi_suf\", \"_suf\")  // trims suffix",
      "strings.Fields(\"a b  c\")  // splits on whitespace (handles multiple)",
      "strings.HasPrefix(\"hello\", \"he\")  // checks prefix",
      "strings.HasSuffix(\"hello\", \"lo\")  // checks suffix",
      "strings.Contains(\"hello\", \"ell\")  // checks substring",
      "strings.Index(\"hello\", \"l\")  // first index of substring",
      "strings.LastIndex(\"hello\", \"l\")  // last index of substring",
      "strings.ToLower(\"HELLO\")  // converts to lowercase",
      "strings.ToUpper(\"hello\")  // converts to uppercase",
      "strings.Replace(\"hello\", \"l\", \"x\", 2)  // replaces n occurrences",
      "strings.ReplaceAll(\"hello\", \"l\", \"x\")  // replaces all occurrences",
      "strings.Count(\"hello\", \"l\")  // counts occurrences",
      "strings.Repeat(\"ha\", 3)  // repeats string n times",
      "strings.Compare(\"a\", \"b\")  // -1, 0, or 1 (lexicographic)",
      "strings.Cut(\"a=b\", \"=\")  // splits at first sep, returns before, after, found",
      "strconv.Itoa(42)  // int to ASCII string",
      "n, _ := strconv.Atoi(\"42\")  // ASCII string to int",
      "strconv.FormatFloat(3.14, 'f', 2, 64)  // float to string",
      "f, _ := strconv.ParseFloat(\"3.14\", 64)  // string to float64",
      "strconv.FormatBool(true)  // bool to string",
      "b, _ := strconv.ParseBool(\"true\")  // string to bool",
      "strconv.FormatInt(255, 16)  // int64 to string base 16",
      "n, _ := strconv.ParseInt(\"ff\", 16, 32)  // parse hex to int64",
      "strconv.Quote(\"hello\")  // returns quoted Go string literal",
      "strconv.Unquote(\"\\\"hello\\\"\")  // unquotes string literal",
    ],
    "fmt": [
      "fmt.Print(\"hello\")  // prints without newline",
      "fmt.Println(\"hello\", 42)  // prints with spaces and newline",
      "fmt.Printf(\"hello %s %d\\n\", \"world\", 42)  // formatted print",
      "fmt.Sprintf(\"hello %s\", \"world\")  // returns formatted string",
      "fmt.Sprintf(\"%%\")  // literal percent sign",
      "fmt.Sprintf(\"%v\", val)  // default format (value)",
      "fmt.Sprintf(\"%+v\", val)  // struct field names with values",
      "fmt.Sprintf(\"%#v\", val)  // Go-syntax representation",
      "fmt.Sprintf(\"%T\", val)  // type of value",
      "fmt.Sprintf(\"%d\", 42)  // decimal integer",
      "fmt.Sprintf(\"%b\", 42)  // binary",
      "fmt.Sprintf(\"%x\", 255)  // hexadecimal (lowercase)",
      "fmt.Sprintf(\"%X\", 255)  // hexadecimal (uppercase)",
      "fmt.Sprintf(\"%s\", \"hi\")  // string",
      "fmt.Sprintf(\"%q\", \"hi\")  // quoted string (safe for Go source)",
      "fmt.Sprintf(\"%f\", 3.14)  // float (default precision 6)",
      "fmt.Sprintf(\"%.2f\", 3.14159)  // float with precision",
      "fmt.Sprintf(\"%e\", 3.14)  // scientific notation",
      "fmt.Sprintf(\"%g\", 3.14)  // compact scientific or decimal",
      "fmt.Sprintf(\"%t\", true)  // boolean",
      "fmt.Sprintf(\"%p\", &x)  // pointer (hex)",
      "fmt.Sprintf(\"%6d\", 42)  // width 6, right-justified",
      "fmt.Sprintf(\"%-6d\", 42)  // width 6, left-justified",
      "fmt.Sprintf(\"%06d\", 42)  // zero-padded width 6",
      "fmt.Sprintf(\"%+d\", 42)  // always show sign",
      "fmt.Errorf(\"error: %s\", msg)  // creates error with formatting",
      "fmt.Fprint(w, \"hello\")  // prints to io.Writer",
      "fmt.Fprintf(w, \"val=%d\", 42)  // formatted write to io.Writer",
      "fmt.Scan(&x)  // reads whitespace-delimited from stdin",
      "fmt.Sscanf(\"42\", \"%d\", &n)  // scans formatted string",
      "fmt.Fscan(r, &x)  // reads from io.Reader",
    ],
    "Errors": [
      "errors.New(\"something broke\")  // creates a simple error",
      "fmt.Errorf(\"wrapped: %w\", err)  // wraps error with %w (Go 1.13+)",
      "errors.Is(err, target)  // checks if error wraps target",
      "errors.As(err, &target)  // finds first error matching target type",
      "errors.Unwrap(err)  // returns wrapped error (or nil)",
      "err != nil  // standard error check idiom",
      "if err != nil {\n    return err\n}  // propagate error",
      "if err != nil {\n    log.Fatal(err)\n}  // log and exit",
      "type MyError struct {\n    Msg string\n}\nfunc (e *MyError) Error() string { return e.Msg }  // custom error type",
    ],
    "File I/O": [
      "os.Open(\"f.txt\")  // opens file for reading (*File, error)",
      "os.Create(\"f.txt\")  // creates/truncates file for writing",
      "os.ReadFile(\"f.txt\")  // reads entire file into []byte (Go 1.16+)",
      "os.WriteFile(\"f.txt\", data, 0644)  // writes bytes to file (Go 1.16+)",
      "os.OpenFile(\"f.txt\", os.O_RDWR|os.O_CREATE, 0644)  // open with flags",
      "f.Read(buf)  // reads into []byte, returns n, err",
      "f.ReadAt(buf, offset)  // reads at specific offset",
      "f.Write(data)  // writes []byte, returns n, err",
      "f.WriteString(\"hello\")  // writes string to file",
      "f.Seek(0, io.SeekStart)  // seeks to position",
      "f.Close()  // closes file",
      "f.Sync()  // flushes to disk",
      "defer f.Close()  // idiomatic deferred close",
      "os.Stat(\"f.txt\")  // returns FileInfo",
      "os.IsNotExist(err)  // checks if error is file not found",
      "os.Rename(\"old\", \"new\")  // renames file",
      "os.Remove(\"f.txt\")  // deletes file",
      "os.Mkdir(\"dir\", 0755)  // creates directory",
      "os.MkdirAll(\"a/b/c\", 0755)  // creates nested directories",
      "os.ReadDir(\".\")  // reads directory entries (Go 1.16+)",
      "bufio.NewReader(f)  // buffered reader",
      "bufio.NewScanner(f)  // scanner for line-by-line reading",
      "scanner.Scan()  // advances scanner, returns bool",
      "scanner.Text()  // returns current token as string",
      "scanner.Err()  // returns scanner error",
      "bufio.NewWriter(f)  // buffered writer",
      "w.Flush()  // flushes buffered writer",
      "io.Copy(w, r)  // copies from Reader to Writer until EOF",
      "io.ReadAll(r)  // reads all from Reader (Go 1.16+)",
      "os.Stdin  // standard input (io.Reader)",
      "os.Stdout  // standard output (io.Writer)",
      "os.Stderr  // standard error (io.Writer)",
      "os.Args  // command-line arguments slice",
    ],
    "Struct & JSON": [
      "type Point struct {\n    X int\n    Y int\n}  // struct definition",
      "p := Point{X: 1, Y: 2}  // struct literal with field names",
      "p := Point{1, 2}  // struct literal positional (order matters)",
      "p := &Point{1, 2}  // pointer to struct",
      "p := Point{}  // zero-valued struct",
      "p.X = 42  // field access",
      "v := p.X  // field read",
      "type Rect struct {\n    Width, Height int\n}  // struct with adjacent types",
      "type Employee struct {\n    Name string\n    Manager *Employee\n}  // recursive struct (pointer)",
      "type Circle struct {\n    Radius float64\n}\nfunc (c Circle) Area() float64 {\n    return math.Pi * c.Radius * c.Radius\n}  // struct method",
      "type Manager struct {\n    Employee\n    Level int\n}  // struct embedding (promotion)",
      "type Person struct {\n    Name string `json:\"name\"`\n}  // struct tags (json)",
      "type Person struct {\n    Name string `json:\"name,omitempty\"`\n}  // json tag with omitempty",
      "type Person struct {\n    Name string `json:\"-\"`\n}  // json tag: skip field",
      "data, _ := json.Marshal(p)  // struct to JSON bytes",
      "data, _ := json.MarshalIndent(p, \"\", \"  \")  // pretty-printed JSON",
      "var p Person\njson.Unmarshal(data, &p)  // JSON bytes to struct",
      "json.NewEncoder(w).Encode(p)  // stream JSON to writer",
      "json.NewDecoder(r).Decode(&p)  // stream JSON from reader",
      "json.Valid(data)  // checks if bytes are valid JSON",
      "json.RawMessage  // defers JSON decoding",
    ],
    "Testing": [
      "import \"testing\"  // testing package",
      "func TestX(t *testing.T) {\n}  // basic test function (run with go test)",
      "t.Error(\"got\", got, \"want\", want)  // test failure (non-fatal)",
      "t.Errorf(\"got %d, want %d\", got, want)  // formatted error",
      "t.Fatal(\"failed\")  // test failure (fatal, stops immediately)",
      "t.Fatalf(\"got %d, want %d\", got, want)  // formatted fatal",
      "t.Log(\"info\")  // log message (shown with -v)",
      "t.Run(\"subcase\", func(t *testing.T) {\n})  // subtest",
      "t.Cleanup(func() {\n})  // cleanup function (Go 1.14+)",
      "t.Helper()  // marks calling function as test helper",
      "t.Skip(\"not ready\")  // skips test",
      "t.Parallel()  // marks test for parallel execution",
      "func TestTable(t *testing.T) {\n    tests := []struct {\n        input int\n        want  int\n    }{\n        {1, 2},\n        {3, 4},\n    }\n    for _, tc := range tests {\n        got := f(tc.input)\n        if got != tc.want {\n            t.Errorf(\"got %d, want %d\", got, tc.want)\n        }\n    }\n}  // table-driven test",
      "func ExampleAdd() {\n    fmt.Println(Add(1, 2))\n    // Output: 3\n}  // example test (also generates docs)",
      "func BenchmarkX(b *testing.B) {\n    for i := 0; i < b.N; i++ {\n        doWork()\n    }\n}  // benchmark (go test -bench=.)",
      "b.ResetTimer()  // resets benchmark timer",
      "b.RunParallel(func(pb *testing.PB) {\n    for pb.Next() {\n        work()\n    }\n})  // parallel benchmark",
      "require.Equal(t, 42, got)  // testify require (fatal on failure)",
      "assert.Equal(t, 42, got)  // testify assert (non-fatal)",
      "testify/assert  // popular assertion package",
    ],
    "net/http": [
      "http.Get(url)  // HTTP GET request",
      "http.Post(url, \"application/json\", body)  // HTTP POST",
      "http.PostForm(url, url.Values{\"k\": {\"v\"}})  // form-encoded POST",
      "resp.Body.Close()  // must close response body",
      "io.ReadAll(resp.Body)  // read entire response body",
      "http.HandleFunc(\"/api\", handler)  // registers handler function",
      "http.Handle(\"/static\", http.FileServer(http.Dir(\"./static\")))  // file server",
      "http.Handle(\"/api\", http.StripPrefix(\"/api\", mux))  // strip prefix",
      "http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {\n})  // handler function type",
      "func handler(w http.ResponseWriter, r *http.Request) {\n}  // standard handler signature",
      "http.ListenAndServe(\":8080\", nil)  // starts HTTP server",
      "http.ListenAndServeTLS(\":443\", \"cert.pem\", \"key.pem\", nil)  // HTTPS server",
      "http.Server{Addr: \":8080\", Handler: mux}  // custom server",
      "srv.ListenAndServe()  // starts custom server",
      "srv.Shutdown(ctx)  // graceful shutdown",
      "mux := http.NewServeMux()  // creates new serve mux",
      "mux.HandleFunc(\"GET /users/{id}\", handler)  // method + path pattern (Go 1.22+)",
      "r.Method  // HTTP method string",
      "r.URL  // *url.URL of request",
      "r.URL.Path  // path component of URL",
      "r.URL.Query().Get(\"key\")  // query parameter",
      "r.Header.Get(\"Content-Type\")  // request header",
      "r.Header.Set(\"Content-Type\", \"application/json\")  // set header",
      "r.Body  // request body (io.ReadCloser)",
      "r.ParseForm()  // parses form data (URL query and body)",
      "r.FormValue(\"name\")  // shortcut for form value",
      "r.Form  // parsed form (map[string][]string)",
      "r.PostForm  // parsed form from body only",
      "r.PathValue(\"id\")  // path parameter (Go 1.22+)",
      "w.Write([]byte(\"hello\"))  // writes bytes to response",
      "w.WriteHeader(404)  // sets HTTP status code",
      "w.Header().Set(\"Content-Type\", \"application/json\")  // sets response header",
      "json.NewEncoder(w).Encode(data)  // JSON response",
      "http.Error(w, \"bad request\", http.StatusBadRequest)  // error response",
      "http.Redirect(w, r, \"/login\", http.StatusFound)  // redirect",
      "http.NotFound(w, r)  // 404 response",
      "http.StatusOK  // 200",
      "http.StatusBadRequest  // 400",
      "http.StatusNotFound  // 404",
      "http.StatusInternalServerError  // 500",
      "http.TimeoutHandler(h, 5*time.Second, \"timeout\")  // wraps handler with timeout",
      "http.Cookie{Name: \"token\", Value: \"abc123\"}  // cookie struct",
      "http.SetCookie(w, &cookie)  // sets response cookie",
    ],
    "time": [
      "time.Now()  // current local time",
      "t := time.Date(2024, time.January, 15, 0, 0, 0, 0, time.UTC)  // creates specific time",
      "t := time.Unix(1700000000, 0)  // Unix timestamp to Time",
      "t.Unix()  // Unix timestamp (seconds since epoch)",
      "t.UnixNano()  // Unix timestamp in nanoseconds",
      "time.Parse(\"2006-01-02\", \"2024-01-15\")  // parses string (reference layout)",
      "t.Format(\"2006-01-02 15:04:05\")  // formats time to string",
      "t.Year()  // year component",
      "t.Month()  // month component",
      "t.Day()  // day component",
      "t.Weekday()  // day of week",
      "t.Hour()  // hour component",
      "t.Minute()  // minute component",
      "t.Second()  // second component",
      "t.Add(2 * time.Hour)  // adds duration",
      "t.AddDate(1, 2, 3)  // adds years, months, days",
      "t.Sub(t2)  // difference as Duration",
      "t.Before(t2)  // True if t < t2",
      "t.After(t2)  // True if t > t2",
      "t.Equal(t2)  // True if same instant (handles timezone)",
      "time.Since(t)  // shortcut for time.Now().Sub(t)",
      "time.Until(t)  // shortcut for t.Sub(time.Now())",
      "time.Sleep(100 * time.Millisecond)  // pauses goroutine",
      "time.Duration  // nanoseconds int64 (type)",
      "time.Nanosecond  // 1ns constant",
      "time.Microsecond  // 1000ns constant",
      "time.Millisecond  // 1M ns constant",
      "time.Second  // 1e9 ns constant",
      "time.Minute  // 60s constant",
      "time.Hour  // 3600s constant",
      "5 * time.Second  // duration literal",
      "time.ParseDuration(\"2h45m\")  // parses duration string",
      "d.Round(time.Second)  // rounds to nearest second",
      "d.Truncate(time.Second)  // truncates to second",
      "ticker := time.NewTicker(1 * time.Second)\nfor range ticker.C {\n}  // periodic ticker",
      "timer := time.NewTimer(1 * time.Second)\n<-timer.C  // single-shot timer",
      "time.After(1 * time.Second)  // returns channel that fires once",
      "time.AfterFunc(1*time.Second, fn)  // runs fn after duration",
    ],
    "Sort": [
      "sort.Ints([]int{3,1,2})  // sorts ints in-place",
      "sort.Strings([]string{\"c\",\"a\",\"b\"})  // sorts strings in-place",
      "sort.Float64s([]float64{3.0,1.0,2.0})  // sorts float64s in-place",
      "sort.Slice(s, func(i, j int) bool {\n    return s[i] < s[j]\n})  // slice sorting with less function",
      "sort.SliceStable(s, func(i, j int) bool {\n    return s[i].Age < s[j].Age\n})  // stable sort",
      "sort.Sort(data)  // sorts using sort.Interface",
      "sort.Reverse(sort.IntSlice(s))  // reverse sort",
      "sort.SearchInts([]int{1,3,5}, 3)  // binary search returns index",
      "sort.Search(n, func(i int) bool { return arr[i] >= x })  // general binary search",
      "sort.IsSorted(sort.IntSlice(s))  // checks if sorted",
    ],
    "Built-in Functions": [
      "len(s)  // length of string, slice, array, map, channel",
      "cap(s)  // capacity of slice, channel",
      "make([]int, 5, 10)  // creates slice, map, or channel",
      "new(int)  // allocates zero value, returns pointer",
      "append(s, 1, 2, 3)  // appends to slice",
      "copy(dst, src)  // copies elements between slices",
      "close(ch)  // closes channel (must not send after close)",
      "delete(m, \"k\")  // deletes key from map",
      "complex(3, 4)  // creates complex number from real, imag",
      "real(3+4i)  // real part of complex",
      "imag(3+4i)  // imaginary part of complex",
      "panic(\"error\")  // triggers panic",
      "recover()  // catches panic (only in deferred function)",
      "print(\"hello\")  // built-in print (not for production)",
      "println(\"hello\")  // built-in println (not for production)",
    ],
    "Math": [
      "math.Abs(-5.0)  // absolute value (float64)",
      "math.Pow(2.0, 10.0)  // power",
      "math.Sqrt(9.0)  // square root",
      "math.Ceil(3.14)  // ceiling",
      "math.Floor(3.14)  // floor",
      "math.Round(3.14)  // round to nearest",
      "math.Max(1.0, 2.0)  // maximum (float64)",
      "math.Min(1.0, 2.0)  // minimum (float64)",
      "math.Sin(math.Pi/2)  // sine",
      "math.Cos(math.Pi)  // cosine",
      "math.Tan(0)  // tangent",
      "math.Log(2.718)  // natural logarithm",
      "math.Log10(100)  // base-10 logarithm",
      "math.Exp(1)  // e**x",
      "math.Mod(10, 3)  // floating-point remainder",
      "math.Modf(3.14)  // returns (integer, fractional)",
      "math.Pi  // 3.141592653589793",
      "math.E  // 2.718281828459045",
      "math.Inf(1)  // positive infinity",
      "math.NaN()  // NaN (not-a-number)",
      "rand.Intn(100)  // random int 0..99",
      "rand.Float64()  // random float64 0.0..1.0",
    ],
    "Encoding": [
      "json.Marshal(v)  // struct to JSON bytes",
      "json.Unmarshal(data, &v)  // JSON bytes to struct",
      "json.NewEncoder(w).Encode(v)  // stream encode to writer",
      "json.NewDecoder(r).Decode(&v)  // stream decode from reader",
      "encoding/csv.NewReader(r)  // CSV reader",
      "reader.Read()  // reads one CSV record",
      "reader.ReadAll()  // reads all CSV records",
      "encoding/csv.NewWriter(w)  // CSV writer",
      "writer.Write(record)  // writes one CSV record",
      "writer.Flush()  // flushes CSV writer",
      "encoding/xml.Marshal(v)  // struct to XML bytes",
      "encoding/xml.Unmarshal(data, &v)  // XML to struct",
      "encoding/base64.StdEncoding.EncodeToString(data)  // base64 encode",
      "encoding/base64.StdEncoding.DecodeString(s)  // base64 decode",
    ],
  },

  rs: {
    "Keywords": [
      "fn main() {}  // function declaration",
      "let x = 5  // immutable variable binding",
      "let mut x = 5  // mutable variable binding",
      "const MAX: u32 = 100  // constant (compile-time, no fixed address)",
      "static GREETING: &str = \"hello\"  // static variable (fixed address)",
      "if x > 0 {}  // conditional",
      "if x > 0 {} else {}  // if-else",
      "if x > 0 {} else if y > 0 {} else {}  // if-else chain",
      "match val {\n    1 => \"one\",\n    _ => \"other\"\n}  // pattern matching",
      "loop { break }  // infinite loop",
      "while cond {}  // conditional loop",
      "for x in iter {}  // iteration loop",
      "for (i, v) in iter.enumerate() {}  // enumerate iteration",
      "in  // part of for..in syntax",
      "return x  // returns value from function",
      "break  // exits loop",
      "break 'label  // breaks outer loop",
      "continue  // skips to next iteration",
      "continue 'label  // continues outer loop",
      "struct Point { x: i32, y: i32 }  // struct definition",
      "enum Option<T> { None, Some(T) }  // enum definition",
      "trait Display { fn fmt(&self) -> String }  // trait definition",
      "impl Struct { fn method(&self) {} }  // implementation block",
      "impl Trait for Struct {}  // trait implementation",
      "use std::collections::HashMap  // imports path",
      "mod foo  // declares module",
      "pub fn f() {}  // visibility modifier (public)",
      "crate::module::Item  // referring to crate root",
      "super::module::Item  // referring to parent module",
      "self::Item  // referring to current module",
      "Self  // type alias for implementing type",
      "type String = std::string::String  // type alias",
      "where T: Display  // trait bound clause",
      "x as i64  // type cast",
      "dyn Trait  // trait object",
      "async fn f() {}  // async function",
      "await val  // await future",
      "move || x + 1  // moves captured variables into closure",
      "ref x  // takes reference in pattern",
      "=>  // match arm separator",
      "unsafe { /* dangerous code */ }  // unsafe block",
      "extern \"C\" { fn printf(); }  // foreign function interface",
      "abstract  // reserved keyword",
      "become  // reserved keyword",
      "box  // reserved keyword (formerly used for Box)",
      "do  // reserved keyword",
      "final  // reserved keyword",
      "macro  // reserved keyword",
      "override  // reserved keyword",
      "priv  // reserved keyword",
      "try  // reserved keyword",
      "typeof  // reserved keyword",
      "unsized  // reserved keyword",
      "virtual  // reserved keyword",
      "yield  // reserved keyword",
    ],
    "Ownership & Borrowing": [
      "let s = String::from(\"hello\")  // s owns the string",
      "let s2 = s  // ownership moves from s to s2 (s invalidated)",
      "fn take(s: String) {}  // takes ownership, s dropped at end",
      "fn give() -> String { String::from(\"x\") }  // returns ownership",
      "let s3 = s2.clone()  // deep copy (not move)",
      "let r = &s  // immutable borrow (reference)",
      "let r = &mut s  // mutable borrow",
      "fn borrow(s: &String) {}  // borrows, no ownership transfer",
      "fn mutate(s: &mut String) {}  // mutable borrow allows mutation",
      "// At any time you have either one mutable reference OR any number of immutable references  // borrowing rule",
      "// References must always be valid  // borrowing rule (no dangling references)",
      "fn first_word(s: &str) -> &str { s.split(' ').next().unwrap() }  // returns reference derived from input lifetime",
      "fn longest<'a>(x: &'a str, y: &'a str) -> &'a str { if x.len() > y.len() { x } else { y } }  // lifetime annotations",
      "struct S<'a> { r: &'a i32 }  // struct with lifetime parameter",
      "'static  // static lifetime (valid for entire program)",
      "let s: &'static str = \"hello\"  // string literal has 'static lifetime",
      "fn f(x: &i32) -> &i32 { x }  // lifetime elision: single input = output",
      "fn f<'a>(x: &'a str) -> &'a str {}  // explicit lifetime equivalent",
      "fn f(x: &i32) { let r: &i32 = &*x; }  // reborrowing",
    ],
    "Types": [
      "i8  // 8-bit signed integer (-128..127)",
      "i16  // 16-bit signed integer",
      "i32  // 32-bit signed integer (default int type)",
      "i64  // 64-bit signed integer",
      "i128  // 128-bit signed integer",
      "u8  // 8-bit unsigned integer (0..255, byte)",
      "u16  // 16-bit unsigned integer",
      "u32  // 32-bit unsigned integer",
      "u64  // 64-bit unsigned integer",
      "u128  // 128-bit unsigned integer",
      "f32  // 32-bit IEEE 754 float",
      "f64  // 64-bit IEEE 754 float (default float type)",
      "bool  // boolean (true or false)",
      "char  // Unicode scalar value (4 bytes)",
      "String  // heap-allocated, growable UTF-8 string",
      "&str  // string slice (reference to UTF-8 bytes)",
      "()  // unit type (empty tuple)",
      "!  // never type (diverging function)",
      "Option<T>  // Some(T) or None",
      "Result<T, E>  // Ok(T) or Err(E)",
      "Vec<T>  // heap-allocated growable array",
      "HashMap<K, V>  // hash map",
      "HashSet<T>  // hash set",
      "Box<T>  // owned pointer to heap-allocated value",
      "Rc<T>  // reference-counted pointer (single-threaded)",
      "Arc<T>  // atomically reference-counted (multi-threaded)",
      "Cell<T>  // interior mutability for Copy types",
      "RefCell<T>  // interior mutability with runtime borrow checking",
      "Mutex<T>  // mutual exclusion for thread-safe mutation",
      "RwLock<T>  // reader-writer lock",
      "Cow<'a, T>  // clone-on-write (borrowed or owned)",
    ],
    "Pattern Matching": [
      "match x {\n    1 => \"one\",\n    2 => \"two\",\n    _ => \"other\"\n}  // basic match",
      "match x {\n    1 | 2 => \"one or two\",\n    3..=5 => \"three to five\",\n    _ => \"other\"\n}  // OR and range patterns",
      "match x {\n    n if n > 5 => \"big\",\n    _ => \"small\"\n}  // match guard",
      "match opt {\n    Some(x) if x > 0 => x,\n    _ => 0\n}  // match guard on enum variant",
      "match pair {\n    (0, y) => y,\n    (x, 0) => x,\n    _ => 0\n}  // destructuring tuple",
      "match p {\n    Point { x, y } => x + y\n}  // destructuring struct",
      "match val {\n    Some(x) => x,\n    None => 0\n}  // destructuring enum",
      "match arr {\n    [a, b, c] => a + b + c\n}  // destructuring array/slice",
      "if let Some(x) = opt { println!(\"{}\", x) }  // concise match for single arm",
      "while let Some(val) = iter.next() { process(val) }  // loop while pattern matches",
      "let Point { x, y } = p  // destructuring in let binding",
      "let (a, b, _) = (1, 2, 3)  // tuple destructuring with wildcard",
      "match x {\n    n @ 1..=10 => n,\n    _ => 0\n}  // @ binding (value + pattern)",
      "match x {\n    Some(ref r) => println!(\"{}\", r)\n}  // ref pattern (borrows inner value)",
      "match x {\n    Some(ref mut r) => *r = 42\n}  // ref mut pattern",
      "match s.as_str() {\n    \"hello\" => println!(\"hi\")\n}  // string literal pattern",
      "match x {\n    .. => {}\n}  // rest pattern (ignore rest of fields)",
    ],
    "Collections": [
      "let v = vec![1, 2, 3]  // vec! macro",
      "let mut v: Vec<i32> = Vec::new()  // creates empty Vec",
      "v.push(4)  // adds element to end",
      "v.pop()  // removes and returns last element (Option)",
      "v.insert(1, 10)  // inserts at index",
      "v.remove(0)  // removes at index, shifts rest",
      "v.len()  // number of elements",
      "v.is_empty()  // true if empty",
      "v.clear()  // removes all elements",
      "v.contains(&x)  // checks if contains value",
      "v.sort()  // sorts in-place",
      "v.sort_by(\x7ca, b\x7c a.cmp(b))  // sort with custom comparator",
      "v.dedup()  // removes consecutive duplicates",
      "v.truncate(2)  // keeps only first n elements",
      "v.retain(\x7c&x\x7c x > 2)  // keeps elements matching predicate",
      "v.resize(10, 0)  // resize with fill value",
      "v.append(&mut other)  // moves all elements from other into v",
      "v.extend([4,5,6])  // adds all elements from iterable",
      "v.splice(1..3, [10,20])  // replaces range with new values",
      "v.get(2)  // safe index: Option<&T>",
      "v[2]  // unsafe index: panics if out of bounds",
      "v.first()  // Option<&T> first element",
      "v.last()  // Option<&T> last element",
      "v.split_at(2)  // splits into two slices at index",
      "v.chunks(3)  // iterator of non-overlapping chunks",
      "v.windows(3)  // iterator of overlapping windows",
      "let mut m: HashMap<String, i32> = HashMap::new()  // creates empty HashMap",
      "m.insert(String::from(\"k\"), 42)  // inserts key-value (returns old value)",
      "m.get(\"k\")  // Option<&V>",
      "m.entry(\"k\").or_insert(0)  // gets entry, inserts default if absent",
      "m.remove(\"k\")  // removes key, returns Option<V>",
      "m.contains_key(\"k\")  // boolean check",
      "m.len()  // number of entries",
      "m.keys()  // iterator over keys",
      "m.values()  // iterator over values",
      "m.iter()  // iterator over (&K, &V)",
      "let mut s: HashSet<i32> = HashSet::new()  // creates empty HashSet",
      "s.insert(1)  // true if inserted, false if already present",
      "s.contains(&1)  // boolean check",
      "s.remove(&1)  // removes element",
      "s.union(&other)  // iterator over union",
      "s.intersection(&other)  // intersection iterator",
      "s.difference(&other)  // difference iterator",
      "BTreeMap::new()  // sorted map (based on B-tree)",
      "BTreeSet::new()  // sorted set",
      "VecDeque::new()  // double-ended queue",
      "deque.push_front(1)  // adds to front",
      "deque.push_back(2)  // adds to back",
      "BinaryHeap::new()  // max-heap",
      "heap.push(5)  // adds element",
      "heap.pop()  // removes max element",
      "LinkedList::new()  // doubly-linked list",
    ],
    "Iterators": [
      "iter.iter()  // borrows elements (&T)",
      "iter.into_iter()  // consumes, yields owned T",
      "iter.iter_mut()  // mutable borrow (&mut T)",
      "iter.map(\x7cx\x7c x + 1)  // transforms each element",
      "iter.filter(\x7cx\x7c x > 0)  // keeps matching elements",
      "iter.for_each(\x7cx\x7c println!(\"{}\", x))  // side-effect for each",
      "iter.collect::<Vec<_>>()  // collects into collection",
      "iter.fold(0, \x7cacc, x\x7c acc + x)  // reduce with initial value",
      "iter.reduce(\x7ca, b\x7c a + b)  // reduce without initial (Option)",
      "iter.sum::<i32>()  // sum of elements",
      "iter.product::<i32>()  // product of elements",
      "iter.count()  // number of elements",
      "iter.any(\x7cx\x7c x > 0)  // true if any element matches",
      "iter.all(\x7cx\x7c x > 0)  // true if all elements match",
      "iter.find(\x7cx\x7c x == 5)  // first matching element (Option)",
      "iter.position(\x7cx\x7c x == 5)  // index of first match (Option)",
      "iter.nth(3)  // element at index (Option, consumes up to it)",
      "iter.cycle()  // repeats iterator endlessly",
      "iter.zip(other)  // pairs elements from two iterators",
      "iter.enumerate()  // attaches index (i, val)",
      "iter.skip(2)  // skips first n elements",
      "iter.take(3)  // limits to first n elements",
      "iter.chain(other)  // concatenates two iterators",
      "iter.flatten()  // flattens nested iterables",
      "iter.flat_map(\x7cx\x7c x.split(' '))  // map then flatten",
      "iter.inspect(\x7cx\x7c println!(\"{}\", x))  // debug peek at each element",
      "iter.peekable()  // allows peek() on iterator",
      "iter.rev()  // reverse iterator",
      "iter.step_by(2)  // yields every nth element",
      "iter.cloned()  // clones each element (&T -> T where T: Clone)",
      "iter.copied()  // copies each element (&T -> T where T: Copy)",
      "iter.filter_map(\x7cx\x7c x.parse().ok())  // filter + map combined",
      "iter.max()  // max element (Ordered) (Option)",
      "iter.min()  // min element (Ordered) (Option)",
      "iter.max_by(\x7ca, b\x7c a.cmp(b))  // max with custom comparator",
      "iter.min_by(\x7ca, b\x7c a.cmp(b))  // min with custom comparator",
      "iter.partition(\x7cx\x7c x > 0)  // splits into two collections",
      "iter.unzip()  // splits (K,V) tuples into two collections",
      "iter.cmp(other)  // lexicographic comparison",
      "iter.eq(other)  // element-wise equality",
      "iter.ne(other)  // element-wise inequality",
      "iter.lt(other)  // less than comparison",
      "iter.le(other)  // less than or equal",
      "iter.gt(other)  // greater than",
      "iter.ge(other)  // greater than or equal",
    ],
    "Strings": [
      "String::new()  // creates new empty String",
      "String::from(\"hello\")  // creates String from &str",
      "\"hello\".to_string()  // converts &str to String",
      "\"hello\".to_owned()  // same as to_string, more explicit",
      "s.as_str()  // returns &str from String",
      "s.push('!')  // appends char to String",
      "s.push_str(\" world\")  // appends &str to String",
      "s.len()  // byte length (not char count)",
      "s.is_empty()  // true if empty",
      "s.clear()  // removes all contents",
      "s.contains(\"sub\")  // checks substring",
      "s.starts_with(\"pre\")  // checks prefix",
      "s.ends_with(\"suf\")  // checks suffix",
      "s.find(\"sub\")  // Option<usize> first index",
      "s.rfind(\"sub\")  // Option<usize> last index",
      "s.replace(\"old\", \"new\")  // replaces all occurrences",
      "s.replacen(\"old\", \"new\", 1)  // replaces at most n",
      "s.replace_range(1..3, \"xy\")  // replaces range in place",
      "s.trim()  // trims whitespace both ends",
      "s.trim_start()  // trims leading whitespace",
      "s.trim_end()  // trims trailing whitespace",
      "s.split(\",\")  // splits on delimiter (iterator)",
      "s.split_whitespace()  // splits on whitespace",
      "s.split_terminator(\",\")  // splits without trailing empty",
      "s.split_once(\",\")  // splits at first occurrence",
      "s.rsplit_once(\",\")  // splits at last occurrence",
      "s.lines()  // splits on line breaks",
      "s.chars()  // iterator over chars (Unicode scalar values)",
      "s.bytes()  // iterator over bytes",
      "s.char_indices()  // iterator over (index, char)",
      "s.to_uppercase()  // uppercase String",
      "s.to_lowercase()  // lowercase String",
      "s.repeat(3)  // repeats string n times",
      "format!(\"hello {}\", name)  // formatted String",
      "concat!(\"a\", \"b\", \"c\")  // concatenates literals at compile time",
      "stringify!(expr)  // converts expr to string literal at compile time",
      "\"hello\".as_ptr()  // *const u8 pointer to string buffer",
      "s.capacity()  // allocated capacity (bytes)",
      "s.reserve(100)  // reserves capacity",
      "s.shrink_to_fit()  // shrinks to fit current length",
      "s.retain(\x7cc\x7c c != 'x')  // keeps chars matching predicate",
    ],
    "Error Handling": [
      "fn f() -> Result<i32, String> { Ok(42) }  // return Result type",
      "fn f() -> Result<i32, String> { Err(\"error\".into()) }  // return Err variant",
      "let val = result.unwrap()  // panics on Err",
      "let val = result.expect(\"msg\")  // panics with custom message",
      "let val = result.unwrap_or(0)  // default value on Err",
      "let val = result.unwrap_or_else(\x7c_\x7c 0)  // closure default on Err",
      "let val = result.ok()  // Result -> Option (discards error)",
      "let e = result.err()  // Result -> Option (discards value)",
      "let val = result.map_err(\x7ce\x7c MyError::from(e))  // transforms error",
      "let val = result.and_then(\x7cx\x7c Ok(x + 1))  // chains Results",
      "result?  // ? operator: unwrap or early return Err",
      "option?  // ? operator on Option: unwrap or return None",
      "fn f() -> Result<i32, MyErr> {\n    let a = do_thing()?;\n    let b = do_other()?;\n    Ok(a + b)\n}  // ? operator for clean error propagation",
      "panic!(\"unexpected state\")  // unrecoverable error",
      "unreachable!()  // marks code that should never be reached",
      "todo!()  // marks unimplemented code",
      "unimplemented!()  // marks unimplemented function",
      "assert!(x > 0)  // panic if false",
      "assert_eq!(a, b)  // panic if not equal",
      "assert_ne!(a, b)  // panic if equal",
      "debug_assert!(x > 0)  // assert only in debug builds",
      "anyhow::Result<T>  // anyhow: generic error type",
      "anyhow::bail!(\"error {}\", val)  // anyhow: early Err return",
      "anyhow::Context  // anyhow: adds context to errors",
      "thiserror::Error  // thiserror: derive macro for custom errors",
      "fn f() -> Result<(), Box<dyn std::error::Error>> { Ok(()) }  // type-erased error return",
    ],
    "Traits": [
      "trait Display {\n    fn fmt(&self) -> String\n}  // trait declaration",
      "impl Display for Point {\n    fn fmt(&self) -> String {\n        format!(\"({},{})\", self.x, self.y)\n    }\n}  // implementing a trait",
      "#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Default)]\nstruct Point { x: i32, y: i32 }  // derive macro for common traits",
      "#[derive(Debug)]  // debug formatting {:?}",
      "#[derive(Clone)]  // clone method",
      "#[derive(Copy)]  // implicit copy on move (for Copy types)",
      "#[derive(PartialEq)]  // == and != operators",
      "#[derive(Eq)]  // total equality (for hash maps)",
      "#[derive(Hash)]  // hash method",
      "#[derive(Default)]  // default() method",
      "#[derive(PartialOrd)]  // < <= > >= (partial order)",
      "#[derive(Ord)]  // total ordering",
      "fn print_it<T: Display>(t: T)  // trait bound on generic parameter",
      "fn print_it(t: &impl Display)  // impl Trait syntax (sugar)",
      "fn print_it(t: impl Display)  // impl Trait in parameter position",
      "fn returns_display() -> impl Display { 42 }  // impl Trait in return position",
      "fn print_it<T>(t: T) where T: Display + Clone  // where clause with multiple bounds",
      "let obj: &dyn Display = &point  // trait object (dynamic dispatch)",
      "let obj: Box<dyn Display> = Box::new(point)  // boxed trait object",
      "trait Iter {\n    type Item;\n    fn next(&mut self) -> Option<Self::Item>;\n}  // trait with associated type",
      "impl<T> Iter for MyIter<T> {\n    type Item = T;\n    fn next(&mut self) -> Option<T> { None }\n}  // implementing associated type",
      "trait SuperTrait: BaseTrait {}  // trait inheritance (supertrait)",
      "trait Default {\n    fn default() -> Self;\n}  // marker traits (no methods)",
      "unsafe trait Send {}  // send to another thread (auto trait)",
      "unsafe trait Sync {}  // shared across threads (auto trait)",
      "impl Trait for T where T: OtherTrait {}  // blanket implementation",
    ],
    "Generics": [
      "fn identity<T>(x: T) -> T { x }  // generic function",
      "fn first<T: Clone>(x: &[T]) -> T { x[0].clone() }  // generic with trait bound",
      "struct Point<T> { x: T, y: T }  // generic struct",
      "impl<T> Point<T> {\n    fn x(&self) -> &T { &self.x }\n}  // generic impl block",
      "impl Point<f64> {\n    fn distance(&self) -> f64 { (self.x*self.x + self.y*self.y).sqrt() }\n}  // specialized impl for concrete type",
      "enum Option<T> { Some(T), None }  // generic enum",
      "impl<T: Display> fmt::Display for Point<T> { fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result { write!(f, \"({},{})\", self.x, self.y) } }  // generic trait impl with bound",
      "fn pair<T, U>(t: T, u: U) -> (T, U) { (t, u) }  // multiple generic params",
      "struct Array<T, const N: usize> {\n    data: [T; N]\n}  // const generics (compile-time values)",
      "trait Container<T> {}  // generic trait",
      "type Result<T> = std::result::Result<T, MyError>  // generic type alias",
      "impl<T: Clone + Debug + Default> MyTrait for T {}  // multiple trait bounds",
    ],
    "Lifetimes": [
      "fn f<'a>(x: &'a str) -> &'a str { x }  // explicit lifetime parameter",
      "struct Borrowed<'a> { x: &'a str }  // struct with lifetime",
      "enum Either<'a> { Left(&'a str), Right(&'a str) }  // enum with lifetime",
      "impl<'a> Borrowed<'a> { fn get(&self) -> &'a str { self.x } }  // impl with lifetime",
      "'static  // lifetime valid for entire program duration",
      "fn f(x: &i32) -> &i32 { x }  // lifetime elision (one input = output)",
      "fn f<'a>(x: &'a str, y: &'a str) -> &'a str  // both inputs same lifetime",
      "fn f(x: &str, y: &str) -> &str  // error: need explicit lifetimes",
      "fn f<'a>(x: &'a str) -> &str  // output lifetime can be elided if not tied to inputs",
      "trait Trait<'a> { fn f(&'a self) -> &str }  // trait with lifetime",
      "fn f<'a, 'b>(x: &'a str, y: &'b str) -> &'a str { x }  // different lifetimes",
      "fn f<'a>(x: &'a mut str) -> &'a mut str { x }  // mutable reference lifetime",
      "fn f<'a, F>(f: F) where F: Fn(&'a str)  // HRTB (higher-ranked trait bound)",
      "fn f<F>(f: F) where F: for<'a> Fn(&'a str)  // for<'a> HRTB syntax",
    ],
    "Closures & Function Pointers": [
      "let add = \x7cx, y\x7c x + y  // closure (anonymous function)",
      "let add = \x7cx, y\x7c { x + y }  // closure with block body",
      "let add_one = \x7cx\x7c x + 1  // single parameter closure",
      "add_one(5)  // calling closure",
      "let c = move \x7cx\x7c x + y  // move closure takes ownership of captured variables",
      "fn call_fn(f: impl Fn(i32) -> i32) { f(5) }  // Fn: immutable capture",
      "fn call_fn_mut(f: impl FnMut(i32) -> i32) { f(5) }  // FnMut: mutable capture",
      "fn call_fn_once(f: impl FnOnce(i32) -> i32) { f(5) }  // FnOnce: can be called once",
      "let f: fn(i32) -> i32 = \x7cx\x7c x + 1  // function pointer type",
      "fn add(x: i32) -> i32 { x + 1 }\nlet f: fn(i32) -> i32 = add  // function pointer from named function",
      "closures can capture variables by reference, mutable reference, or value  // capture modes",
    ],
    "Smart Pointers": [
      "let b = Box::new(42)  // allocates value on heap",
      "Box::new(T)  // returns Box<T> with T on heap",
      "let rc = Rc::new(42)  // reference-counted pointer (single-threaded)",
      "Rc::clone(&rc)  // increments ref count (not deep copy)",
      "Rc::strong_count(&rc)  // returns reference count",
      "Rc::try_unwrap(rc)  // returns inner value if count==1",
      "let arc = Arc::new(42)  // atomic reference counting (multi-threaded)",
      "let cell = Cell::new(42)  // interior mutability for Copy types",
      "cell.set(10)  // sets value (no borrow checking)",
      "cell.get()  // gets value (Copy)",
      "let refcell = RefCell::new(42)  // interior mutability with runtime borrow checking",
      "refcell.borrow()  // returns Ref<T> (panics if already mutably borrowed)",
      "refcell.borrow_mut()  // returns RefMut<T> (panics if already borrowed)",
      "refcell.replace(10)  // replaces value, returns old",
      "let cow: Cow<str> = Cow::Borrowed(\"hello\")  // clone-on-write",
      "cow.to_mut()  // ensures owned (clones if borrowed)",
      "let rc = Rc::new(RefCell::new(42))  // combine Rc + RefCell for shared mutability",
      "fn deref_example(x: &i32) {}\nderef_example(&Box::new(42))  // Deref coercion (Box<T> -> &T)",
      "// Drop trait: fn drop(&mut self) {} called when value goes out of scope  // Drop trait",
      "std::mem::drop(val)  // explicitly drops value before end of scope",
      "ManuallyDrop::new(val)  // prevents Drop from running",
    ],
    "Concurrency": [
      "use std::thread;\nlet h = thread::spawn(move || {\n    println!(\"from thread\")\n})\nh.join().unwrap()  // spawn and join thread",
      "thread::spawn(move || { /* captured vars moved */ })  // move into thread closure",
      "let pool = ThreadPool::new(4)  // thread pool (or use rayon)",
      "let (tx, rx) = std::sync::mpsc::channel()  // multiple-producer, single-consumer channel",
      "tx.send(val).unwrap()  // sends value through channel",
      "rx.recv().unwrap()  // blocking receive",
      "rx.try_recv()  // non-blocking receive",
      "let (tx, rx) = std::sync::mpsc::sync_channel(0)  // synchronous (bounded) channel",
      "use std::sync::mpsc::Sender  // clone sends to produce multiple producers",
      "let m = Mutex::new(0)  // mutual exclusion wrapper",
      "{\n    let mut guard = m.lock().unwrap();\n    *guard += 1\n}  // lock mutex, guard auto-unlocks at end of scope",
      "let rw = RwLock::new(0)  // reader-writer lock",
      "let r = rw.read().unwrap()  // read lock (multiple concurrent)",
      "let mut w = rw.write().unwrap()  // write lock (exclusive)",
      "use std::sync::atomic::{AtomicBool, Ordering}\nlet flag = AtomicBool::new(false)\nflag.store(true, Ordering::SeqCst)\nlet v = flag.load(Ordering::SeqCst)  // atomic operations",
      "AtomicI32::new(0)  // atomic integer types",
      "atomic.fetch_add(1, Ordering::SeqCst)  // atomic add-return",
      "use std::sync::atomic::AtomicUsize  // atomic usize",
      "use std::sync::Arc  // Arc for shared ownership across threads",
      "Send trait: types that can be transferred across threads  // Send",
      "Sync trait: types that can be shared across threads via reference  // Sync",
      "use std::sync::Barrier\nlet barrier = Barrier::new(3)  // barrier synchronization",
      "use std::sync::Condvar  // condition variable",
    ],
    "Async/Await": [
      "async fn fetch_data() -> Result<String, Error> { Ok(\"data\".into()) }  // async function definition",
      "let result = fetch_data().await  // await a future",
      "async { /* async block */ }  // async block",
      "async move { /* moves captured variables */ }  // async block with move",
      "#[tokio::main]\nasync fn main() {}  // tokio runtime entry point",
      "tokio::spawn(async { /* concurrent task */ })  // spawn async task on tokio runtime",
      "let handle = tokio::spawn(async { 42 })\nhandle.await.unwrap()  // await spawned task result",
      "tokio::time::sleep(Duration::from_secs(1)).await  // async sleep",
      "use tokio::sync::mpsc  // tokio async channel",
      "use tokio::sync::Mutex  // tokio async mutex (must use .lock().await)",
      "use futures::future::join_all(futures)  // joins multiple futures",
      "use futures::future::select(future1, future2)  // races two futures",
      "use futures::stream::FuturesUnordered  // drives many futures concurrently",
      "tokio::select! {\n    result = op1() => handle1(result),\n    result = op2() => handle2(result),\n}  // tokio select macro",
      "use tokio::sync::broadcast  // broadcast channel (1:N)",
      "use tokio::sync::oneshot  // one-shot channel (sends once)",
    ],
    "Macros": [
      "println!(\"hello {}\", name)  // print to stdout with newline",
      "eprintln!(\"error: {}\", msg)  // print to stderr with newline",
      "format!(\"hello {}\", name)  // returns formatted String",
      "vec![1, 2, 3]  // creates Vec",
      "vec![0; 10]  // creates Vec with 10 zeros",
      "assert!(expr)  // panic if false",
      "assert_eq!(a, b)  // panic if not equal",
      "assert_ne!(a, b)  // panic if equal",
      "debug_assert!(expr)  // assert in debug builds only",
      "todo!()  // placeholder for unimplemented code",
      "unreachable!()  // marks unreachable code",
      "panic!(\"msg\")  // unrecoverable error",
      "unimplemented!()  // marks unimplemented function",
      "include!(\"file.rs\")  // includes file as source",
      "include_str!(\"file.txt\")  // includes file content as &str at compile time",
      "include_bytes!(\"file.bin\")  // includes file content as &[u8] at compile time",
      "stringify!(expr)  // converts expr to string literal",
      "concat!(\"a\", \"b\", \"c\")  // concatenates string literals at compile time",
      "cfg!(target_os = \"linux\")  // checks configuration at runtime return bool",
      "env!(\"PATH\")  // includes environment variable at compile time",
      "option_env!(\"PATH\")  // Option<&str> env var at compile time",
      "file!()  // current file path (compile time)",
      "line!()  // current line number (compile time)",
      "column!()  // current column number (compile time)",
      "dbg!(expr)  // prints expr with file/line and returns value",
      "write!(w, \"{}\", val)  // write formatted to writer",
      "writeln!(w, \"{}\", val)  // write with newline",
      "macro_rules! my_macro {\n    ($x:expr) => { $x * 2 }\n}  // declarative macro definition",
      "macro_rules! map {\n    ($($k:expr => $v:expr),*) => {{\n        let mut m = std::collections::HashMap::new();\n        $(m.insert($k, $v);)*\n        m\n    }}\n}  // complex declarative macro with repetition",
      "#[macro_export]  // makes macro available to crate users",
    ],
    "Attributes": [
      "#[derive(Debug, Clone, PartialEq)]  // auto-implement traits",
      "#[cfg(test)]  // conditional compilation: only in test config",
      "#[cfg(target_os = \"linux\")]  // conditional on target OS",
      "#[cfg(feature = \"my_feature\")]  // conditional on Cargo feature",
      "#[cfg(not(debug_assertions))]  // conditional: not debug",
      "#[allow(warnings)]  // suppresses warnings",
      "#[allow(dead_code)]  // suppress unused code warning",
      "#[allow(unused_variables)]  // suppress unused variable warning",
      "#[allow(non_snake_case)]  // suppress naming convention warning",
      "#[deny(unsafe_code)]  // makes unsafe code a hard error",
      "#[deny(warnings)]  // treats warnings as errors",
      "#[inline]  // hint to inline function",
      "#[inline(always)]  // force inline (or never)",
      "#[repr(C)]  // C-compatible memory layout",
      "#[repr(transparent)]  // type with same layout as inner field",
      "#[repr(u8)]  // enum discriminant type",
      "#[test]  // marks test function",
      "#[bench]  // marks benchmark function",
      "#[should_panic]  // test must panic to pass",
      "#[should_panic(expected = \"msg\")]  // test must panic with expected message",
      "#[ignore]  // skip this test",
      "#[must_use]  // warns if return value is ignored",
      "#[no_mangle]  // disables name mangling (for FFI)",
      "#[link(name = \"foo\")]  // links native library",
      "#[macro_export]  // exports macro from crate",
      "#[macro_use]  // imports macros from external crate",
      "#[allow(clippy::*)]  // suppress clippy lints",
      "#[doc = \"documentation\"]  // inline doc attribute",
      "#[non_exhaustive]  // enum/struct may get more variants later",
    ],
    "Modules": [
      "mod math  // declares module (looks for math.rs or math/mod.rs)",
      "pub mod math  // public module (accessible outside crate)",
      "pub use math::add  // re-exports symbol",
      "use std::collections::HashMap  // imports path",
      "use std::collections::*  // glob import",
      "use crate::module::Item  // absolute path from crate root",
      "use super::module::Item  // path from parent module",
      "use std::io::{Read, Write}  // nested import",
      "use std::collections::{self, HashMap}  // module and item combined",
      "pub(crate) fn f() {}  // visible only within crate",
      "pub(self) fn f() {}  // visible only in current module",
      "pub(super) fn f() {}  // visible to parent module",
      "pub(in crate::module) fn f() {}  // visible within specific module tree",
      "mod.rs  // legacy module root file",
      "module_name.rs  // module root file (Rust 2018+)",
      "lib.rs  // library crate root",
      "main.rs  // binary crate root",
      "extern crate serde  // old style: explicitly depends on crate",
    ],
    "Struct": [
      "struct Point {\n    x: i32,\n    y: i32,\n}  // struct with named fields",
      "struct Point(i32, i32)  // tuple struct (newtype)",
      "struct UnitStruct  // unit struct (zero size)",
      "let p = Point { x: 1, y: 2 }  // struct literal",
      "let p = Point { x: 1, ..Default::default() }  // struct update syntax",
      "p.x  // field access",
      "let Point { x, y } = p  // struct destructuring",
      "let Point { x: a, y: b } = p  // destructuring with renaming",
      "#[derive(Debug)]\nstruct DebugPoint { x: i32 }  // printable with {:?}",
      "#[derive(Default)]\nstruct DefaultPoint { x: i32, y: i32 }  // default value",
      "impl Point {\n    fn new(x: i32, y: i32) -> Self { Self { x, y } }\n}  // struct constructor pattern",
      "impl Point {\n    fn origin() -> Self { Self { x: 0, y: 0 } }\n}  // static method (no &self)",
    ],
    "Enum": [
      "enum Direction {\n    Up, Down, Left, Right\n}  // C-like enum (unit variants)",
      "enum IpAddr {\n    V4(u8, u8, u8, u8),\n    V6(String),\n}  // enum variants with data",
      "enum Message {\n    Quit,\n    Move { x: i32, y: i32 },\n    Write(String),\n}  // mixed variant types",
      "let m = Message::Write(\"hello\".into())  // constructing enum variant",
      "if let Message::Write(s) = m { println!(\"{}\", s) }  // matching enum variant",
      "enum Color {\n    Red = 0xff0000,\n    Green = 0x00ff00,\n    Blue = 0x0000ff,\n}  // explicit discriminant",
      "fn discriminant() -> u8 { Color::Red as u8 }  // cast enum to discriminant",
      "#[repr(u8)]\nenum Small {\n    A = 1,\n    B = 2,\n}  // specify repr size for C-like enum",
      "Option<T>  // Some(T) or None (built-in enum)",
      "Result<T, E>  // Ok(T) or Err(E) (built-in enum)",
      "Ordering  // Less, Equal, Greater (built-in enum)",
    ],
    "I/O": [
      "std::fs::read_to_string(\"f.txt\")  // reads file to String (Result)",
      "std::fs::read(\"f.bin\")  // reads file to Vec<u8>",
      "std::fs::write(\"f.txt\", \"content\")  // writes bytes/str to file",
      "let mut f = std::fs::File::open(\"f.txt\")?  // opens file (read)",
      "let mut f = std::fs::File::create(\"f.txt\")?  // opens file (write/truncate)",
      "f.read_to_string(&mut s)?  // reads file content to String",
      "f.read(&mut buf)?  // reads into byte buffer",
      "f.write_all(b\"data\")?  // writes all bytes to file",
      "f.seek(SeekFrom::Start(0))?  // seeks to position",
      "f.metadata()?  // returns file metadata",
      "use std::io::{BufReader, BufRead}\nlet r = BufReader::new(f)  // buffered reader",
      "r.read_line(&mut s)?  // reads one line",
      "r.lines()  // iterator over lines (Result<String>)",
      "let w = BufWriter::new(f)  // buffered writer",
      "w.write_all(b\"data\")?  // write with buffering",
      "w.flush()?  // flush buffered writer",
      "let path = std::path::Path::new(\"a/b/c.txt\")  // creates Path",
      "path.exists()  // checks path existence",
      "path.is_file()  // checks if file",
      "path.is_dir()  // checks if directory",
      "path.file_name()  // Option<&OsStr>",
      "path.extension()  // Option<&OsStr>",
      "path.parent()  // Option<&Path>",
      "path.join(\"file.txt\")  // joins path",
      "let pb = std::path::PathBuf::from(\"/a/b/c\")  // owned, mutable path",
      "pb.push(\"d\")  // appends component",
      "pb.pop()  // removes last component",
      "std::io::stdin().read_line(&mut s)?  // read from stdin",
      "std::io::stdout().write_all(b\"hello\")?  // write to stdout",
      "use std::io::{Read, Write, Seek, BufRead}  // common io trait imports",
      "let cursor = std::io::Cursor::new(vec![0u8; 10])  // in-memory reader/writer",
      "std::fs::create_dir(\"dir\")?  // creates directory",
      "std::fs::create_dir_all(\"a/b/c\")?  // creates recursive dirs",
      "std::fs::remove_file(\"f.txt\")?  // deletes file",
      "std::fs::remove_dir(\"dir\")?  // removes empty dir",
      "std::fs::remove_dir_all(\"dir\")?  // removes dir and contents",
      "std::fs::rename(\"old\", \"new\")?  // renames file",
      "std::fs::copy(\"src\", \"dst\")?  // copies file",
      "std::fs::canonicalize(\"rel\")?  // absolute path resolve",
    ],
    "Testing": [
      "#[test]\nfn test_add() {\n    assert_eq!(add(2, 3), 5)\n}  // test function",
      "#[test]\nfn test_with_result() -> Result<(), String> {\n    Ok(())\n}  // test returning Result",
      "#[should_panic(expected = \"overflow\")]\nfn test_panic() {\n    panic!(\"overflow\")\n}  // expected panic test",
      "#[ignore]\nfn test_slow() {}  // skip test (run with --include-ignored)",
      "#[test]\nfn test_concurrent() {\n    let _ = std::thread::spawn(move || {}).join();\n}  // multi-threaded test",
      "cargo test  // runs all tests",
      "cargo test test_name  // runs specific test",
      "cargo test -- --ignored  // runs only ignored tests",
      "#[cfg(test)]\nmod tests {\n    use super::*;\n}  // test module (compiled only during testing)",
      "/// ```\n/// assert_eq!(add(1, 2), 3)\n/// ```\nfn add(a: i32, b: i32) -> i32 { a + b }  // doc test (cargo test runs doc examples)",
      "fn setup() -> TestContext {}\nfn teardown(ctx: TestContext) {}  // manual setup/teardown pattern",
      "use pretty_assertions::assert_eq  // pretty assertion diffs",
    ],
    "Standard Library (std)": [
      "std::mem::size_of::<i32>()  // size in bytes",
      "std::mem::replace(&mut x, 42)  // replaces value, returns old",
      "std::mem::swap(&mut a, &mut b)  // swaps two values",
      "std::mem::take(&mut x)  // replaces with default, returns value",
      "std::mem::drop(x)  // drops value early",
      "std::mem::forget(x)  // prevents Drop from running",
      "std::cmp::min(a, b)  // minimum",
      "std::cmp::max(a, b)  // maximum",
      "std::cmp::Ordering  // Less, Equal, Greater",
      "std::cmp::Reverse(x)  // reverse ordering",
      "std::fmt::Display  // display trait ({})",
      "std::fmt::Debug  // debug trait ({:?})",
      "std::fmt::Formatter  // custom formatting",
      "write!(f, \"{}\", val)  // write to formatter",
      "std::str::from_utf8(&[u8])  // &[u8] to &str (validates UTF-8)",
      "std::str::Chars  // char iterator",
      "std::env::args()  // command-line arguments iterator",
      "std::env::var(\"PATH\")  // environment variable (Result)",
      "std::env::current_dir()  // current working directory",
      "std::env::temp_dir()  // temp directory path",
      "std::process::Command::new(\"ls\").arg(\"-l\").output()  // run external command",
      "std::process::exit(0)  // exit process with code",
      "std::net::TcpListener::bind(\"127.0.0.1:8080\")  // TCP listener",
      "std::net::TcpStream::connect(\"127.0.0.1:8080\")  // TCP connection",
      "std::time::Duration::from_secs(5)  // time duration",
      "std::time::Instant::now()  // high-resolution time measurement",
      "std::time::SystemTime::now()  // system clock",
      "std::os::unix::fs::PermissionsExt  // Unix permission extensions",
      "std::ffi::CString::new(\"hello\").unwrap()  // null-terminated C string",
      "std::ffi::OsStr  // OS string type",
      "std::ops::Range { start: 0, end: 10 }  // range type",
      "std::ops::RangeFrom { start: 0 }  // unbounded start range",
      "std::ops::RangeTo { end: 10 }  // unbounded end range",
      "std::ops::RangeFull  // .. range",
      "std::ops::RangeInclusive::new(0, 10)  // inclusive range",
    ],
    "Conversions & Numerics": [
      "x as i64  // type cast (numeric casts only)",
      "From::from(val)  // conversion via From trait",
      "val.into()  // conversion via Into trait (type inference)",
      "TryFrom::try_from(val)  // fallible conversion",
      "val.try_into()  // fallible conversion via TryInto",
      "i32::from_str_radix(\"ff\", 16)  // parse from base",
      "\"42\".parse::<i32>()  // parse string to type (Result)",
      "\"42\".parse::<i32>().unwrap()  // parse with unwrap",
      "x.saturating_add(5)  // saturating addition (no overflow)",
      "x.checked_add(5)  // checked addition (Option, None on overflow)",
      "x.overflowing_add(5)  // overflow returns (result, overflow_bool)",
      "x.wrapping_add(5)  // wrapping addition (wraps on overflow)",
      "x.abs_diff(3)  // absolute difference (u8, etc.)",
      "std::convert::identity(x)  // identity function (returns x)",
      "bool::from(true)  // bool to integer",
      "usize::try_from(i).unwrap()  // fallible usize conversion",
    ],
  },

  react: {
    "JSX Syntax": [
      "<div className=\"container\">  // className instead of class",
      "<label htmlFor=\"email\">Email</label>  // htmlFor instead of for",
      "onClick={() => setCount(c + 1)}  // onClick event handler",
      "onChange={e => setName(e.target.value)}  // onChange handler for inputs",
      "onSubmit={handleSubmit}  // onSubmit on form element",
      "style={{ color: 'red', fontSize: 16 }}  // inline style with double curlies",
      "<>hello</>  // fragment shorthand (no extra DOM node)",
      "<React.Fragment><Child /></React.Fragment>  // Fragment with key support",
      "dangerouslySetInnerHTML={{ __html: '<p>raw</p>' }}  // raw HTML injection",
      "suppressHydrationWarning={true}  // suppress SSR hydration mismatch warning",
      "key={item.id}  // key prop for list reconciliation",
      "{condition && <span>shown</span>}  // conditional rendering with &&",
      "{isLoggedIn ? <Profile /> : <Login />}  // ternary conditional rendering",
      "{items.map(i => <li key={i.id}>{i.name}</li>)}  // mapping arrays to elements",
      "{...props}  // spread props onto component",
      "children  // special prop for nested content",
      "ref={myRef}  // ref attribute for DOM access",
      "className={clsx('base', active && 'active')}  // dynamic classes with clsx",
    ],
    "Hooks": [
      "const [count, setCount] = useState(0)  // state hook with initial value",
      "const [state, setState] = useState(() => computeInit())  // lazy initial state",
      "useEffect(() => { document.title = count }, [count])  // effect with dependency",
      "useEffect(() => { fn(); return () => cleanup() }, [])  // effect with cleanup, run once",
      "useEffect(() => {})  // effect runs on every render (no deps)",
      "const ctx = useContext(ThemeContext)  // access context value",
      "const [state, dispatch] = useReducer(reducer, initState)  // reducer for complex state",
      "useReducer(reducer, init, initFn)  // lazy init for useReducer",
      "const memoized = useMemo(() => compute(a, b), [a, b])  // memoized computation",
      "const cb = useCallback(() => doThing(a, b), [a, b])  // memoized callback",
      "const ref = useRef(initialValue)  // mutable ref object (persists across renders)",
      "useRef<HTMLDivElement>(null)  // typed ref in TypeScript",
      "useImperativeHandle(ref, () => ({ focus }))  // expose methods to parent via ref",
      "useLayoutEffect(() => { measure() }, [])  // sync effect before browser paint",
      "useDebugValue(isOnline ? 'Online' : 'Offline')  // label for custom hook in DevTools",
      "useDebugValue(value, v => format(v))  // formatted debug value",
      "const deferred = useDeferredValue(value)  // defer re-render for non-urgent updates",
      "const [isPending, startTransition] = useTransition()  // mark state update as transition",
      "const id = useId()  // generate unique ID for accessibility attributes",
      "const snap = useSyncExternalStore(subscribe, getSnapshot)  // subscribe to external store",
      "useInsertionEffect(() => { injectStyles() }, [])  // inject styles before DOM mutations",
    ],
    "Custom Hooks": [
      "function useOnlineStatus() { const [online, setOnline] = useState(navigator.onLine); useEffect(() => { window.addEventListener('online', () => setOnline(true)); window.addEventListener('offline', () => setOnline(false)); return () => { window.removeEventListener('online', () => setOnline(true)); window.removeEventListener('offline', () => setOnline(false)); }; }, []); return online; }  // custom hook: useOnlineStatus",
      "function useLocalStorage(key, initial) { const [value, setValue] = useState(() => { const stored = localStorage.getItem(key); return stored !== null ? JSON.parse(stored) : initial; }); useEffect(() => { localStorage.setItem(key, JSON.stringify(value)); }, [key, value]); return [value, setValue]; }  // custom hook: useLocalStorage",
      "function useDebounce(value, delay) { const [debounced, setDebounced] = useState(value); useEffect(() => { const timer = setTimeout(() => setDebounced(value), delay); return () => clearTimeout(timer); }, [value, delay]); return debounced; }  // custom hook: useDebounce",
      "useCounter, useToggle, useClipboard  // common custom hook naming pattern",
    ],
    "Rules of Hooks": [
      "useState(0) // ERROR: called conditionally  // hooks must not be inside conditions",
      "for (...) { useEffect(...) } // ERROR  // hooks must not be inside loops",
      "function nested() { useState(0) } // ERROR  // hooks must not be inside nested functions",
      "// Only call hooks at the top level of your component or custom hook  // top-level rule",
      "// Only call hooks from React function components or custom hooks  // React function only",
    ],
    "Context": [
      "const ThemeCtx = createContext('light')  // create context with default value",
      "const theme = useContext(ThemeCtx)  // consume context in component",
      "<ThemeCtx.Provider value=\"dark\">  // provider wraps tree with value",
      "<ThemeCtx.Consumer>{val => <span>{val}</span>}</ThemeCtx.Consumer>  // legacy consumer pattern",
      "createContext(null)  // context without default (check for undefined)",
    ],
    "Refs & Forwarding": [
      "const ref = useRef<HTMLInputElement>(null); ref.current?.focus()  // focus input via ref",
      "forwardRef<HTMLInputElement, Props>((props, ref) => <input ref={ref} />)  // forward ref to child DOM",
      "useImperativeHandle(ref, () => ({ validate: () => true }), [])  // customize exposed ref methods",
      "<div ref={el => setEl(el)}>  // callback ref (called when ref changes)",
      "const ref = useRef(null); ref.current = new ResizeObserver(fn)  // ref for non-DOM values",
      "useRef(0)  // ref for mutable value that doesnt trigger re-render on change",
    ],
    "Effects & Cleanup": [
      "useEffect(() => { conn = createConn(); return () => conn.close() }, [])  // subscribe/unsubscribe pattern",
      "useEffect(() => { fetch(id).then(setData) }, [id])  // data fetching in effect",
      "useEffect(() => { let cancelled = false; fetch(id).then(d => { if (!cancelled) setData(d) }); return () => { cancelled = true } }, [id])  // race condition fix with cleanup",
      "useEffect(() => { const handler = (e) => setX(e.clientX); window.addEventListener('mousemove', handler); return () => window.removeEventListener('mousemove', handler) }, [])  // event listener with cleanup",
    ],
    "State Management": [
      "const [count, setCount] = useState(0); setCount(c => c + 1)  // functional update",
      "const [state, dispatch] = useReducer(todoReducer, [])  // useReducer for complex logic",
      "const [state, dispatch] = useReducer(reducer, initialState, initFn)  // lazy init reducer",
      "const { count } = useContext(CounterContext)  // context for global state",
      "const store = useSyncExternalStore(store.subscribe, store.getState)  // connect external store",
      "// Zustand: const useStore = create((set) => ({ count: 0, inc: () => set(s => ({ count: s.count + 1 })) }))  // zustand store",
      "// Jotai: const countAtom = atom(0); const [count, setCount] = useAtom(countAtom)  // jotai atomic state",
    ],
    "Event Handling": [
      "onClick={e => console.log(e)}  // synthetic click event",
      "onChange={e => setVal(e.target.value)}  // change event (input, select, textarea)",
      "onSubmit={e => { e.preventDefault(); submit() }}  // form submit with preventDefault",
      "onMouseEnter={() => setHover(true)}  // mouse enter",
      "onMouseLeave={() => setHover(false)}  // mouse leave",
      "onKeyDown={e => { if (e.key === 'Enter') save() }}  // keydown with key check",
      "onKeyUp={e => setKey(e.key)}  // keyup event",
      "onFocus={() => setFocused(true)}  // focus event",
      "onBlur={() => setFocused(false)}  // blur event",
      "onScroll={e => setPos(e.target.scrollTop)}  // scroll event",
      "onWheel={e => deltaY}  // wheel/scroll delta",
      "e.stopPropagation()  // stop event bubbling",
      "e.preventDefault()  // prevent default browser behavior",
      "e.nativeEvent  // access underlying native DOM event",
    ],
    "Forms": [
      "const [val, setVal] = useState(''); <input value={val} onChange={e => setVal(e.target.value)} />  // controlled input",
      "<input defaultValue=\"initial\" />  // uncontrolled input with default",
      "<form onSubmit={handleSubmit}>  // form with submit handler",
      "const ref = useRef(null); <input ref={ref} />; ref.current.value  // uncontrolled via ref",
      "<input type=\"checkbox\" checked={checked} onChange={e => setChecked(e.target.checked)} />  // controlled checkbox",
      "<input type=\"radio\" value=\"a\" checked={val === 'a'} onChange={e => setVal(e.target.value)} />  // controlled radio",
      "<select value={selected} onChange={e => setSelected(e.target.value)}>  // controlled select",
      "<select multiple value={arr} onChange={e => setArr([].slice.call(e.target.selectedKeys))}>  // multi-select",
      "<textarea value={text} onChange={e => setText(e.target.value)} />  // controlled textarea",
      "<input type=\"file\" onChange={e => setFile(e.target.files[0])} />  // file input",
      "<input required minLength={3} pattern=\"[A-Z]+\\d+\" />  // validation attributes",
      "<input disabled={isSubmitting} />  // disabled state",
      "<input readOnly />  // readOnly prevents editing",
      "<input type=\"color\" value={color} onChange={e => setColor(e.target.value)} />  // color picker",
      "<input type=\"date\" value={date} onChange={e => setDate(e.target.value)} />  // date input",
      "<input type=\"range\" min={0} max={100} value={range} onChange={e => setRange(e.target.value)} />  // range slider",
      "<input type=\"number\" value={num} onChange={e => setNum(Number(e.target.value))} />  // number input",
      "<input type=\"password\" value={pw} onChange={e => setPw(e.target.value)} autoComplete=\"off\" />  // password",
      "<input type=\"email\" value={email} onChange={e => setEmail(e.target.value)} />  // email input",
      "<input type=\"search\" value={q} onChange={e => setQ(e.target.value)} />  // search input",
    ],
    "Component Patterns": [
      "function withAuth(Component) { return function Authenticated(props) { const user = useUser(); return user ? <Component {...props} user={user} /> : <Login /> } }  // HOC pattern",
      "<DataProvider render={data => <Child data={data} />} />  // render prop pattern",
      "<select><option value=\"a\">A</option></select>  // compound component pattern",
      "{React.cloneElement(child, { extraProp: val })}  // clone element with extra props",
      "class ErrorBoundary extends React.Component { static getDerivedStateFromError(error) { return { hasError: true } }; componentDidCatch(error, info) { logError(error, info) }; render() { return this.state.hasError ? <Fallback /> : this.props.children } }  // error boundary class",
      "const LazyComp = React.lazy(() => import('./Heavy'))  // lazy load component",
      "<Suspense fallback={<Spinner />}><LazyComp /></Suspense>  // suspense with fallback",
      "const MemoChild = React.memo(Child)  // memoize component (shallow prop compare)",
      "React.memo(Child, (prev, next) => prev.id === next.id)  // custom compare for memo",
      "class PureChild extends React.PureComponent {}  // PureComponent with shallow shouldComponentUpdate",
      "ReactDOM.createPortal(child, document.body)  // portal to render outside parent DOM",
      "<React.Profiler id=\"Nav\" onRender={(id, phase, time) => log(time)}>  // profile component render time",
      "<React.StrictMode><App /></React.StrictMode>  // strict mode (double-invoke effects in dev)",
      "React.startTransition(() => setState(val))  // mark update as non-urgent transition",
      "ReactDOM.flushSync(() => setState(val))  // flush state update synchronously",
    ],
    "React Router": [
      "<BrowserRouter><Routes><Route path=\"/\" element={<Home />} /></Routes></BrowserRouter>  // router setup",
      "<HashRouter>  // hash-based routing (file://, no server needed)",
      "<Route path=\"/users/:id\" element={<User />} />  // route with param",
      "<Route index element={<Dashboard />} />  // index route (default child)",
      "<Route element={<Layout />}><Route index element={<Home />} /><Route path=\"about\" element={<About />} /></Route>  // nested layout routes",
      "<Link to=\"/about\">About</Link>  // declarative navigation",
      "<NavLink to=\"/about\" className={({ isActive }) => isActive ? 'active' : ''}>  // link with active state",
      "<Navigate to=\"/login\" replace />  // imperative redirect element",
      "<Outlet />  // render nested route children",
      "const { userId } = useParams()  // access route params",
      "const navigate = useNavigate(); navigate('/home')  // imperative navigation",
      "navigate('/home', { replace: true })  // navigate without history entry",
      "navigate(-1)  // go back one step",
      "const { pathname, search, hash } = useLocation()  // current location info",
      "const [searchParams, setSearchParams] = useSearchParams(); searchParams.get('q')  // query string hook",
      "const match = useMatch('/users/:id')  // match current URL against pattern",
      "useResolvedPath('/about')  // resolve relative path",
      "const router = createBrowserRouter([{ path: '/', element: <Root />, errorElement: <ErrorPage />, loader: rootLoader, action: rootAction, children: [...] }]); <RouterProvider router={router} />  // data router",
      "async function loader({ params }) { return fetch(`/api/${params.id}`) }  // loader for data fetching",
      "async function action({ request }) { const form = await request.formData(); return update(form) }  // form action",
      "const data = useLoaderData()  // access loader data in component",
      "const actionData = useActionData()  // access action data",
      "const fetcher = useFetcher(); fetcher.submit(formData, { method: 'post' })  // submit without navigation",
      "const data = useFetcher().data  // access fetcher result data",
      "const { data, pending } = useAsyncValue();  // use deferred data (unstable)",
      "// const def = defer({ data: fetchData() }); const { data } = useAsyncValue(); // Await <Await resolve={data}><Result /></Await>  // defer/await pattern",
      "createRoutesFromElements(<Route>...</Route>)  // create routes from JSX elements",
      "<Route errorElement={<ErrorBoundary />}>  // error boundary per route",
    ],
    "Testing": [
      "render(<Component prop=\"val\" />)  // render component for testing",
      "screen.getByText('Submit')  // find element by text content",
      "screen.getByRole('button', { name: /submit/i })  // find by ARIA role",
      "screen.getByTestId('element-id')  // find by data-testid",
      "screen.getByLabelText('Email')  // find input by associated label",
      "screen.getByPlaceholderText('Enter email')  // find by placeholder",
      "screen.getByDisplayValue('initial')  // find element by display value",
      "await screen.findByText('Loaded')  // async find (waits up to 1s)",
      "expect(screen.queryByText('Error')).toBeNull()  // query returns null if not found",
      "fireEvent.click(screen.getByText('Submit'))  // fire DOM event",
      "await userEvent.click(screen.getByText('Submit'))  // user-like event (preferred)",
      "await userEvent.type(input, 'hello')  // type into input character by character",
      "await userEvent.selectOptions(select, ['option'])  // select option",
      "await userEvent.upload(fileInput, new File(['data'], 'test.png'))  // file upload",
      "await waitFor(() => expect(el).toBeVisible())  // wait for assertion to pass",
      "await waitForElementToBeRemoved(() => screen.getByText('Loading'))  // wait for element removal",
      "const { container } = render(<Comp />); within(container).getByText('inner')  // scoped queries",
      "await act(async () => { render(<Comp />) })  // wrap state-updating code in act",
      "jest.fn(() => 'mocked')  // create mock function",
      "jest.spyOn(obj, 'method').mockReturnValue(42)  // spy on and mock method",
    ],
    "Server Components": [
      "'use client'  // marks file as client component (has interactivity)",
      "'use server'  // marks file as server action",
      "async function Page() { const data = await db.query() }  // async server component (RSC)",
      "// 'use server'; export async function createItem(formData) { await db.insert(formData) }  // server action",
      "import { serverOnly } from 'server-only'  // ensure code never reaches client",
      "import 'client-only'  // ensure code only runs on client",
    ],
    "Next.js": [
      "// App Router: app/page.js  // root route",
      "// app/layout.js  // root layout (wraps all pages)",
      "// app/loading.js  // loading UI (Suspense boundary)",
      "// app/error.js  // error boundary UI ('use client')",
      "// app/not-found.js  // 404 page",
      "// app/route.js  // API route handler",
      "// app/template.js  // re-renders on navigation (unlike layout)",
      "// app/default.js  // fallback for parallel routes",
      "// app/@analytics/page.js  // parallel route slot",
      "// app/(shop)/products/page.js  // route group (no segment in URL)",
      "// app/user/[id]/page.js  // dynamic route [id]",
      "// app/docs/[...slug]/page.js  // catch-all segment",
      "// app/docs/[[...slug]]/page.js  // optional catch-all",
      "// (.)photos page for intercepting routes  // intercepting route (.) same level",
      "export async function generateMetadata({ params }) { return { title: `Post ${params.id}` } }  // dynamic metadata",
      "export const metadata = { title: 'Home', description: 'Welcome' }  // static metadata",
      "import Link from 'next/link'; <Link href=\"/about\">About</Link>  // next/link prefetch-enabled",
      "import { useRouter } from 'next/navigation'; router.push('/about')  // app router navigation",
      "import { usePathname } from 'next/navigation'  // current pathname",
      "import { useSearchParams } from 'next/navigation'  // search params (client)",
      "import { useParams } from 'next/navigation'  // dynamic route params",
      "import { redirect } from 'next/navigation'; redirect('/login')  // server redirect",
      "import { notFound } from 'next/navigation'; notFound()  // trigger 404",
      "import Image from 'next/image'; <Image src=\"/hero.png\" width={800} height={600} alt=\"Hero\" />  // optimized Image",
      "import Script from 'next/script'; <Script src=\"/analytics.js\" strategy=\"afterInteractive\" />  // script loading",
      "import { revalidatePath } from 'next/cache'; revalidatePath('/posts')  // on-demand revalidate",
      "import { revalidateTag } from 'next/cache'; revalidateTag('posts')  // revalidate by tag",
      "export const dynamic = 'force-dynamic'  // force dynamic rendering",
      "export const dynamic = 'force-static'  // force static generation",
      "export const revalidate = 3600  // ISR: revalidate every hour",
      "export async function generateStaticParams() { const posts = await fetchPosts(); return posts.map(p => ({ id: p.id })) }  // static params for SSG",
      "export const dynamicParams = false  // 404 for ungenerated dynamic params",
      "import { useFormStatus } from 'react-dom'; const { pending } = useFormStatus()  // form pending state",
      "import { useOptimistic } from 'react'; const [optimistic, addOptimistic] = useOptimistic(state, (s, v) => s + v)  // optimistic updates",
      "import { useActionState } from 'react'; const [state, formAction, pending] = useActionState(serverAction, init)  // form action state",
      "// next.config.js: output: 'standalone'  // self-hosted deployment",
      "// next.config.js: output: 'export'  // static HTML export",
      "// next.config.js: images: { domains: ['cdn.com'] }  // remote image domains",
      "// middleware.ts: export { default } from 'next-auth/middleware'; export const config = { matcher: ['/dashboard'] }  // auth middleware",
    ],
  },

  vue: {
    "Options API": [
      "export default { data() { return { count: 0 } } }  // reactive data in options API",
      "computed: { doubled() { return this.count * 2 } }  // computed property",
      "watch: { count(newVal, oldVal) { console.log(newVal) } }  // watcher on data property",
      "watch: { count: { handler(n, o) {}, deep: true, immediate: true } }  // deep + immediate watch",
      "methods: { increment() { this.count++ } }  // component methods",
      "created() { fetchData() }  // called after instance created",
      "mounted() { this.$el }  // called after DOM mounted",
      "updated() { console.log('re-rendered') }  // called after reactive update",
      "destroyed() { cleanup() }  // called after teardown (Vue 2)",
      "beforeCreate() { /* no data access yet */ }  // called before reactivity init",
      "beforeMount() { /* template compiled, not yet mounted */ }  // before DOM mount",
      "beforeUpdate() { /* before DOM re-render */ }  // before reactive update",
      "beforeDestroy() { cleanup() }  // before teardown (Vue 2)",
      "activated() { /* kept-alive component shown */ }  // called when <KeepAlive> component activated",
      "deactivated() { /* kept-alive component hidden */ }  // called when <KeepAlive> component deactivated",
      "errorCaptured(err, vm, info) { return false }  // capture descendant errors",
      "props: { name: String, age: { type: Number, required: true, default: 18, validator: v => v > 0 } }  // prop declaration with validation",
      "emits: ['submit', 'update']  // declared emitted events",
      "provide() { return { user: this.user } }  // provide values for descendants",
      "inject: ['user']  // inject provided values",
      "components: { MyChild }  // local component registration",
    ],
    "Composition API": [
      "const count = ref(0)  // reactive primitive (wraps in .value)",
      "const state = reactive({ count: 0 })  // reactive object (deep)",
      "const doubled = computed(() => count.value * 2)  // computed ref",
      "const writable = computed({ get: () => count.value * 2, set: v => count.value = v / 2 })  // writable computed",
      "watch(count, (n, o) => console.log(n))  // watch a single ref",
      "watch([count, name], ([c, n], [oc, on]) => {})  // watch multiple sources",
      "watchEffect(() => console.log(count.value))  // auto-tracks deps, immediate + cleanup",
      "watchPostEffect(() => {})  // watchEffect fired after DOM update",
      "watchSyncEffect(() => {})  // watchEffect fired synchronously",
      "onMounted(() => fetchData())  // lifecycle: mounted",
      "onUpdated(() => {})  // lifecycle: updated",
      "onUnmounted(() => cleanup())  // lifecycle: unmounted (replaces destroyed)",
      "onBeforeMount(() => {})  // before mount",
      "onBeforeUpdate(() => {})  // before update",
      "onBeforeUnmount(() => {})  // before unmount",
      "onErrorCaptured((err, vm, info) => { return false })  // error capture",
      "onRenderTracked(e => {})  // track reactive dependency",
      "onRenderTriggered(e => {})  // trigger re-render origin",
      "onActivated(() => {})  // keep-alive activate",
      "onDeactivated(() => {})  // keep-alive deactivate",
      "provide('key', value)  // provide in composition API",
      "const value = inject('key', 'default')  // inject with default",
      "const val = readonly(original)  // readonly reactive proxy",
      "const sRef = shallowRef(0)  // shallow ref (no deep reactivity)",
      "const sReactive = shallowReactive({})  // shallow reactive (root level only)",
      "triggerRef(sRef)  // manually trigger shallowRef effect",
      "const cRef = customRef((track, trigger) => ({ get() { track(); return val }, set(v) { val = v; trigger() } }))  // custom ref with manual track/trigger",
      "const raw = markRaw(obj)  // mark object to never become reactive",
      "const r = toRef(reactiveObj, 'key')  // create ref to a property",
      "const refs = toRefs(reactiveObj)  // destructure reactive to refs without losing reactivity",
      "const plain = toRaw(reactiveObj)  // get original object (breaks reactivity)",
      "isRef(val)  // check if value is a ref",
      "isReactive(val)  // check if value is reactive",
      "isProxy(val)  // check if object is reactive/readonly proxy",
      "const v = unref(val)  // shorthand: val if ref, else val (no .value)",
      "const scope = effectScope(); scope.run(() => { /* effects */ }); scope.stop()  // scope effects to stop together",
      "const scope = getCurrentScope()  // get current effect scope if active",
      "onScopeDispose(() => {})  // cleanup when scope is disposed",
    ],
    "Directives": [
      "v-if=\"isVisible\"  // conditional render (removes DOM node)",
      "v-else  // else branch for v-if",
      "v-else-if=\"cond\"  // else-if for v-if chain",
      "v-show=\"isVisible\"  // toggle visibility via display:none (keeps DOM)",
      "v-for=\"item in items\" :key=\"item.id\"  // list rendering with key",
      "v-for=\"(item, index) in items\"  // with index",
      "v-for=\"(value, key, index) in object\"  // iterate object",
      "v-model=\"text\"  // two-way binding (input -> data -> input)",
      "v-model.number=\"age\"  // automatically cast to number",
      "v-model.trim=\"name\"  // automatically trim whitespace",
      "v-model.lazy=\"text\"  // sync on change instead of input",
      "v-model:title=\"title\"  // component v-model with named prop (Vue 3)",
      ":href=\"url\"  // v-bind shorthand: dynamic attribute",
      ":class=\"{ active: isActive }\"  // dynamic class with object",
      ":class=\"['base', { active: isActive }]\"  // class with array+object",
      ":style=\"{ color: 'red', fontSize: size + 'px' }\"  // dynamic style",
      ":style=\"[baseStyles, overrides]\"  // style with array of objects",
      "@click=\"handle\"  // v-on shorthand: event listener",
      "@click.stop=\"handle\"  // event modifier: stopPropagation",
      "@submit.prevent=\"handle\"  // preventDefault",
      "@click.capture=\"handle\"  // use capture phase",
      "@click.self=\"handle\"  // only fire on self (not children)",
      "@click.once=\"handle\"  // fire once then unbind",
      "@scroll.passive=\"handle\"  // passive listener (no preventDefault)",
      "@click.exact=\"handle\"  // require exact modifier keys",
      "@keydown.enter=\"submit\"  // key modifier: enter",
      "@keydown.ctrl.enter=\"submit\"  // key combo modifier",
      "@keydown.esc=\"close\"  // escape key",
      "@click.ctrl.exact=\"handle\"  // exact Ctrl only (not Ctrl+Shift)",
      "@click.left=\"handle\"  // left mouse button modifier",
      "v-once  // render once, no future updates",
      "v-memo=\"[depA]\"  // skip re-render if deps unchanged (Vue 3.2+)",
      "v-cloak  // hide uncompiled template until Vue mounts",
      "v-pre  // skip compilation for this element",
      "v-html=\"rawHtml\"  // output raw HTML (XSS risk!)",
      "v-text=\"text\"  // sets innerText (escaped)",
    ],
    "Template Syntax": [
      "{{ message }}  // text interpolation (escaped)",
      "{{ message.toUpperCase().split('').reverse().join('') }}  // any valid JS expression",
      "v-html=\"raw\"  // raw HTML (sanitize user input!)",
      ":id=\"dynamicId\"  // dynamic attribute binding",
      ":[attrName]=\"value\"  // dynamic attribute name (Vue 3)",
      ":class=\"{ active: isActive }\"  // class binding with object",
      ":class=\"['static', dynamicClass]\"  // class binding with array",
      ":style=\"{ color: 'red' }\"  // inline style binding",
      "{{ count }}  // interpolation",
      "{{ message.split('').reverse().join('') }}  // JS expressions in interpolation",
    ],
    "Reactivity": [
      "ref(0)  // reactive primitive with .value wrapper",
      "reactive({ count: 0 })  // deeply reactive object proxy",
      "computed(() => val * 2)  // derived value auto-recalculates",
      "readonly(original)  // reactive but immutable proxy",
      "shallowRef(0)  // only .value change triggers reactivity (not nested)",
      "shallowReactive({})  // only root-level properties are reactive",
      "customRef((track, trigger) => ({ get, set }))  // custom ref with explicit track/trigger",
      "markRaw(obj)  // opt out of reactivity permanently",
      "toRef(obj, 'key')  // ref linked to reactive source property",
      "toRefs(obj)  // convert reactive object to plain refs",
      "isRef(v) / isReactive(v) / isReadonly(v) / isProxy(v)  // type checks",
      "toRaw(proxy)  // unwrap proxy to original object",
      "unref(r)  // r if isRef(r) else r (no .value needed)",
      "watch(source, callback, { deep, immediate, flush })  // flexible watcher",
      "watchEffect(fn)  // auto-tracks deps, runs immediately",
    ],
    "Vue Router": [
      "const router = createRouter({ history: createWebHistory(), routes: [...] })  // create router",
      "history: createWebHistory()  // HTML5 history mode (requires server config)",
      "history: createWebHashHistory()  // hash mode (#/path)",
      "history: createMemoryHistory()  // SSR mode (no URL)",
      "<RouterView />  // render matched route component",
      "<RouterLink to=\"/about\">About</RouterLink>  // navigation link",
      "<RouterLink :to=\"{ name: 'user', params: { id: 1 } }\">  // named route link",
      "const route = useRoute(); route.params.id  // access route params",
      "const router = useRouter(); router.push('/about')  // programmatic navigation",
      "router.replace('/about')  // navigate without history entry",
      "router.back()  // go back",
      "router.push({ name: 'user', params: { id: 1 }, query: { tab: 'profile' } })  // named + params + query",
      "{ path: '/users/:id', component: User, props: true }  // route params as component props",
      "{ path: '/users/:id', children: [ { path: 'profile', component: Profile } ] }  // nested routes",
      "{ path: '/users/:id(\\\\d+)' }  // param with regex constraint",
      "beforeEach((to, from, next) => { if (!isAuth) next('/login'); else next() })  // global guard",
      "beforeResolve((to, from, next) => {})  // called after beforeEach, before route resolved",
      "afterEach((to, from) => {})  // called after navigation confirmed",
      "{ path: '/admin', beforeEnter: (to, from) => { return false } }  // per-route guard",
      "onBeforeRouteLeave((to, from) => { if (unsaved) return false })  // component guard: leave",
      "onBeforeRouteUpdate((to, from) => { fetch(to.params.id) })  // component guard: param change",
      "scrollBehavior(to, from, savedPosition) { return { top: 0 } }  // scroll reset on navigation",
      "const route = useRoute(); route.query.page  // access query params",
    ],
    "Pinia": [
      "export const useStore = defineStore('main', { state: () => ({ count: 0 }), getters: { doubled: (s) => s.count * 2 }, actions: { increment() { this.count++ } } })  // options store",
      "export const useStore = defineStore('main', () => { const count = ref(0); function increment() { count.value++ }; return { count, increment } })  // setup store",
      "const store = useStore(); store.$id  // store identifier",
      "store.$state  // access full state object",
      "store.$patch({ count: store.count + 1 })  // partial state update",
      "store.$patch((state) => state.count++)  // patch with mutation function",
      "store.$reset()  // reset state to initial values (options store only)",
      "store.$subscribe((mutation, state) => { localStorage.setItem('store', JSON.stringify(state)) })  // subscribe to state changes",
      "store.$onAction(({ name, args, after, onError }) => { after(() => console.log('done')) })  // intercept actions",
      "store.$dispose()  // stop store reactivity",
      "const { count } = storeToRefs(store)  // destructure without losing reactivity",
      "store.count  // direct state access (reactive)",
      "store.increment()  // call action directly",
      "createPinia()  // create pinia instance",
      "// pinia plugin: (ctx) => { ctx.store.$subscribe(...) }  // store plugin",
    ],
    "Composables": [
      "export function useCounter(init = 0) { const count = ref(init); const inc = () => count.value++; const dec = () => count.value--; return { count, inc, dec } }  // reusable counter composable",
      "export function useFetch(url) { const data = ref(null); const error = ref(null); const loading = ref(true); watchEffect(async () => { loading.value = true; try { data.value = await (await fetch(url.value)).json() } catch (e) { error.value = e } finally { loading.value = false } }); return { data, error, loading } }  // async data fetching composable",
      "export function useLocalStorage(key, initial) { const value = ref(initial); watch(value, v => localStorage.setItem(key, JSON.stringify(v)), { deep: true, immediate: true }); return value }  // localStorage composable",
      "export function useEventListener(target, event, handler) { onMounted(() => target.addEventListener(event, handler)); onUnmounted(() => target.removeEventListener(event, handler)) }  // event listener composable",
      "export function useMediaQuery(query) { const matches = ref(window.matchMedia(query).matches); onMounted(() => { const mq = window.matchMedia(query); mq.addEventListener('change', e => matches.value = e.matches) }); return matches }  // responsive media query composable",
      "export function useMouse() { const x = ref(0); const y = ref(0); useEventListener(window, 'mousemove', e => { x.value = e.clientX; y.value = e.clientY }); return { x, y } }  // track mouse position",
      "export function useInterval(fn, delay) { onMounted(() => { const id = setInterval(fn, delay.value); onUnmounted(() => clearInterval(id)) }) }  // interval composable",
      "export function useDebounce(fn, delay) { let timer; return (...args) => { clearTimeout(timer); timer = setTimeout(() => fn(...args), delay) } }  // debounce composable",
      "export function useClipboard() { const copied = ref(false); async function copy(text) { await navigator.clipboard.writeText(text); copied.value = true; setTimeout(() => copied.value = false, 2000) }; return { copy, copied } }  // clipboard composable",
    ],
    "Nuxt": [
      "navigateTo('/dashboard')  // programmatic navigation (Nuxt)",
      "definePageMeta({ layout: 'admin', middleware: 'auth' })  // page metadata",
      "const { data, pending, error, refresh } = await useFetch('/api/posts')  // data fetching (auto-keyed)",
      "const { data } = await useAsyncData('posts', () => $fetch('/api/posts'))  // async data with custom key",
      "const { data } = useLazyFetch('/api/posts')  // lazy fetch (no blocking navigation)",
      "const { data } = useLazyAsyncData('posts', () => queryContent().find())  // lazy async data",
      "refresh()  // refetch data from server",
      "clearNuxtData('posts')  // clear cached data",
      "const router = useRouter(); const route = useRoute()  // router and route in Nuxt",
      "const count = useState('counter', () => 0)  // global shared state (SSR-safe)",
      "const cookie = useCookie('theme', { maxAge: 3600 })  // reactive cookie",
      "const headers = useRequestHeaders(['cookie'])  // access request headers (server)",
      "const url = useRequestURL()  // full request URL",
      "const config = useRuntimeConfig(); config.public.apiBase  // runtime config",
      "const appConfig = useAppConfig()  // reactive app config",
      "defineNuxtRouteMiddleware((to, from) => { if (!isAuth) return navigateTo('/login') })  // route middleware",
      "// server/api/hello.ts: export default defineEventHandler(() => 'Hello')  // API route",
      "// server/middleware/log.ts: export default defineEventHandler((e) => { console.log(e.path) })  // server middleware",
    ],
  },

  angular: {
    "Components": [
      "@Component({ selector: 'app-root', template: '<h1>{{title}}</h1>', styleUrls: ['./app.component.css'] })  // component decorator",
      "@Component({ templateUrl: './my.component.html', styles: ['h1 { color: red }'] })  // external template + inline styles",
      "@Component({ changeDetection: ChangeDetectionStrategy.OnPush })  // on-push change detection (only check @Input changes)",
      "@Component({ encapsulation: ViewEncapsulation.Emulated })  // default: scoped CSS via attribute",
      "@Component({ encapsulation: ViewEncapsulation.ShadowDom })  // native Shadow DOM",
      "@Component({ encapsulation: ViewEncapsulation.None })  // global styles (no scoping)",
      "@Component({ providers: [MyService] })  // component-level providers",
      "@Component({ host: { '[class.active]': 'isActive', '(click)': 'onClick()' } })  // host bindings + listeners",
      "@Component({ inputs: ['name'], outputs: ['change'] })  // legacy input/output declaration",
      "@Component({ exportAs: 'child' })  // export component for template reference #child",
      "@Component({ standalone: true, imports: [CommonModule] })  // standalone component (Angular 14+)",
    ],
    "Directives": [
      "@Directive({ selector: '[appHighlight]' })  // attribute directive (matches [appHighlight])",
      "@HostListener('click', ['$event']) onClick(e) {}  // listen to host element events",
      "@HostBinding('class.active') isActive = false  // bind to host element property",
      "@Input('appHighlight') color = 'yellow'  // input property binding",
      "@Output() change = new EventEmitter<number>()  // output event emitter",
      "selector: '[appUnless]'  // structural directive selector",
      "selector: 'appCard'  // component-style directive selector",
    ],
    "Pipes": [
      "@Pipe({ name: 'exponential', pure: true })  // custom pipe (pure = only recalc on input change)",
      "transform(value: number, exponent = 1): number { return Math.pow(value, exponent) }  // pipe transform method",
      "{{ today | date:'fullDate' }}  // DatePipe: 'Monday, January 15, 2024'",
      "{{ name | uppercase }}  // UpperCasePipe",
      "{{ name | lowercase }}  // LowerCasePipe",
      "{{ price | currency:'USD':'symbol':'1.2-2' }}  // CurrencyPipe",
      "{{ num | number:'3.1-2' }}  // DecimalPipe",
      "{{ pct | percent:'1.0-2' }}  // PercentPipe",
      "{{ observable$ | async }}  // AsyncPipe (auto-subscribe/unsubscribe)",
      "{{ obj | json }}  // JsonPipe (pretty-print)",
      "{{ arr | slice:1:3 }}  // SlicePipe (array/string slice)",
      "{{ obj | keyvalue }}  // KeyValuePipe (iterate object)",
      "{{ count | i18nPlural: { '=0': 'none', 'other': '#' } }}  // pluralization pipe",
      "{{ gender | i18nSelect: { male: 'Mr.', female: 'Ms.' } }}  // select pipe",
      "{{ title | titlecase }}  // TitleCasePipe",
    ],
    "Services & DI": [
      "@Injectable({ providedIn: 'root' })  // tree-shakable singleton service",
      "@Injectable({ providedIn: 'platform' })  // singleton across multiple Angular apps",
      "@Injectable({ providedIn: 'any' })  // new instance per lazy module",
      "@Injectable()  // service without providedIn (declare in module providers)",
      "constructor(@Optional() @Inject(TOKEN) private svc: MyService)  // optional injection with token",
      "@Self()  // restrict DI to current injector only",
      "@SkipSelf()  // start lookup from parent injector",
      "@Host()  // restrict to host injector",
      "const TOKEN = new InjectionToken<string>('desc')  // create injection token",
      "constructor(private svc: MyService) {}  // constructor-based DI",
      "private svc = inject(MyService)  // inject function (Angular 14+)",
      "@Inject(TOKEN) private config: string  // inject by token",
      "forwardRef(() => MyComponent)  // forward reference for circular deps",
      "// providers: [{ provide: MyService, useClass: MockService }]  // provider: use class",
      "// providers: [{ provide: API_URL, useValue: 'https://api.example.com' }]  // provider: use value",
      "// providers: [{ provide: Logger, useFactory: (config) => new Logger(config.level), deps: [Config] }]  // factory provider",
      "// providers: [{ provide: OldService, useExisting: NewService }]  // alias provider",
      "// providers: [{ provide: HTTP_INTERCEPTORS, useClass: AuthInterceptor, multi: true }]  // multi provider",
    ],
    "Lifecycle Hooks": [
      "ngOnChanges(changes: SimpleChanges) { console.log(changes) }  // called when @Input changes (first call before ngOnInit)",
      "ngOnInit() { fetchData() }  // called once after first ngOnChanges",
      "ngDoCheck() { console.log('change detection ran') }  // called on every change detection run",
      "ngAfterContentInit() { console.log('content projected') }  // after ng-content projection",
      "ngAfterContentChecked() { }  // after projected content checked",
      "ngAfterViewInit() { this.viewChild.nativeElement.focus() }  // after component view initialized",
      "ngAfterViewChecked() { }  // after view checked",
      "ngOnDestroy() { this.sub.unsubscribe() }  // cleanup before destruction",
    ],
    "Forms": [
      "<form #f=\"ngForm\" (ngSubmit)=\"onSubmit(f)\" novalidate>  // template-driven form",
      "<input name=\"email\" ngModel #email=\"ngModel\" required email />  // ngModel with validation",
      "<input name=\"name\" [(ngModel)]=\"user.name\" #name=\"ngModel\" minlength=\"3\" />  // two-way binding with ngModel",
      "f.valid  // form validity",
      "f.invalid  // form invalid",
      "f.pristine / f.dirty  // whether form has been modified",
      "f.touched / f.untouched  // whether form has been blurred",
      "<input #name=\"ngModel\" /> {{ name.errors | json }}  // access validation errors",
      "import { ReactiveFormsModule } from '@angular/forms'  // reactive forms module",
      "form = new FormGroup({ name: new FormControl('', Validators.required), email: new FormControl('', [Validators.required, Validators.email]) })  // FormGroup with validators",
      "<form [formGroup]=\"form\" (ngSubmit)=\"submit()\">  // reactive form binding",
      "<input formControlName=\"name\" />  // bind FormControl to input",
      "<div formGroupName=\"address\">  // nested FormGroup",
      "<div formArrayName=\"items\">  // FormArray binding",
      "fb = inject(FormBuilder); this.form = this.fb.group({ name: ['', Validators.required], email: [''] })  // FormBuilder",
      "fb.array([fb.control('')])  // FormArray via FormBuilder",
      "this.form.get('name')?.value  // access control value",
      "this.form.patchValue({ name: 'John' })  // partial update",
      "this.form.setValue({ name: 'John', email: 'j@e.co' })  // full update (all fields required)",
      "this.form.reset()  // reset form to initial state",
      "form.controls.name.setValidators([Validators.required, Validators.email])  // set validators dynamically",
      "form.controls.name.updateValueAndValidity()  // recalculate validity",
      "Validators.required  // required field validator",
      "Validators.minLength(3)  // min length",
      "Validators.maxLength(100)  // max length",
      "Validators.pattern('[a-z]+')  // regex pattern",
      "Validators.email  // email format",
      "Validators.min(18)  // min value (number/date)",
      "Validators.max(99)  // max value (number/date)",
      "Validators.compose([Validators.required, Validators.email])  // combine validators",
      "asyncValidator(control: AbstractControl): Promise<ValidationErrors | null> { return fetch('/check').then(r => r.ok ? null : { taken: true }) }  // async validator",
      "form.controls.name.valueChanges.subscribe(v => console.log(v))  // react to value changes",
      "form.controls.name.statusChanges.subscribe(s => console.log(s))  // VALID, INVALID, PENDING, DISABLED",
    ],
    "HttpClient": [
      "import { provideHttpClient, withInterceptors } from '@angular/common/http'  // provideHttpClient (Angular 15+)",
      "http = inject(HttpClient)  // inject HttpClient",
      "this.http.get<User[]>('/api/users').subscribe(data => this.users = data)  // GET request",
      "this.http.post<User>('/api/users', { name: 'John' }).subscribe()  // POST request",
      "this.http.put('/api/users/1', { name: 'John' }).subscribe()  // PUT request",
      "this.http.patch('/api/users/1', { name: 'John' }).subscribe()  // PATCH request",
      "this.http.delete('/api/users/1').subscribe()  // DELETE request",
      "this.http.get('/api/users', { params: { page: '1', sort: 'name' }, headers: { Authorization: 'Bearer token' } }).subscribe()  // GET with params + headers",
      "let params = new HttpParams().set('page', '1').append('sort', 'name')  // HttpParams builder",
      "let headers = new HttpHeaders().set('Authorization', 'Bearer ' + token)  // HttpHeaders builder",
      "this.http.get('/api/file', { responseType: 'blob' }).subscribe(blob => { const url = window.URL.createObjectURL(blob) })  // file download",
      "this.http.post('/api/users', formData).subscribe()  // upload FormData",
      "this.http.get('/api/users').pipe(catchError(err => { console.error(err); return throwError(() => err) })).subscribe()  // error handling",
      "this.http.intercept(req: HttpRequest<unknown>, next: HttpHandler): Observable<HttpEvent<unknown>> { const cloned = req.clone({ setHeaders: { Authorization: 'Bearer ' + token } }); return next.handle(cloned) }  // HTTP interceptor",
    ],
    "Routing": [
      "RouterModule.forRoot(routes)  // root routing config",
      "RouterModule.forChild(routes)  // feature module routing",
      "const routes: Routes = [{ path: '', component: Home }, { path: 'users/:id', component: UserDetail }]  // route definition",
      "<router-outlet></router-outlet>  // view container for routed components",
      "<a routerLink=\"/users/1\">User</a>  // routerLink directive",
      "<a [routerLink]=\"['/users', user.id]\">  // routerLink with params array",
      "<a routerLink=\"/users\" routerLinkActive=\"active\" [routerLinkActiveOptions]=\"{ exact: true }\">  // active link class",
      "this.router.navigate(['/users', id])  // programmatic navigation",
      "this.router.navigate(['/users'], { queryParams: { page: 1 }, fragment: 'section' })  // navigate with query + fragment",
      "this.router.navigateByUrl('/users?page=1#section')  // navigate by URL string",
      "const route = inject(ActivatedRoute); route.paramMap.subscribe(params => console.log(params.get('id')))  // observe route params",
      "route.snapshot.paramMap.get('id')  // snapshot param (one-time read)",
      "route.queryParamMap.subscribe(params => params.get('page'))  // query params observable",
      "route.data.subscribe(data => data.title)  // resolved data",
      "route.fragment.subscribe(f => f)  // URL fragment",
      "route.children  // child routes",
      "route.outlet  // named outlet",
      "{ path: 'old', redirectTo: '/new', pathMatch: 'full' }  // redirect with full match",
      "{ path: 'user/:id', component: User, canActivate: [AuthGuard], data: { role: 'admin' } }  // route guard + data",
      "{ path: 'admin', loadComponent: () => import('./admin.component').then(m => m.AdminComponent) }  // lazy load standalone component",
      "{ path: 'admin', loadChildren: () => import('./admin.module').then(m => m.AdminModule) }  // lazy load module",
      "canActivate: [() => inject(AuthService).isLoggedIn()]  // functional guard (Angular 15+)",
      "canDeactivate: [component => !component.hasUnsavedChanges]  // canDeactivate guard",
      "canMatch: [() => isFeatureEnabled()]  // canMatch guard",
      "import { withComponentInputBinding } from '@angular/router'; provideRouter(routes, withComponentInputBinding())  // bind route params to @Input",
      "provideRouter(routes, withViewTransitions())  // enable view transitions (Angular 17+)",
    ],
    "Standalone": [
      "@Component({ standalone: true, imports: [CommonModule, RouterModule] })  // standalone component",
      "bootstrapApplication(AppComponent, { providers: [provideRouter(routes), provideHttpClient()] })  // bootstrap standalone app",
      "provideRouter(routes)  // provide router in standalone",
      "provideHttpClient(withInterceptors([authInterceptor]))  // provide HttpClient with interceptors",
      "provideAnimations()  // provide animations",
      "provideZoneChangeDetection({ eventCoalescing: true })  // zone change detection config",
      "provideExperimentalZonelessChangeDetection()  // zoneless change detection (Angular 18+)",
      "import { ApplicationConfig } from '@angular/core'  // ApplicationConfig type",
      "mergeApplicationConfig(baseConfig, overrideConfig)  // merge app configs",
    ],
    "Change Detection": [
      "ChangeDetectionStrategy.Default  // default: check entire tree",
      "ChangeDetectionStrategy.OnPush  // only check if @Input changed",
      "changeDetectorRef.markForCheck()  // mark OnPush component for check",
      "changeDetectorRef.detectChanges()  // run change detection immediately",
      "changeDetectorRef.detach()  // detach from change detector tree",
      "changeDetectorRef.reattach()  // reattach to change detector tree",
      "changeDetectorRef.checkNoChanges()  // verify no changes after development",
      "inject(ApplicationRef).tick()  // trigger application-wide change detection",
      "inject(NgZone).run(() => { /* run inside Angular zone */ })  // run code within NgZone",
      "inject(NgZone).runOutsideAngular(() => { /* run without change detection */ })  // run code outside NgZone (performance)",
      "// signal: count = signal(0); count.set(5); count.update(v => v + 1)  // signals in change detection",
    ],
  },

  svelte: {
    "Reactive Declarations": [
      "$: doubled = count * 2  // reactive assignment (recomputes when deps change)",
      "$: { console.log(count); console.log(name) }  // reactive block (runs when deps change)",
      "$: if (count > 10) { console.log('big') }  // reactive conditional block",
      "$: each(items) { console.log(item) }  // reactive each block",
      "$: debug count  // reactive debug statement (logs count on change)",
    ],
    "Reactivity & Runes": [
      "let count = $state(0)  // rune: reactive state (Svelte 5)",
      "let doubled = $derived(count * 2)  // rune: derived value",
      "$effect(() => { console.log(count) })  // rune: reactive side-effect",
      "count = 5  // simple assignment triggers reactivity",
      "items = [...items, newItem]  // array assignment triggers reactivity",
      "obj = { ...obj, updated: true }  // object assignment triggers reactivity",
      "$state({ nested: { count: 0 } })  // deep reactive state",
    ],
    "Stores": [
      "import { writable } from 'svelte/store'; export const count = writable(0)  // writable store",
      "import { readable } from 'svelte/store'; export const time = readable(new Date(), (set) => { const interval = setInterval(() => set(new Date()), 1000); return () => clearInterval(interval) })  // readable store with start/stop",
      "import { derived } from 'svelte/store'; export const doubled = derived(count, $count => $count * 2)  // derived store",
      "import { derived } from 'svelte/store'; export const sum = derived([a, b], ([$a, $b]) => $a + $b)  // derived from multiple stores",
      "store.set(5)  // set store value",
      "store.update(n => n + 1)  // update store with callback",
      "store.subscribe(val => console.log(val))  // subscribe to store changes",
      "{$count}  // auto-subscription in template (unsubscribes on destroy)",
      "import { get } from 'svelte/store'; const val = get(store)  // one-time read (no subscription)",
      "function createStore() { const { subscribe, set, update } = writable(0); return { subscribe, increment: () => update(n => n + 1) } }  // custom store (only exposes subscribe for store contract)",
    ],
    "Lifecycle": [
      "onMount(() => { fetchData(); return () => cleanup() })  // after component mounts, returns cleanup",
      "beforeUpdate(() => { console.log('before DOM update') })  // runs before DOM updates",
      "afterUpdate(() => { console.log('after DOM update') })  // runs after DOM updates",
      "onDestroy(() => { clearInterval(timer) })  // cleanup on component destroy",
      "await tick()  // resolve after pending DOM updates",
      "setContext('key', value)  // set context for children",
      "const val = getContext('key')  // get context from parent",
      "hasContext('key')  // check if context exists",
      "getAllContexts()  // get all contexts",
    ],
    "Components & Props": [
      "export let name = 'default'  // prop declaration with default",
      "$$props  // all props passed to component (object)",
      "$$restProps  // props not declared with export let (remaining)",
      "$$slots  // object of slot names",
    ],
    "Slots": [
      "<slot></slot>  // default slot outlet",
      "<slot name=\"header\"></slot>  // named slot outlet",
      "<slot name=\"item\" {data}></slot>  // slot with props",
      "<div slot=\"header\">Title</div>  // named slot content (legacy)",
      "{#snippet header()}<h1>Title</h1>{/snippet}  // snippet (Svelte 5)",
    ],
    "Special Elements": [
      "<svelte:component this={currentComponent} />  // dynamic component",
      "<svelte:element this={tag}>  // dynamic HTML element",
      "<svelte:window bind:scrollY={y} on:keydown={handle} />  // window bindings",
      "<svelte:document on:click={handle} />  // document event listener",
      "<svelte:body on:mouseenter={handle} />  // body event listener",
      "<svelte:head><title>Page Title</title></svelte:head>  // inject to <head>",
      "<svelte:options immutable={true} />  // component options (immutable mode)",
      "<svelte:fragment></svelte:fragment>  // fragment element (no wrapper)",
      "<svelte:boundary onerror={e => console.error(e)}>  // error boundary",
    ],
    "Events": [
      "on:click={handler}  // DOM event listener",
      "on:submit|preventDefault={handler}  // event with preventDefault modifier",
      "on:click|stopPropagation|preventDefault  // chained modifiers",
      "on:click|passive  // passive listener (scroll perf)",
      "on:click|once  // fire once then remove",
      "on:click|capture  // use capture phase",
      "on:click|self  // only fire if event.target === element",
      "on:click|trusted  // only fire if event.isTrusted",
      "import { createEventDispatcher } from 'svelte'; const dispatch = createEventDispatcher(); dispatch('submit', { data })  // component event dispatch",
      "on:submit={e => console.log(e.detail)}  // receive component event",
      "<button on:click>  // forward event to parent (no handler)",
    ],
    "Bindings": [
      "bind:value={name}  // two-way bind input value",
      "bind:checked={isChecked}  // checkbox checked binding",
      "bind:group={selected}  // radio/checkbox group binding",
      "bind:files={fileList}  // file input files binding",
      "bind:this={elementRef}  // get reference to DOM element",
      "bind:this={componentRef}  // get reference to component instance",
      "bind:clientWidth={w} bind:clientHeight={h}  // element dimensions",
      "bind:offsetWidth={w} bind:offsetHeight={h}  // offset dimensions",
      "bind:scrollLeft={left} bind:scrollTop={top}  // scroll position",
      "bind:innerHTML={html}  // two-way innerHTML (XSS risk!)",
      "bind:textContent={text}  // two-way textContent",
      "bind:property={value}  // two-way bind to component export let prop",
    ],
    "Transitions & Animations": [
      "transition:fade  // fade transition on enter/leave",
      "transition:slide={{ duration: 500, delay: 100 }}  // slide with params",
      "transition:fly={{ x: 200, y: -200, opacity: 0 }}  // fly transition",
      "transition:scale={{ start: 0.5 }}  // scale transition",
      "transition:draw={{ duration: 1000 }}  // draw transition (SVG paths)",
      "transition:blur  // blur transition",
      "in:fade out:slide  // separate in/out transitions",
      "transition:crossfade  // crossfade between elements",
      "import { spring } from 'svelte/motion'; const size = spring(100, { stiffness: 0.5, damping: 0.3 })  // spring physics store",
      "import { tweened } from 'svelte/motion'; const val = tweened(0, { duration: 1000, easing: cubicOut })  // tweened store",
      "import { cubicInOut, elasticOut, bounceInOut } from 'svelte/easing'  // built-in easing functions",
      "animate:flip  // FLIP animation for list reordering",
    ],
    "Actions": [
      "use:myAction  // apply action to element",
      "use:myAction={{ param: 'value' }}  // action with parameters",
      "function myAction(node, param) { node.addEventListener('click', () => {}); return { update(newParam) { param = newParam }, destroy() { node.removeEventListener('click') } } }  // action with lifecycle",
    ],
    "Logic Blocks": [
      "{#if condition}<p>shown</p>{/if}  // conditional block",
      "{#if a}<p>A</p>{:else if b}<p>B</p>{:else}<p>C</p>{/if}  // if-else chain",
      "{#each items as item}<li>{item}</li>{/each}  // each block",
      "{#each items as item, index}<li>{index}: {item}</li>{/each}  // each with index",
      "{#each items as { id, name }}<li>{name}</li>{/each}  // each with destructuring",
      "{#each items as item (item.id)}<li>{item.name}</li>{/each}  // each with key",
      "{#each items as item}<li>{item}</li>{:else}<p>No items</p>{/each}  // each with empty fallback",
      "{#await promise}<p>loading...</p>{:then value}<p>{value}</p>{:catch error}<p>{error}</p>{/await}  // async block",
      "{#key value}<p>{value}</p>{/key}  // force re-render when value changes",
    ],
    "Template Syntax": [
      "{expression}  // text interpolation (escaped)",
      "{@html rawHtml}  // raw HTML output (XSS risk!)",
      "{@debug count}  // debug breakpoint when count changes",
    ],
    "SvelteKit": [
      "// +page.svelte: default page component  // file-based route page",
      "// +layout.svelte: wraps all child pages  // persistent layout",
      "// +error.svelte: error boundary  // error page",
      "// +server.js: API route handler  // server endpoint",
      "// +page.server.js: server load for page  // server data loading",
      "// +layout.server.js: server load for layout  // layout server data",
      "// +page.js: universal load function  // universal load (runs on server + client)",
      "export async function load({ params, fetch, cookies, locals, url, route, depends, parent, setHeaders, untrack }) { const data = await fetch(`/api/${params.id}`).then(r => r.json()); return { data } }  // load function",
      "export const actions = { default: async ({ request, cookies, locals }) => { const form = await request.formData(); return { success: true } } }  // form actions",
      "export const load = async ({ fetch }) => { return { items: fetch('/api/items').then(r => r.json()) } }  // streaming with deferred data",
      "import { error } from '@sveltejs/kit'; error(404, 'Not found')  // throw HTTP error",
      "import { redirect } from '@sveltejs/kit'; redirect(302, '/login')  // redirect in load/action",
      "import { json } from '@sveltejs/kit'; return json({ ok: true })  // JSON response from API",
      "import { text } from '@sveltejs/kit'; return text('OK')  // text response",
      "import { fail } from '@sveltejs/kit'; return fail(400, { message: 'Invalid' })  // form error response",
      "setHeaders({ 'Cache-Control': 'max-age=3600' })  // set response headers in load",
      "import { goto } from '$app/navigation'; goto('/about')  // client-side navigation",
      "import { invalidate } from '$app/navigation'; invalidate('/api/data')  // re-run load for URL",
      "import { invalidateAll } from '$app/navigation'; invalidateAll()  // re-run all loads",
      "import { page } from '$app/stores'; $page.url.pathname  // current page info store",
      "import { navigating } from '$app/stores'; $navigating?.to?.url  // navigation in progress",
      "import { updated } from '$app/stores'; $updated  // true if app version updated",
      "import { enhance } from '$app/forms'; <form use:enhance>  // progressive form enhancement",
      "// hooks.server.js: export const handle = async ({ event, resolve }) => { const res = await resolve(event); return res }  // server handle hook",
      "// hooks.server.js: export const handleFetch = async ({ request, fetch }) => { return fetch(request) }  // intercept fetch",
      "// hooks.server.js: export const handleError = ({ error, event }) => { return { message: error.message } }  // server error handler",
      "export const prerender = true  // static prerendering",
      "export const ssr = false  // disable SSR (CSR only)",
      "export const csr = true  // enable client-side rendering",
      "export const trailingSlash = 'always'  // URL trailing slash policy",
    ],
  },

  node: {
    "process": [
      "process.env  // object containing environment variables",
      "process.env.NODE_ENV  // environment mode (development/production)",
      "process.argv  // command-line arguments array",
      "process.cwd()  // current working directory path",
      "process.chdir('/tmp')  // change working directory",
      "process.exit(0)  // exit process with status code",
      "process.exit(1)  // exit with error",
      "process.on('uncaughtException', err => console.error(err))  // catch uncaught exceptions",
      "process.on('unhandledRejection', reason => console.error(reason))  // catch unhandled promise rejections",
      "process.nextTick(() => {})  // schedule callback for next iteration of event loop",
      "process.stdout.write('hello')  // write to stdout",
      "process.stderr.write('error')  // write to stderr",
      "process.stdin.on('data', chunk => {})  // read from stdin",
      "process.pid  // current process PID",
      "process.ppid  // parent process PID",
      "process.platform  // OS platform ('linux', 'darwin', 'win32')",
      "process.arch  // CPU architecture ('x64', 'arm64')",
      "process.uptime()  // process uptime in seconds",
      "process.memoryUsage()  // { rss, heapTotal, heapUsed, external, arrayBuffers }",
      "process.cpuUsage()  // { user, system } microseconds",
      "process.hrtime()  // high-resolution [seconds, nanoseconds] tuple",
      "process.hrtime.bigint()  // high-resolution time as bigint",
      "process.release  // { name: 'node', lts: true, sourceUrl, headersUrl }",
      "process.versions  // object of dependency versions",
      "process.kill(pid, 'SIGTERM')  // send signal to process",
      "process.umask(0o022)  // set/get file creation mask",
      "process.allowedNodeEnvironmentFlags  // Set of allowed NODE_OPTIONS flags",
    ],
    "Path": [
      "path.join('dir', 'sub', 'file.txt')  // join path segments with correct separator",
      "path.resolve('dir', '..', 'file.txt')  // resolve to absolute path",
      "path.normalize('a/../b//c')  // normalize path (resolves .. and .)",
      "path.relative('/from', '/to')  // relative path between two abs paths",
      "path.dirname('/a/b/c.js')  // get directory portion: '/a/b'",
      "path.basename('/a/b/c.js')  // get filename: 'c.js'",
      "path.basename('/a/b/c.js', '.js')  // filename without extension: 'c'",
      "path.extname('/a/b/c.js')  // extension: '.js'",
      "path.parse('/a/b/c.js')  // { root, dir, base, ext, name }",
      "path.format({ dir: '/a/b', base: 'c.js' })  // build path from object",
      "path.isAbsolute('/a')  // true if absolute path",
      "path.sep  // path separator: '/' or '\\\\'",
      "path.delimiter  // PATH delimiter: ':' or ';'",
      "path.toNamespacedPath('C:\\\\')  // Windows namespace path (no-op on POSIX)",
      "path.win32  // Windows path implementation (always available)",
      "path.posix  // POSIX path implementation (always available)",
    ],
    "fs": [
      "fs.readFileSync('f.txt', 'utf8')  // synchronous file read",
      "fs.writeFileSync('f.txt', 'content')  // synchronous file write",
      "fs.appendFileSync('f.txt', 'more')  // synchronous append",
      "fs.unlinkSync('f.txt')  // delete file synchronously",
      "fs.mkdirSync('dir', { recursive: true })  // create directory (with parents)",
      "fs.rmdirSync('dir')  // remove empty directory",
      "fs.rmSync('dir', { recursive: true, force: true })  // remove file/dir recursively",
      "fs.readdirSync('.')  // list directory contents (array of strings)",
      "fs.readdirSync('.', { withFileTypes: true })  // list with Dirent objects",
      "fs.statSync('f.txt')  // file stats (size, mtime, isFile, isDirectory)",
      "fs.lstatSync('symlink')  // stat without following symlink",
      "fs.existsSync('f.txt')  // check if path exists",
      "fs.renameSync('old', 'new')  // rename file/directory",
      "fs.copyFileSync('src', 'dst', fs.constants.COPYFILE_EXCL)  // copy file (EXCL = error if exists)",
      "fs.chmodSync('f.txt', 0o644)  // change file permissions",
      "fs.chownSync('f.txt', uid, gid)  // change file owner",
      "fs.linkSync('target', 'link')  // create hard link",
      "fs.symlinkSync('target', 'link')  // create symbolic link",
      "fs.truncateSync('f.txt', 100)  // truncate file to length",
      "fs.watch('f.txt', (event, filename) => {})  // watch file/directory for changes",
      "fs.watchFile('f.txt', (curr, prev) => {})  // poll-based file change monitoring",
      "fs.createReadStream('f.txt', { highWaterMark: 64 * 1024 })  // readable stream from file",
      "fs.createWriteStream('f.txt')  // writable stream to file",
      "fs.open('f.txt', 'r', (err, fd) => {})  // open file descriptor",
      "fs.read(fd, buf, 0, buf.length, 0, () => {})  // read from fd into buffer",
      "fs.write(fd, buf, 0, buf.length, 0, () => {})  // write buffer to fd",
      "fs.close(fd, () => {})  // close file descriptor",
      "fs.promises.readFile('f.txt', 'utf8')  // promise-based file read",
      "fs.promises.writeFile('f.txt', 'content')  // promise-based file write",
      "fs.promises.appendFile('f.txt', 'more')  // promise-based append",
      "fs.promises.unlink('f.txt')  // promise-based delete",
      "fs.promises.mkdir('dir', { recursive: true })  // promise-based mkdir",
      "fs.promises.readdir('.')  // promise-based readdir",
      "fs.promises.stat('f.txt')  // promise-based stat",
      "fs.promises.rename('old', 'new')  // promise-based rename",
      "fs.promises.copyFile('src', 'dst')  // promise-based copy",
      "fs.constants.F_OK  // existence check constant",
      "fs.constants.R_OK / W_OK / X_OK  // read/write/execute permission constants",
    ],
    "Buffer": [
      "Buffer.from('hello')  // create buffer from string (default utf8)",
      "Buffer.from('ff', 'hex')  // from hex string",
      "Buffer.from([0x48, 0x65])  // from byte array",
      "Buffer.alloc(10)  // allocate buffer of 10 zero-filled bytes",
      "Buffer.allocUnsafe(10)  // allocate unfilled buffer (may contain old data, faster)",
      "Buffer.byteLength('hello', 'utf8')  // byte length of string",
      "Buffer.isBuffer(val)  // check if value is Buffer",
      "Buffer.isEncoding('utf8')  // check if encoding is supported",
      "Buffer.compare(buf1, buf2)  // compare two buffers for sorting",
      "Buffer.concat([buf1, buf2])  // concatenate buffers into one",
      "buf.toString('utf8')  // decode buffer to string",
      "buf.toString('hex')  // to hex string",
      "buf.toString('base64')  // to base64 string",
      "buf.toJSON()  // { type: 'Buffer', data: [...] }",
      "buf.length  // number of bytes",
      "buf.readInt8(offset)  // read signed 8-bit int at offset",
      "buf.readUInt8(offset)  // read unsigned 8-bit int",
      "buf.readInt16LE(offset)  // read signed 16-bit LE int",
      "buf.readInt16BE(offset)  // read signed 16-bit BE int",
      "buf.readUInt32LE(offset)  // read unsigned 32-bit LE int",
      "buf.readUInt32BE(offset)  // read unsigned 32-bit BE int",
      "buf.readFloatBE(offset)  // read 32-bit BE float",
      "buf.readDoubleBE(offset)  // read 64-bit BE double",
      "buf.write('hello')  // write string to buffer",
      "buf.writeInt32BE(value, offset)  // write signed 32-bit BE int",
      "buf.slice(start, end)  // create view into buffer (no copy)",
      "buf.subarray(start, end)  // alias for slice",
      "buf.copy(target, targetStart, sourceStart, sourceEnd)  // copy bytes to another buffer",
      "buf.fill(0)  // fill buffer with value",
      "buf.equals(other)  // byte-by-byte equality check",
      "buf.indexOf('hello')  // find first occurrence of value",
      "buf.includes('hell')  // check if value exists in buffer",
      "buf.swap16()  // swap byte order 16-bit in-place",
      "buf.swap32()  // swap byte order 32-bit in-place",
    ],
    "Stream": [
      "const { Readable } = require('stream')  // Readable stream class",
      "const { Writable } = require('stream')  // Writable stream class",
      "const { Transform } = require('stream')  // Transform stream class",
      "const { Duplex } = require('stream')  // Duplex stream class",
      "const { pipeline } = require('stream/promises')  // promise-based pipeline",
      "const { finished } = require('stream/promises')  // promise-based finished detection",
      "Readable.from([1, 2, 3])  // create readable from iterable",
      "readable.pipe(writable)  // pipe readable to writable",
      "readable.on('data', chunk => {})  // consume data chunks",
      "readable.on('end', () => {})  // no more data signal",
      "readable.push(chunk)  // push data into readable",
      "readable.unshift(chunk)  // unshift data back into queue",
      "writable.write(chunk)  // write data to stream",
      "writable.end()  // signal no more writes",
      "writable.on('finish', () => {})  // all data flushed",
      "pipeline(readable, transform, writable, err => {})  // pipe with error handling",
      "finished(stream, err => {})  // detect when stream ends/errors",
      "stream.setDefaultEncoding('utf8')  // set default encoding",
      "stream.cork() / stream.uncork()  // buffer writes for efficiency",
      "stream.destroy()  // destroy stream",
      "const { PassThrough } = require('stream')  // pass-through stream (usable as both)",
      "new Transform({ transform(chunk, enc, cb) { this.push(chunk.toString().toUpperCase()); cb() } })  // transform stream",
      "new Writable({ write(chunk, enc, cb) { fs.appendFile('f.txt', chunk, cb) } })  // custom writable",
      "new Readable({ read(size) { this.push(data) } })  // custom readable",
      "stream.addAbortSignal(signal, stream)  // abort stream with AbortSignal",
      "stream.compose(stream1, stream2)  // compose streams",
    ],
    "Events": [
      "const EventEmitter = require('events'); const emitter = new EventEmitter()  // create event emitter",
      "emitter.on('event', handler)  // register event listener",
      "emitter.emit('event', arg1, arg2)  // emit event with args",
      "emitter.once('event', handler)  // listen for event once",
      "emitter.off('event', handler)  // remove listener",
      "emitter.removeAllListeners('event')  // remove all listeners for event",
      "emitter.removeListener('event', handler)  // alias for off",
      "emitter.listeners('event')  // get array of listeners",
      "emitter.rawListeners('event')  // get listeners (including wrappers)",
      "emitter.listenerCount('event')  // number of listeners",
      "emitter.eventNames()  // get event names with listeners",
      "emitter.setMaxListeners(20)  // increase max listeners (default 10)",
      "emitter.getMaxListeners()  // get max listeners limit",
      "emitter.prependListener('event', handler)  // add listener to front of array",
      "emitter.prependOnceListener('event', handler)  // add once-listener to front",
      "EventEmitter.defaultMaxListeners  // global default max (can set)",
      "events.once(emitter, 'event')  // promise-based once",
      "const { EventEmitterAsyncResource } = require('events')  // async resource tracking",
      "emitter.on('error', handler)  // always handle error events",
    ],
    "os": [
      "os.hostname()  // hostname string",
      "os.platform()  // 'linux', 'darwin', 'win32'",
      "os.release()  // OS release version string",
      "os.type()  // 'Linux', 'Darwin', 'Windows_NT'",
      "os.arch()  // CPU architecture 'x64', 'arm64'",
      "os.uptime()  // system uptime in seconds",
      "os.loadavg()  // [1min, 5min, 15min] load averages",
      "os.totalmem()  // total system memory in bytes",
      "os.freemem()  // free system memory in bytes",
      "os.cpus()  // array of CPU info objects (model, speed, times)",
      "os.networkInterfaces()  // network interface info",
      "os.userInfo()  // { username, uid, gid, shell, homedir }",
      "os.homedir()  // current user's home directory",
      "os.tmpdir()  // system temp directory",
      "os.endianness()  // 'BE' or 'LE'",
      "os.version()  // kernel version string (Node 13+)",
      "os.machine()  // machine type string (Node 18+)",
      "os.EOL  // OS end-of-line marker ('\\n' or '\\r\\n')",
      "os.constants.signals  // signal constant table",
      "os.constants.errno  // error number constant table",
    ],
    "URL": [
      "new URL('https://example.com/path?q=search#hash')  // parse URL",
      "url.href  // full URL string",
      "url.protocol  // 'https:'",
      "url.hostname  // 'example.com'",
      "url.port  // '' or '443'",
      "url.pathname  // '/path'",
      "url.search  // '?q=search'",
      "url.hash  // '#hash'",
      "url.host  // 'example.com' (hostname:port)",
      "url.origin  // 'https://example.com'",
      "url.searchParams.get('q')  // get query param",
      "url.searchParams.set('q', 'new')  // set query param",
      "url.searchParams.has('q')  // check query param",
      "url.searchParams.append('filter', 'active')  // append duplicate param",
      "url.searchParams.delete('q')  // delete query param",
      "url.searchParams.getAll('filter')  // get all values for param",
      "url.searchParams.forEach((v, k) => {})  // iterate params",
      "url.searchParams.sort()  // sort params alphabetically",
      "url.searchParams.toString()  // serialize to query string",
      "const params = new URLSearchParams('q=search&page=1')  // create from string",
      "params.get('q')  // 'search'",
      "URL.canParse('https://example.com')  // check if URL is valid (Node 19+)",
      "url.pathToFileURL('/path/to/file')  // file path to file:// URL",
      "url.fileURLToPath('file:///path')  // file:// URL to file path",
      "url.domainToASCII('über.example.com')  // domain to ASCII (punycode)",
      "url.domainToUnicode('xn--ber-0ra.example.com')  // ASCII domain to Unicode",
    ],
    "http / https": [
      "http.createServer((req, res) => { res.end('Hello') }).listen(3000)  // create HTTP server",
      "https.createServer(options, (req, res) => { res.end('Secure') }).listen(443)  // HTTPS server",
      "http.get('http://example.com', res => { res.on('data', chunk => {}) })  // HTTP GET request",
      "const data = JSON.stringify({ name: 'John' }); const req = http.request('http://example.com/api', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(data) } }, res => {}); req.write(data); req.end()  // HTTP POST request",
      "req.method  // HTTP method (GET, POST, etc.)",
      "req.url  // request URL string",
      "req.headers  // request headers object",
      "req.on('data', chunk => { buffer += chunk })  // read request body chunks",
      "req.on('end', () => { console.log(buffer) })  // body fully received",
      "req.socket.remoteAddress  // client IP address",
      "res.writeHead(200, { 'Content-Type': 'application/json' })  // write status + headers",
      "res.setHeader('Content-Type', 'application/json')  // set header",
      "res.getHeader('Content-Type')  // get header value",
      "res.removeHeader('Content-Type')  // remove header",
      "res.statusCode = 404  // set status code",
      "res.write(JSON.stringify({ ok: true }))  // write response body",
      "res.end()  // end response (no body)",
      "res.end('hello')  // end with body string/Buffer",
      "res.json({ ok: true })  // JSON response helper (not built-in, from express)",
      "http.Server({ keepAliveTimeout: 5000, headersTimeout: 60000 })  // server options",
      "server.listen(3000, () => console.log('listening'))  // start listening",
      "server.close(() => console.log('closed'))  // stop server gracefully",
      "server.closeAllConnections()  // immediately close all connections",
      "http.Agent  // HTTP connection pooling agent",
      "http.globalAgent  // default global HTTP agent",
      "https.Agent({ keepAlive: true })  // HTTPS agent with keep-alive",
      "http.METHODS  // array of supported HTTP methods",
      "http.STATUS_CODES  // map of status codes to descriptions",
      "http.validateHeaderName('X-Custom')  // validate header name",
      "http.validateHeaderValue('x-custom', 'value')  // validate header value",
    ],
    "child_process": [
      "const { spawn } = require('child_process'); const ls = spawn('ls', ['-la']); ls.stdout.on('data', d => console.log(d.toString()))  // spawn process with streaming output",
      "const { exec } = require('child_process'); exec('ls -la', (err, stdout, stderr) => { console.log(stdout) })  // exec: run command in shell",
      "const { execSync } = require('child_process'); const out = execSync('ls -la', { encoding: 'utf8' })  // sync exec",
      "const { spawnSync } = require('child_process'); const result = spawnSync('ls', ['-la']); console.log(result.stdout.toString())  // sync spawn",
      "const { fork } = require('child_process'); const child = fork('./worker.js')  // fork: spawn Node.js process",
      "child.on('message', msg => {})  // receive IPC message from child",
      "child.send({ cmd: 'work', data: [1, 2, 3] })  // send message to child",
      "child.kill('SIGTERM')  // send signal to child process",
      "child.pid  // child process PID",
      "child.exitCode  // exit code of child",
      "{ stdio: ['pipe', 'pipe', 'pipe'] }  // pipe stdin/stdout/stderr",
      "{ stdio: 'inherit' }  // inherit parent's stdio",
      "{ cwd: '/tmp' }  // set child working directory",
      "{ env: { ...process.env, NODE_ENV: 'production' } }  // set child environment",
      "{ shell: true }  // run command through shell",
      "{ timeout: 5000 }  // kill child if timeout exceeded",
      "{ maxBuffer: 1024 * 1024 }  // max stdout/stderr buffer size",
    ],
    "Cluster": [
      "const cluster = require('cluster'); if (cluster.isPrimary) { cluster.fork(); cluster.on('exit', (worker) => { cluster.fork() }) }  // primary cluster process",
      "cluster.fork()  // fork new worker process",
      "cluster.isWorker  // true if running in worker",
      "cluster.worker  // current worker object",
      "cluster.workers  // object of all worker objects",
      "cluster.isPrimary  // true if primary process (Node 16+), formerly isMaster",
      "cluster.schedulingPolicy  // SCHED_RR or SCHED_NONE",
      "cluster.SCHED_RR  // round-robin scheduling (default on Linux)",
      "cluster.SCHED_NONE  // OS scheduling",
      "worker.send(msg)  // send message to worker",
      "worker.kill()  // kill worker",
      "worker.process.pid  // worker PID",
      "cluster.on('online', worker => {})  // worker came online",
      "cluster.on('exit', (worker, code, signal) => {})  // worker exited",
      "cluster.on('message', (worker, msg) => {})  // message from worker",
    ],
    "Crypto": [
      "crypto.createHash('sha256').update('data').digest('hex')  // hash data with SHA-256",
      "crypto.createHash('md5').update('data').digest('hex')  // MD5 hash (insecure, use sha256)",
      "crypto.createHmac('sha256', 'secret').update('data').digest('hex')  // HMAC with secret key",
      "crypto.randomBytes(16).toString('hex')  // generate random bytes",
      "crypto.randomInt(1, 100)  // random integer in range [1, 100)",
      "crypto.randomUUID()  // generate a random UUID v4",
      "const cipher = crypto.createCipheriv('aes-256-gcm', key, iv); let encrypted = cipher.update('data', 'utf8', 'hex'); encrypted += cipher.final('hex')  // AES-256-GCM encryption",
      "const decipher = crypto.createDecipheriv('aes-256-gcm', key, iv); let decrypted = decipher.update(encrypted, 'hex', 'utf8'); decrypted += decipher.final('utf8')  // AES-256-GCM decryption",
      "const { publicKey, privateKey } = crypto.generateKeyPairSync('rsa', { modulusLength: 2048 })  // generate RSA key pair",
      "crypto.createSign('SHA256').update('data').sign(privateKey, 'hex')  // sign data with private key",
      "crypto.createVerify('SHA256').update('data').verify(publicKey, signature, 'hex')  // verify signature",
      "crypto.pbkdf2Sync('password', 'salt', 100000, 64, 'sha512').toString('hex')  // PBKDF2 password hashing",
      "crypto.scryptSync('password', 'salt', 64)  // scrypt password hashing (memory-hard)",
      "crypto.timingSafeEqual(Buffer.from('a'), Buffer.from('a'))  // constant-time comparison (prevents timing attacks)",
      "crypto.createSecretKey(keyBuffer)  // create KeyObject from buffer",
      "crypto.createPublicKey(keyPemString)  // create public KeyObject from PEM",
      "crypto.createPrivateKey(keyPemString)  // create private KeyObject from PEM",
      "crypto.getCiphers()  // list supported ciphers",
      "crypto.getHashes()  // list supported hash algorithms",
      "crypto.getCurves()  // list supported elliptic curves",
      "crypto.generateKey('aes', { length: 256 })  // generate key (async)",
      "crypto.subtle.digest('SHA-256', data)  // Web Crypto API",
      "crypto.webcrypto  // Web Crypto API instance",
    ],
    "Zlib": [
      "zlib.gzipSync('data')  // compress with gzip (sync)",
      "zlib.gunzipSync(compressed)  // decompress gzip (sync)",
      "zlib.deflateSync('data')  // compress with deflate (sync)",
      "zlib.inflateSync(compressed)  // decompress deflate (sync)",
      "zlib.brotliCompressSync('data')  // compress with brotli (sync)",
      "zlib.brotliDecompressSync(compressed)  // decompress brotli (sync)",
      "zlib.deflateRawSync('data')  // compress raw deflate (no header)",
      "zlib.inflateRawSync(compressed)  // decompress raw deflate",
      "zlib.unzipSync(compressed)  // auto-detect and decompress",
      "zlib.createGzip()  // gzip compression stream",
      "zlib.createGunzip()  // gzip decompression stream",
      "zlib.createBrotliCompress()  // brotli compression stream",
      "zlib.createBrotliDecompress()  // brotli decompression stream",
      "zlib.createDeflate()  // deflate compression stream",
      "zlib.createInflate()  // deflate decompression stream",
      "pipeline(readable, zlib.createGzip(), writable)  // compress stream and pipe to file",
      "zlib.constants.BROTLI_PARAM_QUALITY  // brotli compression quality parameter",
      "zlib.constants.Z_OK  // operation success constant",
    ],
  },

  express: {
    "Application": [
      "const app = express()  // create Express application",
      "app.get('/api', (req, res) => res.json({ ok: true }))  // GET route handler",
      "app.post('/api', (req, res) => res.status(201).json({ id: 1 }))  // POST route",
      "app.put('/api/:id', (req, res) => res.json({ updated: req.params.id }))  // PUT route",
      "app.patch('/api/:id', (req, res) => res.json({ patched: true }))  // PATCH route",
      "app.delete('/api/:id', (req, res) => res.status(204).end())  // DELETE route (no content)",
      "app.all('/api/*', (req, res) => res.status(405).end())  // match all methods under /api",
      "app.use('/static', express.static('public'))  // serve static files",
      "app.use(express.json())  // parse JSON request bodies",
      "app.use(express.urlencoded({ extended: true }))  // parse URL-encoded bodies",
      "app.use(cors())  // enable CORS (via cors middleware)",
      "app.use(morgan('dev'))  // request logging (via morgan)",
      "app.use(compression())  // compress responses (gzip/brotli)",
      "app.use(helmet())  // security headers (via helmet)",
      "app.route('/api/users').get(fn1).post(fn2)  // chainable route",
      "app.param('id', (req, res, next, id) => { req.user = users[id]; next() })  // param middleware",
      "app.set('view engine', 'ejs')  // set template engine",
      "app.set('trust proxy', 1)  // trust first proxy (for req.ip)",
      "app.enable('case sensitive routing')  // /Foo != /foo",
      "app.enable('strict routing')  // /foo != /foo/",
      "app.disable('x-powered-by')  // remove X-Powered-By header",
      "app.listen(3000, () => console.log('running'))  // start server on port",
      "app.listen(3000, '127.0.0.1', () => {})  // start on specific host",
      "app.path()  // return the canonical path of the app",
      "app.mountpath  // mount path pattern",
      "app.on('mount', parent => {})  // fired when sub-app mounted",
      "app.locals.title = 'My App'  // app-level locals (available in all templates)",
      "req.app  // reference to Express app instance",
      "res.app  // reference to Express app instance",
    ],
    "Request": [
      "req.params.id  // route parameters (from /:id)",
      "req.query.page  // query string parameter (?page=1)",
      "req.body  // parsed request body (requires body parser)",
      "req.headers  // request headers object",
      "req.get('Content-Type')  // specific header value (case-insensitive)",
      "req.accepts('json')  // check Accept header",
      "req.accepts(['html', 'json'])  // preferred content type",
      "req.acceptsLanguages('en')  // check Accept-Language",
      "req.acceptsCharsets('utf-8')  // check Accept-Charset",
      "req.acceptsEncodings('gzip')  // check Accept-Encoding",
      "req.is('application/json')  // check Content-Type",
      "req.ip  // client IP address (respects trust proxy)",
      "req.ips  // IP addresses when behind proxy",
      "req.protocol  // 'http' or 'https' (respects trust proxy)",
      "req.secure  // true if HTTPS connection",
      "req.subdomains  // array of subdomains",
      "req.path  // request pathname (not including query string)",
      "req.hostname  // hostname from Host header",
      "req.host  // hostname:port from Host header",
      "req.fresh  // true if response is still fresh (conditional GET)",
      "req.stale  // true if response is stale",
      "req.xhr  // true if X-Requested-With: XMLHttpRequest",
      "req.cookies  // parsed cookies (requires cookie-parser)",
      "req.signedCookies  // signed cookies (requires cookie-parser)",
      "req.session  // session data (requires express-session)",
      "req.url  // request URL (including query string)",
      "req.originalUrl  // original request URL (before re-writes)",
      "req.baseUrl  // matched mount path of router",
      "req.route  // matched route (path, methods, regexp)",
    ],
    "Response": [
      "res.send('Hello World')  // send string response",
      "res.send(Buffer.from('hello'))  // send Buffer",
      "res.json({ ok: true })  // JSON response (with content-type)",
      "res.status(404).json({ error: 'Not found' })  // status + JSON",
      "res.sendStatus(204)  // send status code only with standard message",
      "res.redirect('/login')  // 302 redirect",
      "res.redirect(301, '/new-url')  // permanent redirect",
      "res.render('template', { name: 'John' })  // render template (requires view engine)",
      "res.sendFile('/path/to/file.pdf')  // send file",
      "res.download('/path/to/file.pdf', 'report.pdf')  // send file as download",
      "res.attachment('/path/to/file.pdf')  // set Content-Disposition: attachment",
      "res.type('application/json')  // set Content-Type",
      "res.set('X-Custom', 'value')  // set response header",
      "res.set({ 'X-Custom': 'value', 'X-API': '1' })  // set multiple headers",
      "res.header('X-Custom', 'value')  // alias for set",
      "res.get('Content-Type')  // get current response header",
      "res.cookie('token', 'abc123', { httpOnly: true, maxAge: 3600000 })  // set cookie",
      "res.clearCookie('token')  // clear cookie",
      "res.append('Set-Cookie', 'cookie2=val')  // append to existing header",
      "res.vary('User-Agent')  // set Vary header",
      "res.format({ 'text/plain': () => res.send('text'), 'application/json': () => res.json({}) })  // content negotiation",
      "res.links({ next: '/page=2', prev: '/page=0' })  // set Link header",
      "res.locals.user = req.user  // response-local variables (available in templates)",
      "res.end()  // end response without body",
      "res.end('ok')  // end with string body",
      "res.headersSent  // true if headers already sent",
      "res.writableEnded  // true if response finished",
      "res.writableFinished  // true if all data flushed",
    ],
    "Router": [
      "const router = express.Router()  // create isolated router",
      "router.get('/', handler)  // register route on router",
      "router.post('/', handler)  // POST route",
      "router.put('/:id', handler)  // PUT route",
      "router.patch('/:id', handler)  // PATCH route",
      "router.delete('/:id', handler)  // DELETE route",
      "router.all('/webhook', handler)  // match any HTTP method",
      "router.use(middleware)  // apply middleware to all routes in this router",
      "router.use('/admin', adminRoutes)  // mount sub-router",
      "router.param('id', (req, res, next, val) => { next() })  // param middleware",
      "router.route('/').get(getAll).post(create)  // chainable route",
      "const router = express.Router({ mergeParams: true })  // merge params from parent route",
      "const router = express.Router({ caseSensitive: true })  // case-sensitive matching",
      "const router = express.Router({ strict: true })  // strict trailing slash",
      "app.use('/api', router)  // mount router on app",
    ],
    "Middleware": [
      "express.json()  // built-in JSON body parser",
      "express.urlencoded({ extended: true })  // URL-encoded body parser",
      "express.static('public')  // serve static files from directory",
      "express.Router()  // create modular router",
      "express.query()  // auto-parse query strings (built-in)",
      "const cors = require('cors'); app.use(cors({ origin: 'https://example.com' }))  // CORS middleware",
      "const morgan = require('morgan'); app.use(morgan('combined'))  // HTTP request logger",
      "const helmet = require('helmet'); app.use(helmet())  // security headers",
      "const compression = require('compression'); app.use(compression())  // gzip/brotli compression",
      "const session = require('express-session'); app.use(session({ secret: 'key', resave: false, saveUninitialized: true, cookie: { secure: true } }))  // session middleware",
      "const cookieParser = require('cookie-parser'); app.use(cookieParser())  // cookie parsing",
      "app.use((req, res, next) => { console.log(req.method, req.url); next() })  // custom middleware",
      "app.use('/api', (req, res, next) => { if (!req.headers.authorization) return res.status(401).end(); next() })  // path-scoped middleware",
      "const passport = require('passport'); app.use(passport.initialize()); app.use(passport.session())  // authentication middleware",
      "const multer = require('multer'); const upload = multer({ dest: 'uploads/' }); app.post('/upload', upload.single('file'), handler)  // file upload middleware",
      "const rateLimit = require('express-rate-limit'); const limiter = rateLimit({ windowMs: 15*60*1000, max: 100 }); app.use(limiter)  // rate limiting",
      "const timeout = require('connect-timeout'); app.use(timeout('5s'))  // request timeout",
      "const responseTime = require('response-time'); app.use(responseTime())  // X-Response-Time header",
    ],
    "Error Handling": [
      "app.use((err, req, res, next) => { console.error(err.stack); res.status(500).send('Something broke!') })  // global error handler (4 params)",
      "app.use((err, req, res, next) => { if (err instanceof ValidationError) { return res.status(400).json({ error: err.message }) }; next(err) })  // typed error handling",
      "class HttpError extends Error { constructor(status, message) { super(message); this.status = status } }; throw new HttpError(404, 'Not found')  // custom HTTP error class",
      "app.use((req, res, next) => { res.status(404).json({ error: 'Not found' }) })  // 404 handler (must be after routes)",
      "next(err)  // pass error to error handler",
      "// npm install express-async-errors; then async route errors auto-forward  // async error handling",
      "app.use((err, req, res, next) => { res.status(err.status || 500).json({ message: err.message, stack: process.env.NODE_ENV === 'development' ? err.stack : undefined }) })  // JSON error response with stack in dev",
      "process.on('uncaughtException', err => { console.error('Uncaught:', err); process.exit(1) })  // catch uncaught exceptions",
      "process.on('unhandledRejection', (reason, promise) => { console.error('Unhandled Rejection:', reason) })  // catch unhandled rejections",
    ],
  },

  next: {
    "Routing Conventions": [
      "// app/page.js → /  // root route",
      "// app/layout.js → shared layout wrapping all children  // root layout (required, replaces _app + _document)",
      "// app/loading.js → loading UI with Suspense  // loading state component",
      "// app/error.js → error boundary ('use client')  // error UI component",
      "// app/not-found.js → 404 page  // not found page",
      "// app/template.js → re-renders on each navigation  // template (like layout but re-mounts)",
      "// app/default.js → fallback for parallel route slots  // parallel route default",
      "// app/@modal/page.js → parallel route slot named 'modal'  // parallel route with @slot",
      "// app/(marketing)/about/page.js → route group (no segment in URL)  // route group (parentheses)",
      "// app/blog/[slug]/page.js → dynamic route  // dynamic segment [param]",
      "// app/docs/[...slug]/page.js → catch-all segment  // catch-all segment",
      "// app/docs/[[...slug]]/page.js → optional catch-all  // optional catch-all",
      "// app/(.)photo/page.js → intercept photo route at same level  // intercepting route (.)",
      "// app/(..)photo/page.js → intercept one level above  // intercepting route (..)",
      "// app/api/route.js → API route handler  // API route (GET, POST, etc.)",
    ],
    "Data Fetching": [
      "async function Page() { const data = await fetch('https://api.example.com/data'); const json = await data.json(); return <div>{json.title}</div> }  // server component fetch (auto-cached)",
      "async function Page() { const data = await fetch('https://api.example.com/data', { cache: 'no-store' }); return <div>...</div> }  // fetch with no cache (dynamic)",
      "async function Page() { const data = await fetch('https://api.example.com/data', { next: { revalidate: 60 } }); return <div>...</div> }  // fetch with time-based revalidation (ISR)",
      "async function Page({ params }) { const data = await db.query(`SELECT * FROM posts WHERE id = ${params.id}`); return <div>{data.title}</div> }  // direct database query in server component",
      "export async function generateStaticParams() { const posts = await fetch('https://api.example.com/posts').then(r => r.json()); return posts.map(p => ({ id: String(p.id) })) }  // generate static params at build time",
      "export const dynamicParams = false  // return 404 for ungenerated dynamic params",
      "export const dynamic = 'force-dynamic'  // force dynamic rendering (no caching)",
      "export const dynamic = 'force-static'  // force static rendering",
      "export const revalidate = 3600  // ISR: revalidate page every hour",
      "import { revalidatePath } from 'next/cache'; revalidatePath('/blog')  // on-demand revalidation by path",
      "import { revalidateTag } from 'next/cache'; revalidateTag('posts')  // on-demand revalidation by fetch tag",
      "fetch(url, { next: { tags: ['posts'] } })  // tag a fetch for targeted revalidation",
      "import { unstable_cache } from 'next/cache'; const getCachedData = unstable_cache(async (id) => { return await db.query(id) }, ['key'], { revalidate: 3600, tags: ['posts'] })  // custom cache function",
      "import { unstable_noStore } from 'next/cache'; unstable_noStore()  // opt out from caching for the current scope",
    ],
    "Server Actions": [
      "'use server'; export async function createItem(formData) { const name = formData.get('name'); await db.insert({ name }); revalidatePath('/items') }  // server action in a file",
      "export default function Page() { async function createItem(formData) { 'use server'; await db.insert({ name: formData.get('name') }); revalidatePath('/items') }; return <form action={createItem}>...</form> }  // inline server action",
      "import { useActionState } from 'react'; const [state, formAction, pending] = useActionState(updateItem, null)  // useActionState hook",
      "'use server'; export async function updateUser(prevState, formData) { const name = formData.get('name'); await db.update({ name }); return { success: true } }  // server action with state return",
      "import { useFormStatus } from 'react-dom'; const { pending } = useFormStatus(); <button disabled={pending}>Submit</button>  // form pending status",
      "import { useOptimistic } from 'react'; const [optimisticItems, addOptimistic] = useOptimistic(items, (state, newItem) => [...state, newItem])  // optimistic updates",
    ],
    "Metadata & SEO": [
      "export const metadata = { title: 'Home', description: 'Welcome to my site' }  // static metadata export",
      "export async function generateMetadata({ params }) { const post = await getPost(params.id); return { title: post.title, description: post.excerpt } }  // dynamic metadata generation",
      "export const metadata = { title: { default: 'My Site', template: '%s | My Site' }, description: '...', keywords: ['next', 'react'] }  // metadata with title template",
      "export const metadata = { openGraph: { title: '...', description: '...', images: [{ url: '/og.png', width: 1200, height: 630 }] } }  // Open Graph meta tags",
      "export const metadata = { twitter: { card: 'summary_large_image', title: '...', description: '...', images: ['/twitter.png'] } }  // Twitter card meta",
      "export const metadata = { robots: { index: true, follow: true, nocache: true } }  // robots meta",
      "export const metadata = { icons: { icon: '/favicon.ico', apple: '/apple-icon.png' } }  // favicon and apple icon",
      "export const metadata = { manifest: '/manifest.json' }  // web app manifest",
      "export const metadata = { verification: { google: 'google-site-verification-code' } }  // site verification",
      "export const metadata = { alternates: { canonical: 'https://example.com', languages: { 'en-US': 'https://example.com/en', 'fr': 'https://example.com/fr' } } }  // canonical and alternate URLs",
      "export const metadata = { other: { 'custom-meta': 'value' } }  // custom meta tags",
    ],
    "Image & Asset Optimization": [
      "import Image from 'next/image'; <Image src=\"/hero.jpg\" width={800} height={600} alt=\"Hero\" priority />  // optimized Image with priority",
      "<Image src={require('../assets/logo.png')} alt=\"Logo\" />  // local image (automatic width/height)",
      "<Image src=\"https://cdn.example.com/photo.jpg\" width={800} height={600} alt=\"Remote\" />  // remote image",
      "// next.config.js: images: { remotePatterns: [{ protocol: 'https', hostname: 'cdn.example.com' }] }  // allow remote images",
      "<Image fill style={{ objectFit: 'cover' }} alt=\"Cover\" />  // fill parent container",
      "<Image src=\"/img.png\" alt=\"\" sizes=\"(max-width: 768px) 100vw, 50vw\" />  // responsive sizes",
      "<Image placeholder=\"blur\" blurDataURL=\"data:image/webp;base64,...\" alt=\"LQIP\" />  // blur placeholder (LQIP)",
      "import { getImageProps } from 'next/image'  // get image props for <picture> element",
      "import Script from 'next/script'; <Script src=\"/analytics.js\" strategy=\"afterInteractive\" />  // script with loading strategy",
      "<Script strategy=\"lazyOnload\" src=\"/chat-widget.js\" />  // lazy load script",
      "<Script strategy=\"beforeInteractive\" src=\"/critical.js\" />  // load before page interactive",
      "<Script id=\"structured-data\" type=\"application/ld+json\">{JSON.stringify(ldJson)}</Script>  // inline JSON-LD",
      "import { fontFamily } from 'next/font/google'; const inter = Inter({ subsets: ['latin'] })  // Google Font optimization",
      "const localFont = localFont({ src: './my-font.woff2', display: 'swap' })  // local font optimization",
    ],
    "Navigation & Routing": [
      "import Link from 'next/link'; <Link href=\"/about\">About</Link>  // client-side navigation with prefetch",
      "<Link href=\"/post/[id]\" as={`/post/${id}`}>Post</Link>  // dynamic route link (pages router)",
      "<Link href=\"/dashboard\" prefetch={false}>  // disable prefetch",
      "<Link href=\"/dashboard\" replace>  // replace current history entry",
      "<Link href=\"/dashboard\" scroll={false}>  // disable scroll to top",
      "import { useRouter } from 'next/navigation'; const router = useRouter(); router.push('/dashboard')  // programmatic navigation (app router)",
      "router.replace('/login')  // replace history entry",
      "router.back()  // go back",
      "router.refresh()  // refresh current route (re-render server components)",
      "router.prefetch('/dashboard')  // prefetch route manually",
      "import { usePathname } from 'next/navigation'  // get current pathname (app router)",
      "import { useSearchParams } from 'next/navigation'  // get search params (app router, client)",
      "import { useParams } from 'next/navigation'  // get dynamic route params",
      "import { redirect } from 'next/navigation'; redirect('/login')  // redirect in server component/action",
      "import { permanentRedirect } from 'next/navigation'; permanentRedirect('/new-url')  // 308 redirect",
      "import { notFound } from 'next/navigation'; notFound()  // trigger not-found page",
      "// middleware.ts: export function middleware(request) { return NextResponse.redirect(new URL('/login', request.url)) }  // middleware redirect",
      "// middleware.ts: export { default } from 'next-auth/middleware'; export const config = { matcher: ['/dashboard/:path*'] }  // auth middleware with matcher",
    ],
    "Route Handlers": [
      "// app/api/route.js: export async function GET(request) { return Response.json({ ok: true }) }  // GET API route",
      "// app/api/items/route.js: export async function POST(request) { const body = await request.json(); return Response.json({ id: 1, ...body }, { status: 201 }) }  // POST API route",
      "// app/api/items/[id]/route.js: export async function PUT(request, { params }) { return Response.json({ updated: params.id }) }  // PUT with params",
      "// app/api/items/[id]/route.js: export async function DELETE(request, { params }) { return new Response(null, { status: 204 }) }  // DELETE (no content)",
      "// app/api/route.js: export async function GET(request) { const { searchParams } = new URL(request.url); const page = searchParams.get('page'); return Response.json({ page }) }  // query params in API",
      "// app/api/route.js: export async function GET() { const data = await db.query(); return Response.json(data) }  // API route with database",
      "// app/api/route.js: export async function GET() { return NextResponse.json({ ok: true }) }  // NextResponse helper",
      "// app/api/redirect/route.js: export async function GET() { return NextResponse.redirect(new URL('/login', request.url)) }  // redirect from API route",
      "// app/api/revalidate/route.js: export async function POST(request) { revalidatePath('/blog'); return Response.json({ revalidated: true }) }  // on-demand revalidation webhook",
      "// app/api/cookies/route.js: export async function GET() { const cookieStore = await cookies(); cookieStore.set('theme', 'dark'); return Response.json({ cookies: cookieStore.getAll() }) }  // cookie handling",
    ],
    "Middleware": [
      "// middleware.ts: export function middleware(request: NextRequest) { const token = request.cookies.get('token')?.value; if (!token) return NextResponse.redirect(new URL('/login', request.url)); return NextResponse.next() }  // auth middleware",
      "// middleware.ts: export const config = { matcher: ['/dashboard/:path*', '/admin/:path*'] }  // middleware path matcher",
      "// middleware.ts: export const config = { matcher: ['/((?!api|_next|static|favicon.ico).*)'] }  // exclude static files from middleware",
      "request.nextUrl  // NextURL object with search params, pathname, etc.",
      "request.cookies.get('token')  // read cookie",
      "request.cookies.set('token', 'value')  // set cookie",
      "request.cookies.delete('token')  // delete cookie",
      "request.geo.city / request.geo.country  // geo location (Vercel)",
      "request.ip  // request IP address",
      "request.headers.get('user-agent')  // read request header",
      "NextResponse.next()  // continue to next handler",
      "NextResponse.redirect(url)  // redirect response",
      "NextResponse.rewrite(url)  // rewrite to different URL (without redirect)",
      "NextResponse.json({ ok: true })  // JSON response from middleware",
      "import { NextRequest, NextResponse } from 'next/server'  // middleware types",
    ],
    "Configuration": [
      "// next.config.js: /** @type {import('next').NextConfig} */ const nextConfig = {}; module.exports = nextConfig  // base config",
      "output: 'standalone'  // self-contained deployment (Docker-friendly)",
      "output: 'export'  // static HTML export (no server needed)",
      "basePath: '/docs'  // serve app under sub-path",
      "assetPrefix: 'https://cdn.example.com'  // serve assets from CDN",
      "trailingSlash: true  // add trailing slash to all routes",
      "poweredByHeader: false  // remove X-Powered-By header",
      "distDir: 'build'  // custom build output directory (default '.next')",
      "compress: true  // enable gzip compression",
      "generateEtags: true  // generate ETag headers",
      "reactStrictMode: true  // enable React strict mode",
      "swcMinify: true  // use SWC for minification",
      "transpilePackages: ['library-name']  // transpile specific npm packages",
      "modularizeImports: { lodash: { transform: 'lodash/{{member}}' } }  // modularize imports for tree-shaking",
      "optimizePackageImports: ['lucide-react', '@mui/material']  // optimize package imports",
      "serverExternalPackages: ['bcrypt']  // exclude packages from bundle (server-only)",
      "experimental: { serverActions: true, typedRoutes: true }  // experimental features",
      "images: { formats: ['image/webp'], deviceSizes: [640, 750, 828, 1080, 1200, 1920] }  // image optimization config",
      "env: { CUSTOM_KEY: 'value' }  // build-time env vars (inlined)",
      "publicRuntimeConfig: { apiUrl: process.env.API_URL }  // runtime config available on both client and server (pages router)",
      "serverRuntimeConfig: { secret: process.env.SECRET }  // server-only runtime config (pages router)",
      "rewrites: async () => [{ source: '/api/:path*', destination: 'https://api.example.com/:path*' }]  // proxy rewrites",
      "redirects: async () => [{ source: '/old', destination: '/new', permanent: true }]  // URL redirects",
      "headers: async () => [{ source: '/(.*)', headers: [{ key: 'X-Frame-Options', value: 'DENY' }] }]  // custom headers",
      "webpack: (config, { isServer }) => { config.plugins.push(new Plugin()); return config }  // custom webpack config",
      "devIndicators: { buildActivity: true, buildActivityPosition: 'bottom-right' }  // dev indicators config",
    ],
  },
};

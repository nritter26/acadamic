"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const AI_TUTOR_RESPONSES = [
    { keywords: ['variable', 'declare', 'let', 'const', 'var'], response: "Variables store data in memory so you can reuse and manipulate values.\n\n**Syntax by language:**\n- **JS:** `let name = value;` (mutable), `const name = value;` (immutable)\n- **Python:** `name = value` (no keyword needed)\n- **Go:** `var name type = value` or `name := value` (type inference)\n- **Kotlin:** `var name = value` (mutable), `val name = value` (read-only)\n\n**Try this to experiment:**\n1. Declare a variable with your name\n2. Declare another with your age\n3. Print both using console.log / print()\n\n**Why this matters:** Variables are the foundation of all programs — every app stores and manipulates data.\n\n**Try to avoid:**\n- Using `var` in JS (function-scoped, causes bugs) — prefer `let`/`const`\n- Forgetting keywords creates globals: `x = 5` without `let`/`const`/`var` creates a global\n- Using `const` for values that need to change later" },
    { keywords: ['function', 'method', 'def', 'func', '=>'], response: "Functions are reusable blocks of code that perform a specific task. They help you avoid repetition and organize logic.\n\n**Syntax by language:**\n- **JS:** `function name(params) { ... }` or `const name = (params) => { ... }`\n- **Python:** `def name(params):`\n- **Go:** `func name(params) returnType { ... }`\n- **Rust:** `fn name(params) -> returnType { ... }`\n\n**Hands-on exercise:**\n```\nWrite a function that takes two numbers and returns their sum.\nThen call it and log the result.\n```\n\n**Design principle:** Each function should do ONE thing well. If a function does multiple things, split it up.\n\n**Common pitfalls:**\n- Missing `return` → function returns `undefined` (JS) / `None` (Python)\n- Calling without `()`: `myFunc` vs `myFunc()` — first one is the function itself, not a call\n- Mutating input parameters directly creates side effects" },
    { keywords: ['class', 'object', 'oop', 'inherit', 'extends', 'prototype', 'struct'], response: "Object-Oriented Programming organizes code around objects that contain both data (properties) and behavior (methods).\n\n**Key concepts:**\n- **Encapsulation:** bundle data + methods, hide internal details\n- **Inheritance:** a class can extend another, reusing behavior\n- **Polymorphism:** same method name, different implementations\n- **Composition:** building objects from other objects (prefer this over inheritance)\n\n**Language differences:**\n- **JS/Python/Kotlin/C#:** traditional `class` syntax\n- **Go:** no classes — uses structs + methods (composition-focused)\n- **Rust/Zig:** structs with traits/protocols for shared behavior\n\n**Try this:**\n1. Create a simple class (e.g., `Car` with `brand` and `year`)\n2. Add a method (e.g., `honk()`)\n3. Create an instance and call the method\n\n**Golden rule:** Favor composition over inheritance. Instead of `Dog extends Animal`, give Dog an `energy` property and a `tired()` method." },
    { keywords: ['array', 'list', 'collection', 'vector', 'slice', 'map', 'set', 'dictionary', 'hash'], response: "Collections let you store and manipulate groups of values.\n\n**Common collection types:**\n- **Array/Slice/List:** ordered sequence of values\n- **Map/Dict/Hash:** key-value pairs for fast lookups\n- **Set:** unique values (no duplicates)\n\n**Language specifics:**\n- **JS:** `[]`, `new Map()`, `new Set()`\n- **Python:** `[]`, `list()`, `{}`, `dict()`, `set()`\n- **Go:** `[]T`, `map[K]V`\n- **Rust:** `Vec<T>`, `HashMap<K, V>`, `HashSet<T>`\n\n**Exercise:**\n1. Create an array of 5 numbers\n2. Write a loop to double each number\n3. Store the results in a new array\n\n**Watch out for:**\n- Off-by-one: `arr[arr.length]` is always out of bounds — indices go 0 to length-1\n- Using `delete arr[i]` in JS leaves a hole — use `.splice()` instead\n- Arrays are 0-indexed in virtually all languages" },
    { keywords: ['loop', 'for', 'while', 'iterate', 'foreach', 'for...of', 'for...in', 'range'], response: "Loops let you repeat code — essential for processing collections, waiting for conditions, and automating repetitive tasks.\n\n**Types of loops:**\n- **`for` loop:** when you know how many iterations (`for (let i=0; i<5; i++)`)\n- **`while` loop:** when you don't know the count (`while (condition)`)\n- **`for...of` / `foreach`:** iterating over collections (cleaner)\n- **`map` / `filter` / `reduce`:** functional iteration (JS/Python)\n\n**Control flow:**\n- `break` — exit the loop immediately\n- `continue` — skip to the next iteration\n\n**Try this:**\n```\nWrite a loop that prints numbers 1 to 10, but skips 5 and stops at 8.\n```\n\n**Most common bugs:**\n- **Infinite loop:** forgetting to increment your counter: `for (let i=0; i<10;)`\n- **Off-by-one:** using `<=` when you mean `<` (or vice versa)\n- **Modifying an array while iterating:** skips elements or causes unexpected behavior" },
    { keywords: ['error', 'exception', 'try', 'catch', 'panic', 'throw', 'result', 'option'], response: "Error handling is how programs deal with unexpected situations. Different languages take very different approaches!\n\n**Approaches by language:**\n- **JS/Python/C#/Kotlin:** `try { risky() } catch (e) { handle(e) }`\n- **Go:** functions return errors: `result, err := doSomething()` — check `err != nil`\n- **Rust:** `Result<T, E>` and `Option<T>` — pattern match or use `?`\n- **Zig:** error union types — `catch` handles, `try` propagates\n\n**Best practices:**\n- Catch specific error types, not generic `Exception` — you might hide bugs\n- Always clean up resources (files, connections) in `finally`\n- Log errors WITH context (what were you doing, what inputs)\n- Don't silently swallow errors — at minimum log them\n\n**Exercise:**\n1. Write a function that divides two numbers\n2. Add error handling for division by zero\n3. Test both valid and invalid inputs" },
    { keywords: ['async', 'await', 'promise', 'future', 'coroutine', 'callback', 'goroutine', 'thread'], response: "Async programming lets your code handle time-consuming operations (network requests, file I/O, timers) without blocking.\n\n**How each language handles it:**\n- **JS:** `async function` + `await promise` — single-threaded, event loop\n- **Python:** `async def` + `await` — asyncio event loop\n- **C#:** `async Task` + `await` — built into the runtime\n- **Go:** `go func()` starts a goroutine, `chan` for communication\n- **Kotlin:** `suspend fun` + coroutines\n\n**Mental model:** Think of async code like ordering coffee: instead of waiting at the counter (blocking), you get a buzzer (promise) and do other things until it buzzes (resolved).\n\n**Common mistakes:**\n- Forgetting `await` — you get a Promise object instead of the value\n- Not handling rejections — unhandled promise rejections crash Node.js\n- Callback hell — use Promises (.then chain) or async/await\n- Passing async function where sync is expected without handling the promise" },
    { keywords: ['type', 'string', 'int', 'bool', 'float', 'null', 'undefined', 'void', 'any', 'generic'], response: "Types describe what kind of data a value is — this determines what you can do with it.\n\n**Static vs Dynamic typing:**\n- **Static (TS, Go, Rust, C#, Java):** types checked at compile time, catch errors early\n- **Dynamic (JS, Python):** types checked at runtime, more flexible but error-prone\n\n**Type annotations are your friends!** Even in dynamic languages, using clear type names helps readability.\n\n**Language oddities to know:**\n- **JS:** `typeof null === 'object'` — it's a longstanding bug!\n- **JS:** `'5' + 3 = '53'` (string concat wins), but `'5' - 3 = 2` (coercion)\n- **Python:** everything is an object, even functions and classes\n- **Go:** zero values — `int` defaults to `0`, `string` to `\"\"`, `bool` to `false`\n\n**Try this:**\n1. Declare a variable with a specific type annotation (if your language supports it)\n2. Try assigning a different type — see what error you get\n3. Experiment with type conversion/coercion" },
    { keywords: ['git', 'commit', 'push', 'pull', 'branch', 'merge', 'rebase'], response: "Git is the industry-standard version control system. It tracks changes to your code over time.\n\n**Essential workflow:**\n1. `git add .` — stage your changes\n2. `git commit -m \"message\"` — save a snapshot\n3. `git push` — upload to remote\n\n**Branching strategy:**\n- `main` — production-ready code\n- `feature/xyz` — work on new features\n- `bugfix/xyz` — fix bugs\n- Never commit directly to main! Use pull requests.\n\n**For learning, try this:**\n1. `git init` in a project folder\n2. Make some changes, commit them\n3. Create a branch: `git checkout -b my-experiment`\n4. Make more commits, then merge back\n\n**Pro tips to avoid disaster:**\n- Use `--force-with-lease` instead of `--force` on shared branches\n- Always pull before pushing: `git pull --rebase`\n- Commit early and often with clear messages\n- Use `.gitignore` to keep secrets and build artifacts out of the repo" },
    { keywords: ['sql', 'select', 'join', 'table', 'database', 'query', 'where', 'insert', 'update', 'delete', 'index'], response: "SQL is the language of relational databases. It's declarative — you say WHAT you want, not HOW to get it.\n\n**Core operations (CRUD):**\n- `SELECT columns FROM table WHERE condition` — retrieve data\n- `INSERT INTO table (cols) VALUES (vals)` — add data\n- `UPDATE table SET col=val WHERE condition` — modify data\n- `DELETE FROM table WHERE condition` — remove data\n\n**JOINs combine tables:**\n- `INNER JOIN` — only matching rows from both tables\n- `LEFT JOIN` — all rows from left table, NULLs where right doesn't match\n- `RIGHT JOIN` — opposite of LEFT\n- `FULL JOIN` — all rows from both tables\n\n**Most common errors:**\n- **Missing WHERE in UPDATE/DELETE** — modifies/deletes ALL rows!\n- **N+1 query problem:** looping queries instead of using JOIN\n- **Not using parameterized queries** — leads to SQL injection\n- **Missing indexes** on frequently queried columns = slow queries\n\n**Try the Schema Designer (click 'Schema' below the editor)** to build tables visually!" },
    { keywords: ['debug', 'bug', 'fix', 'issue', 'wrong', 'not working', 'broken', 'error'], response: "Debugging is a systematic process. Here's a methodical approach:\n\n**1. READ the error message**\nIt tells you WHAT went wrong and WHERE (line number). Don't skip this step!\n\n**2. REPRODUCE**\nCan you make the bug happen consistently? If not, find the exact conditions.\n\n**3. ISOLATE**\nComment out code until the bug disappears. The last thing you removed is likely the culprit.\n\n**4. INSPECT**\nUse `console.log()` (JS), `print()` (Python), `fmt.Println()` (Go), or a debugger to check values at each step.\n\n**5. HYPOTHESIZE**\nForm a theory: \"If X is wrong, then Y should happen.\" Test it.\n\n**6. FIX**\nMake the smallest possible change. Don't rewrite everything.\n\n**7. VERIFY**\nDoes the fix actually solve it? Does it break anything else?\n\n**Remember:** Every bug is a learning opportunity! The error message is trying to help you." },
    { keywords: ['help', 'how', 'what is', 'explain', 'understand', 'confused', 'beginner', 'start', 'learn'], response: "I'm here to help you learn! Here's my advice for effective learning:\n\n**The 4-step practice method:**\n1. **Read** the topic explanation in the curriculum\n2. **Type** the code example yourself (don't copy-paste — muscle memory matters!)\n3. **Modify** it — change values, add features, break it intentionally\n4. **Build** something small with the concept\n\n**Active recall technique:** After reading a topic, close it and try to explain it in your own words. If you can't, review and try again.\n\n**I can help with:**\n- Explaining a specific topic (ask \"Explain [topic]\")\n- Debugging your code (share what you've tried)\n- Showing examples (\"Show me an example of X\")\n- Best practices (\"What's the best way to do X?\")\n\n**What are you working on right now?** Tell me the topic and I'll give you a clear explanation." },
    { keywords: ['pointer', 'reference', 'memory', 'malloc', 'free', 'heap', 'stack', 'alloc', 'borrow', 'ownership'], response: "Memory management is essential in systems languages (C, C++, Rust, Zig). Here's the conceptual foundation:\n\n**Stack vs Heap:**\n- **Stack:** Fast, small, automatic. Local variables go here. LIFO order (last in, first out).\n- **Heap:** Slower, flexible, manual. Dynamic allocations go here. You must free/delete.\n\n**Key concepts by language:**\n- **C:** `malloc()`/`free()` — completely manual, error-prone\n- **C++:** `new`/`delete`, smart pointers (`unique_ptr`, `shared_ptr`)\n- **Rust:** Ownership system — compiler enforces memory safety at compile time. NO garbage collector!\n- **Zig:** Manual but safe — explicit allocators, no hidden allocations\n\n**Classic memory bugs:**\n- **Memory leak:** forgetting to free → program uses more and more RAM\n- **Dangling pointer:** using memory after freeing → crashes or security holes\n- **Buffer overflow:** writing past array bounds → corrupts adjacent memory\n- **Double free:** freeing the same memory twice → crash\n\n**Rust's ownership rules (simplified):**\n1. Each value has ONE owner\n2. When the owner goes out of scope, the value is dropped\n3. You can either borrow (&) or move, but not both in certain ways" },
    { keywords: ['closure', 'scope', 'hoist', 'temporal dead zone', 'tdz', 'lexical'], response: "Scope determines WHERE variables are accessible in your code. Closures are a powerful consequence of how scope works.\n\n**Types of scope:**\n- **Global:** accessible everywhere (avoid polluting this)\n- **Function scope:** inside a function (`var` in JS, `function` declarations)\n- **Block scope:** inside `{}` (`let`, `const` in JS)\n\n**What's a closure?**\nA closure is a function that \"remembers\" the variables from where it was defined, even after that outer function has finished running.\n\n```js\nfunction makeCounter() {\n  let count = 0;\n  return function() { return ++count; };\n}\nconst counter = makeCounter();\ncounter(); // 1\ncounter(); // 2  <-- count is still accessible!\n```\n\n**Common closure bug (the loop problem):**\n```js\nfor (var i = 0; i < 5; i++) {\n  setTimeout(() => console.log(i), 100); // prints 5,5,5,5,5\n}\n```\n**Fix:** use `let` instead of `var` (creates a new binding per iteration).\n\n**Hoisting:** `var` declarations are hoisted (moved to top) and initialized as `undefined`. `let`/`const` are hoisted but NOT initialized (Temporal Dead Zone — accessing them before the declaration throws an error)." },
    { keywords: ['syntax', 'semicolon', 'bracket', 'parenthesis', 'brace', 'colon'], response: "Syntax errors mean the computer can't understand your code — you've broken the grammar rules of the language. This is NORMAL and happens to every programmer, every day.\n\n**Quick debugging checklist:**\n1. Are all `(`, `{`, `[` properly closed with `)`, `}`, `]`?\n2. Are strings quoted correctly? `\"...\"`, `'...'`, or backticks must match\n3. Are statements terminated? (JS/C#/C++ need `;`, Python uses newlines)\n4. Are variable/function names spelled identically everywhere?\n5. Are you missing a comma between array/object items?\n6. Did you use a reserved keyword as a variable name?\n\n**The error message is your friend!** It tells you:\n- **The line number** where it got confused (or slightly after)\n- **What it expected** vs what it found\n- **The specific character** that doesn't belong\n\n**Pro tip:** When you get a syntax error, look at the LINE BEFORE the error. The parser often doesn't realize something's wrong until the next line." },
    { keywords: ['hello', 'hi', 'hey', 'good morning', 'good evening', 'greeting', 'sup'], response: "Hey there! Welcome to Kodex's Lab!\n\nI'm your AI programming tutor. Here's what I can do for you:\n- **Explain concepts** from the curriculum (just ask!)\n- **Debug your code** (tell me what's not working)\n- **Show examples** with runnable code\n- **Guide your learning** with exercises and challenges\n\n**To get started:**\n1. Pick a language from the top bar\n2. Click a topic on the left\n3. Read the explanation and try the code\n4. Modify the code and click \"Run\"\n5. Ask me anything if you get stuck!\n\n**What language are you learning today?**" },
    { keywords: ['string', 'concatenat', 'interpolat', 'template', 'char', 'substring', 'slice', 'split', 'trim'], response: "Strings are sequences of characters. They're one of the most common data types in any program.\n\n**Common string operations:**\n- **Length:** `str.length` (JS), `len(str)` (Python), `strlen(s)` (C)\n- **Substring:** `str.slice(0, 5)`, `str.substring(0, 5)` — first 5 chars\n- **Split:** `str.split(',')` → array of strings\n- **Join:** `arr.join(',')` → string\n- **Case:** `str.toUpperCase()`, `str.toLowerCase()`\n- **Trim whitespace:** `str.trim()`\n\n**String interpolation (build strings with variables):**\n- **JS:** `` `Hello, ${name}!` `` (template literals)\n- **Python:** `f\"Hello, {name}!\"` (f-strings)\n- **C#:** `$\"Hello, {name}!\"` (string interpolation)\n- **Go:** `fmt.Sprintf(\"Hello, %s!\", name)`\n\n**Important:** Strings are IMMUTABLE in virtually all languages. Methods like `.toUpperCase()` return a NEW string — the original stays the same.\n\n**Common gotchas:**\n- Off-by-one in substring: `\"hello\".slice(1, 3)` is `\"el\"` (end index is exclusive)\n- Using `==` vs `.equals()` in some languages for comparison\n- Forgetting to trim user input: `\"  input  \".trim()`" },
    { keywords: ['test', 'testing', 'unit test', 'assert', 'jest', 'mocha', 'pytest', 'testing library'], response: "Testing is how you verify your code works correctly. Good tests give you confidence to refactor and add features.\n\n**Testing levels:**\n- **Unit tests:** test individual functions/classes in isolation\n- **Integration tests:** test how components work together\n- **End-to-end tests:** test the full system from user perspective\n\n**The AAA pattern:**\n1. **Arrange** — set up test data and conditions\n2. **Act** — call the function/method you're testing\n3. **Assert** — check the result is what you expected\n\n```js\n// Example (Jest)\ntest('adds 1 + 2 to equal 3', () => {\n  expect(add(1, 2)).toBe(3);\n});\n```\n\n**Why test?**\n- Catches regressions (stuff you broke by accident)\n- Documents how your code should behave\n- Forces you to write testable (modular) code\n- Saves time in the long run\n\n**Getting started:** Write a test BEFORE you fix a bug — this is called \"red-green testing\" (test fails first, then you make it pass)." },
    { keywords: ['recursion', 'recursive', 'base case', 'stack overflow', 'tail call'], response: "Recursion is when a function calls itself. It's an elegant way to solve problems that have a repetitive structure (trees, fractals, divide-and-conquer).\n\n**Every recursive function needs two parts:**\n1. **Base case** — when to STOP (without this, infinite recursion!)\n2. **Recursive case** — call itself with a simpler version of the problem\n\n```js\n// Factorial: n! = n * (n-1) * ... * 1\nfunction factorial(n) {\n  if (n <= 1) return 1;       // base case\n  return n * factorial(n - 1); // recursive case\n}\n```\n\n**When to use recursion vs loops:**\n- **Recursion:** tree traversal, parsing, divide-and-conquer algorithms\n- **Loops:** simple iteration, performance-critical code\n\n**Watch out for:**\n- **Stack overflow:** too many recursive calls exhausts the call stack\n- **Missing base case:** infinite recursion = crash\n- **Tail recursion optimization:** some languages optimize this (not JS/Python)\n\n**Exercise:** Write a recursive function that computes the nth Fibonacci number. Then compare it with a loop-based version." },
    { keywords: ['rust', 'rustlang'], response: "**Rust** is a systems programming language focused on three pillars: **safety**, **speed**, and **concurrency**.\n\n**Key features:**\n- **No garbage collector** — memory safety is enforced at compile time via an ownership system\n- **Zero-cost abstractions** — high-level constructs compile down to efficient machine code\n- **Fearless concurrency** — the type system prevents data races at compile time\n- **Excellent tooling** — `cargo` for package management, `rustfmt` for formatting, `clippy` for linting\n\n**Who uses it?**\n- Mozilla (Servo browser engine), Dropbox, Figma, Cloudflare\n- Systems programming, WebAssembly, CLI tools, game engines\n\n**Getting started:**\n```rust\nfn main() {\n    println!(\"Hello, Rust!\");\n    let x = 42;\n    println!(\"x = {}\", x);\n}\n```\n\n**Compared to C/C++:** Rust gives you the same low-level control but with a much safer type system. The compiler catches memory errors at compile time instead of crashing at runtime.\n\nWant to try Rust? Select it from the language bar at the top and start with the first topic!" },
    { keywords: ['go language', 'golang'], response: "**Go** (or Golang) is a statically typed, compiled programming language designed at Google for building scalable, concurrent systems.\n\n**Key features:**\n- **Simple syntax** — easy to read and learn, similar to C but cleaner\n- **Built-in concurrency** — goroutines and channels make concurrent programming straightforward\n- **Fast compilation** — compiles to a single binary in seconds\n- **Standard library** — includes HTTP server, JSON, testing, and more out of the box\n\n**Who uses it?**\n- Google, Uber, Dropbox, Docker, Kubernetes are written in Go\n- Perfect for REST APIs, microservices, CLI tools, network servers\n\n**Getting started:**\n```go\npackage main\nimport \"fmt\"\n\nfunc main() {\n    fmt.Println(\"Hello, Go!\")\n}\n```\n\n**Compared to Python:** Go is much faster (compiled), catches errors at compile time, and has excellent built-in concurrency. Python is more flexible for quick scripting. Both are great for different use cases!\n\nWant to try Go? Select it from the language bar at the top and start with the first topic!" },
    { keywords: ['python language'], response: "**Python** is a high-level, interpreted programming language known for its readability and versatility. It's one of the best languages for beginners.\n\n**Key features:**\n- **Readable syntax** — uses indentation instead of braces, reads like plain English\n- **Dynamically typed** — no type declarations needed, great for rapid prototyping\n- **Massive ecosystem** — PyPI has over 400,000 packages for everything from web dev to AI\n- **Batteries included** — extensive standard library covers file I/O, networking, data processing\n\n**Who uses it?**\n- Data scientists, ML engineers, web developers (Django/Flask), DevOps\n- Google, Instagram, Spotify, Netflix — all use Python extensively\n\n**Getting started:**\n```python\nprint(\"Hello, Python!\")\nname = input(\"What's your name? \")\nprint(f\"Nice to meet you, {name}!\")\n```\n\n**Compared to JavaScript:** Python is more readable and has better data science libraries. JS is faster in the browser and has a larger web ecosystem. Both are excellent for beginners!\n\nWant to try Python? Select it from the language bar at the top!" },
    { keywords: ['javascript language'], response: "**JavaScript** is the language of the web — it runs in every browser and on servers via Node.js. It's one of the most widely-used programming languages.\n\n**Key features:**\n- **Universal** — runs in every web browser without any installation\n- **Event-driven** — naturally suited for interactive UIs and real-time apps\n- **Flexible** — supports procedural, object-oriented, and functional programming styles\n- **Huge ecosystem** — npm is the largest package registry in the world\n\n**Who uses it?**\n- Every web developer (frontend AND backend with Node.js)\n- React, Vue, Angular, Svelte — all major frontend frameworks use JS/TypeScript\n\n**Getting started:**\n```js\nconsole.log(\"Hello, JavaScript!\");\nconst name = \"World\";\nconsole.log(`Hello, ${name}!`);\n```\n\n**Why learn JavaScript?** It's the only language that runs natively in web browsers. Once you know JS, you can build websites, mobile apps (React Native), desktop apps (Electron), and server apps (Node.js) — all with one language!\n\nYou're already in JavaScript mode! Just pick a topic on the left to get started." },
    { keywords: ['api', 'rest', 'http', 'fetch', 'endpoint', 'request', 'response', 'axios'], response: "APIs (Application Programming Interfaces) let different software systems communicate. **REST APIs** are the most common type on the web.\n\n**Common HTTP methods:**\n- **GET** — retrieve data (read)\n- **POST** — create new data\n- **PUT / PATCH** — update existing data\n- **DELETE** — remove data\n\n**Working with fetch (JS):**\n```js\nasync function getData() {\n  const res = await fetch('https://api.example.com/users');\n  const data = await res.json();\n  console.log(data);\n}\n```\n\n**Status codes to know:**\n- `200 OK` — success\n- `201 Created` — resource created\n- `400 Bad Request` — client error\n- `401 Unauthorized` — need auth\n- `404 Not Found` — endpoint doesn't exist\n- `500 Server Error` — server crashed\n\n**Tips:** Always handle errors with try/catch around fetch calls. Use the built-in API Client tab (click \"API\" next to the editor) to test endpoints interactively!" },
    { keywords: ['dom', 'document', 'window', 'event', 'click', 'listener', 'handler', 'queryselector', 'addeventlistener'], response: "The **DOM (Document Object Model)** is JavaScript's interface to HTML. It lets you read, modify, and respond to the page.\n\n**Essential DOM operations:**\n```js\n// Select elements\nconst btn = document.querySelector('#myButton');\nconst allItems = document.querySelectorAll('.item');\n\n// Modify content\nbtn.textContent = 'Click Me!';\nbtn.style.backgroundColor = 'blue';\n\n// Add event listeners\nbtn.addEventListener('click', () => {\n  alert('Button clicked!');\n});\n```\n\n**Event phases:**\n1. **Capture** — event travels DOWN from `document` to target\n2. **Target** — event reaches the element\n3. **Bubble** — event travels UP from target to `document`\n\n**Common events:** `click`, `submit`, `keydown`, `keyup`, `input`, `change`, `mouseover`, `scroll`, `load`\n\n**Best practice:** Use event delegation — attach ONE listener to a parent instead of many on children:\n```js\ndocument.querySelector('ul').addEventListener('click', (e) => {\n  if (e.target.tagName === 'LI') console.log('Item clicked:', e.target.textContent);\n});\n```" },
    { keywords: ['module', 'import', 'export', 'require', 'package', 'npm', 'dependency', 'node_modules'], response: "Modules let you split code into separate files, each with a clear responsibility. This keeps projects organized and reusable.\n\n**ES Modules (JS/TS):**\n```js\n// math.js\nexport function add(a, b) { return a + b; }\nexport const PI = 3.14;\n\n// app.js\nimport { add, PI } from './math.js';\n```\n\n**CommonJS (Node.js):**\n```js\n// math.js\nmodule.exports = { add: (a, b) => a + b, PI: 3.14 };\n\n// app.js\nconst { add, PI } = require('./math');\n```\n\n**NPM basics:**\n- `npm init` — create a package.json\n- `npm install <package>` — add a dependency\n- `npm install -D <package>` — add a dev dependency (testing, build tools)\n- `npx <tool>` — run a tool without installing globally\n\n**Key concepts:**\n- `dependencies` — needed to RUN the app\n- `devDependencies` — needed to DEVELOP/test/build\n- `package-lock.json` — locks exact versions for reproducible builds\n\n**Best practice:** One module = one responsibility. Keep files small (< 200 lines)." },
    { keywords: ['regex', 'regular expression', 'pattern', 'match', 'replace', 'test', 'search'], response: "Regular expressions (regex) are patterns for matching text. They're powerful for validation, search-and-replace, and parsing.\n\n**Basic syntax:**\n- `.` — any character except newline\n- `*` — zero or more of preceding\n- `+` — one or more of preceding\n- `?` — optional (zero or one)\n- `\\d` — digit, `\\w` — word char, `\\s` — whitespace\n- `[abc]` — character set (a, b, or c)\n- `(x|y)` — alternation (x or y)\n- `^` — start of string, `$` — end of string\n\n**In JS:**\n```js\nconst emailRegex = /^[\\w.-]+@[\\w.-]+\\.\\w{2,}$/;\nemailRegex.test('user@example.com'); // true\n\n'Hello World'.replace(/World/, 'JavaScript'); // 'Hello JavaScript'\n```\n\n**Common patterns:**\n- Email: `/^[\\w.-]+@[\\w.-]+\\.\\w{2,}$/`\n- Phone: `/^\\d{3}-\\d{3}-\\d{4}$/`\n- URL: `/^https?:\\/\\/[\\w.-]+/`\n\n**Pro tip:** Use regex101.com to test and debug your patterns! Be careful — regex can be hard to read. Add comments for complex patterns." },
    { keywords: ['file', 'filesystem', 'fs', 'read', 'write', 'path', 'directory', 'stream', 'filename'], response: "File I/O lets programs read from and write to disk. Each language has its own approach.\n\n**Node.js (using fs/promises):**\n```js\nimport { readFile, writeFile } from 'fs/promises';\n\nconst data = await readFile('input.txt', 'utf-8');\nawait writeFile('output.txt', data.toUpperCase());\n```\n\n**Python:**\n```python\nwith open('input.txt') as f:\n    content = f.read()\nwith open('output.txt', 'w') as f:\n    f.write(content.upper())\n```\n\n**Path operations:**\n- `path.join('dir', 'file.txt')` — cross-platform paths\n- `path.basename('/a/b/file.txt')` → `'file.txt'`\n- `path.extname('file.txt')` → `'.txt'`\n\n**Error handling is MANDATORY for file I/O:**\n```js\ntry {\n  const data = await readFile('config.json', 'utf-8');\n} catch (err) {\n  console.error('Failed to read config:', err.message);\n}\n```\n\n**Gotchas:**\n- Always specify encoding (utf-8 for text files)\n- Close file handles or use `with` blocks (Python) / try-finally\n- File paths are relative to the working directory, not the script file" },
    { keywords: ['data structure', 'stack', 'queue', 'tree', 'graph', 'linked list', 'hash table', 'hashmap'], response: "Data structures organize data for efficient access and modification. Choosing the right one is key to writing fast programs.\n\n**Common structures:**\n- **Array/List:** Ordered, indexed access — O(1) lookup by index, O(n) search\n- **Stack:** Last-In-First-Out (LIFO) — push/pop are O(1). Used in undo, call stack\n- **Queue:** First-In-First-Out (FIFO) — enqueue/dequeue are O(1). Used in task scheduling\n- **Hash Table (Map/Dict):** Key-value pairs — O(1) average lookup. Used everywhere!\n- **Linked List:** Nodes with pointers — O(1) insert/delete at ends, O(n) lookup\n- **Tree:** Hierarchical data — O(log n) search (balanced). Used in DOM, filesystems\n- **Graph:** Nodes + edges — models relationships, networks, social connections\n\n**Picking the right one:**\n- Need fast lookups by key? → **Hash Table**\n- Need ordered iteration? → **Array/List**\n- Need frequent insert/delete at both ends? → **Linked List** (or Deque)\n- Need hierarchical queries? → **Tree**\n- Need shortest path? → **Graph**\n\n**In most languages, the built-in collection types (Array, Map, Set, etc.) are highly optimized — use them!**" },
    { keywords: ['algorithm', 'sort', 'search', 'binary', 'big o', 'time complexity', 'space complexity', 'asymptotic'], response: "Algorithms are step-by-step procedures for solving problems. Understanding their efficiency helps you write fast, scalable code.\n\n**Big O Notation** describes how runtime grows with input size:\n- **O(1)** — constant: array lookup by index\n- **O(log n)** — logarithmic: binary search\n- **O(n)** — linear: searching an unsorted array\n- **O(n log n)** — linearithmic: efficient sorting (merge sort, quicksort)\n- **O(n²)** — quadratic: nested loops, bubble sort\n\n**Common algorithms to know:**\n- **Binary Search:** O(log n) on sorted arrays — cut the search space in half each step\n- **Two Pointers:** O(n) — solve array problems with two moving indices\n- **Sliding Window:** O(n) — maintain a window over array/substring data\n- **BFS/DFS:** O(V+E) — traversing trees and graphs\n- **Dynamic Programming:** break problems into overlapping subproblems with memoization\n\n**Quick tip:** Optimize in stages:\n1. Make it work (brute force)\n2. Make it right (correct)\n3. Make it fast (optimize)\n4. Make it clean (refactor)" },
    { keywords: ['map', 'filter', 'reduce', 'functional', 'immutable', 'pure function', 'side effect'], response: "Functional programming focuses on **pure functions** (same input → same output, no side effects) and **immutable data** (don't change, create new).\n\n**The three amigos: map, filter, reduce**\n\n**`map`** — transform every element:\n```js\n[1, 2, 3].map(x => x * 2); // [2, 4, 6]\n```\n\n**`filter`** — keep elements that pass a test:\n```js\n[1, 2, 3, 4].filter(x => x > 2); // [3, 4]\n```\n\n**`reduce`** — accumulate into a single value:\n```js\n[1, 2, 3].reduce((sum, x) => sum + x, 0); // 6\n```\n\n**Python equivalents:**\n```python\nlist(map(lambda x: x * 2, [1, 2, 3]))\nlist(filter(lambda x: x > 2, [1, 2, 3, 4]))\nfrom functools import reduce\nreduce(lambda s, x: s + x, [1, 2, 3], 0)\n```\n\n**Benefits of immutability:**\n- Easier to reason about (no hidden state changes)\n- Safer concurrency (no race conditions)\n- Predictable debugging\n\n**Rule of thumb:** Prefer `map`/`filter`/`reduce` over imperative loops when transforming collections. Your code will be more declarative and readable." },
    { keywords: ['terminal', 'command line', 'bash', 'shell', 'cli', 'console', 'zsh'], response: "The terminal (command line) is a text-based interface to your computer. It's essential for programming.\n\n**Essential commands:**\n```bash\npwd          # print working directory\nls           # list files\ncd dir       # change directory\nmkdir dir    # create directory\ntouch file   # create empty file (macOS/Linux)\necho text    # print text\ncat file     # print file contents\ncp src dst   # copy\nmv src dst   # move/rename\nrm file      # delete (careful! no trash bin)\n```\n\n**Pipes and redirection:**\n```bash\ncommand1 | command2   # pipe output to next command\ncommand > file         # save output to file\ncommand >> file        # append output to file\n```\n\n**Useful shortcuts:**\n- `↑`/`↓` — cycle through command history\n- `Ctrl+C` — cancel running command\n- `Ctrl+R` — search command history\n- `Ctrl+A` / `Ctrl+E` — jump to start/end of line\n- `Tab` — auto-complete\n\n**Pro tip:** Learn basic shell scripting to automate repetitive tasks. For example, renaming 100 files:\n```bash\nfor f in *.txt; do mv \"$f\" \"${f%.txt}.md\"; done\n```" },
    { keywords: ['performance', 'optimize', 'bottleneck', 'profile', 'cache', 'memory leak', 'throughput', 'latency'], response: "Performance optimization makes your code faster and more efficient. The golden rule: **measure first, optimize second**.\n\n**The optimization process:**\n1. **Profile** — use browser DevTools (Performance tab) or Node.js profiler to find bottlenecks\n2. **Identify** — find the slowest operation (database query, loop, rendering)\n3. **Optimize** — apply targeted fix\n4. **Measure again** — confirm improvement without breaking correctness\n\n**Common performance patterns:**\n- **Caching:** Store expensive computation results — `Map`/`WeakMap` for memoization\n- **Debouncing:** Limit how often a function fires (e.g., search input)\n- **Lazy loading:** Load data only when needed, not upfront\n- **Batch processing:** Group DB queries instead of N+1 individual ones\n- **Avoid premature optimization:** Write clean code first, then optimize the hot paths\n\n**Memory leaks to watch for:**\n- Forgotten timers/intervals (`setInterval` without `clearInterval`)\n- Detached DOM nodes (event listeners holding references)\n- Growing caches without eviction\n- Closures holding large arrays in scope\n\n**Quick wins:** Use `Set` instead of `Array.includes()` for large lookups. Use object pooling in games/real-time apps." },
    { keywords: ['build', 'bundle', 'webpack', 'vite', 'transpile', 'compile', 'minify', 'source map'], response: "Build tools transform your source code into production-ready files — converting modern JS/TS to browser-compatible code, bundling modules, and optimizing output.\n\n**Common tools:**\n- **Vite** — fast dev server with instant hot reload (modern, recommended)\n- **Webpack** — most popular, highly configurable (older but battle-tested)\n- **esbuild** — extremely fast bundler, written in Go (used by Vite internally)\n- **Babel** — transpiles modern JS to older syntax for browser compatibility\n- **TSC** — TypeScript compiler, type-checks and emits JS\n\n**What a build pipeline does:**\n1. **Transpile** — TS/JSX → plain JS\n2. **Bundle** — multiple files → one (or a few) files\n3. **Minify** — remove whitespace, shorten names\n4. **Source maps** — map minified code back to original for debugging\n\n**Config file examples:**\n- `vite.config.ts` — Vite configuration\n- `webpack.config.js` — Webpack configuration\n- `tsconfig.json` — TypeScript configuration\n- `.babelrc` — Babel configuration\n\n**For this project:** The frontend is vanilla JS (no build needed). TypeScript backend is compiled with `tsc` to `dist/`." },
    { keywords: ['security', 'auth', 'authentication', 'hash', 'encrypt', 'injection', 'xss', 'csrf', 'sanitize'], response: "Security is everyone's responsibility. Here are the most important concepts every developer should know:\n\n**OWASP Top 10 — the biggest threats:**\n- **Injection (SQL, NoSQL, Command):** Never trust user input. Use parameterized queries (`?` placeholders).\n- **XSS (Cross-Site Scripting):** Never render raw user HTML. Use `.textContent` not `.innerHTML`.\n- **CSRF:** Use anti-CSRF tokens on state-changing requests.\n- **Broken Authentication:** Hash passwords (bcrypt, argon2). Never store plaintext. Use HTTPS.\n- **Sensitive Data Exposure:** Don't log passwords, tokens, or PII. Use environment variables.\n\n**Password hashing (do NOT roll your own):**\n```js\nimport bcrypt from 'bcryptjs';\nconst hash = await bcrypt.hash(password, 12);  // salt rounds\nconst match = await bcrypt.compare(password, hash);\n```\n\n**Environment variables for secrets:**\n```\n# .env (never commit this!)\nDB_PASSWORD=secret123\nAPI_KEY=sk-abc123\n```\n\n**Golden rule:** Never trust user input — validate, sanitize, escape. Use HTTPS everywhere. Keep dependencies updated." },
    { keywords: ['docker', 'container', 'deploy', 'host', 'server', 'cloud', 'devops', 'kubernetes'], response: "Containers package your application with everything it needs to run (code, runtime, libraries, config). This ensures it works the same everywhere.\n\n**Docker basics:**\n```dockerfile\n# Dockerfile\nFROM node:18\nWORKDIR /app\nCOPY package*.json ./\nRUN npm install\nCOPY . .\nEXPOSE 3000\nCMD [\"npm\", \"start\"]\n```\n\n**Commands:**\n```bash\ndocker build -t my-app .   # build image\ndocker run -p 3000:3000 my-app  # run container\ndocker compose up          # run multi-container setup\n```\n\n**Deployment options:**\n- **Netlify** (this project!) — static sites + serverless functions\n- **Vercel** — frontend + serverless\n- **Railway/Fly.io** — full-stack apps\n- **AWS/GCP/Azure** — cloud providers with every service imaginable\n- **Self-hosted** — your own VPS (DigitalOcean, Linode)\n\n**Key concepts:**\n- **Image** — the blueprint (read-only)\n- **Container** — a running instance of an image\n- **Volume** — persistent storage (survives container restarts)\n- **Port mapping** — `host:container` (e.g., `3000:3000`)\n\n**Why this matters:** \"It works on my machine!\" — containers eliminate this problem forever." },
    { keywords: ['config', 'environment', 'env', 'settings', 'dotenv', 'configuration'], response: "Configuration keeps your code flexible. Instead of hardcoding values, use config files and environment variables.\n\n**Environment variables (the standard):**\n```bash\n# .env file (never commit to git!)\nPORT=3000\nDB_URL=postgresql://localhost:5432/mydb\nAI_PROVIDER=openai\nOPENAI_API_KEY=sk-...\n```\n\n**Loading in Node.js:**\n```js\nimport 'dotenv/config';\nconst port = process.env.PORT || 3000;\n```\n\n**Best practices:**\n- Use `.env.example` (committed to git) to document required vars\n- Never commit actual `.env` files — add to `.gitignore`\n- Use sensible defaults (e.g., `PORT || 3000`)\n- Validate config at startup (fail fast, not at runtime)\n- Keep secrets out of code, logs, and error messages\n\n**Configuration levels (12-factor app):**\n1. Hardcoded defaults (fallback)\n2. Config files (`config.json`, `config.yaml`)\n3. Environment variables (override)\n4. Runtime flags / CLI arguments\n\n**This project** uses `.env` for `AI_PROVIDER`, `OPENAI_API_KEY`, database connections, and more. See `.env.example` for the full list." },
    { keywords: ['math', 'number', 'random', 'date', 'timeout', 'interval', 'parseint', 'parsefloat', 'math.floor', 'math.random'], response: "Math and timing utilities are essential for data processing, games, animations, and scheduling.\n\n**Common Math operations (JS):**\n```js\nMath.random()     // 0 to 1 (exclusive)\nMath.floor(n)     // round down\nMath.ceil(n)      // round up\nMath.round(n)     // nearest integer\nMath.max(a, b)    // larger of two\nMath.min(a, b)    // smaller of two\nMath.abs(n)       // absolute value\n```\n\n**Random numbers in range:**\n```js\nfunction randInt(min, max) {\n  return Math.floor(Math.random() * (max - min + 1)) + min;\n}\nrandInt(1, 10); // 1 to 10 inclusive\n```\n\n**Working with dates:**\n```js\nconst now = new Date();\nnow.getFullYear();  // 2026\nnow.getMonth();     // 0-11 (0 = January)\nnow.getDate();      // 1-31\nnow.toISOString();  // \"2026-05-20T...\"\n```\n\n**Timers (JS):**\n```js\nsetTimeout(() => console.log('once'), 1000);       // 1 second delay\nsetInterval(() => console.log('every second'), 1000); // repeat\n```\n\n**Parsing numbers:**\n```js\nparseInt('42');     // 42\nparseInt('42px');   // 42 (ignores trailing)\nparseFloat('3.14'); // 3.14\nNumber('42');       // 42 (stricter — rejects '42px')\n```\n\n**Gotcha:** `Math.random()` is NOT cryptographically secure. Use `crypto.getRandomValues()` for security." },
    { keywords: ['state', 'props', 'react', 'component', 'vue', 'svelte', 'frontend framework', 'jsx', 'template'], response: "Frontend frameworks help you build interactive UIs by managing state and rendering efficiently.\n\n**Core concepts (shared across frameworks):**\n- **State:** data that changes over time (user input, API results)\n- **Props:** data passed from parent to child components (read-only)\n- **Component:** reusable UI piece with its own state + rendering\n- **Reactivity:** when state changes, the UI updates automatically\n\n**Framework comparison:**\n- **React** — component-based, virtual DOM, JSX. Huge ecosystem.\n- **Vue** — template-based, reactive refs, easier learning curve.\n- **Svelte** — compiles away the framework, no virtual DOM, smallest bundles.\n- **Solid** — fine-grained reactivity, no virtual DOM, fastest.\n\n**React example:**\n```jsx\nfunction Counter() {\n  const [count, setCount] = useState(0);\n  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;\n}\n```\n\n**Key principle (all frameworks):**\n- State flows **down** (parent → child via props)\n- Events flow **up** (child → parent via callbacks)\n- Keep state as **local** as possible — lift up only when shared\n\n**This project** uses vanilla JS (no framework) — a great way to understand how frameworks work under the hood!" },
    { keywords: ['devops', 'ci/cd', 'continuous integration', 'continuous deployment', 'pipeline', 'github actions', 'gitlab ci', 'jenkins', 'automation'], response: "DevOps bridges development and operations — automating the process from code commit to production deployment.\n\n**The CI/CD Pipeline:**\n1. **Code** — developer pushes to repository\n2. **Build** — compile, bundle, lint, type-check\n3. **Test** — run unit, integration, and e2e tests\n4. **Deploy** — push to staging/production\n\n**Popular CI/CD tools:**\n- **GitHub Actions** — deeply integrated with GitHub repos, YAML config\n- **GitLab CI** — built into GitLab, `.gitlab-ci.yml`\n- **Jenkins** — self-hosted, highly customizable (older but powerful)\n- **Netlify** — automatic deploy from git (used by this project!)\n\n**Infrastructure as Code (IaC):**\n- **Terraform** — declarative cloud infrastructure\n- **Ansible** — configuration management\n- **Docker Compose** — local multi-container setup\n\n**Key practices:**\n- **Immutable infrastructure** — never modify servers, redeploy fresh\n- **Monitoring & Alerting** — know before users do\n- **Rollback strategy** — always have a way to revert\n- **Shift left** — catch issues earlier in the pipeline" },
    { keywords: ['graphql', 'gql', 'query language', 'schema', 'resolver', 'apollo', 'relay', 'mutation', 'subscription'], response: "GraphQL is a query language for APIs that lets clients request exactly the data they need — nothing more, nothing less.\n\n**Key concepts:**\n- **Schema** — defines types and relationships (e.g., `type User { id: ID!, name: String! }`)\n- **Query** — fetch data (like GET)\n- **Mutation** — modify data (like POST/PUT/DELETE)\n- **Subscription** — real-time updates via WebSocket\n\n**Why GraphQL over REST:**\n- **No over-fetching** — get only the fields you ask for\n- **No under-fetching** — request related data in one query (vs N+1 REST endpoints)\n- **Strongly typed** — the schema serves as contract and documentation\n- **Single endpoint** — `/graphql` handles everything\n\n**Example query:**\n```graphql\nquery {\n  user(id: \"1\") {\n    name\n    email\n    posts { title }\n  }\n}\n```\n\n**Popular tools:**\n- **Apollo** — client + server (most popular)\n- **Relay** — by Facebook, optimized for React\n- **GraphiQL** — in-browser IDE for exploring GraphQL APIs" },
    { keywords: ['webassembly', 'wasm', 'wasmtime', 'wasi'], response: "WebAssembly (Wasm) is a low-level binary instruction format that runs in browsers (and increasingly on servers) at near-native speed.\n\n**Why Wasm matters:**\n- **Performance** — runs 10-50x faster than JavaScript for compute-heavy tasks\n- **Language-agnostic** — compile C, C++, Rust, Go, Zig, C# → Wasm\n- **Safe** — runs in a sandboxed environment with no direct system access\n- **Portable** — same binary runs on any platform with a Wasm runtime\n\n**Use cases:**\n- **Game engines** (Unity, Unreal) in the browser\n- **Image/video processing, audio codecs**\n- **CAD, design tools** (Figma uses Wasm!)\n- **Desktop apps** via Electron with Wasm for heavy lifting\n- **Edge compute** (Cloudflare Workers, Fastly Compute@Edge)\n\n**Getting started:**\n```rust\n// Rust → Wasm example\n#[wasm_bindgen]\npub fn fibonacci(n: u32) -> u32 {\n    match n { 0 => 0, 1 => 1, _ => fibonacci(n - 1) + fibonacci(n - 2) }\n}\n```\n\n**Key tools:**\n- **wasm-pack** — build Rust → Wasm packages\n- **emscripten** — compile C/C++ → Wasm\n- **TinyGo** — compile Go → Wasm\n- **Wasmtime/Wasmer** — server-side Wasm runtimes\n\n**Note:** Wasm can't access the DOM directly — it communicates with JS through function calls." },
    { keywords: ['cloud', 'aws', 'azure', 'gcp', 'google cloud', 'amazon web services'], response: "Cloud computing delivers compute power, storage, and services on-demand over the internet. The three major providers are AWS, Azure, and GCP.\n\n**Core cloud services (common across all providers):**\n- **Compute:** VMs (EC2, VM, Compute Engine), serverless (Lambda, Functions, Cloud Functions)\n- **Storage:** Object storage (S3, Blob, Cloud Storage), databases (RDS, Cosmos DB, Cloud SQL)\n- **Networking:** Load balancers, DNS, CDN, VPC\n- **AI/ML:** Pre-trained APIs, custom model training\n\n**Provider comparison:**\n| Feature | AWS | Azure | GCP |\n|---------|-----|-------|-----|\n| Compute | EC2 / Lambda | VM / Functions | Compute Engine / Cloud Functions |\n| Containers | ECS / EKS | AKS | GKE |\n| Serverless DB | DynamoDB | Cosmos DB | Firestore |\n| AI | SageMaker | Azure AI | Vertex AI |\n\n**Key concepts:**\n- **Region & Zone** — where your infrastructure physically runs\n- **IAM** — Identity and Access Management (who can do what)\n- **Auto-scaling** — automatically add/remove resources based on demand\n- **Pay-as-you-go** — only pay for what you use (but costs can spiral!)\n\n**This project** deploys to Netlify (serverless functions + static hosting), which runs on AWS Lambda under the hood." },
    { keywords: ['websocket', 'socket.io', 'real-time', 'realtime', 'live update', 'bidirectional', 'event stream', 'server-sent events', 'sse'], response: "WebSockets enable real-time, bidirectional communication between client and server — unlike HTTP where the client always initiates requests.\n\n**How it works:**\n1. Client sends an HTTP upgrade request\n2. Server responds with 101 Switching Protocols\n3. Persistent TCP connection stays open\n4. Both sides can send messages anytime\n\n**Alternatives comparison:**\n| Technology | Direction | Best for |\n|------------|-----------|----------|\n| HTTP Polling | Client→Server | Simple periodic updates |\n| Server-Sent Events | Server→Client | One-way updates (feeds, notifications) |\n| WebSocket | Bidirectional | Chat, games, collaborative editing |\n| WebRTC | Peer-to-peer | Video/audio calls, file sharing |\n\n**Node.js with ws (lightweight):**\n```js\nconst WebSocket = require('ws');\nconst wss = new WebSocket.Server({ port: 8080 });\nwss.on('connection', ws => {\n  ws.on('message', msg => console.log('received:', msg));\n  ws.send('Hello!');\n});\n```\n\n**Socket.IO** adds fallbacks (long-polling when WebSocket isn't available), rooms, and automatic reconnection.\n\n**This project** uses SSE for streaming AI responses from the LLM providers." },
    { keywords: ['prototype', 'prototypal inheritance', '__proto__', 'prototype chain', 'javascript prototype'], response: "Prototypal inheritance is JavaScript's unique approach to object-oriented programming. Unlike classical inheritance (classes inheriting from classes), objects inherit directly from other objects.\n\n**How the prototype chain works:**\n```js\nconst parent = { greet() { return \"Hello!\"; } };\nconst child = { name: \"Alice\" };\nObject.setPrototypeOf(child, parent);\nchild.greet(); // \"Hello!\" — found on parent\n```\n\n**Key concepts:**\n- **`[[Prototype]]`** — internal link from an object to its prototype\n- **`__proto__`** — legacy getter/setter for the prototype (use `Object.getPrototypeOf()` instead)\n- **`.prototype`** — a property on constructor functions (not instances)\n- **`Object.create(proto)`** — create object with a specific prototype\n\n**Property lookup order:**\n1. Own properties (directly on the object)\n2. Prototype's properties\n3. Prototype's prototype... until `null`\n\n**Classes are syntactic sugar:**\n```js\n// These are equivalent:\nclass Dog { bark() { return \"Woof!\"; } }\nfunction Dog() {}\nDog.prototype.bark = function() { return \"Woof!\"; };\n```\n\n**Gotcha:** `for...in` iterates inherited enumerable properties — use `hasOwnProperty()` to filter." },
    { keywords: ['event loop', 'event loop', 'microtask', 'macrotask', 'task queue', 'call stack', 'callback queue'], response: "The event loop is JavaScript's concurrency model — it's how a single-threaded language handles async operations without blocking.\n\n**The event loop cycle:**\n1. Execute all synchronous code on the **call stack**\n2. Process **microtask queue** (Promise callbacks, queueMicrotask) — empty entirely\n3. Pick ONE task from the **macrotask queue** (setTimeout, setInterval, I/O)\n4. Go back to step 2 (check microtasks again)\n5. Re-render the UI (browser only)\n\n**Execution order example:**\n```js\nconsole.log(1);                          // sync\nsetTimeout(() => console.log(2), 0);     // macrotask\nPromise.resolve().then(() => console.log(3)); // microtask\nconsole.log(4);                          // sync\n// Output: 1, 4, 3, 2\n```\n\n**Why this matters:**\n- **Blocking the stack** (infinite loop, heavy computation) freezes everything — UI, events, everything\n- **Microtasks can starve the event loop** — a microtask that keeps adding more microtasks prevents rendering\n- **`process.nextTick()`** (Node.js) is like a microtask that runs before other microtasks\n\n**Pro tip:** Use `setTimeout(cb, 0)` to defer work until after the browser has rendered (for UI updates). Use `queueMicrotask()` for promise-like scheduling." },
];
exports.default = AI_TUTOR_RESPONSES;
//# sourceMappingURL=responses-data.js.map
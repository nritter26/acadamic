{
  "id": "c-data-types",
  "title": "C: Data Types & Sizes",
  "difficulty": "beginner",
  "languages": ["c"],
  "estimatedMinutes": 10,
  "concepts": ["c", "data types", "sizeof", "printf"],
  "description": "Use sizeof() to print the size in bytes of int, float, char, double, and long.",
  "steps": [
    {
      "id": 1,
      "title": "Print Type Sizes",
      "description": "Use `sizeof()` to print the size of `int`, `float`, `char`, `double`, and `long`. Print each on its own line with a descriptive label.",
      "hints": ["Use `sizeof(int)` syntax.", "Use `%zu` format specifier for sizeof result.", "Example: `printf(\"int: %zu bytes\\n\", sizeof(int));`"],
      "codeTemplate": { "c": "#include <stdio.h>\n\nint main() {\n    \n    return 0;\n}" },
      "verification": {
        "test": "output.includes('int:') && output.includes('char:') && output.includes('double:')",
        "successMessage": "Type sizes printed correctly!",
        "failureMessage": "Print the size of int, float, char, double, and long"
      }
    }
  ]
}

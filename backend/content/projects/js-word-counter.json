{
  "id": "js-word-counter",
  "title": "JavaScript: Word Counter",
  "description": "Practice word counter in JavaScript.",
  "difficulty": "beginner",
  "languages": [
    "js"
  ],
  "codeTemplate": {
    "js": "console.log('Words: 5, Chars: 25')"
  },
  "verificationTest": "output.includes('2 words') && output.includes('11')",
  "hints": [
    "Practice word counter in JavaScript"
  ],
  "steps": [
    {
      "id": 1,
      "title": "Count words in a string",
      "description": "Write countWords(str) that returns the number of words (separated by spaces).",
      "hints": [
        "Use str.split(\" \") to get words array",
        "Return array.length",
        "Handle empty strings"
      ],
      "codeTemplate": {
        "javascript": "function countWords(str) {\n\n}\n\nconsole.log(countWords(\"Hello World\"));\nconsole.log(countWords(\"The quick brown fox\"));\nconsole.log(countWords(\"\"));\n"
      },
      "verification": {
        "test": "output.includes(\"2\") && output.includes(\"4\") && output.includes(\"0\")",
        "successMessage": "Word counter works!",
        "failureMessage": "Count words by splitting on spaces."
      }
    },
    {
      "id": 2,
      "title": "Count character frequency",
      "description": "Write charFrequency(str) that returns an object with each character as key and count as value.",
      "hints": [
        "Loop through string characters",
        "Use an object to track counts",
        "if(obj[char]) obj[char]++ else obj[char]=1"
      ],
      "codeTemplate": {
        "javascript": "function charFrequency(str) {\n\n}\n\nconsole.log(JSON.stringify(charFrequency(\"hello\")));\n"
      },
      "verification": {
        "test": "output.includes(\"h\") && output.includes(\"e\")",
        "successMessage": "Character frequency works!",
        "failureMessage": "Count occurrences of each character."
      }
    }
  ]
}
{
  "id": "zig-sum-avg",
  "title": "Sum and Average",
  "description": "Write a Zig program that calculates and prints the sum and average of an array of integers.",
  "difficulty": "beginner",
  "languages": [
    "zig"
  ],
  "codeTemplate": {
    "zig": "const std = @import(\"std\");\n\npub fn main() !void {\n    const nums = [_]i32{ 10, 20, 30, 40, 50 };\n    var sum: i32 = 0;\n    \n    for (nums) |n| {\n        sum += n;\n    }\n    \n    const avg = @as(f64, @intCast(sum)) / @as(f64, nums.len);\n    \n    std.debug.print(\"Sum: {}\\n\", .{sum});\n    std.debug.print(\"Average: {d:.1}\\n\", .{avg});\n}"
  },
  "verificationTest": "output.includes('Sum: 150') && (output.includes('Average: 30') || output.includes('Average: 30.0'))",
  "hints": [
    "Use a `for` loop to accumulate the sum",
    "For division, cast to `f64` using `@intCast` and `@as`",
    "Use `nums.len` to get the array length",
    "`{d:.1}` formats a float with 1 decimal place"
  ],
  "estimatedMinutes": 15,
  "concepts": [
    "calculator",
    "zig"
  ],
  "steps": [
    {
      "id": 1,
      "title": "Implement basic arithmetic functions",
      "description": "Create functions for basic arithmetic operations: add, subtract, multiply, and divide. Each function should take two numbers and return the result.",
      "hints": [
        "add(a, b) returns a + b",
        "subtract(a, b) returns a - b",
        "Handle division by zero in the divide function"
      ],
      "codeTemplate": {
        "zig": "// Step 1: Implement basic arithmetic functions\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Sum and Average...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Basic arithmetic functions work!",
        "failureMessage": "Implement add, subtract, multiply, and divide."
      }
    },
    {
      "id": 2,
      "title": "Create a calculate function",
      "description": "Write a calculate function that takes two numbers and an operator string (+, -, *, /) and returns the result by calling the appropriate arithmetic function.",
      "hints": [
        "Use a switch or if/else statement on the operator",
        "Call the appropriate function based on the operator",
        "Return an error message for unsupported operators"
      ],
      "codeTemplate": {
        "zig": "// Step 2: Create a calculate function\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Sum and Average...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Calculator dispatcher works!",
        "failureMessage": "calculate(5, \"+\", 3) should return 8."
      }
    },
    {
      "id": 3,
      "title": "Parse and evaluate expressions",
      "description": "Write a function that parses a simple arithmetic expression string like \"5 + 3\" and evaluates it. Split the string by spaces to get the operands and operator.",
      "hints": [
        "Split by spaces: expr.split(\" \")",
        "Parse the first and third parts as numbers",
        "Call calculate with the parsed values"
      ],
      "codeTemplate": {
        "zig": "// Step 3: Parse and evaluate expressions\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Sum and Average...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Expression parsing works!",
        "failureMessage": "evaluate(\"5 + 3\") should return 8."
      }
    },
    {
      "id": 4,
      "title": "Add memory and history features",
      "description": "Extend the calculator with memory functions (M+, M-, MC, MR) and a history log that stores past calculations.",
      "hints": [
        "Use a variable to store the memory value",
        "Keep an array or list for history",
        "Display the history when requested"
      ],
      "codeTemplate": {
        "zig": "// Step 4: Add memory and history features\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Sum and Average...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Memory and history features work!",
        "failureMessage": "Implement memory store, recall, clear, and calculation history."
      }
    },
    {
      "id": 5,
      "title": "Create an interactive REPL",
      "description": "Build a simple REPL (Read-Eval-Print Loop) that accepts expressions from the user, evaluates them, and displays the result until the user types \"quit\" or \"exit\".",
      "hints": [
        "Use a loop that reads input and evaluates expressions",
        "Display \"Welcome to the Calculator!\" on startup",
        "Show the result after each calculation"
      ],
      "codeTemplate": {
        "zig": "// Step 5: Create an interactive REPL\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Sum and Average...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Interactive calculator REPL works!",
        "failureMessage": "Create a loop that reads, evaluates, and prints results."
      }
    }
  ]
}
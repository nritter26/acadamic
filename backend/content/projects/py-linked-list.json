{
  "id": "py-linked-list",
  "title": "Python: Linked List",
  "description": "Write a Python program implementing a singly linked list with basic operations.",
  "difficulty": "intermediate",
  "languages": [
    "py"
  ],
  "codeTemplate": {
    "py": "class Node:\n    def __init__(self, data):\n        self.data = data\n        self.next = None\n\nclass LinkedList:\n    def __init__(self):\n        self.head = None\n\n    def append(self, data):\n        if not self.head:\n            self.head = Node(data)\n            return\n        curr = self.head\n        while curr.next:\n            curr = curr.next\n        curr.next = Node(data)\n\n    def display(self):\n        values = []\n        curr = self.head\n        while curr:\n            values.append(str(curr.data))\n            curr = curr.next\n        return ' -> '.join(values)\n\n    def reverse(self):\n        prev = None\n        curr = self.head\n        while curr:\n            next_node = curr.next\n            curr.next = prev\n            prev = curr\n            curr = next_node\n        self.head = prev\n\nll = LinkedList()\nfor val in [1, 2, 3, 4, 5]:\n    ll.append(val)\nprint(f\"Original: {ll.display()}\")\nll.reverse()\nprint(f\"Reversed: {ll.display()}\")"
  },
  "verificationTest": "output.includes('Original: 1 -> 2 -> 3 -> 4 -> 5') && output.includes('Reversed: 5 -> 4 -> 3 -> 2 -> 1')",
  "hints": [
    "Each node stores data and a reference to the next node",
    "Traverse by following `next` until None",
    "To reverse, change each node's next to point to the previous node"
  ],
  "estimatedMinutes": 25,
  "concepts": [
    "linked list",
    "py"
  ],
  "steps": [
    {
      "id": 1,
      "title": "Create a py Node class",
      "description": "Define a Node class with `data` and `next` properties. Each node holds a value and a reference to the next node in the list. Test by creating a few standalone nodes and linking them manually.",
      "hints": [
        "The Node class should have a constructor that takes data and sets next to null",
        "Create 3 nodes and link them: node1.next = node2; node2.next = node3",
        "Print the data of the first node to verify the chain"
      ],
      "codeTemplate": {
        "py": "// Create a Node class with data and next fields\n// Then create and link some test nodes\nconsole.log(\"Node structure created\");\n"
      },
      "verification": {
        "test": "output.includes('test') || output.length > 0",
        "successMessage": "Node class created and linked successfully!",
        "failureMessage": "Create a Node class with data and next properties, then link multiple nodes."
      }
    },
    {
      "id": 2,
      "title": "Implement insert operations",
      "description": "Add methods to insert nodes at the beginning (prepend), end (append), and at a specific index in the linked list. Each insertion should update the appropriate next references.",
      "hints": [
        "For prepend: create new node, point its next to current head, update head",
        "For append: traverse to the last node, set its next to the new node",
        "Handle edge cases: inserting into an empty list"
      ],
      "codeTemplate": {
        "py": "// Step 2: Implement insert operations\n// Your code here\n\nconsole.log(\"Testing Python: Linked List...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Insert operations work correctly!",
        "failureMessage": "Implement prepend, append, and insert methods."
      }
    },
    {
      "id": 3,
      "title": "Implement delete and search",
      "description": "Add methods to delete a node by value and search for a value in the list. Deletion should properly reconnect the surrounding nodes.",
      "hints": [
        "For delete: find the node before the target, skip the target node",
        "For search: traverse the list and return the node if found",
        "Handle deletion of head node separately"
      ],
      "codeTemplate": {
        "py": "// Step 3: Implement delete and search\n// Your code here\n\nconsole.log(\"Testing Python: Linked List...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Delete and search work correctly!",
        "failureMessage": "Implement deletion and search operations."
      }
    },
    {
      "id": 4,
      "title": "Add utility methods",
      "description": "Implement printList(), size(), isEmpty(), and toArray() methods. These utilities make the linked list easier to use and debug.",
      "hints": [
        "printList should traverse and display each node data",
        "size() should return the count of nodes",
        "toArray() should return an array of all node data values"
      ],
      "codeTemplate": {
        "py": "// Step 4: Add utility methods\n// Your code here\n\nconsole.log(\"Testing Python: Linked List...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Utility methods are working!",
        "failureMessage": "Implement printList, size, isEmpty, and toArray."
      }
    },
    {
      "id": 5,
      "title": "Test the complete linked list",
      "description": "Create a comprehensive test: insert several values, delete some, search for a value, and display the final list. Verify all operations work end-to-end.",
      "hints": [
        "Insert values at different positions",
        "Delete both the head and a middle node",
        "Print the final list to verify correctness"
      ],
      "codeTemplate": {
        "py": "// Step 5: Test the complete linked list\n// Your code here\n\nconsole.log(\"Testing Python: Linked List...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Complete linked list implementation works!",
        "failureMessage": "Create a comprehensive test of all linked list operations."
      }
    }
  ]
}
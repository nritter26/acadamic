{
  "id": "ts-arrays-tuples",
  "title": "TypeScript: Arrays and Tuples",
  "description": "Write a TypeScript program using typed arrays, tuples, and readonly arrays.",
  "difficulty": "beginner",
  "languages": [
    "ts"
  ],
  "codeTemplate": {
    "ts": "// Typed arrays\nconst numbers: number[] = [1, 2, 3, 4, 5];\nconst doubled: number[] = numbers.map(n => n * 2);\nconsole.log('Doubled:', doubled);\n\n// Tuples\nconst person: [string, number, boolean] = ['Alice', 30, true];\nconsole.log(`Name: ${person[0]}, Age: ${person[1]}, Active: ${person[2]}`);\n\n// Readonly array\nconst fruits: readonly string[] = ['apple', 'banana', 'cherry'];\nconsole.log('Fruits:', fruits);\n// fruits.push('date'); // Error: Property 'push' does not exist\n\n// Tuple with optional and rest\ntype Score = [string, number, ...number[]];\nconst scores: Score = ['Alice', 95, 90, 85, 92];\nconsole.log(`${scores[0]}: ${scores[1]} (all: ${scores.slice(2)})`);\n\n// Array destructuring\nconst [first, second, ...rest] = [10, 20, 30, 40, 50];\nconsole.log(`First: ${first}, Second: ${second}, Rest: ${rest}`);"
  },
  "verificationTest": "output.includes('Doubled: [2, 4, 6, 8, 10]') && output.includes('Name: Alice, Age: 30') && output.includes('Fruits: [')",
  "hints": [
    "Use `type[]` or `Array<type>` for typed arrays",
    "Tuples have fixed-length with specific types per position",
    "`readonly` prevents array mutation"
  ],
  "estimatedMinutes": 15,
  "concepts": [
    "general",
    "ts"
  ],
  "steps": [
    {
      "id": 1,
      "title": "Set up the TypeScript: Arrays and Tuples project",
      "description": "Initialize the project structure and create the main entry point. Set up any necessary configuration files and import required libraries.",
      "hints": [
        "Create the main source file for TypeScript: Arrays and Tuples",
        "Import necessary standard libraries",
        "Set up the basic program structure"
      ],
      "codeTemplate": {
        "ts": "// Step 1: Set up the TypeScript: Arrays and Tuples project\n// Your code here\n\nconsole.log(\"Testing TypeScript: Arrays and Tuples...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Project structure set up correctly!",
        "failureMessage": "Initialize the project with proper structure."
      }
    },
    {
      "id": 2,
      "title": "Implement the core logic for TypeScript: Arrays and Tuples",
      "description": "Write the main algorithm or core functionality that drives this project. Break down the problem into smaller functions for clarity.",
      "hints": [
        "Start with the simplest part of the algorithm",
        "Test each function as you write it",
        "Use descriptive variable and function names"
      ],
      "codeTemplate": {
        "ts": "// Step 2: Implement the core logic for TypeScript: Arrays and Tuples\n// Your code here\n\nconsole.log(\"Testing TypeScript: Arrays and Tuples...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Core logic implemented successfully!",
        "failureMessage": "Implement the main functionality."
      }
    },
    {
      "id": 3,
      "title": "Handle edge cases and errors",
      "description": "Add checks for edge cases such as empty inputs, invalid data, boundary conditions, and error states. Provide clear feedback for invalid inputs.",
      "hints": [
        "Test with empty strings or zero values",
        "Handle negative numbers if applicable",
        "Provide user-friendly error messages"
      ],
      "codeTemplate": {
        "ts": "// Step 3: Handle edge cases and errors\n// Your code here\n\nconsole.log(\"Testing TypeScript: Arrays and Tuples...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Edge cases handled properly!",
        "failureMessage": "Add error handling for edge cases."
      }
    },
    {
      "id": 4,
      "title": "Format and display the output",
      "description": "Format the output in a clear, readable way. If applicable, display results in a structured format with labels and proper formatting.",
      "hints": [
        "Add labels to output values for clarity",
        "Use consistent formatting throughout",
        "Consider adding a summary section"
      ],
      "codeTemplate": {
        "ts": "// Step 4: Format and display the output\n// Your code here\n\nconsole.log(\"Testing TypeScript: Arrays and Tuples...\");\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Output is clean and well-formatted!",
        "failureMessage": "Format the output for readability."
      }
    }
  ]
}
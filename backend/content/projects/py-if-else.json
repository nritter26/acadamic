{
  "id": "py-if-else",
  "title": "Python: If-Else",
  "description": "Write a Python program that uses if-elif-else to determine if a number is positive, negative, or zero.",
  "difficulty": "beginner",
  "languages": ["py"],
  "codeTemplate": {
    "py": "num = -7\n\nif num > 0:\n    print(f\"{num} is positive\")\nelif num < 0:\n    print(f\"{num} is negative\")\nelse:\n    print(f\"{num} is zero\")"
  },
  "verificationTest": "output.includes('-7 is negative')",
  "hints": [
    "Use `if`, `elif`, `else` for conditional logic",
    "Python uses indentation (not braces) for code blocks",
    "Comparison operators: `>`, `<`, `>=`, `<=`, `==`, `!=`"
  ]
}

{
  "id": "py-sorting-algorithms",
  "title": "Python: Sorting Algorithms",
  "description": "Write a Python program implementing bubble sort and merge sort algorithms.",
  "difficulty": "intermediate",
  "languages": ["py"],
  "codeTemplate": {
    "py": "def bubble_sort(arr):\n    n = len(arr)\n    for i in range(n):\n        for j in range(0, n - i - 1):\n            if arr[j] > arr[j + 1]:\n                arr[j], arr[j + 1] = arr[j + 1], arr[j]\n    return arr\n\ndef merge_sort(arr):\n    if len(arr) <= 1:\n        return arr\n    mid = len(arr) // 2\n    left = merge_sort(arr[:mid])\n    right = merge_sort(arr[mid:])\n    return merge(left, right)\n\ndef merge(left, right):\n    result = []\n    i = j = 0\n    while i < len(left) and j < len(right):\n        if left[i] <= right[j]:\n            result.append(left[i])\n            i += 1\n        else:\n            result.append(right[j])\n            j += 1\n    result.extend(left[i:])\n    result.extend(right[j:])\n    return result\n\ndata = [64, 34, 25, 12, 22, 11, 90]\nprint(f\"Original: {data}\")\nprint(f\"Bubble: {bubble_sort(data.copy())}\")\nprint(f\"Merge: {merge_sort(data.copy())}\")"
  },
  "verificationTest": "output.includes('Original: [64, 34, 25, 12, 22, 11, 90]') && output.includes('Bubble: [11, 12, 22, 25, 34, 64, 90]')",
  "hints": [
    "Bubble sort repeatedly swaps adjacent elements if they're in wrong order",
    "Merge sort divides the array in half, sorts each half, then merges",
    "Use list slicing to divide arrays"
  ]
}

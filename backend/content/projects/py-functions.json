{
  "id": "py-functions",
  "title": "Python: Functions",
  "description": "Write a Python program that defines and calls functions with parameters and return values.",
  "difficulty": "beginner",
  "languages": ["py"],
  "codeTemplate": {
    "py": "def greet(name):\n    return f\"Hello, {name}!\"\n\ndef add(a, b):\n    return a + b\n\ndef is_even(n):\n    return n % 2 == 0\n\nprint(greet(\"Python\"))\nprint(f\"3 + 7 = {add(3, 7)}\")\nprint(f\"Is 10 even? {is_even(10)}\")\nprint(f\"Is 7 even? {is_even(7)}\")"
  },
  "verificationTest": "output.includes('Hello, Python!') && output.includes('3 + 7 = 10') && output.includes('Is 10 even? True') && output.includes('Is 7 even? False')",
  "hints": [
    "Define functions with `def function_name(parameters):`",
    "Use `return` to send a value back from a function",
    "Functions in Python can return any type, including strings, numbers, and booleans"
  ]
}

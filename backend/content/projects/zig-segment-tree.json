{
  "id": "zig-segment-tree",
  "title": "Segment Tree",
  "description": "Write a Zig program that implements a segment tree for range sum queries.",
  "difficulty": "expert",
  "languages": ["zig"],
  "codeTemplate": {
    "zig": "const std = @import(\"std\");\n\nfn build(arr: []i32, tree: []i32, node: usize, left: usize, right: usize) void {\n    if (left == right) {\n        tree[node] = arr[left];\n        return;\n    }\n    const mid = (left + right) / 2;\n    build(arr, tree, 2 * node + 1, left, mid);\n    build(arr, tree, 2 * node + 2, mid + 1, right);\n    tree[node] = tree[2 * node + 1] + tree[2 * node + 2];\n}\n\nfn query(tree: []i32, node: usize, left: usize, right: usize, ql: usize, qr: usize) i32 {\n    if (ql > right or qr < left) return 0;\n    if (ql <= left and right <= qr) return tree[node];\n    const mid = (left + right) / 2;\n    return query(tree, 2 * node + 1, left, mid, ql, qr) +\n           query(tree, 2 * node + 2, mid + 1, right, ql, qr);\n}\n\npub fn main() !void {\n    const arr = [_]i32{ 1, 3, 5, 7, 9, 11 };\n    var tree: [24]i32 = undefined;\n    build(arr, tree, 0, 0, arr.len - 1);\n    \n    std.debug.print(\"Sum 0-3: {}\\n\", .{query(tree, 0, 0, arr.len - 1, 0, 3)});\n    std.debug.print(\"Sum 2-5: {}\\n\", .{query(tree, 0, 0, arr.len - 1, 2, 5)});\n}"
  },
  "verificationTest": "output.includes('Sum 0-3: 16') && output.includes('Sum 2-5: 32')",
  "hints": [
    "Segment tree stores range aggregates (sums) in a binary tree structure",
    "Leaf nodes store individual array elements, internal nodes store combined values",
    "Range query traverses the tree, combining results from relevant nodes",
    "Use 4× N array size for safe segment tree storage"
  ]
}

{
  "id": "zig-comptime-json",
  "title": "Comptime JSON",
  "description": "Write a Zig program that extracts values from a JSON string at compile time using comptime evaluation.",
  "difficulty": "expert",
  "languages": ["zig"],
  "codeTemplate": {
    "zig": "const std = @import(\"std\");\n\nfn extractName(comptime json: []const u8) []const u8 {\n    comptime {\n        const start = 8;\n        var end = start;\n        while (end < json.len and json[end] != '\"') end += 1;\n        return json[start..end];\n    }\n}\n\npub fn main() !void {\n    const json = \"{\\\"name\\\":\\\"Zig\\\",\\\"year\\\":2024}\";\n    const name = comptime extractName(json);\n    std.debug.print(\"Extracted: {s}\\n\", .{name});\n}"
  },
  "verificationTest": "output.includes('Extracted: Zig')",
  "hints": [
    "Use `comptime` blocks to force compile-time evaluation of string processing",
    "Comptime JSON parsing extracts metadata without runtime overhead",
    "`comptime` can be used on a function parameter or inside a block",
    "The result is embedded as a constant in the compiled binary"
  ]
}

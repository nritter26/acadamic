{
  "id": "py-args-kwargs",
  "title": "Python: *args and **kwargs",
  "description": "Write a Python program that uses *args and **kwargs to create flexible functions.",
  "difficulty": "intermediate",
  "languages": ["py"],
  "codeTemplate": {
    "py": "def sum_all(*args):\n    return sum(args)\n\ndef print_info(**kwargs):\n    for key, value in kwargs.items():\n        print(f\"  {key}: {value}\")\n\ndef combine(prefix, *args, suffix='', **kwargs):\n    print(f\"{prefix} {sum(args)} {suffix}\")\n    if kwargs:\n        print(\"Extra info:\")\n        print_info(**kwargs)\n\nprint(f\"Sum: {sum_all(1, 2, 3, 4, 5)}\")\nprint_info(name='Bob', age=25, role='Developer')\ncombine('Result:', 10, 20, 30, suffix='is the total', status='complete')"
  },
  "verificationTest": "output.includes('Sum: 15') && output.includes('name: Bob') && output.includes('Result:') && output.includes('60')",
  "hints": [
    "Use `*args` to accept any number of positional arguments as a tuple",
    "Use `**kwargs` to accept any number of keyword arguments as a dict",
    "Args and kwargs can be combined with regular parameters"
  ]
}

{
  "id": "dice-roller",
  "title": "Dice Roller",
  "difficulty": "beginner",
  "languages": ["javascript", "typescript", "python"],
  "estimatedMinutes": 15,
  "concepts": ["random", "loops", "arrays", "functions", "statistics"],
  "description": "Simulate dice rolls, display ASCII dice faces, and analyze roll statistics.",
  "steps": [
    {
      "id": 1,
      "title": "Roll a single die",
      "description": "Write a `rollDie` function that returns a random integer between 1 and 6 using `Math.random()`.",
      "hints": [
        "Math.floor(Math.random() * 6) + 1 gives 1-6",
        "Return the random number"
      ],
      "codeTemplate": {
        "javascript": "function rollDie() {\n  \n}\n\nconsole.log(rollDie());\nconsole.log(rollDie());\n",
        "typescript": "function rollDie(): number {\n  \n}\n\nconsole.log(rollDie());\nconsole.log(rollDie());\n",
        "python": "import random\n\ndef rollDie():\n  \n\nprint(rollDie())\nprint(rollDie())\n"
      },
      "verification": {
        "test": "let nums = output.trim().split('\\n').map(Number); return nums.every(n => n >= 1 && n <= 6) && nums.length === 2;",
        "successMessage": "Both rolls are valid die values (1-6)!",
        "failureMessage": "Each roll should be a random number between 1 and 6."
      }
    },
    {
      "id": 2,
      "title": "Roll multiple dice",
      "description": "Write a `rollDice` function that rolls N dice and returns an array of the results.",
      "hints": [
        "Use a loop that calls rollDie() N times",
        "Push each result into an array",
        "Return the array"
      ],
      "codeTemplate": {
        "javascript": "function rollDie() {\n  return Math.floor(Math.random() * 6) + 1;\n}\n\nfunction rollDice(n) {\n  \n}\n\nconsole.log(rollDice(5));\n",
        "typescript": "function rollDie(): number {\n  return Math.floor(Math.random() * 6) + 1;\n}\n\nfunction rollDice(n: number): number[] {\n  \n}\n\nconsole.log(rollDice(5));\n",
        "python": "import random\n\ndef rollDie():\n  return random.randint(1, 6)\n\ndef rollDice(n):\n  \n\nprint(rollDice(5))\n"
      },
      "verification": {
        "test": "let arr = JSON.parse(output.trim()); return arr.length === 5 && arr.every(n => n >= 1 && n <= 6);",
        "successMessage": "5 dice rolled and returned as an array!",
        "failureMessage": "rollDice(5) should return an array of 5 numbers, each between 1 and 6."
      }
    },
    {
      "id": 3,
      "title": "Sum of dice",
      "description": "Write a `rollDiceSum` function that rolls N dice and returns the sum of all rolls.",
      "hints": [
        "Call rollDice to get the array",
        "Sum the array using .reduce() or a loop",
        "Return the total"
      ],
      "codeTemplate": {
        "javascript": "function rollDie() { return Math.floor(Math.random() * 6) + 1; }\nfunction rollDice(n) {\n  const results = [];\n  for (let i = 0; i < n; i++) results.push(rollDie());\n  return results;\n}\n\nfunction rollDiceSum(n) {\n  \n}\n\nconsole.log(rollDiceSum(4));\n",
        "typescript": "function rollDie(): number { return Math.floor(Math.random() * 6) + 1; }\nfunction rollDice(n: number): number[] {\n  const results: number[] = [];\n  for (let i = 0; i < n; i++) results.push(rollDie());\n  return results;\n}\n\nfunction rollDiceSum(n: number): number {\n  \n}\n\nconsole.log(rollDiceSum(4));\n",
        "python": "import random\n\ndef rollDie():\n  return random.randint(1, 6)\n\ndef rollDice(n):\n  return [rollDie() for _ in range(n)]\n\ndef rollDiceSum(n):\n  \n\nprint(rollDiceSum(4))\n"
      },
      "verification": {
        "test": "Number(output.trim()) >= 4 && Number(output.trim()) <= 24",
        "successMessage": "Sum of 4 dice is between 4 and 24 — looks right!",
        "failureMessage": "The sum of 4 dice should be between 4 (all 1s) and 24 (all 6s)."
      }
    },
    {
      "id": 4,
      "title": "Display dice faces",
      "description": "Write a `displayDie` function that returns an ASCII representation of a die face (using dots/lines).",
      "hints": [
        "Use a string or array to represent each face pattern",
        "Center dots to represent the number",
        "For example, a 5 might be shown with 4 corner dots and 1 center"
      ],
      "codeTemplate": {
        "javascript": "function displayDie(value) {\n  \n}\n\nconsole.log(displayDie(5));\n",
        "typescript": "function displayDie(value: number): string {\n  \n}\n\nconsole.log(displayDie(5));\n",
        "python": "def displayDie(value):\n  \n\nprint(displayDie(5))\n"
      },
      "verification": {
        "test": "output.includes('o') || output.includes('*') || output.includes('.')",
        "successMessage": "The die face is displayed with ASCII art!",
        "failureMessage": "displayDie should return a multi-line string showing the die face."
      }
    },
    {
      "id": 5,
      "title": "Roll statistics",
      "description": "Write a function that rolls a die many times and counts how many times each value appears.",
      "hints": [
        "Roll a die N times (e.g., 1000)",
        "Use an object or array to count occurrences of each value 1-6",
        "Print the frequency of each face"
      ],
      "codeTemplate": {
        "javascript": "function rollDie() { return Math.floor(Math.random() * 6) + 1; }\n\nfunction rollStats(rolls) {\n  \n}\n\nconsole.log(rollStats(1000));\n",
        "typescript": "function rollDie(): number { return Math.floor(Math.random() * 6) + 1; }\n\nfunction rollStats(rolls: number): object {\n  \n}\n\nconsole.log(rollStats(1000));\n",
        "python": "import random\n\ndef rollDie():\n  return random.randint(1, 6)\n\ndef rollStats(rolls):\n  \n\nprint(rollStats(1000))\n"
      },
      "verification": {
        "test": "output.includes('1') && output.includes('2') && output.includes('3') && output.includes('4') && output.includes('5') && output.includes('6')",
        "successMessage": "All six faces appear in the statistics!",
        "failureMessage": "The stats object should have keys for each die face (1-6) with their counts."
      }
    }
  ]
}

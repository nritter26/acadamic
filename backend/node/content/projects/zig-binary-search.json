{
  "id": "zig-binary-search",
  "title": "Binary Search",
  "description": "Write a Zig program that implements binary search to find a target value in a sorted array.",
  "difficulty": "intermediate",
  "languages": [
    "zig"
  ],
  "codeTemplate": {
    "zig": "const std = @import(\"std\");\n\npub fn main() !void {\n    const arr = [_]i32{ 3, 7, 11, 18, 25, 42, 56, 71, 88, 99 };\n    const target: i32 = 42;\n    var left: usize = 0;\n    var right: usize = arr.len - 1;\n    var found: ?usize = null;\n    \n    while (left <= right) {\n        const mid = left + (right - left) / 2;\n        if (arr[mid] == target) {\n            found = mid;\n            break;\n        } else if (arr[mid] < target) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    \n    if (found) |idx| {\n        std.debug.print(\"Found {} at index {}\\n\", .{target, idx});\n    } else {\n        std.debug.print(\"{} not found\\n\", .{target});\n    }\n}"
  },
  "verificationTest": "output.includes('Found 42 at index 5')",
  "hints": [
    "Binary search works on sorted arrays by repeatedly dividing the search range in half",
    "Use `while (left <= right)` as the main loop",
    "Calculate mid with `left + (right - left) / 2` to avoid overflow",
    "Adjust left or right based on comparison with the target"
  ],
  "estimatedMinutes": 25,
  "concepts": [
    "sorting",
    "zig"
  ],
  "steps": [
    {
      "id": 1,
      "title": "Implement bubble sort",
      "description": "Write a function that sorts an array using the bubble sort algorithm. Repeatedly step through the array, comparing adjacent elements and swapping them if they are in the wrong order.",
      "hints": [
        "Use nested loops: outer loop for passes, inner loop for comparisons",
        "After each pass, the largest element bubbles to the end",
        "Optimize: stop early if no swaps occur in a pass"
      ],
      "codeTemplate": {
        "zig": "// Step 1: Implement bubble sort\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Binary Search...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.includes('sorted') || output.includes('[1')",
        "successMessage": "Bubble sort implementation works!",
        "failureMessage": "Implement bubble sort and display the sorted array."
      }
    },
    {
      "id": 2,
      "title": "Implement selection sort",
      "description": "Write a selection sort function that divides the array into sorted and unsorted regions, repeatedly selecting the smallest element from the unsorted region and adding it to the sorted region.",
      "hints": [
        "Find the minimum element in the unsorted portion",
        "Swap it with the first unsorted element",
        "Advance the boundary between sorted and unsorted regions"
      ],
      "codeTemplate": {
        "zig": "// Step 2: Implement selection sort\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Binary Search...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Selection sort works correctly!",
        "failureMessage": "Implement selection sort and display the sorted array."
      }
    },
    {
      "id": 3,
      "title": "Implement merge sort",
      "description": "Write a merge sort function that recursively divides the array into halves, sorts each half, and merges them back together. This is a classic divide-and-conquer algorithm.",
      "hints": [
        "Base case: arrays with 0 or 1 elements are already sorted",
        "Split the array in half and recursively sort each half",
        "The merge function combines two sorted arrays into one"
      ],
      "codeTemplate": {
        "zig": "// Step 3: Implement merge sort\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Binary Search...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Merge sort works correctly!",
        "failureMessage": "Implement merge sort (divide, conquer, merge)."
      }
    },
    {
      "id": 4,
      "title": "Implement quick sort",
      "description": "Write a quick sort function that picks a pivot element, partitions the array around it, and recursively sorts the sub-arrays. Quick sort is one of the most efficient sorting algorithms.",
      "hints": [
        "Choose a pivot (last element, first element, or median-of-three)",
        "Partition: rearrange so elements < pivot come before it, > pivot after",
        "Recursively sort the sub-arrays on either side of the pivot"
      ],
      "codeTemplate": {
        "zig": "// Step 4: Implement quick sort\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Binary Search...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Quick sort works correctly!",
        "failureMessage": "Implement quick sort with partitioning and recursion."
      }
    },
    {
      "id": 5,
      "title": "Compare sorting algorithm performance",
      "description": "Create a test that generates a large random array and times each sorting algorithm. Display the time each algorithm takes and verify all produce the same sorted result.",
      "hints": [
        "Generate a random array of at least 1000 elements",
        "Copy the array before sorting (sorting modifies in-place)",
        "Use timing functions to measure execution time"
      ],
      "codeTemplate": {
        "zig": "// Step 5: Compare sorting algorithm performance\nconst std = @import(\"std\");\n\npub fn main() void {\n  // Your code here\n  std.debug.print(\"Testing Binary Search...\\n\", .{});\n}\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "All sorting algorithms implemented and compared!",
        "failureMessage": "Test all algorithms with timing comparison."
      }
    }
  ]
}
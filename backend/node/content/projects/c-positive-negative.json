{
  "id": "c-positive-negative",
  "title": "C: Positive or Negative",
  "difficulty": "beginner",
  "languages": ["c"],
  "estimatedMinutes": 10,
  "concepts": ["c", "conditionals", "comparison", "printf"],
  "description": "Check if a number is positive, negative, or zero using if-else statements.",
  "steps": [
    {
      "id": 1,
      "title": "Check Sign",
      "description": "Declare `int num = -5`. Use if-else to print 'Positive', 'Negative', or 'Zero'.",
      "hints": ["Check if `num > 0` for positive.", "Check if `num < 0` for negative.", "Otherwise it's zero."],
      "codeTemplate": { "c": "#include <stdio.h>\n\nint main() {\n    int num = -5;\n    \n    return 0;\n}" },
      "verification": {
        "test": "output.includes('Negative') || output.includes('negative')",
        "successMessage": "Sign detection works!",
        "failureMessage": "Check if -5 is positive, negative, or zero"
      }
    }
  ]
}

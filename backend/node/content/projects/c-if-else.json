{
  "id": "c-if-else",
  "title": "C: If-Else Statements",
  "difficulty": "beginner",
  "languages": ["c"],
  "estimatedMinutes": 10,
  "concepts": ["c", "if-else", "conditionals", "printf"],
  "description": "Use if-else statements to check conditions and print different outputs based on the result.",
  "steps": [
    {
      "id": 1,
      "title": "If-Else Basics",
      "description": "Declare `int num = 7`. Use if-else to check if num is positive, negative, or zero. Print the result.",
      "hints": ["Use `if (num > 0) else if (num < 0) else` construct.", "Print statements like `printf(\"Positive\\n\");`.", "Test with num = 7 (positive)."],
      "codeTemplate": { "c": "#include <stdio.h>\n\nint main() {\n    int num = 7;\n    \n    return 0;\n}" },
      "verification": {
        "test": "output.includes('Positive') || output.includes('positive')",
        "successMessage": "If-else works correctly!",
        "failureMessage": "Check if num is positive, negative, or zero and print"
      }
    }
  ]
}

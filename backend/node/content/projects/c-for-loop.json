{
  "id": "c-for-loop",
  "title": "C: For Loops",
  "difficulty": "beginner",
  "languages": ["c"],
  "estimatedMinutes": 10,
  "concepts": ["c", "for loop", "iteration", "printf"],
  "description": "Use a for loop to print even numbers from 2 to 20, each on a new line.",
  "steps": [
    {
      "id": 1,
      "title": "Even Numbers with For",
      "description": "Use a for loop to print the even numbers from 2 to 20 (inclusive), one per line.",
      "hints": ["For loop syntax: `for (init; condition; increment)`", "Start at 2, go up to 20, increment by 2.", "Example: `for (int i = 2; i <= 20; i += 2)`"],
      "codeTemplate": { "c": "#include <stdio.h>\n\nint main() {\n    \n    return 0;\n}" },
      "verification": {
        "test": "output.includes('2') && output.includes('20') && !output.includes('19')",
        "successMessage": "For loop prints evens correctly!",
        "failureMessage": "Print even numbers 2 to 20 using a for loop"
      }
    }
  ]
}

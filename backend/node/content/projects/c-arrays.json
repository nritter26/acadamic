{
  "id": "c-arrays",
  "title": "C: Arrays",
  "difficulty": "beginner",
  "languages": ["c"],
  "estimatedMinutes": 10,
  "concepts": ["c", "arrays", "indexing", "printf"],
  "description": "Declare an array of integers, initialize it, and print all elements using a loop.",
  "steps": [
    {
      "id": 1,
      "title": "Array Declaration and Print",
      "description": "Declare an `int arr[5] = {10, 20, 30, 40, 50}` and print each element on a new line using a for loop.",
      "hints": ["Array indices start at 0.", "Loop from 0 to array length - 1.", "Access elements with `arr[i]`."],
      "codeTemplate": { "c": "#include <stdio.h>\n\nint main() {\n    int arr[5] = {10, 20, 30, 40, 50};\n    \n    return 0;\n}" },
      "verification": {
        "test": "output.includes('10') && output.includes('30') && output.includes('50')",
        "successMessage": "Array printed correctly!",
        "failureMessage": "Print all 5 elements of the array"
      }
    }
  ]
}

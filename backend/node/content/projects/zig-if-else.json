{
  "id": "zig-if-else",
  "title": "Zig: If-Else",
  "difficulty": "beginner",
  "languages": ["zig"],
  "estimatedMinutes": 10,
  "concepts": ["zig", "if-else", "conditionals"],
  "description": "Use if-else to check if a number is positive, negative, or zero.",
  "steps": [
    {
      "id": 1,
      "title": "Check Sign",
      "description": "Declare const num: i32 = -5. Use if-else to print 'Positive', 'Negative', or 'Zero'.",
      "hints": ["Zig uses `if (condition) { } else if (condition) { } else { }`", "Comparison operators: >, <, ==.", "No parentheses needed around conditions."],
      "codeTemplate": { "zig": "const std = @import(\"std\");\n\npub fn main() !void {\n    const num: i32 = -5;\n    \n}" },
      "verification": {
        "test": "output.includes('Negative') || output.includes('negative')",
        "successMessage": "Sign detection works!",
        "failureMessage": "Check if -5 is positive, negative, or zero"
      }
    }
  ]
}

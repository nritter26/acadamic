{
  "id": "lua-tables",
  "title": "Lua: Tables",
  "description": "Write a Lua program demonstrating tables as arrays, dictionaries, and objects with metatables.",
  "difficulty": "beginner",
  "languages": [
    "lua"
  ],
  "codeTemplate": {
    "lua": "-- Table as array\nlocal fruits = {\"apple\", \"banana\", \"cherry\"}\ntable.insert(fruits, \"date\")\nprint(\"Fruits: \" .. table.concat(fruits, \", \"))\nprint(\"Count: \" .. #fruits)\n\n-- Table as dictionary\nlocal person = {\n    name = \"Alice\",\n    age = 30,\n    city = \"New York\"\n}\nprint(\"Name: \" .. person.name)\nprint(\"Age: \" .. person.age)\n\n-- Iteration\nprint(\"\\nIterating fruits:\")\nfor i, fruit in ipairs(fruits) do\n    print(\"  \" .. i .. \". \" .. fruit)\nend\n\nprint(\"\\nIterating person:\")\nfor key, value in pairs(person) do\n    print(\"  \" .. key .. \": \" .. value)\nend\n\n-- Metatables\nlocal vec = {x = 3, y = 4}\nlocal mt = {\n    __tostring = function(t)\n        return \"(\" .. t.x .. \", \" .. t.y .. \")\"\n    end\n}\nsetmetatable(vec, mt)\nprint(\"\\nVector: \" .. vec)\nprint(\"Length: \" .. math.sqrt(vec.x^2 + vec.y^2))"
  },
  "verificationTest": "output.includes('Fruits: apple, banana, cherry, date') && output.includes('Name: Alice') && output.includes('Vector: (3, 4)') && output.includes('Length: 5')",
  "hints": [
    "Tables are the only data structure in Lua — used for arrays, dicts, and objects",
    "Use `ipairs()` for array-like iteration, `pairs()` for dictionary iteration",
    "Metatables customize table behavior with metamethods like __tostring",
    "`#` operator gives the length of an array-like table"
  ],
  "estimatedMinutes": 15,
  "concepts": [
    "general",
    "lua"
  ],
  "steps": [
    {
      "id": 1,
      "title": "Set up the Lua: Tables project",
      "description": "Initialize the project structure and create the main entry point. Set up any necessary configuration files and import required libraries.",
      "hints": [
        "Create the main source file for Lua: Tables",
        "Import necessary standard libraries",
        "Set up the basic program structure"
      ],
      "codeTemplate": {
        "lua": "-- Step 1: Set up the Lua: Tables project\n-- Your code here\n\nprint(\"Testing Lua: Tables...\")\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Project structure set up correctly!",
        "failureMessage": "Initialize the project with proper structure."
      }
    },
    {
      "id": 2,
      "title": "Implement the core logic for Lua: Tables",
      "description": "Write the main algorithm or core functionality that drives this project. Break down the problem into smaller functions for clarity.",
      "hints": [
        "Start with the simplest part of the algorithm",
        "Test each function as you write it",
        "Use descriptive variable and function names"
      ],
      "codeTemplate": {
        "lua": "-- Step 2: Implement the core logic for Lua: Tables\n-- Your code here\n\nprint(\"Testing Lua: Tables...\")\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Core logic implemented successfully!",
        "failureMessage": "Implement the main functionality."
      }
    },
    {
      "id": 3,
      "title": "Handle edge cases and errors",
      "description": "Add checks for edge cases such as empty inputs, invalid data, boundary conditions, and error states. Provide clear feedback for invalid inputs.",
      "hints": [
        "Test with empty strings or zero values",
        "Handle negative numbers if applicable",
        "Provide user-friendly error messages"
      ],
      "codeTemplate": {
        "lua": "-- Step 3: Handle edge cases and errors\n-- Your code here\n\nprint(\"Testing Lua: Tables...\")\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Edge cases handled properly!",
        "failureMessage": "Add error handling for edge cases."
      }
    },
    {
      "id": 4,
      "title": "Format and display the output",
      "description": "Format the output in a clear, readable way. If applicable, display results in a structured format with labels and proper formatting.",
      "hints": [
        "Add labels to output values for clarity",
        "Use consistent formatting throughout",
        "Consider adding a summary section"
      ],
      "codeTemplate": {
        "lua": "-- Step 4: Format and display the output\n-- Your code here\n\nprint(\"Testing Lua: Tables...\")\n"
      },
      "verification": {
        "test": "output.length > 0",
        "successMessage": "Output is clean and well-formatted!",
        "failureMessage": "Format the output for readability."
      }
    }
  ]
}
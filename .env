# ════════════════════════════════════════════════════════════════
# KODEX'S LAB - Environment Configuration
# ════════════════════════════════════════════════════════════════

# ── PORT & RUNTIME ──
PORT=3000
NODE_ENV=development

# ── AI PROVIDER CONFIGURATION ──
# Options: hybrid (default), keyword, openai, anthropic, local
# - hybrid: Keyword cascade + transformers.js (~60MB). Works out of the box.
# - keyword: Free, no API key, uses pattern matching (good for learning)
# - openai: GPT-4, GPT-3.5, etc. (requires API key)
# - anthropic: Claude models (requires API key)
# - local: Ollama, LM Studio, etc. (requires local server running)
# ╔══════════════════════════════════════════════════════════════╗
# ║  CURRENT: qwen2.5-coder:1.5b-instruct-q5_K_M (via Ollama)  ║
# ║  TO REVERT: cp .env.hybrid-backup .env                      ║
# ╚══════════════════════════════════════════════════════════════╝
# AI_PROVIDER=local
# LOCAL_LLM_ENDPOINT=http://localhost:11434/v1
# LOCAL_LLM_MODEL=qwen2.5-coder:1.5b-instruct-q5_K_M
# AI_MAX_TOKENS=4096

# ── OpenAI Configuration ──
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_ENDPOINT=https://api.openai.com/v1

# ── Anthropic Configuration ──
# ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_MODEL=claude-3-haiku-20240307

# ── Local LLM Configuration ──
# For Ollama: Install from https://ollama.ai, run `ollama serve`

# ── Database Connections (Optional) ──
# SQLite is built-in with sample data — no configuration needed.

# PostgreSQL connection string:
PG_CONNECTION_STRING=postgresql://postgres:postgres@localhost:5432/postgres

# MySQL (optional):
MYSQL_CONNECTION_STRING=mysql://user:@192.168.1.50:3306/mydb


# ── AI Tutor Settings ──
# AI_MAX_TOKENS=1024
# AI_SYSTEM_PROMPT=You are an expert programming tutor...
